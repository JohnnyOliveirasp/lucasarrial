# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build outputs
.next/
out/
build/
dist/
*.tsbuildinfo

# Environment files — NEVER COMMIT
.env
.env.*
!.env.example
!.env.*.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs/*
!logs/.gitkeep

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
*.egg-info/

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Misc
.vercel
.turbo
coverage/
*.pem
*.key
*.cert
.cache/

# Claude Code local settings (per-machine perms)
.claude/settings.local.json
.claude-flow/

# Audit screenshots (heavy, regenerable). Keep markdown reports.
frontend/docs/audits/*/screenshots/

# Playwright report cache
playwright-report/
test-results/
.playwright-mcp/

# Artefatos de scratch/debug (não vão pro deploy; deploy.yml já os exclui)
**/_scrape_build.mjs
frontend/runpod-build.*

# REGRA (10/08): nada que não seja o produto fica solto na árvore. Scripts
# operacionais, investigações, smokes, prints e sobras vão para _Bugs/ (na raiz
# ou dentro de frontend/), organizados por finalidade. Ver _Bugs/README.md.
_Bugs/

# Reference materials — heavy folder, will live in separate git branch
_backup_apenasReferencia/

# Old Python project leftovers (root only — não ignora runpod-worker/requirements.txt)
/main.py
/requirements.txt
downloads/

# Temp / playground
.tmp/
tmp/

# Screenshots temp (configurações RunPod)
_ConfiguracoesMaqina/
voxbr/tagarela/.r2env
# voxbr: sobem SÓ scripts e docs. Dados pesados ficam fora (17/08 — o dataset
# mora no R2; parquet/wav/jsonl local é cache de trabalho, 10GB+):
voxbr/VoxCPM/
voxbr/amostras/
voxbr/smoke/eval/
voxbr/tagarela/*.parquet
voxbr/tagarela/_train_final*.jsonl
voxbr/tagarela/_work/
voxbr/**/*.wav
voxbr/**/*.safetensors

# Dados pessoais de aluno (backup de exclusao LGPD) e exports da planilha:
# NUNCA versionar. Em 23/08 commitei planilha.csv com 536 linhas e 2 colunas
# de senha em texto puro e tive que reescrever o historico e force-push.
_frank/prova/lgpd/
*planilha*.csv
*planilha*.xlsx
