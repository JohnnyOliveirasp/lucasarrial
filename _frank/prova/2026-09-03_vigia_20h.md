# Vigia — ronda de 03/09/2026, 20hZ (17h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 3** (`702cc916`/#226,
`f8587cef`/#234, `93ef23c4`/#190). Não fechei, não reabri, não mudei status de nada, não respondi
aluno, não escrevi rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Medição feita às **20:11Z**. Ronda de vigia anterior: `18h` (18:16Z). Ronda das falhas mais recente
lida antes de tocar em qualquer coisa: o commit `afb1031` (19:40Z), cujo log é o arquivo nomeado
`rotina_falhas_23h`. Confirmo a nota de nomenclatura da ronda anterior: **os nomes de arquivo não
batem com o relógio**; confie no carimbo `Z` dentro do texto.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 🟡 Órfãos do #222: eram 9, agora são **4** — e o pedido de "pode" continua de pé

**Correção ao meu próprio relatório das 18hZ.** Eu reportei "9 pagantes órfãos sem contato real" e
pedi decisão. **O número está velho**: entre 18:16Z e agora o Frank trabalhou a fila e mandou carta
para mais gente (commits `9251119`, `b2cfcde`, `afb1031` — isaias, dropweb, Rodolfo, flaviamalavazi).

Funil remedido por mim agora, não herdado (`entitlements`, `user_id is null`):

| etapa | n |
|---|---|
| órfãs | 92 |
| `status='active'` | 46 |
| janela vigente (`access_until > now()`) | **26** |
| pagante de verdade (`price.value > 0`) | **15** |

Destes 15: **10 já têm carta** (480, 481, 483, 488, 491, 492, 493, 494, 495, 496) e **2 saem da fila
de carta** por serem cobrança em dobro, não falta de acesso (`caplastica`, `jkakorio`).

**Restam 4:** `rutifortuna8` (20/09) · `qooqi.criacoes` (21/09) · `fmgimael` (29/09) ·
`malmeida313` (30/09).

**A pergunta, em 1 linha: posso escrever para os 4 de uma vez?** Mudou a urgência, não a pergunta —
a janela mais próxima agora é **20/09**, não "amanhã". Sai 1 por ronda enquanto não vier o "pode".

⚠️ **Armadilha de instrumento nova, medida nesta ronda.** Em `entitlements` o preço está em
`raw_event->'purchase'->'price'->>'value'` (**caminho direto**): lê 15. Pelo caminho embrulhado em
`data`, que é o que o Frank descobriu ser o certo em `payment_events` (`afb1031`), lê **0 de 26**.
São **duas tabelas com dois caminhos opostos**, e usar o da outra devolve zero em silêncio. Quem
medir órfão pela primeira vez cai nisto.

### 0.2 Migration 102 — **décima terceira** ronda com a mesma pergunta

Conferido agora pelo banco, não pelo registro anterior:

```sql
select tgname from pg_trigger where not tgisinternal and tgrelid='incidents'::regclass;  -- []
```

`scripts/102_incidents_resolved_guard.sql` segue **não aplicada**.

### 0.3 PRs abertos: **26**, parado

Nenhum entrou e nenhum saiu desde as 18hZ. Os `#41`/`#42` (teto de 2MB) estão no **décimo sexto
dia** — e a Alana levou exatamente esse defeito na cara em 02/09 (uid 427: *"veio com um anexo
grande demais (2 MB) e o nosso suporte não consegue abrir"*, resposta a um e-mail dela de 2,2MB).

---

## 1. O que eu ABRI

**Nenhum chamado.** Nenhum e-mail novo (§4), nenhum incidente novo nasceu na janela
(`max(numero)` segue **242**, `criados_4h = 0`), nenhuma classe nova. Todo achado desta ronda passou
pelo teste de bolso da ordem de 27/08 e nenhum sobreviveu como chamado novo: os dois achados
técnicos são **ocorrência de classe já aberta**, e os dois de processo vão em 1 linha no relatório.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`702cc916` / `#226`** — nota 25: a série andou **17 → 18** e são **quatro** pós-build, não uma. §5.
- **`f8587cef` / `#234`** — nota 13: `coverage_rescue` ainda **n=0** pós-build, 7h50 depois. §6.
- **`93ef23c4` / `#190`** — nota 3: **objeção de processo**, a nota de fechamento exigida pela ordem
  de 27/08 §5 está vazia. §7. Status segue `ignored`; não reabri.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de 19/08).

**5 abertos — 1 de aluno, 4 técnicos.** Todos em `investigating`; `open` = 0. Mesma composição das
18hZ.

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **368,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) — `categoria=atendimento` |
| `3ca22d47` `#222` | 52,3h | 5 no título, **26 medidos** | compra órfã casa só por e-mail |
| `702cc916` `#226` | 50,3h | **159 alunos** | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 27,9h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 23,5h | *(irrespondível)* | relato manual sem e-mail e sem anexo |

**Movimento desde as 18hZ: saiu 1, não entrou nenhum.** O `#213` (*aluno pediu reembolso*) foi
fechado por **humano** (`suporte@lucasarrial.com`) às **20:08Z**, 3 minutos antes desta medição,
com nota `enviado email`. É fechamento de gente, com desfecho; **não tenho objeção a registrar** —
anoto só que a nota é de uma linha e não diz o que foi decidido sobre o dinheiro.

**Mais 9 em `aguardando_aluno`**, que o número acima não mostra: `#99` (**267,4h = 11,1 dias**),
`#172` (148,6h), `#206` (79,1h), `#207` (78,2h), `#214` (70,3h), `#216` (55,8h), `#218` (53,2h),
`#224` (51,3h), `#229` (47,3h). Sem mudança desde as 18hZ.

**Placar honesto da ronda: 5 abertos + 9 esperando aluno = 14 cards com gente do outro lado.**

### 3-A. Crédito conferido — e um recorte MAIOR que o das rondas anteriores

Os 16 e-mails citados nos abertos têm **profile** (16 de 16).

As rondas das 16hZ e 18hZ mediram gerações `failed` **só dentro dos e-mails dos incidentes abertos**
(a das 18hZ achou 3). Eu rodei o recorte **global**, porque dívida com aluno não pede licença ao
incidente:

| | n |
|---|---|
| gerações `failed` no banco inteiro | **61** |
| nunca cobradas (`trans_total = 0`) — não há o que estornar | 11 |
| **cobradas e SEM estorno** | **0** |

Por `ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por `kind`, armadilha de
20/08). **Nenhum estorno pendente em lugar nenhum.** A mais recente das 61 tem 146,0h. Registro o
recorte maior de propósito: o número menor das rondas anteriores não estava errado, estava
**estreito**, e um recorte estreito que dá zero é indistinguível de um sistema saudável.

### 3-B. Filas e sinal de vida

`generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. **`voices/awaiting_training` = 17** (igual às 18hZ) — não
é chamado, é o estado em que a bola está com o aluno; o PR **#15**, que daria dono a essa lista,
está **OPEN desde 20/08**.

**Conferi que o silêncio não é cron morto**, porque "nada novo" e "o sensor parou" se parecem: a
plataforma fez **73 gerações nas últimas 24h**, a última às **20:14Z** (2 min antes desta linha), e
o `fast-help` abriu `#241`/`#242` há 4,7h e 5,5h. O pipeline está vivo; a caixa é que está quieta.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 700`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Nenhum e-mail novo desde as 18hZ:** o uid mais alto continua **429** (Vinícius, 03/09 04:10Z), há
**16,1h**, e o card dele (`#240`) já foi fechado por humano. Bate com o `#240` ser o último incidente
`reported_by='fast'`, também de 16,1h atrás — os dois instrumentos concordam.

Nenhum aluno cobrando pela segunda vez **sem resposta**, nenhum *"já pedi ajuda várias vezes"* novo,
nenhum *"a equipe vai verificar"* sem card. Os quatro da janela (Katia, Alana, Silmara/Rufino,
Johnathan) seguem com o card que as rondas anteriores mapearam; **não reescalei nenhum**, inclusive
o Johnathan (ameaça jurídica, uid 417 *"são bandidos profissionais, já estou buscando as
providências cabíveis"*), porque já tem dono e repetir seria reapresentar o velho como novidade.

---

## 5. O `#226`: são **QUATRO** entregas ruins pós-build, não uma — e a mais nova é de 18 minutos atrás

A ronda das 18hZ reportou **uma** entrega abaixo da régua posterior ao build. Eu listei todas as de
hoje e são **quatro** depois do `Build RunPod Worker` do `3bc1535` fechar (`success`, 12:23:08Z):

| geração | quando | visto | régua | `exhausted` | `regens` | passou pelo resgate? |
|---|---|---|---|---|---|---|
| `5d5b7866` | 13:08:05Z | 0,667 | 0,85 | 4 | 14 | não |
| `84e3a400` | 14:44:26Z | 0,667 | 0,85 | 1 | 16 | não |
| `79309199` | 16:10:22Z | 0,800 | 0,85 | 1 | 2 | não |
| **`49996c3e`** | **19:52:45Z** | **0,727** | 0,85 | 1 | 16 | não |

Todas `status = ready`. **Todas entregues ao aluno.** A mais nova saiu **18 minutos** antes desta
medição e é **pior** que a que as 18hZ reportaram (0,727 contra 0,800) — então não há tendência de
melhora, há mais amostra.

**A leitura honesta, com o limite dito:** nenhuma das quatro carrega `coverage_rescue`, e o
`3bc1535` conserta o gate **no caminho do resgate**. O conserto **não teve chance de agir sobre
nenhuma delas** — não é que falhou, é que **não cobre este ramo**. O defeito do `#226`
(`loop.py:341-344` esgota as tentativas e entrega assim mesmo) segue inteiro e segue disparando.

Janela instrumentada desde 02/09 02:32Z, régua **por linha** (`qa->>'coverage_min'`, 0,85 em todas —
não um limiar chutado, armadilha que a ronda das 18hZ documentou):

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 39 | 25 | **0 (0,0%)** | 0,857 |
| `exhausted > 0` | 44 | 24 | **18 (40,9%)** | **0,333** |

O controle **continua nunca** furando a régua, agora com 39 entregas. Série acumulada:
**360 de 783** com `exhausted>0` (**46,0%**), **159 alunos** distintos (era 357/779 e 157 às 18hZ).

---

## 6. O `#234`: o `n=0` continua, e o que a instrumentação mostra no lugar

1. **A predição segue sem resultado.** Medido: **18** gerações com `jsonb qa` posteriores a
   12:23:08Z — primeira 12:30:36Z, última 19:52:45Z — e **zero** carregam `coverage_rescue`. O
   conserto está no ar e **ainda não foi exercitado nem uma vez**, agora **7h50** depois do build
   (eram 5h48 às 18hZ). Não há resultado a favor nem contra.

2. ⚠️ **Uma armadilha que eu quase caí.** Varrendo as chaves do `qa` na janela desde 02/09,
   `coverage_rescue` aparece com **count = 1** — parece que o `n=0` acabou. **Não acabou:** a
   geração é a `a8caaae1`, de hoje **11:11:08Z**, portanto **1h12 ANTES** do build. Ela testa o
   código velho, não o conserto. Registro para a próxima ronda não contar isto como evidência a
   favor. (De quebra ela é mais um caso do `#226`: `visto 0,615` contra régua 0,85, `ready`.)

3. **O dado novo, com o limite dito.** Das **14** gerações pós-build que carregam
   `tail_interno_entregue_n`: **14 de 14** têm `tail_interno_entregue_sem_veredito = 1`, e **9 de
   14** têm `tail_interno_flagged > 0` (soma 18 fronteiras marcadas). **Eu não ouvi nenhum desses
   áudios** e não sei provar que `entregue_n > 0` equivale a palavra decapitada audível. O que está
   medido é que a fronteira interna está sendo **entregue sem veredito em 100% do pós-build**.

4. **O limite de instrumento continua de pé:** *"build success"* **não é** *"imagem nova rodando no
   endpoint do RunPod"*, e não existe no banco marcador que distinga imagem velha de nova. O `#234`
   **não pode** ser fechado por *"o fix subiu"* enquanto a primeira geração com `coverage_rescue=1`
   posterior a 12:23:08Z não existir.

---

## 7. A ordem de 27/08 venceu hoje: bateu o alvo — e a contagem das 18hZ estava 1 a mais

Refiz a conta em vez de herdar, e ela mudou. **Recorte honesto (só o que nasceu DEPOIS da ordem,
`#158` em diante): 14 chamados, não 15.** A ronda das 18hZ contou 15; conferindo um a um, os do
vigia desde 27/08 são 18 no total e apenas 14 têm `numero >= 158` (`#150`/`#151`/`#152` de 12:17 e
`#153` de 14:15 são o "antes" que a própria ordem usa como exemplo do problema).

| métrica | alvo | medido | |
|---|---|---|---|
| `ignored` | ≤ 10 % | **1 de 14 = 7,1 %** | ✅ (era 21 %) |
| `categoria='atendimento'` | zero | **0** | ✅ |
| `kind` começando com `processo`/`atendimento` | zero | **0** | ✅ |

**A ordem funcionou.** A conclusão das 18hZ estava certa; o denominador é que estava frouxo.

⚠️ **O furo de processo, que agora está anotado no lugar certo.** A ordem de 27/08 §5 manda que, ao
fechar um chamado do vigia como `ignored`, a nota diga **qual das três checagens do §3 faltou** — é
o mecanismo pelo qual a ordem se corrige sozinha. O único `ignored` é o `#190`, fechado por `claude`
em 30/08 00:01Z com `resolution_note` **VAZIA (0 chars)**. Como só houve um, **o mecanismo nunca
rodou**. As 18hZ reportaram isso só no relatório; a 14-A manda a objeção ir **no próprio chamado**,
então **anotei no `#190`** (nota 3, status intacto). Objeção é de processo, não de mérito: o
fechamento está certo, o aprendizado é que não ficou registrado.

---

## 8. Fechados que voltaram a disparar

`status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` `#8` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
**288,8h atrás**. Não é reincidência ativa. **Nada fechado voltou a disparar nesta janela.**

⚠️ **O ponto cego continua e piorou de tamanho.** O `#226` tem `last_seen_at` congelado em
**01/09 17:52:45Z** e `occurrences` em **290** contra **360** reais — a defasagem era 67 às 18hZ,
agora é **70**. Ele está `investigating`, então sai desta consulta por **reabertura, não por
conserto**. Se alguém fechar o `#226` de novo, a consulta volta a mentir na hora. O PR **#163** faz
o detector **declarar** o ponto cego; ele **não descongela** o contador.

---

## 9. Uma coisa que eu vi e decidi NÃO transformar em chamado

`rate_flagged > 0` em **408 de 421** gerações instrumentadas (**97%**), sendo 14 de 14 no pós-build.
Um número desses parece classe nova. **Não abri chamado e não vou afirmar que é defeito**, porque
não abri o código que escreve essa chave e não sei se ela marca *"ritmo corrigido"* (rotina, esperado
em quase toda geração) ou *"ritmo errado entregue"* (defeito). Chamar de bug uma contagem alta sem
ler quem escreve o contador é exatamente o erro que a ordem de 27/08 mediu em `#100`/`#125`/`#152`,
e é o mesmo formato do falso *"precisão ~57%"* que a ronda das falhas das 17hZ já derrubou como
artefato de contador por tentativa. Fica registrado como **pista para quem for abrir o código**, com
a pergunta em 1 linha: *`rate_flagged` conta correção aplicada ou defeito entregue?*

---

## 10. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada — em particular **não
toquei nos 9 de `aguardando_aluno`**, nem no `#213` que o humano fechou às 20:08Z, nem no `#190`
(anotei, segue `ignored`). Não respondi aluno nem escrevi rascunho: **não escrevi aos 4 pagantes
órfãos restantes** (comunicação em massa, precisa do "pode"), **não escrevi ao Johnathan** (ameaça
jurídica, já tem dono) e **não escrevi à Katia** (o `#47` é do Frank). Não mexi em crédito, acesso,
voz ou áudio. Não mergeei PR (26 abertos) nem apliquei migration (102, **13ª ronda**). Não apliquei
o patch do `#237`. Não ouvi nenhum áudio e não vi nenhuma imagem: toda afirmação aqui é de
telemetria, `gh`, leitura de código, leitura de caixa e cronologia. Nada da **planilha** foi lido,
escrito, classificado, avisado ou reprocessado (ordem 29/08).
