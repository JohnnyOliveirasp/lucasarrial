# 28/08 10h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → veio 2 commits atrás
(mensagens en/es/pt-BR). Índice de ordens lido antes de tocar em qualquer coisa.
Ordem vigente do assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C) — só
erro de SISTEMA vira chamado. Também valem `2026-08-20_dono_da_fila_e_fila_zerada.md`,
a `2026-08-20_REGRA_FINAL_CREDITO.md` (ENCERRADA — não reabri) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **00h UTC**. Esta: **10h UTC**. **A janela é de ~10h, não
de 2h** — quatro rondas minhas (02h, 04h, 06h, 08h) não rodaram. Isso é
**processo**, vai como 1 linha aqui e 1 mensagem pro Johnny (§7), não vira
chamado. No meio da janela rodou a **Rotina das Falhas das 01h** (o Frank), que
tratou o `108` e abriu o PR #78.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **7** — 3 `atendimento` (`120`, `151`, `153`), 4 `técnicos` (`11`, `108`, `164`, `165`) |
| Variação vs. 00h | **0** — a fila não mexeu em 10h |
| **Abertos por mim nesta ronda** | **0** — pela 14-C, nada da janela era erro de sistema novo |
| **Anotados por mim** | **0** — as 2 candidatas já estavam registradas (§4 e §5) |
| **Fechados que voltaram a disparar** | **0** — por 2 réguas independentes (§3) |
| Fora da contagem | 8 em `aguardando_aluno` (`47`, `72`, `99`, `124`, `133`, `139`, `156`, `162`) |
| E-mails novos desde as 00h | **2** — uid 344 (Cássio) e uid 345 (Itamar) |
| Aluno sem resposta | **0** — os 2 novos são agradecimento/encerramento (§2) |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 12h | **32 — todas `ready`**, 0 falhas, **0 presas** >30min |
| Falhas de geração em 24h | 6, **todas 17:01Z–17:30Z de 27/08** — anteriores ao fix, classe do `52` |
| Cenas do estúdio em 24h | **59 — todas `ready`** |
| Imagens em 24h | **179 — todas `ready`**, 0 presas |
| Vozes em 24h | **22 — todas `ready`** (0 em `awaiting_training` nas últimas 24h) |
| Estornos em 12h | **0** (`ref_type='generation_refund'`, nunca por `kind`) |
| PRs abertos | **22** (o mais velho de 18/08). **#75 foi MERGEADO** às 01:46Z (§6) |
| Commits novos na `origin/main` | **1** — o merge do #75 |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**, incluindo `fixing` e `open`.
`status` distintos na tabela: `fixed` 111, `ignored` 31, `aguardando_aluno` 8,
`investigating` 7 — **zero `open`, zero `fixing`**. Total 157, **igual às 00h**:
nenhum incidente novo desde o `165` (27/08 22:52Z).

---

## 1. Nada de sistema novo nesta janela

Todas as réguas de produção vieram limpas:

| régua | resultado |
|---|---|
| gerações 12h | 32, **todas `ready`** |
| gerações presas >30min | **0** |
| falhas de geração 24h | 6, **todas entre 17:01Z e 17:30Z de 27/08** |
| cenas do estúdio 24h | 59, **todas `ready`** |
| `image_generations` 24h | 179, **todas `ready`** |
| vozes 24h | 22, **todas `ready`** |
| estornos 12h | 0 |

As 6 falhas são a **mesma classe do `52`** (`qa_coverage: audio gerado nao
contem o texto completo apos esgotar regeneracoes`), fechado como `fixed` às
19:23Z de 27/08. **Todas as 6 são anteriores ao fechamento** (a mais nova
17:30Z). Depois do fix: **32 gerações, zero falha**. Imprimi o `error_message`
cru antes de acreditar no zero — a coluna se chama `error_message`, não `error`
(a primeira consulta quebrou com 42703 e eu não tratei o erro como "0 falhas").

## 2. A caixa: 2 e-mails novos, nenhum aluno esperando

| uid | quem | o que é |
|---|---|---|
| 344 (00:18Z) | Cássio Fialho | **agradecimento e desculpa** pelas cobranças anteriores ("torço pra que o seu sistema faça sucesso"). Fecha o ciclo das 7 mensagens dele. Não pede nada. |
| 345 (00:43Z) | Itamar Vanzin | **agradecimento** pela análise e pelos créditos devolvidos + **crítica de produto**: a Fast escreve respostas longas demais para dúvidas simples, deveria responder curto primeiro e detalhar só se o aluno pedir. |

Nenhum dos dois é aluno cobrando pela segunda vez, nenhum é erro de sistema.
**Não abri chamado** e **não escrevi rascunho de resposta** (14-A). A crítica do
Itamar é **produto/processo** — vai pro Johnny no §7, sem chamado.

Conferi que os remetentes da janela anterior seguem respondidos (Zethe 21:07Z →
21:10Z e 21:48Z; Rosilene 19:01Z → 19:10Z; Gabriel 23:46Z → 23:50Z). **Fila de
não-lidos da Fast: 0, e eu não toquei em nenhum não-lido.**

## 3. Fechado que voltou a disparar: zero

Duas réguas, nenhuma com cláusula de assinatura:

1. **Fechado com `last_seen_at` nas últimas 48h** — 20 incidentes, e em **todos**
   o `last_seen_at` é **anterior** ao `resolved_at` (`h_depois` ≤ 0). Foram
   fechados **depois** da última ocorrência, que é o desenho certo.
2. **Fechado que disparou 10min+ DEPOIS do fechamento** — devolve **1 linha só**,
   o `#8` (`acf8acd6`, `training:user_dataset`), visto pela última vez em
   **22/08 19:26**, ou seja **5,6 dias frio**. Não está disparando; é resíduo
   histórico, já registrado em rondas anteriores.

O caso que a minha ordem manda vigiar de nome, o **`8d370ef5`** (`#34`), não
aparece em nenhuma das duas réguas: `last_seen_at` 19/08, fechado 20/08.
**Encerrado de verdade.**

## 4. O `training_jobs` órfão: já anotado, não é achado novo

De novo **1 linha**: job `ebf5cc56` preso em `queued` desde 25/08 01:33, com a
voz `f4b9b0f2` já **`ready`** (escrituração pendente, ninguém esperando).

**Não abri chamado — checagem §3.1 da 14-C ("já existe?") segurou:** este job
está medido na **nota [15] do `108`**, que está aberto e com dono, e aparece em
6 relatórios anteriores. Chamado novo aqui seria duplicata de classe.

## 5. `awaiting_training`: não é bug, mas **cresceu** em 10h

Re-medi o acúmulo que reporto desde 26/08:

| | 00h | 10h |
|---|---|---|
| vozes em `awaiting_training` com +24h | 14 | **16** |
| alunos distintos | 13 | **15** |
| a mais velha | 44,9 dias | **45,3 dias** |

**+2 alunos em 10 horas.** São pessoas que subiram o áudio, **passaram na
validação** e nunca tiveram voz porque o fluxo termina num botão que ninguém
clicou. **Não é defeito de código, é desenho** — por isso, pela 14-C, não vira
chamado. Mas o número está subindo, não estável. Vai pro Johnny como decisão
(§7, item 2).

## 6. O PR #75 foi mergeado — e dois chamados seguem abertos em cima dele

`gh pr view 75` → **MERGED em 28/08 01:46:45Z** (`fix/inc151-botao-treinar-voz`,
merge `ed38289`). É o conserto que a minha ronda das 00h escalou como item 1
("aluna pagante com prazo curto, acesso vence 31/08").

O **`151`** (a aluna Zethe) e o **`164`** (o botão "Gerar nova voz", mesma aluna,
mesma tela) continuam **`investigating`**. **Não fechei nem toquei em nenhum dos
dois** — quem fecha é o Frank (14-A). Fica **anotado aqui** para a próxima rotina
das falhas conferir se o merge basta para fechar os dois ou só o `151`.

Também não confirmei deploy: só o merge está provado, não a subida em produção.
Não afirmo o que não medi.

## 7. Pro Johnny — o que precisa de decisão

1. **A minha ronda de 2h não rodou entre 00h e 10h UTC** — 4 janelas puladas
   (02h, 04h, 06h, 08h). Se isso é o turno da noite desligado de propósito
   (ordem de 27/08, Executor 08h–23h BRT), está tudo certo e eu paro de apontar.
   Se não é, o agendamento morreu e a operação ficou 10h sem sensor. **Só o
   Johnny sabe qual dos dois é.** Pela 14-C isto é processo: 1 linha, sem chamado.
2. **16 vozes / 15 alunos parados num botão** (§5), a mais antiga há 45,3 dias, e
   **subindo** (+2 em 10h). Decisão de produto: o fluxo passa a cutucar esse
   aluno, ou o treino dispara sozinho?
3. **22 PRs abertos**, o mais velho de 18/08, vários consertando defeito já
   diagnosticado — inclusive o **#78** (cauda fantasma do `108`) e o **#73**
   (fecha o `153`). O #75 saiu da fila esta madrugada e resolveu o item 1 da
   ronda anterior; o gargalo segue sendo o aval, não o diagnóstico.
4. **Crítica do Itamar (uid 345), que eu não vou fazer virar chamado:** a Fast
   responde certo mas comprido demais, e isso "dificulta encontrar a orientação
   principal". Veio de um aluno satisfeito, não de um reclamante — é o tipo de
   recado que costuma sumir.

## 8. O que eu NÃO fiz (14-A)

- **Não abri chamado nenhum** — nada da janela era erro de sistema novo.
- **Não fechei nem reabri incidente.** O `151` e o `164` seguem `investigating`
  mesmo com o PR #75 mergeado: a decisão é do Frank.
- **Não respondi aluno e não escrevi rascunho.**
- **Não toquei em crédito, acesso ou assinatura.**
- **Não toquei em e-mail não lido** (fila da Fast: 0).
- **Não mergeei PR, não apliquei migration, não gastei GPU.**
