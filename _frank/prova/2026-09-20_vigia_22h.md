# VIGIA — ronda de 20/09/2026, 22hZ

Sensor (regra 14-A): varre, abre e anota. Não fecha, não reabre, não responde
aluno, não mexe em crédito. Calibrado pela ordem de 27/08 (só erro de SISTEMA
vira chamado) e pela de 17/09 (percepção é despacho, não estado de parada).
Ordem de 29/08 respeitada: **nada da planilha** foi lido, escrito, classificado
ou reprocessado. Canal: ordem de 31/08 — o aviso desta ronda saiu **no grupo**
(`notify-grupo.sh`), nada no privado do Johnny.

**Chamados abertos nesta ronda: 0.** E isso é resultado, não ausência de
trabalho: a classe que eu fui investigar existia, eu tinha a hipótese pronta, e
a medição derrubou a hipótese. Está escrito no §2 para ninguém repetir.

---

## 1. Corrijo um número que eu mesmo inflei duas rondas seguidas (#492)

Escrevi **7** às 18hZ e **6** às 20hZ para a classe "aluno travado no passo de
áudio do SGP porque a tela não diz o que falta". Fui medir item a item, e a
classe de **tela muda é 1**.

Dos 6 pedidos em `status='audio'` com áudio anexado, **um só** está acima da
régua e travado exclusivamente pelo que a tela não diz:

| pedido | aluno | fala distinta | falta | parado | por que está parado |
|---|---|---|---|---|---|
| `afc43532` | Roster Lopez Rivas | **1400s** (693+707, 2 arquivos diferentes) | **0s** | 77,4 h | ⚠ **tela muda** — contador verde, Continuar cinza, e nenhuma linha diz que faltam os 4 checkboxes |
| `1f241294` | Andreia Marques | 1062s | 138s | 233,8 h | falta áudio, e a tela diz |
| `4765c697` | Andreia Debastiani | 947s | 253s | 74,2 h | falta áudio, e a tela diz |
| `aeafd4a0` | André Luis Viana | 213s | 987s | 73,4 h | falta áudio, e a tela diz |
| `864d653a` | Lucisna Sato | 1092s | 108s | 48,7 h | falta áudio, e a tela diz |
| `cb708d51` | Nayara Valeriano | 1140s | 60s | 34,6 h | falta áudio — **e isso mudou hoje**, §3 |

O que separa o Roster dos outros cinco é medível, não é opinião:
`step-audio-form.tsx:188` renderiza **sempre** o `acumulado` — *"{atual} de fala
aprovada (mínimo {minimo} min)"* — mais a barra de progresso da linha 192. Quem
está abaixo do mínimo **vê o próprio número**. Não é tela muda; é material
faltando. Mudo é só o muro dos 4 checkboxes de `CIENCIA_AUDIO`, que não tem
nenhuma linha na tela.

A Lucisna merece a nota que me obriga a não escolher o número bonito: ela tem
**18 arquivos** e nenhuma cópia confirmada no R2. Os 1092s dela são fala
distinta de verdade — não dá pra jogar o caso dela na conta do #501.

**Isto não derruba o PR #366, muda o que ele destrava HOJE.** Hoje: 1 pessoa.
Depois: todo mundo que vier a fechar a régua, porque o muro dos checkboxes é o
último de todos — os outros cinco batem nele assim que subirem o áudio que falta.
Vender "6 alunos travados" pra priorizar o PR seria eu usando um número que a
minha própria medição não sustenta.

> Anotado no **#492 `2fd36981`**. Sem chamado novo (§3.1: a classe já está
> aberta). Sem mudança de status.

---

## 2. A hipótese da ronda deu NEGATIVO, e o negativo é o entregável (#501)

O #501 (régua de 20 min somando o mesmo arquivo) subiu hoje em `fed87f4a`, e eu
vim atrás do padrão que esta casa repete: **conserta a conta e esquece de dizer
ao aluno.** É literalmente o defeito do cartão vizinho (#492). Se fosse verdade,
o aluno que reenviou o mesmo áudio veria o arquivo listado, o contador parado, e
nenhuma explicação — um #492 novinho, criado pelo conserto de uma hora atrás.

**Não é o caso.** Conferido arquivo a arquivo, não por leitura de commit:

| onde | o que tem |
|---|---|
| `lib/sgp/fala-distinta.ts` | `somaFalaDistinta`, `chavesRepetidas`; identidade = **ETag + ContentLength**, nunca o nome |
| `api/v1/sgp/audio/slot/route.ts`, `.../concluir/route.ts` | a régua do servidor usa a soma distinta |
| `sgp/revisao/page.tsx`, `lib/sgp/processar.ts`, `lib/sgp/ajuda.ts` | idem |
| `components/sgp/step-audio-form.tsx` | `:8` importa, `:49` soma, `:50` `chavesRepetidas`, **`:205` renderiza o aviso por arquivo** |
| `messages/pt-BR.json`, `en.json`, `es.json` | `sgp.audio.repetido` existe **nos três**, e diz o que fazer: *"arquivo repetido — já enviado antes, não conta nos minutos. Pode remover."* |

Deploy `Deploy Frontend (production)` do `fed87f4a`: **SUCCESS às 20:45:52Z**.

Escrevo o negativo de propósito, no cartão e aqui. Uma hipótese testada e
derrubada é trabalho entregue: quem pegar o #501 amanhã não precisa gastar a
ronda dele repetindo esta checagem.

### O que eu achei de verdade enquanto procurava a coisa errada

**(a) A classe estava VIVA até o dia do conserto.** Eu vinha tratando os 12
pedidos inflados como dívida histórica. Ordenados por `voz_pronta_em`, não são:

```
 ...
 15/09 00:02Z  Cladston Ramon
 19/09 20:50Z  Danilo Paganucci    48m06s na régua → 16m02s distintos (PIOR caso)
 20/09 01:25Z  Grazianne Oliveira  34m30s na régua → 17m38s distintos
```

O pior caso da classe inteira treinou **24 h** antes do fix, e o mais recente
treinou **19 h 20 min** antes de ele entrar no ar. O defeito produziu **dois
casos novos nas últimas 48 h**. O conserto chegou a tempo de impedir o 13º, não
de desfazer os 11. Isso pesa na decisão que está com o Johnny desde as 20hZ
(procurar os alunos × esperar queixa): dois deles receberam a voz **ontem e
hoje** e provavelmente ainda nem usaram.

**(b) A guarda existia há 22 dias — na tela gêmea.** O `090bf730`
(*"barra foto REPETIDA — mesma imagem não vira 4 ângulos"*, **29/08 11:43**)
nasceu de uma frase do próprio Johnny: *"subi a mesma foto e ele deixou subir."*
Ele deu ao lado da FOTO duas impressões digitais (`sha256` + `dhash` 16×16) e um
motivo de recusa próprio — `"repetida"`, em `anexar.ts:20` — decidido **dentro**
da trava atômica. O áudio passa pelo **mesmo** `anexar.ts` e ficou sem nada:
`anexar.ts:62` hoje admite por escrito que *"o passo atômico não tem dedup"*.

É a doença do #492 outra vez — conserto aplicado numa gêmea só — com os papéis
invertidos: aqui a gêmea consertada foi a foto, e a esquecida foi o áudio. Não
abro chamado disso: é a causa do #501, que já está aberto (§3.1), e agora está
escrita lá.

> Anotado no **#501 `bfcb8a50`**. Sem chamado novo. Sem mudança de status.

**Limites declarados:** conferi o fonte da main e o deploy verde, **não abri a
tela em produção com sessão de aluno**; re-rodei o instrumento de md5 nesta ronda
e o retrato não mudou (16 pedidos com cópia, 12 que só passaram por causa dela,
*"0 grupo(s) em que o R2 NÃO confirma cópia"*); e segue valendo que eu provei a
**régua furada**, não que a voz de alguém saiu pior.

---

## 3. O conserto de hoje empurrou uma aluna para trás, e ninguém a avisou

`cb708d51` **Nayara Valeriano**. Até as 20:45Z o contador dela marcava **2280s**,
verde, régua satisfeita — porque a régua somava o mesmo objeto duas vezes (2
chaves, mesmo ETag, 19.238.863 bytes). Depois do `fed87f4a` ele passa a marcar
**1140s de 1200s**.

O conserto está **certo** e a tela agora **explica** (um dos dois arquivos
aparece com *"arquivo repetido — já enviado antes, não conta nos minutos"*). Mas
o fato é que uma aluna foi movida para trás por uma decisão nossa, de hoje, e
não recebeu uma linha sobre isso.

Isso é **atendimento**, não é chamado (ordem de 27/08, §2): vai pro grupo.
Eu não escrevo pra ela e não escrevo rascunho (14-A, e a lição de 20/08).

---

## 4. Objeção de PROCESSO: "em produção" ainda não é verdade para o PR #92

A ronda das falhas das 22h fechou o caminho de código do PR #92 e registrou
*"Fix em produção: 1 — PR #379, merge `561f6867`, conferido no conteúdo da
`origin/main`"*.

A parte do frontend está de fato no ar (`Deploy Frontend (production)`,
`561f6867`, **SUCCESS 21:45Z**). Mas **a mudança do #92 não é frontend**: ela
mora em `runpod-worker/voice_pipeline/reference.py`, e o caminho dela até a
produção é o workflow **`Build RunPod Worker`** → job `deploy-runpod`
(`.github/workflows/runpod-worker.yml:88-90`, `if: github.ref == 'refs/heads/main'`),
que aponta o template do RunPod pra imagem nova e recicla os workers.

Medido agora (**22:25Z**): run `35539640313`, job `build` **`in_progress`** desde
21:45:09Z — **40 minutos**, e o job `deploy-runpod` nem começou (ele tem
`needs: build`).

Antes de afirmar *"o caminho é esse"* eu procurei os outros, porque ler **um**
caminho e chamá-lo de único é como se produz certeza falsa. Varri o repositório
inteiro por quem chama a API da RunPod pra trocar imagem de endpoint
(`api.runpod.io/graphql`, `saveTemplate`, `imageName`): fora o workflow, existem
só **`_Bugs/runpod_gql.cjs`** e **`frontend/_Bugs/_infra/_delete_hd_endpoint.cjs`**
— ambos em `_Bugs/`, que é rascunho ignorado, nenhum dos dois no caminho de
produção. Então é isto mesmo: **o workflow é a única estrada**, e um worker só
troca de imagem quando esse job roda.

**Não estou chamando de falha.** Os dois builds anteriores levaram **34,7 min**
(19/09) e **47 min** (15/09) — 40 min está dentro da faixa. O que eu estou
dizendo é que *"conferido no conteúdo da `origin/main`"* é exatamente a armadilha
que esta casa documenta como **merged ≠ deployed**, e que às 22:25Z a frase certa
é **"a caminho"**, não **"em produção"**.

E há um modo de falhar que o próprio workflow declara: o template VOX B é
atualizado por um nome que o arquivo chama de *"chute documentado"*, e quando não
aplica ele emite só `::warning::` (`runpod-worker.yml:156`) — o run fica **verde**
com metade do deploy não feito. Quem for conferir daqui a pouco, confira o
`imageName` dos **dois** templates, não a cor do run.

1 linha no relatório e 1 no grupo (ordem de 27/08 §2, processo). **Sem chamado.**

### ✅ FECHADO às 22:26:14Z — a objeção valeu por 1 minuto, e eu registro o fim

Voltei a conferir antes de encerrar a ronda, e o caminho se fechou sozinho:

```
build          21:45:09Z → 22:25:05Z  success  (39,9 min)
deploy-runpod  22:25:08Z → 22:26:14Z  success  (1,1 min)
```

E o modo de falhar em verde que eu nomeei **não ocorreu** — fui ao log do job
conferir os **dois** templates, que era exatamente o que eu mandei conferir:

```
template -> ghcr.io/.../lucasarrial-runpod:561f686
reciclando workers (0 -> 7)      deploy RunPod ok
VOX B: reciclando workers (0 -> 4)   deploy VOX B ok
```

Nenhum `::warning::`. **O PR #92 está em produção desde 22:26:14Z**, 41 minutos
depois do merge — A e B, com os workers velhos descartados.

Ou seja: a ronda das falhas chegou ao lugar certo, e o que eu apontei foi o
**relógio**, não o destino. Deixo os dois fatos escritos porque a objeção saiu no
grupo: bandeira levantada que não é baixada por escrito vira boato. O que fica
valendo como método é o resto — *"conferido no conteúdo da `origin/main`"* não é
prova de produção quando a mudança é do worker, e o jeito de provar é o
`imageName` dos dois templates, não a cor do run.

---

## FRENTE 1 — a fila inteira, sem filtro de assinatura

Consulta **sem** cláusula de `signature` (o filtro `fast-email:%` foi o que fez a
ronda de 19/08 reportar "0 abertos" existindo quatro). Paginada (o PostgREST
corta em 1000): **487 linhas** lidas.

**93 abertos** — 3 `open` + 90 `investigating`. Por `categoria`:
**59 técnicos, 34 de atendimento**. **39 com 7 dias ou mais.**

| status | qtd | Δ vs. 20hZ |
|---|---|---|
| fixed | 298 | = |
| investigating | 90 | **+2** |
| ignored | 60 | = |
| aguardando_aluno | 36 | = |
| open | 3 | **−2** |

**O total não se moveu: 93 = 93.** Os ±2 são o **#348** e o **#501** saindo de
`open` para `investigating` na ronda das falhas. Nada entrou, nada fechou, em
duas horas.

### Os 3 `open`, um a um

| # | id | idade | alunos | o que é |
|---|---|---|---|---|
| 438 | `0c9eee9f` | 4,0 d | 0 (536 contas) | a casa gasta a única chave do aluno ao criar a conta — 6,5% entram, contra 59,7% de quem pede reset sozinho |
| 492 | `2fd36981` | 0,5 d | 1 | tela de áudio do SGP — **1** de tela muda, 5 de material faltando (§1) |
| 494 | `719c9af6` | 0,5 d | 9 | "realismo" do vídeo clone: corpo parado e olhar morto |

### Os 5 mais velhos da fila inteira (não só dos `open`)

| # | idade | alunos | o que é |
|---|---|---|---|
| 15 | **52,4 d** | 18 | geração de áudio: tempo de execução estourado |
| 226 | 19,2 d | 1 | entregamos áudio que o nosso próprio QA reprovou (`tts_qa/loop.py:341-344`) |
| 234 | 18,3 d | 10 | palavra decapitada no meio do áudio: 609 gerações (14,3%), 237 alunos |
| 249 | 16,2 d | 1 | e-mail não chegou no aluno (inexistente) |
| 250 | 16,2 d | 1 | e-mail não chegou no aluno (caixa cheia) |

### Percepção travada (ordem de 17/09)

`percepcao_travada.cjs` (controle positivo OK — #310 reencontrado, 487 incidentes
varridos): **1** card, o **#450**, parado há **2,4 d** — e a própria nota dele já
diz que **não** é caso de percepção (falso positivo declarado pela quarta ronda
seguida). A classe de percepção real em `open`/`investigating` segue em **ZERO**.

### ⚠️ Os 36 em `aguardando_aluno` — e um aviso sobre o número

`aguardando_aluno` não é aberto nem fechado, e a consulta de abertos é cega pra
ele. Parado em **36**.

**As rondas não estão medindo a idade pelo mesmo relógio, e isso faz o número
andar sozinho.** Medi pelos três:

| relógio | 7 d+ | 14 d+ | mais velho |
|---|---|---|---|
| `created_at` | **17** | 9 | **23,3 d** |
| `first_seen_at` | 17 | 10 | 103,3 d |
| `last_seen_at` | 16 | 9 | 20,4 d |

A ronda das 20hZ publicou *"14 / 9 / 20,3 d"*, que bate com `last_seen_at`. A
minha é por `created_at`. **Não houve piora de 20,3 para 23,3 dias em duas
horas** — mudou a régua, não o mundo. Daqui em diante eu publico por
`created_at` e digo qual é, porque métrica que troca de chave em silêncio é
métrica que mente devagar.

Os 5 mais velhos por `created_at`: **#172** (23,3 d), **#206** (20,4 d),
**#207** (20,3 d), **#214** (20,0 d), **#216** (19,4 d).

### Objeções de rótulo que eu mantenho

- **#430 Walsicleia** (`1a37605a`) — último sinal 18/09 00:30Z. A bola é da casa
  há **2,9 d**, e o próprio card tem nota do Frank dizendo que *"o caso não
  espera a aluna, espera uma decisão da casa"*.
- **#444 Janice** (`2e77bc4e`) — último sinal 17/09 12:25Z: **3,4 d** com a bola
  da casa. PR #324 existe desde 17/09, não mergeado.

Não mudo status (14-A); a objeção já está escrita nos dois cards e não a duplico.

### Fechados que voltaram a disparar: **0** nas últimas 48 h

**8** fechados têm sinal de vida nas últimas 48 h (#11, #475, #476, #479, #482,
#483, #485, #496) — e nos **oito** o `last_seen_at` é **anterior** ao
`resolved_at`. É o disparo que motivou o conserto, não reincidência depois dele.

Pela consulta crua, que é o instrumento independente: dos fechados com
`resolved_at`, **1 único** tem `last_seen_at` posterior ao fechamento — o **#8**
(`acf8acd6`, `training:user_dataset`), disparo de **22/08**. Nada recente.

⚠️ Como sempre: **1 fechado não tem `resolved_at`** e sobre ele o detector não
tem opinião. Não é "nenhum disparou", é "sobre esse não dá pra saber por aqui".

---

## FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`):
**não toquei no não-lido**, que é a fila da Fast.

**Nenhuma mensagem nova desde a ronda das 20hZ** — o último uid segue sendo o
**691** (Ellen, 20:08:45Z).

**Nenhum aluno novo** escrevendo "já pedi ajuda várias vezes".

### Conferi os 9 remetentes recentes contra a fila, um a um

Todos já têm cartão, e por isso **não abri nenhum** (§3.1):

| remetente | cartão |
|---|---|
| `charlesangio@` (cancelamento + 3 mensalidades) | **#498** `investigating` |
| `amandacunhaisa@` (sem acesso, código não chega) | **#497** `investigating` |
| `jkakoalves@` (nova cobrança) | **#499** + **#254** |
| `ellen.atp@` | **#348** + **#500** |
| `katiasalvador32@` | **#473** `aguardando_aluno` |
| `walsicleia_kaka@` | **#430** `aguardando_aluno` |
| `joaosoareskco@` | **#449/#453/#454**, `fixed` — e a última carta dele é **boa notícia**: *"demorou mas carregou os áudios… está treinando"* |
| `mcpaganatto@` | **#358** `fixed` — confirmou que vai repassar à secretária |

### O único sem cartão — e por que isso está CERTO

**Rachel Nigro** (`rachel.nigro@gmail.com`, uid 681, 19/09 18:32Z:
*"não recebo o código para definir a senha!!"*) tem **0 cartão**. Parei e fui
conferir antes de abrir um, porque a cara é exatamente a do **#438** (a casa
queima a única chave do aluno) e a da Walsicleia.

Não abri, e a medição diz por quê:

1. a casa **respondeu em 11 minutos** (`emails_enviados`, `origem='fast-resposta'`,
   19/09 21:35:17Z — a boas-vindas do SGP tinha saído 21:24:58Z);
2. ela **entrou na conta**: `profiles.last_seen_at` = **20/09 22:11:35Z**, ou seja
   **minutos antes desta ronda**, com `plan='pro'`, acesso Hotmart até 26/09 e
   87.795 créditos.

Aluna atendida e dentro da conta não vira chamado. Abrir seria eu inflando a
fila com um caso resolvido — e a fila é o que o Johnny usa pra saber se o produto
está quebrado.

---

## Passo fixo da ronda: reconciliação dos envios (#101)

```
node _frank/ferramentas/2026-09-18_reconciliar_envios_da_pasta.cjs \
     --corte=2026-09-14T14:06:31Z --confirmar
```

```
   883  lidas da pasta "Sent"
   806    já tinham linha
    77    FORA DA JANELA (decisão: --corte)
     0    RECUSADAS (defeito da carta)
     0    DENTRO DA JANELA — escriturável
 ✔ 883 = 883: nenhuma carta sumiu na classificação
```

**Nada a escriturar.** Contra a ronda das falhas das 22h (881/804): saíram
**2 cartas** no intervalo e **as duas já nasceram registradas**. O buraco segue
**passivo**.

Instrumento independente (`2026-09-18_enviados_x_tabela.cjs`):
**"0 carta depois do corte"**. Veredito confirmado pelas duas pontas.

As **77** anteriores a 14/09 14:06:31Z seguem **sem decisão** — é o que o
`--corte` exclui, e continua sendo decisão de produção (o `cobreDesde`
obrigatório em `contato-tentativas.ts`), não de ronda.

---

## Saúde da produção nas últimas 8 h

Sem rajada e sem nada preso:

| tabela | volume | falhas | preso em estado intermediário > 1 h |
|---|---|---|---|
| `generations` | 20 | **0** | **0** |
| `studio_scenes` | 0 | 0 | 0 |
| `voices` | 8 `ready` | **0** | **0** |

A única `generation` em `pending` que eu peguei no meio da varredura completou
sozinha antes do fim da ronda. O treino de voz está saudável: 8 vozes prontas,
nenhuma falha.

---

## PROCESSO — a linha do §2 da ordem de 27/08

**51 PRs abertos** (−1 desde as 20hZ: o **#379** foi mergeado). Os que travam
aluno **agora**:

- **PR #366** (20/09) — a tela de áudio do SGP passa a contar o que falta. É o
  conserto do #492. **Destrava 1 pessoa hoje** (o Roster, 77,4 h) e todo mundo
  que fechar a régua depois — número corrigido no §1.
- **PR #343** (18/09) — "esqueci a senha" pelo SMTP da casa. Aberto há **2 dias**.
- **PR #371** (20/09) — link de entrada por `token_hash` (o `action_link` queima
  o token). É o conserto do **#438**, que é um dos 3 `open` e vale por 536 contas.

E o item novo de processo é o do **§4**: o PR #92 está **a caminho** da produção,
não nela, e o run que o leva tem um passo que pode falhar em verde.

---

## O que eu NÃO fiz, de propósito

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito, não
fechei e não reabri nenhum incidente, não mudei status de nada. Não li, escrevi,
classifiquei nem reprocessei nada da planilha. Não abri chamado nenhum — e o §2
diz qual hipótese eu testei pra chegar a zero, em vez de zero por não ter
olhado. Não afirmei que a voz de ninguém saiu pior. Não afirmei duplicata onde o
R2 não confirmou. Não abri a tela do SGP em produção com sessão de aluno, e digo
que não abri. Não chamei o build do RunPod de falha — ele está dentro da faixa
medida dos dois anteriores. Nada no privado do Johnny (ordem de 31/08).
