# VIGIA — ronda de 21/09/2026, 14hZ

Sensor (regra 14-A): varre, abre e anota. Não fecha, não reabre, não responde
aluno, não mexe em crédito. Calibrado pela ordem de 27/08 (só erro de SISTEMA
vira chamado) e pela de 17/09 (percepção é despacho, não estado de parada).
Ordem de 29/08 respeitada: **nada da planilha** foi lido, escrito, classificado
ou reprocessado. Canal (ordem de 31/08): o aviso desta ronda sai **no grupo**
(`notify-grupo.sh`), nada no privado do Johnny.

**Chamado aberto nesta ronda: 1 (#510). Anotações escritas: 2 (#507, #508).**
O trabalho saiu de **duas linhas novas na fila** que ninguém tinha cruzado: uma
falha de R2 que virou **dois** cartões, e o motivo de ela ter virado dois.

---

## 1. Uma falha de R2 virou dois cartões, e o segundo é um ímã (#510, aberto agora)

Às 12:54Z o treino da voz da **lilikumon.liliane@gmail.com** morreu porque o
worker não conseguiu **baixar** um take do nosso próprio R2. O dono da fila já
tinha tratado o caso às 13:33Z e já tinha escrito que **#507 e #508 são a mesma
falha duplicada**. O que ninguém tinha escrito é **por que** elas não casaram —
e é aí que mora um defeito de sistema.

**Medido, não suposto:**

| | `sample_error` | assinatura |
|---|---|---|
| **#507** `3b7413e0` | **707** chars, termina em `Read timed out.` | **143** chars |
| **#508** `4d80c74d` | **500** chars, cortado **dentro** do `X-Amz-Signature` da URL | **47** chars |

A assinatura do #508 é `training:infra_storage:failed to download <url>`. Ela
nasceu genérica porque o corte de 500 comeu tudo que distinguia a falha.

**Mecanismo, com arquivo:linha conferido no fonte da `main`:**

1. `frontend/src/lib/voices/finalize-training.ts:598` — `adminError = rawError.slice(0, 500)`.
2. `frontend/src/lib/incidents/ingest.ts:251` — trunca de novo no mesmo teto.
3. A URL presignada do R2 passa de 500 chars, então o corte cai **dentro** dela.
4. Dois produtores do mesmo episódio (um vê 707, o outro vê 500) geram
   assinaturas diferentes → o dedupe por `signature` (`ingest.ts:171-172`) não
   casa → **dois cartões**.

**A parte que assusta não é a duplicata, é o ímã.** `ingest.ts:171-172` procura
incidente existente **só por signature, sem olhar status**, e a linha **197**
preserva `existing.status` quando o cartão não está fechado. Como o #508 está em
**`aguardando_aluno`** — que não é `fixed` nem `ignored`, então **não** dispara a
reabertura por reincidência da linha 180 — qualquer falha futura de download do
R2, de **qualquer aluno**, cai lá dentro, vira `+1` em `occurrences` e **continua
em `aguardando_aluno`**. Esse balde **não entra** na contagem de abertos que a
ronda reporta. Falha nova de infra sumiria da fila sem nunca aparecer como aberta.

**O remédio já existe na casa e foi dado só a um irmão.** `classify.ts` dá chave
**constante** (sufixo fixo, texto cru ignorado) a `infra_gpu/cuda-oom` (linha
349), `infra_disk/disco-cheio` (357) e `infra_disk/escrita-checkpoint` (366), e
trata `user_dataset` à parte (374). **`infra_storage` não tem nenhuma** — cai no
caso geral e herda o *head* do texto cru, que é justamente o que varia. O
comentário do próprio arquivo (351-356) descreve a doença com todas as letras, e
a cicatriz citada na linha 371 (`acf8acd6 × 014bb108`, achada pelo Vigia em
23/07) é o **mesmo defeito em outra causa**.

Quem instalou a chave constante do disco cheio foi o **#468**, fechado em 18/09
(`c6b0cfb4`), e a nota dele separa explicitamente `infra_disk` de `infra_storage`
*"que é o bucket R2 remoto"*. **Três dias depois, o irmão que ficou sem remédio
fragmentou.**

⚠️ **Armadilha declarada pra quem for consertar (lição do #410, ainda
`investigating`):** chave constante que sobe **não migra cartão aberto sozinho**.
Se `infra_storage` ganhar sufixo fixo, #507 e #508 ficam com as assinaturas
velhas e o próximo disparo abre um **terceiro** cartão.

**O que o #510 NÃO afirma:** não afirma frequência (o episódio é **n=1**; o risco
do ímã é lido no código, não é série observada), não afirma qual dos dois
produtores deve mudar, e **não afirma dano a aluno** — não houve (§2).

---

## 2. O #507 já tem a resposta que ele estava esperando — e ela é boa

A nota de 13:33Z deixou o cartão em `aguardando_aluno` pela razão certa: *"do
nosso lado acabou, mas o treino só se prova bom quando rodar"*. **Ele rodou.**
Medido por mim agora:

- débito de retreino **−10.000** (`kind=training`, `ref_type=voice`) em **13:46:07Z**
  → a aluna clicou;
- `voices.status = ready`, `trained_at` **13:51:02Z**, `lora_path` **PRESENTE**,
  `runpod_job_id` **diferente** do job que falhou;
- ela **já está usando a voz**: 3 cobranças de `generation` (13:53, 14:06, 14:10)
  e 1 `video_clone` (14:10:52).

O contingente que a nota armou (*"se falhar de novo no MESMO arquivo … eu
investigo a fundo"*) **não disparou**: o mesmo arquivo baixou na segunda
tentativa. A leitura de *"foi transporte, não objeto ausente"* **se sustenta**.

**Dinheiro conferido por mim, de forma independente:** −10.000 às 12:54:19Z e
+10.000 às 12:58:31Z (`ref_type='voice_train_refund'` — conferido pelo
**`ref_type`**, nunca pelo `kind`, que grava `extra_purchase`), mesmo `ref_id`.
O episódio da falha **fecha em zero**. O −10.000 das 13:46Z é o retreino novo,
escolha dela, **não** cobrança em dobro.

**Não fechei o cartão** (regra 14-A). Escrevi a medição na nota; quem fecha é o
dono da fila.

---

## 3. O #503 (Rita) foi fechado `fixed` às 12:28Z — e eu NÃO objeto

Às 12hZ eu tinha anotado que a Rita escalou para **reembolso integral** (188,07
EUR, duas compras COMPLETE de 05/09). O cartão foi fechado 22 minutos depois.
Li a `resolution_note` antes de ter opinião, e **ela se sustenta**:

- a **garantia venceu em 19/09** (medido pela ferramenta da casa, compra 05/09);
- SGP e FCI são **produto de curso**, então a decisão de reembolso é da equipe da
  Liz, e a carta entregou o canal de quem decide;
- **material não apagado**, apesar de ela ter autorizado, com o motivo explicado;
- o patch que eu tinha sugerido numa ronda anterior foi **recusado com razão** — o
  caminho que citei estava errado e a frase permissiva não existe no repo.

**Conferi que a carta saiu de verdade**, em vez de acreditar na nota:
`emails_enviados` tem **21/09 12:28:06Z**, assunto *"Rita: sobre o seu pedido de
reembolso — onde ele está e com quem"*, `bounce_classe` **nulo**.

⚠️ **O que declaro sem objetar:** a decisão de devolver ou não os 188,07 EUR saiu
da nossa fila e foi para um time **fora** do sistema de incidentes. **Nada aqui
rastreia** se ela vai ser tomada. Isso está certo pela ordem de 27/08 (atendimento
e decisão não são chamado), mas significa que, se a equipe da Liz não responder,
**o nosso lado não tem instrumento que acuse**. Vai pro grupo como item de
ciência, não como chamado.

---

## FRENTE 1 — a fila inteira, sem filtro de assinatura

Consulta **sem** cláusula de `signature` (o filtro `fast-email:%` foi o que fez a
ronda de 19/08 reportar "0 abertos" existindo quatro).

**94 abertos** — 3 `open` + 91 `investigating`. Por `categoria`: **60 técnicos,
34 de atendimento**. **40 com 7 dias ou mais. 88 com aluno afetado.**

| status | qtd | Δ vs. 12hZ |
|---|---|---|
| fixed | 303 | **+2** |
| investigating | 91 | **+2** |
| ignored | 61 | = |
| aguardando_aluno | 38 | **+2** |
| open | 3 | **−2** |

**Total de abertos: 94 → 94** (com composição diferente). O movimento, um a um:

| # | id | o que houve |
|---|---|---|
| 503 | `b6b777eb` | **fechado `fixed` 12:28Z** — Rita, §3 |
| 507 | `3b7413e0` | **aberto 12:58Z** (`treino-falho`) → `aguardando_aluno`, §1–2 |
| 508 | `4d80c74d` | **aberto 12:59Z** → `aguardando_aluno`, §1 |
| 509 | `60f72a14` | **aberto 13:12Z** (`fast-help`) e **fechado `fixed` 13:27Z** — Vídeo Estúdio cobrou a mais |
| 438 | `0c9eee9f` | `open` → **`investigating`** pela ronda das falhas (2 PRs em produção) |
| 510 | `c9a8ed9a` | **aberto agora por mim**, §1 |

### Os 3 `open`

| # | id | idade | alunos | o que é |
|---|---|---|---|---|
| 492 | `2fd36981` | 1,2 d | 1 | tela de áudio do SGP — botão morto sem dizer por quê |
| 494 | `719c9af6` | 1,1 d | 9 | "realismo" do vídeo clone: corpo parado e olhar morto |
| 510 | `c9a8ed9a` | 0,0 d | 0 | **`infra_storage` sem chave constante (§1)** |

### Os 5 mais velhos da fila inteira

| # | idade | alunos | o que é |
|---|---|---|---|
| **226** | **19,8 d** | 1 | entregamos áudio que o nosso próprio QA reprovou |
| 234 | 18,9 d | 10 | palavra decapitada no meio do áudio |
| 249 | 16,9 d | 1 | e-mail não chegou no aluno (inexistente) |
| 250 | 16,9 d | 1 | e-mail não chegou no aluno (caixa cheia) |
| 254 | 16,8 d | 15 | cobrança em dobro: 5 alunos com 2 assinaturas ao mesmo tempo |

#226 e #234 seguem parados na **mesma decisão de produto** (rigor do QA), no grupo
desde 17/09. Não duplico o que a ronda das falhas já declarou.

### Percepção (ordem de 17/09)

`percepcao_travada.cjs`, controle positivo OK (#310 reencontrado), **495**
incidentes varridos: **2 travados, mais velho parado há 3,1 d** — **#450** (falso
positivo declarado pela sétima ronda seguida) e **#438** (nota de hoje, 0,0 d).
**A classe de percepção real em `open`/`investigating` segue em ZERO.**

Mantenho a recomendação de processo das rondas anteriores: a consulta crua do
§"Como a ronda encontra esses cards" da ordem de 17/09 **deveria apontar para o
`percepcao_travada.cjs`** — o `ilike` infla em 9×.

### Os 38 em `aguardando_aluno` — e uma varredura nova, por causa do §1

Subiu de 36 para 38: são exatamente o **#507** e o **#508**.

Como o §1 mostrou que este balde **absorve disparo sem aparecer**, varri se algum
cartão parado lá está **acumulando em silêncio**: `aguardando_aluno` com
`last_seen_at` nas últimas 48 h devolveu **apenas os dois de hoje**, ambos com
`occurrences = 1`. **Nada acumulando às escondidas agora.**

O balde acumula mesmo (o #312 carrega **19** ocorrências, o #223 sete), mas o
disparo mais recente de todos os outros é de **18/09**. Registro o número porque
o ímã do §1 é **caminho aberto**, não série observada — e a diferença entre as
duas coisas é o que separa alarme de fato.

### Fechados que voltaram a disparar: **0**

**15** fechados têm sinal de vida, **0 vivos nas últimas 48 h**. Na fila inteira,
**1 único** fechado tem `last_seen_at` posterior ao fechamento: o **#8**
(`acf8acd6`, `training:user_dataset`), último disparo **22/08**, 715 h atrás.
Nada recente.

*(Continuidade que vale anotar: esse mesmo `acf8acd6` é a cicatriz citada em
`classify.ts:371` que fez `user_dataset` ganhar tratamento próprio. É o argumento
do #510 escrito pela casa, três meses antes.)*

⚠️ Como sempre, dois limites do detector: **1** fechado não tem `resolved_at`
(`cb4ae39d`) e sobre ele ele **não tem opinião**; e **195 dos 363** são **cegos**
(`last_seen_at` congelado desde o insert) — ali não é "nenhum disparou", é "não dá
pra saber por aqui".

---

## FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`):
**não toquei no não-lido**, que é a fila da Fast.

**Nenhuma carta nova** desde a ronda das 12hZ. A última continua sendo a **uid
693** (Rita), que já tem cartão e já foi respondida (§3).

**Fila da Fast (não-lidos): 0.** Nenhum aluno esperando a primeira resposta.

**Aluno cobrando pela segunda vez:** nenhum novo. **Nenhum chamado aberto nesta
frente.**

⚠️ **Correção de um quase-erro meu, registrada de propósito:** a primeira leitura
dos 25 veio truncada na minha ponta e a uid 693 não apareceu na lista. Eu estava
a um passo de reportar "carta da Rita sumiu da caixa". Fui conferir com
`--de ritabernardino71@gmail.com` e `--ultimos 3`: **a carta está lá**, intacta.
Era corte do meu lado, não da caixa. Achado fantasma morre aqui e não no grupo.

---

## Passo fixo da ronda: reconciliação dos envios (#101)

```
node _frank/ferramentas/2026-09-18_reconciliar_envios_da_pasta.cjs \
     --corte=2026-09-14T14:06:31Z --confirmar
```

```
   917  lidas da pasta "Sent"
   840    já tinham linha
    77    FORA DA JANELA (decisão: --corte)
     0    RECUSADAS (defeito da carta)
     0    DENTRO DA JANELA — escriturável
 ✔ 917 = 917: nenhuma carta sumiu na classificação
```

**Nada a escriturar.** Irmão de leitura independente
(`2026-09-18_enviados_x_tabela.cjs`): **0 carta depois do corte** fora da tabela,
veredito *"o buraco é PASSIVO"*.

Contra a ronda das falhas das ~14h (913/836): saíram **4 cartas** no intervalo e
**as quatro já nasceram registradas** — entre elas a da Rita (§3). O ledger está
sendo escrito na hora.

As **77** anteriores a 14/09 14:06:31Z seguem **sem decisão** — é o que o `--corte`
exclui, e continua sendo decisão de produção (o `cobreDesde` obrigatório em
`contato-tentativas.ts`), não de ronda.

---

## Saúde da produção nas últimas 8 h

| tabela | volume | falhas | preso em estado intermediário |
|---|---|---|---|
| `generations` | **14** `ready` | **0** | **0** |
| `voices` | 3 `ready` | 0 | **0** |
| `studio_scenes` | 21 `ready` | 0 | **0** |
| `training_jobs` | 3 `completed` | **1 `failed`** | — |

A **única** falha das últimas 8 h é o episódio do §1/§2 — e ele **já foi curado
pelo retreino**. A varredura de estado intermediário nas últimas 48 h voltou
**vazia**. Nada preso, nenhuma rajada.

⚠️ **Honestidade de medição:** a primeira consulta pegou **1 `pending`** em
`generations`; a segunda, minutos depois, **0**. Não era travamento, era uma
geração em voo que terminou entre as duas leituras. Escrevo porque um `pending`
solto num relatório vira alarme falso na ronda seguinte.

---

## A decisão que segue pendente e NÃO mudou (§2 da ronda das 12hZ)

Continua sendo o item mais caro da fila, e continua **fora do meu alcance**:
**regra comercial do Lucas (31/08) × weekly do Johnny (14/09, SGP = 30 dias)**.

**Conferido agora pelo fato, não pelo livro:** `to_regclass('public.trial_periods')`
devolve **NULL** — a tabela onde a data-fim moraria **não existe**, então a
**migration 115 segue NÃO APLICADA**. *(Não usei `schema_migrations` como prova:
ele para na 113 e é sabidamente incompleto — as colunas da 118 existem no
`information_schema` sem linha lá. Livro furado não vira evidência.)*

Enquanto isso, a população medida às 10hZ continua existindo: **39 alunos** com o
clone `pronto` e a porta trancada, dentro de uma classe maior de **231 compradores
pagos sem nada**.

---

## O que eu NÃO fiz, de propósito

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito, acesso,
plano nem saldo de ninguém, não apaguei material de ninguém, **não fechei e não
reabri nenhum incidente**, não mudei status de nada (o #510 nasce `open`, que é
abrir, não mexer). Não objetei ao fechamento do #503 porque li a nota e ela se
sustenta — objeção sem leitura é ruído. Não investiguei e não consertei o #510:
medi, nomeei `arquivo:linha` e deixei com o dono da fila. Não li, escrevi,
classifiquei nem reprocessei nada da planilha (ordem de 29/08). Nada no privado
do Johnny (ordem de 31/08).
