# Ronda do Vigia — 15/09/2026, 22hZ (19h BRT)

Sensor (14-A). Varri, medi, **abri 1 chamado** (`#420`/`9d107109`) e **anotei 2**
(`#404`/`7405e5aa` e o próprio `#420`). Não respondi aluno, não escrevi rascunho,
não mexi em crédito, não fechei e não reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha desligada)
lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido, escrito,
classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso desta ronda
vai **no grupo**, e só no grupo.

Janela coberta: **20:10:51Z → 22:16:46Z** (2,10 h). `now()` medido no banco na
abertura = **2026-09-15 22:16:46,048Z**.

A ronda das 20hZ terminou com uma pergunta em aberto, de propósito: o `492cd0de`
estava em voo e ela deixou o desfecho para mim em vez de adivinhar. A resposta é
**morreu** — e a maneira como eu tive que descobrir isso é o achado da ronda.

---

## 1. 🔴 A PERGUNTA DEIXADA PELA RONDA ANTERIOR: MORREU, E A LINHA JÁ NÃO EXISTE

Anotei no `7405e5aa` (`#404`, **investigating**). Não abri chamado: por §3.1 da
ordem de 27/08 a classe já tem cartão aberto, então isto é **ocorrência**.

### 1.1 O `492cd0de` sumiu da tabela — o dinheiro é que provou a morte

A ronda das 20hZ registrou: `492cd0de`, `alexandre@novaconexao.com`, 480p-v3,
áudio 51,22s, teto **2760s**, vencendo **20:19:11Z**, ainda `generating` às
20:17:18Z. Fui buscar o desfecho e **`video_clones` devolve zero linhas para
esse id**.

Quem contasse morte só pela tabela concluiria que **não houve morte nenhuma**.

O dinheiro, que não é apagado, diz o contrário — conferido por `ref_type`,
**nunca por `kind`** (armadilha medida em 20/08):

| | |
|---|---|
| cobrança `video_clone` | **−5.460** às 19:33:12,225Z |
| estorno `video_clone_refund` | **+5.460** às 20:20:15,710Z |
| atraso cobrança → estorno | **2823,5s** |
| teto calculado | **2760s** |
| excesso | **+63,5s** |

Os **+63,5s** caem **dentro da banda de controle positivo** medida nas 8 mortes
visíveis (**+6,3s a +117,9s**). É a **9ª morte** da classe — e é invisível na
tabela de onde o cartão tira os números.

### 1.2 Uma SEGUNDA morte apagada, dentro da minha janela

`775d8739`, `trabalhoiaclone@gmail.com`: cobrado **−3.280** às **20:37:40,756Z**,
estornado **+3.280** às **21:21:14,110Z**, atraso **2613,4s**, **linha também
ausente**. Sem a linha não há `duration_seconds`, então **não dá para calcular o
teto dele**: classifico como **compatível com timeout, não confirmada**. O que é
fato: o aluno esperou **43min33s** e não recebeu vídeo.

### 1.3 O placar honesto da janela

`video_clones` diz **3 `ready` / 0 `failed`**. Pelo dinheiro, foram **DUAS
MORTES**. **"Zero falhas" nesta tabela não significa zero falhas.**

Isto é a confirmação ao vivo do que o `mortes_do_teto.cjs` foi escrito para medir
— e aconteceu **duas vezes em menos de duas horas** depois de a ferramenta ser
escrita. O placar dela hoje: **8 confirmadas + 6 compatíveis = até 14 mortes**
desde 14/09, contra **8** que a tabela sozinha mostra.

### 1.4 O cartão continua sem conseguir contar

`occurrences` = **7**, `last_seen_at` = **14/09 23:53:51Z**, parados, com a classe
tendo matado pelo menos mais 2 na minha janela. Como a ronda das 20hZ já
registrou, a signature foi escrita à mão e **nenhum detector a emite**. Não mexi
nesses campos.

---

## 2. 🟢 O CONSERTO DO `#404` ENTROU NO MEIO DA MINHA JANELA — E EU **NÃO** AFIRMO QUE CUROU

| | |
|---|---|
| `03adea8` parcela fixa **20min → 40min** | **20:45:28Z** |
| merge `324aaf6` (PR #300) | **20:48:42Z** |
| deploy do workflow **SUCCESS** | **20:50:48Z** |

**As duas mortes do §1 foram despachadas ANTES disso** (19:33:12Z e 20:37:40Z),
então as duas carregavam o teto **antigo** e **nenhuma delas é evidência contra o
fix**.

Depois do deploy: **zero** estornos de `video_clone` (consulta por `ref_type`
sobre cobranças com `created_at > 20:48:42Z`), **2 entregues** (`a989ca2f`
20:55:36Z, 3609,628s; `82b3c532` 21:32:52Z, 1403,111s) e **2 em voo**.

**São 1h26 de produção e 4 jobs. Amostra pequena demais para dizer que curou.**
Registro o observável e deixo o veredito para o dono do cartão (14-A).

### 2.1 Uma corroboração independente da premissa do fix

O `#300` chegou à conclusão decompondo o custo fixo dos jobs. Minha janela
produziu a mesma leitura **por outro caminho**: o `0f7fba33` (`welrisson`,
áudio 36,62s) foi despachado **20:20:34Z**, ainda **sob o teto antigo** (2310s), e
**entregou com `elapsed` 2209,494s** — sobra de **100,5s, 4,35% do teto**.

**Não morreu por 100 segundos.** Um sobrevivente raspando o limite dá no mesmo
lugar que a decomposição do `#300`: o teto antigo estava encostado na população
dos sucessos.

⚠️ **Não inflo isso:** o `a989ca2f` (áudio 86,03s, 3609,628s) *caberia* no teto
antigo por 200,4s — ele **não** é prova de que o teto novo era necessário. O caso
que vale é o `0f7fba33`, porque é **anterior** ao fix. Os dois estão no cartão.

---

## 3. 🔴 ABRI O `#420` — O PRIMEIRO PEDIDO DO SGP A FALHAR DESDE QUE ELE SUBSTITUIU A PLANILHA

`9d107109` (`#420`, `vigia:sgp-onboarding-travado-por-falha-de-treino-de-voz`,
**open**, `categoria='tecnico'`).

**RICARDO OLITO** (`ricardoolito@gmail.com`), pedido SGP `7cc29ec9`:

```
21:45:30,945Z  aceite LGPD + enviado
21:45:36,729Z  voice f58a158a criada
21:47:20,454Z  foto_pronta_em          <- as 4 fotos passaram
21:52:15,606Z  voice -> failed         (6min39s de vida)
21:52:16,648Z  pedido -> status falhou
```

**Por que é chamado e não "áudio ruim do aluno":** as três entradas foram
**aprovadas pelo nosso próprio validador** antes de entrar no treino — em
`sgp_pedidos.audios` os três itens têm `status='aprovado'` e `motivos=[]`:
818s + 600s + 278s = **1696s (28min16s)**, que é o que `voices.duration_seconds`
gravou.

**Conferi a armadilha catalogada** (foto entrando na lista de áudio, que já cravou
causa errada duas vezes): listei os arquivos por
`jsonb_array_elements_text(raw_audio_paths)` **antes** de olhar worker/ffmpeg —
são **três `.ogg`**, nenhuma imagem. **A armadilha não se aplica.**

E a mensagem não é recusa de conteúdo (que o sistema sabe escrever — `#128`,
`#186`): é a genérica de **falha técnica**, em que o próprio código diz "não foi
culpa sua".

**Teste de 27/08** — *"se o código/infra estivesse certo, isto não teria
acontecido?"* **Sim.**

**Por que é maior que um aluno:** `select count(*) from sgp_pedidos where erro is
not null` devolve **1**, e é este. O SGP é o caminho que **substituiu a planilha
em produção em 29/08 por ordem do Johnny** — é o onboarding vivo. Este é o
**primeiro estado terminal de fracasso que ele produz**, e o que ele faz nesse
estado vira o desenho de todos os próximos.

**O que eu NÃO sei:** a causa raiz. Por isso `cause='unknown'`. `voices` **não tem
coluna de `raw_error`** (diferente de `video_clones`); o único ponteiro é
`runpod_job_id=b8d3e030-…-e2`, que eu **não** consultei. Não prescrevo a cura.

### 3.1 Dinheiro: conferido, e **não há nada a estornar**

A mensagem gravada diz *"Seus créditos foram devolvidos automaticamente"*.
**Não houve devolução porque NÃO HOUVE COBRANÇA:** `credit_transactions` com
`ref_id = f58a158a…` está **vazia** (zero linhas), e o extrato do aluno depois das
20:00Z também. Conferido por **`ref_type`**, nunca por `kind`.

**O cartão diz isso em letras maiúsculas de propósito: ele NÃO autoriza estorno
nenhum.** Estornar em cima dessa frase seria pagar por uma cobrança que nunca
existiu — a lição do `#152`.

Medi também a classe inteira: **só existe 1 voz `failed` desde 27/08**, e é esta,
com `cobrado=0`. Então **não** afirmo que o caminho de estorno de treino está
morto (o último `voice_train_refund` é de 27/08) — **não houve falha cobrável
para ele exercer**. Instrumento não pode transformar ausência de caso em acusação.

### 3.2 Corrigi meu próprio cartão na mesma ronda

Depois de abrir, conferi `emails_enviados` e **a régua do onboarding disparou
certo**: 5 e-mails para o aluno, todos com `bounce_em` NULL, o último
(*"Não conseguimos finalizar o seu clone — precisamos de você"*) **4,8 segundos**
depois do pedido virar `falhou`.

**O aluno NÃO ficou no silêncio, e o cartão não deve ser lido assim.** Anotei isso
no próprio `#420` em vez de reescrever a abertura — nota se acrescenta, não se
sobrescreve. O que permanece: o defeito técnico em si, e que *"nossa equipe já foi
notificada"* era **falso** — o e-mail avisou o **aluno**; ninguém avisou a
**equipe**, e não havia incidente nenhum até eu abrir este.

---

## 4. 🟠 PARA O JOHNNY — O QUE CONTINUA PRECISANDO DE GENTE

Nada disto é chamado (27/08 §2).

- **Maria Teresa** (`#356`, `#408`, `#412`, `#415`) — último e-mail nosso
  **14:45:21Z**; **nenhum** na minha janela. São **7h31 de silêncio** desde que ela
  pediu, pela segunda vez em três horas, para falar com uma pessoa. Continuo sem
  conseguir dizer se alguém a atendeu (`#101`).
- **Gregório / `karabachiang`** (`#409`) — **5ª ronda sem desfecho.** Notificação
  extrajudicial (CDC art. 39 III e VI, art. 42 § único), Serasa/SPC, R$ 194.
  **Prazo de 5 dias úteis correndo desde 12:03Z**, já **10,2 h** dentro.
  **Zero e-mails nossos para ele hoje.**
- **Valdir** (`#418`) — saiu dos abertos na minha janela: passou a
  **`aguardando_aluno`** às ~19:2xZ. Pagante, **3 pedidos**, e segue sem linha em
  `video_clones`. Não opino sobre a mudança de status (não é minha).
- **Fichas de bounce: 9** (8 `investigating` + 1 `aguardando_aluno`), **igual às
  20hZ e às 18hZ**. O aval de telefone está parado pela **18ª ronda**.
- **`wallanadaphiny`** entra no **16º dia** parada na tela de foto do SGP
  (**15,3 d**).
- **13 vozes em `awaiting_training`**, a mais velha de **14/07 (63,8 dias)**.
  **Não é achado novo e não abri nada:** é exatamente o que o **PR #15**
  (*"Varredura passa a enxergar voices/awaiting_training"*, de 20/08) existe para
  cobrir, e ele segue **aberto** há 26 dias. Registro porque some do placar.

---

## 5. Placar — cada linha reconciliada contra as 20hZ

- **81 abertos** (`investigating` 80 · `open` 1), **sem filtro de assinatura**.
  Mais **24** em `aguardando_aluno` = **105** não-fechados.
- **A aritmética FECHA:** 83 (abertura das 20hZ) **−1** fechado (`#416`)
  **−1** que saiu para `aguardando_aluno` (`#418`) = **81**. ✅
  E `aguardando_aluno` 23 **+1** (`#418`) = **24**. ✅
- **Criados na janela pelo sistema: 0.** O único criado sou **eu** (`#420`,
  22:22:58Z, já fora da janela medida).
- **Fechados na janela: 1** — `#416` (`gabriel.reis2212.pt`, 20:57:57Z, pelo
  Frank). **Não é meu e não opino sobre o fechamento.** Registro só o encaixe: é
  o aluno que a ronda das 20hZ apontou como *"a pergunta dele é atendimento, o que
  está por trás é o `#404`"* — o cartão de atendimento dele fechou, e o `#404`
  continua aberto, que é a divisão correta.
- **O corte atendimento × técnico, nos dois critérios de propósito:**

  | critério | atendimento | técnico |
  |---|---|---|
  | coluna `categoria` (o que o `/admin` mostra) | **42** | **39** |
  | `signature like '%:atend:%'` | **34** | **47** |

  **O número que eu assino é o total: 81.** Os dois cortes discordam em 8 — são
  réguas diferentes, não erro.
- **Fechados que voltaram a disparar: 0.** Varri os **16** `fixed`/`ignored` com
  `last_seen_at` nas últimas 48 h: em **todos** o `last_seen_at` é **anterior** ao
  `resolved_at` (de −0,05 h a −29,35 h). **O furo declarado continua o mesmo de
  onze rondas:** este teste só enxerga classe cuja `signature` **dispare** — e o
  §1 é a prova viva de que existe classe aberta matando aluno **sem** signature,
  e agora **sem nem a linha na tabela**.
- **Produção na janela** (`created_at > 20:10:51Z`, 2,10 h):

  | tabela | prontas | falhas | em voo |
  |---|---|---|---|
  | `generations` | **5** | 0 | 0 |
  | `image_generations` | **8** | 0 | 0 |
  | `video_clones` | **3** | **0 (ver §1.3)** | 2 |
  | `voices` | **2** | **1** | 1 |

  **18 entregas prontas.** A tabela mostra **1 falha**; **pelo dinheiro são 3**
  (a voz do `#420` + as 2 mortes apagadas do §1).
- **Em voo no fim da janela: 3**, nenhum preso —
  `52f242b1` (`video_clones`, `generating`, **48,0 min**),
  `105c9fc1` (`video_clones`, `generating`, **7,1 min**),
  `b93dab50` (`voices`, `training`, **5,5 min**).
  **Deixo para a próxima ronda, como a anterior fez comigo:** o `52f242b1`
  (`patriciapiocoachoficial`, áudio 78,48s) foi despachado **21:36:09Z**, ou seja
  **depois do deploy** — teto novo **4770s (79,5 min)**, contra **3570s (59,5 min)**
  na regra antiga. **É o primeiro job da casa que vai de fato exercer o teto
  novo.** Não afirmo que vai entregar; registro que ele é o teste.
- **E-mails novos de aluno na janela: 0.** Último uid continua **645** (o mesmo das
  20hZ). Nenhum aluno cobrando pela segunda vez apareceu na janela.
- **E-mails que a casa mandou na janela: 6** — 1 `sgp-boas-vindas` (`cpilar0512`,
  21:07:11Z) e **5 `onboarding-aviso` para o `ricardoolito`** (§3.2). Todos com
  `bounce_em` NULL.
- **Não toquei em e-mail não lido.** Leitura com `BODY.PEEK`, critério `SEEN`.
- **Fila do SGP por status:** `dados` **103** · `pronto` **82** · `foto` **62** ·
  `audio` **19** · **`falhou` 1**. Contra as 20hZ (103 · 82 · 63 · 19 · —):
  **`foto` −1** e **`falhou` +1**, que são **a mesma pessoa** (o `#420`).
  **Aviso de instrumento repetido:** este corte é **mais largo** que a linha de
  "parados na tela de foto"; os dois números são verdadeiros e **não devem ser
  comparados entre si**.
- **Casa na janela: 6 commits** na `main` — `03adea8`+`0d5d88c`+`324aaf6` (o teto
  do `#404`, §2), `133e5e4` (HEAD no R2 antes do Whisper, `#296`) e 2 de log de
  ronda (`a6a4457`, `6388ed0`). **Deploys: 2, ambos SUCCESS** (20:30:48Z e
  20:50:48Z).
- **PRs abertos: 24** (eram 23 às 20hZ; **+1**, o **#302**, painel/busca do SGP).
  O mais antigo é o **#9**, de 19/08 — **27,2 dias**.
- **Mais velho aberto:** `#15` (`d3d8d1b2`), de 30/07 — **47,4 dias**.

### 5.1 Dívida de processo que eu encontrei na abertura e resolvi

`main` estava **1 commit à frente do `origin`**: `9c91599`
(`ferramenta(#404): contar morte do teto incluindo a que sumiu da tabela`) — o
`mortes_do_teto.cjs`, escrito pela ronda das 21hZ e **nunca empurrado**. A
ferramenta mais importante que a casa tem para este incidente estava **só na
máquina local**, invisível para qualquer outra ronda. **Empurrei junto com este
log.** Não é chamado (é processo, 27/08 §2), mas é a segunda vez em poucas rondas
que trabalho bom fica preso fora do `origin`.

---

## 6. O que esta ronda diz

A ronda anterior fez a coisa certa: em vez de adivinhar o destino de um job em
voo, deixou a pergunta escrita para a ronda seguinte. Eu fui buscar a resposta e
**não a encontrei no lugar onde ela deveria estar**. A linha tinha sido apagada.

Isso muda o tamanho do problema do `#404`. Até hoje a dúvida era que o cartão
**não somava** as próprias ocorrências, porque a signature foi escrita à mão.
Agora há uma camada a mais: **a evidência apodrece sozinha.** Um aluno morre no
teto, apaga o vídeo do histórico, e a prova da morte desaparece com ela. Sobra só
o rastro do dinheiro, que ninguém estava lendo até a ferramenta de ontem à noite
existir. O `492cd0de` não foi um caso de sorte — ele é a demonstração de que, sem
essa ferramenta, **a pergunta que a ronda das 20hZ fez seria respondida com "não
houve falha nenhuma"**, e isso seria falso.

Vale dizer o outro lado com a mesma clareza, porque hoje ele é bom. O conserto do
teto entrou em produção às 20:50 e **desde então ninguém morreu**. A minha janela
ainda trouxe uma corroboração que o `#300` não tinha: um job que entregou
raspando o teto velho por **cem segundos**, medido de uma direção completamente
diferente da que o fix usou. Duas medições independentes chegando no mesmo lugar é
o tipo de coisa que dá confiança. Mas são 1h26 e 4 jobs, e eu não vou chamar isso
de curado — o `52f242b1` é o primeiro job que vai realmente esticar o teto novo, e
a próxima ronda vai saber.

O chamado que eu abri hoje é de outra natureza, e é o que eu levaria primeiro para
o Johnny. O SGP substituiu a planilha em 29/08 por ordem dele. Desde então, ele
nunca tinha falhado — **hoje falhou pela primeira vez**. E a primeira falha
mostrou três coisas de uma vez: o treino morreu sobre um áudio que **os nossos
próprios gates aprovaram**; a tela disse ao aluno que devolvemos créditos que
**nunca cobramos**; e disse que **a equipe foi notificada** quando não havia
incidente nenhum — a notificação só existiu porque um sensor passou por ali 24
minutos depois, por acaso de horário. O caminho de e-mail funcionou muito bem, e
eu corrigi meu próprio cartão para não deixar o aluno parecer abandonado. Mas o
caminho que avisa **a casa** não funcionou, e esse é o que decide se o segundo
caso vai ser visto.

E fica a linha que amarra as duas metades da ronda: nas duas, o sistema estava
**certo de si e errado**. `video_clones` disse "zero falhas" numa janela com duas
mortes. `voices.error_message` disse "a equipe já foi notificada" sem ninguém do
outro lado. Nenhum dos dois estava mentindo de propósito — os dois estavam
**lendo a régua que tinham**. O trabalho desta fila, cada vez mais, é desconfiar
da régua antes de desconfiar do número.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não estornei nada, **não afirmei cobrança indevida em lugar
nenhum** (no `#404` conferi por `ref_type` e os estornos saíram 1:1; no `#420`
medi que **não houve cobrança** e escrevi no próprio cartão que ele **não
autoriza estorno**), **não afirmei que o caminho de estorno de treino está morto**
(não houve falha cobrável desde 27/08 para exercê-lo), **não afirmei que o fix do
`#404` curou** (1h26 e 4 jobs é amostra pequena), **não afirmei que o `52f242b1`
vai entregar** (deixei para a próxima ronda, como a anterior fez comigo), **não
jurei que o `775d8739` foi timeout** (sem a linha não dá para calcular o teto —
está como "compatível, não confirmada"), **não inflei a corroboração do §2.1**
(disse que o `a989ca2f` caberia no teto antigo e não serve de prova), não
consultei o `runpod_job_id` e por isso **não afirmei causa raiz** do `#420`
(`cause='unknown'`), não abri chamado para classe que já tem cartão aberto
(`#404`), não abri chamado para o que é processo, atendimento ou decisão (§4 e
§5.1), não reclassifiquei cartão de ninguém, não mudei status nem título de card
nenhum, não fundi cartão e não reescrevi signature de ninguém, não opinei sobre o
fechamento do `#416` nem sobre a mudança de status do `#418` (são do Frank), não
uni conta de ninguém, não cancelei assinatura, não fechei incidente, não reabri
incidente, não toquei nos branches STALE (`feat/fix-image-upload-retry`,
`feat/onedrive-401`, `fix/referencia-fronteira-de-frase-por-palavra`,
`feat/fabricar-referencia-fronteira-por-palavra`), não mergeei, não abri PR, não
toquei em `sgp_pedidos` (só li), **não li nem reprocessei nada da planilha**
(ordem 29/08), não liguei nem mandei WhatsApp pra ninguém, e **não toquei em
e-mail não lido**.
