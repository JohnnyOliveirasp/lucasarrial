# 27/08 14h UTC — Ronda do Vigia (sensor, regra 14-A)

`git checkout main && git pull --ff-only origin main` → já estava em dia.
Índice de ordens lido. Valem a `2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐), a
`2026-08-20_REGRA_FINAL_CREDITO.md`, a
`2026-08-20_decisoes_55_trancadas_e_turno_noite.md` (item 2) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **12h UTC**. Janela desta: **2h00** (12h10Z → 14h10Z).

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **10** — 5 `atendimento` (`97`, `99`, `120`, `151`, `153`), 5 `técnicos` (`11`, `52`, `143`, `146`, `149`) |
| **Fora do placar por status, e isso é um achado** | **1** — o `150` está em **`fixing`**. Não é fechado e não aparece em `status in ('open','investigating')`. §4 |
| Variação vs. 12h | eram 13; **−4 fechados** na janela, **+1 aberto por mim** |
| **Abertos por mim nesta ronda** | **1** — `153` (chamado de aluno fechado 1s depois de ele escrever) |
| **Anotados por mim** | **4** — `152` (retratação minha), `126` (caso vivo), `151` (Elizete), `150` (alerta de instrumentação) |
| Fechados na janela | **4** — `147` (12:32Z), `148` (12:33Z), `152` (12:33Z), `126` (12:35Z). **Não reabri nenhum** |
| Fora da contagem | **6** em `aguardando_aluno` — inalterado |
| Fechados que voltaram a disparar | **1** (`acf8acd6` / #8), última ocorrência **115h** atrás — **0 vivos** em 48h. ⚠️ mas o detector é cego para 5 outros: §1 |
| E-mails novos desde as 12h | **1** (uid 331, Cássio Fialho, 12:29:50Z) |
| Fila de não-lidos da Fast | **0** (`--fila`). **Não toquei em não-lido** |
| Chat do app (`help_messages`) | **0 mensagens** entre 12:10Z e 14:10Z |
| Gerações na janela | **14 em 4h, 14 `ready`**, 0 `failed`, 0 presa |
| Vozes em 24h | **13, todas `ready`**. 1 `rejected_too_short` em 25/08 (mensagem correta, nada cobrado) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**.

---

## 1. O achado da ronda: o e-mail do aluno abre e fecha o chamado no mesmo segundo

**Incidente `153` aberto.**

Medi os fechados em que `resolved_at` vem **depois** de `last_seen_at` por menos
de 5 minutos — isto é, a mensagem do aluno e o fechamento acontecem juntos.
**Cinco casos, todos de aluno, todos assinados `carol (entregue ao time)`:**

| chamado | aluno | ocorrências | intervalo disparo → fecho |
|---|---|---|---|
| `#126` | cassiofialho@hotmail.com | 5 | **1,466 s** (27/08 12:35:22Z) |
| `#82` | lucianodepinho@gmail.com | 9 | **1,246 s** (27/08 11:15:23Z) |
| `#145` | sandradiniz86@outlook.com | 1 | 0,798 s (26/08 19:55:14Z) |
| `#141` | luzielisam@hotmail.com | 3 | 1,601 s (26/08 02:36:06Z) |
| `#130` | itamar.vanzin@gmail.com | 3 | 0,875 s (25/08 14:55:19Z) |

O 6º da lista (`#60`, 252s, `agent`) é de outra natureza e não entra.

Cada e-mail novo do aluno **re-dispara** o chamado e a automação **re-fecha** em
~1,5s com a mesma frase de entrega ao WhatsApp. O contador sobe (`#82` em 9,
`#126` em 5) e o chamado nunca fica aberto tempo nenhum. **Quem olha a fila vê
ZERO. O aluno está escrevendo.**

**Não estou chamando o fechamento de errado** — a regra do Johnny de 24/08
("chamado não fica aberto no limbo") autoriza fechar entregando ao time. O que
está medido é a consequência: não existe sinal de que a bola voltou do lado
humano, e o aluno reaparece.

**Já aconteceu e já foi fechado nesta mesma classe:** `#95` — *"Luciano de Pinho
respondeu a pergunta que fechou o `07af5758` e ficou sem dono"*, `fixed`. Voltou.

**Ponto cego do detector, medido.** O
`2026-08-20_fechados_que_disparam.cjs` procura `last_seen_at > resolved_at +
2min` e por isso **não pega nenhum dos cinco** — aqui o fechamento vem *depois*
do disparo, não antes. Ele reportou hoje "1 de 129, 0 vivos", e está certo dentro
da régua dele. Os 5 estão fora da conta. Mesma família do `#67`.

---

## 2. O caso vivo: o Cássio pede um ser humano pela sexta vez

**Anotado no `126`. Não reabri.**

E-mail **uid 331**, 27/08 **12:29:50Z**, assunto `RES: RES: RES: RES: RES: RES:
(sem assunto)`. Palavras dele:

> *"Favor direcionar essa mensagem para um ser humano (…) Aguardo um contato de
> um ser humano, pois as respostas estão vindas de um agente de IA."*

O `#126` disparou com essa mensagem às 12:35:20Z e foi fechado às 12:35:22Z. A
`resolution_note` registra **cinco** entregas ao WhatsApp: 24/08 20:10, 24/08
20:40, 26/08 22:55, 26/08 23:05 e 27/08 09:35.

**O fato que ele contesta, medido para quem for responder.** A Fast escreveu
*"se você não finalizou nenhuma compra da FastCloner, não há dado de pagamento
armazenado aqui"*; ele respondeu *"Eu completei sim a compra com o cartão de
crédito dentro do fastcloner"*. `pagou_de_verdade.cjs` às 14h00Z: **NUNCA PAGOU**,
1 assinatura, único registro Hotmart **R$ 0 COMPLETE em 19/08**. O dado sustenta
a posição da plataforma — **mas ela já foi repetida por bot seis vezes e não
resolveu**. Ele também pediu exclusão de acesso/cartão, o que puxa lado de LGPD
e não é meu. O técnico dele (`#149`, vídeo de exemplo do site sem áudio) segue
`investigating`, 12h.

---

## 3. Eu errei na ronda das 12h e estou me retratando: a Adriana foi cobrada certo

**Anotado no `152`. O fechamento do `claude` está CERTO no dinheiro.**

O §3 do meu log das 12h afirmou como *"fato, sem ambiguidade"* que a Adriana
pagou 3.600 por 2 cenas não entregues. **Estava errado.** Refiz agora pelo
casamento `ref_id` ↔ `debit_ref` — o método que o próprio `_estornos.cjs` manda
usar e que eu não usei:

O projeto `5edde2a6` tem **QUATRO** cenas, não duas.

| cena | status | kie_task_id | debit_ref |
|---|---|---|---|
| `ad0a0207` | **ready** | `ac7e8a8a…` | `5424c37f0b809c045ef45099dfd391b4` |
| `5e03a9fa` | **ready** | `7c9d9faf…` | `23b18ea314bab33f5dd75d831acb50a6` |
| `f96749fb` | failed (moderação) | **NULL** | **NULL** |
| `7279d92b` | failed (moderação) | **NULL** | **NULL** |

Os dois débitos de −1800 de 26/08 18:05 têm `ref_id` `5424c37f…` e `23b18ea3…`
— são exatamente os `debit_ref` das duas cenas **entregues**. As duas bloqueadas
pela moderação nunca receberam `taskId` e **nunca foram cobradas**, como diz a
nota de fechamento. **Não falta estorno nenhum.**

**Meu erro, registrado pra ninguém repetir:** casei débito com **projeto** em vez
de casar com o **objeto cobrado**. A armadilha do `#113` tem uma irmã — além de
*"nunca por `kind`"*, vale *"nunca por projeto"*: some por `ref_id`.

**O que sobra, e não é dinheiro.** Os dois projetos continuam parados, medido
14h05Z: `5edde2a6` (adrianacdiniz) e `e27acd41` (fabio@matsubaraodontologia),
ambos `status=audio_ready`, `scenes_status=failed`, `montage_status=idle`,
criados 26/08 18h01 — **20 horas**. O fix do redo (`85fcd3e`) está em produção e
permite destravar, mas **não age sozinho sobre o que já travou**. O Fábio Fiuza
foi destravado à mão *e* avisado (respondeu hoje agradecendo, uid 329); **estes
dois não foram destravados nem avisados**, e nenhum dos dois escreveu pro
suporte. Estão travados em silêncio.

---

## 4. O `150` está em `fixing` e some de qualquer contagem por `open/investigating`

**Anotado no `150`.** Contei 9 abertos e ele ficou de fora — não é fechado, está
sendo trabalhado. `select status, count(*)`: `fixed` 100, `ignored` 29,
`aguardando_aluno` 6, `investigating` 6, `open` 3, **`fixing` 1**.

É a mesma família do erro que em 19/08 reportou "0 abertos" existindo 4: ali o
filtro cego era a assinatura, aqui é o conjunto de status. **Quem contar fila
precisa incluir `fixing`**, ou o chamado que está sendo trabalhado vira invisível
no placar.

---

## 5. Elizete (`151`): o relógio andou, ninguém respondeu — e ela voltou a usar

Medido 14h05Z: acesso **ATIVO até 31/08** (4 dias), créditos 60.958, voz
*"Elizete S Castro"* 35min segue `ready`, treinada 26/08 21:19Z, cobrada 10.000,
**nenhum estorno** (conferido por `ref_type`, nunca por `kind`).

**Fato novo desta janela:** ela gerou um **áudio novo às 12:47Z**, `ready`.
Depois de dizer às 10:42Z que a voz *"ficou horrível"* e perguntar como excluir,
ela **continuou tentando usar a plataforma**. Não desistiu — e também não foi
respondida. As três promessas de escalação de hoje seguem sem contrapartida
humana. Continua precisando de **ouvido humano**: julgamento, não consulta.

---

## 6. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500` (`BODY.PEEK`, só `SEEN`).
Fila de não-lidos: **0**. **Não toquei em não-lido.**

**1 e-mail novo** desde as 12h: **uid 331**, Cássio Fialho, 12:29:50Z — §2.

Varri os sinais da rotina (*"a equipe vai verificar"*, *"vou chamar alguém da
equipe"*, aluno cobrando 2ª vez). O único caso é o do Cássio, que **já tem
chamado** (`#126` + `#149`) — por isso **não abri duplicata**; o que abri (`153`)
é o defeito de processo, não o caso dele.

`help_messages`: **zero** mensagens entre 12:10Z e 14:10Z, então a lista de 7
alunos com promessa sem chamado (medida às 12h) **não cresceu**.

---

## 7. O que precisa de decisão do Johnny

1. **`153` — chamado de aluno que abre e fecha no mesmo segundo.** 5 alunos, o
   Cássio na 6ª cobrança pedindo um humano. Ou alguém confirma que a bola voltou
   do WhatsApp, ou a fila continua lendo zero com aluno escrevendo. Decisão de
   processo.
2. **Cássio (`126`/`149`)** — quer um humano, contesta o dado do cartão (o dado
   nos dá razão: R$ 0 COMPLETE, nunca pagou) e pediu exclusão de acesso/cartão
   (**LGPD**).
3. **Elizete (`151`)** — voz `ready` que ela reprovou, 3 promessas ontem/hoje,
   **acesso vence 31/08**, e ela voltou a gerar áudio hoje. Precisa de ouvido
   humano nesta semana.
4. **Adriana e Fabio Matsubara** — **não é dinheiro** (§3, me retratei), mas os
   dois projetos estão travados há **20h** e ninguém os avisou de que precisam
   refazer a cena. O Fábio Fiuza foi curado e avisado; estes dois não.
5. **`#99` Luciano** — 93,5h; prazo da cobrança de 26/08 vai até **02/09**.
6. **`#120` Sandra** — reembolso com CDC, prazo que ela alega: **30/08**.
7. **`#143`** — o turno da noite segue não ligado, **28h**.
8. **`#11`** — **879h** (36 dias). É o mais velho da fila.

---

## 8. Objeções e limites desta ronda

- **Retratação, não objeção:** o `152` (§3). O fechamento do `claude` está certo
  e **o errado era eu**. Registrei o método que me enganou.
- **Objeção nenhuma a fechamento nesta janela.** Os 4 fechados (`147`, `148`,
  `152`, `126`) — os três primeiros com causa raiz e commit citados; o `126` é o
  caso do §1/§2, e ali a anotação é sobre o *padrão*, não sobre o mérito.
- O `153` cobre um **defeito de processo**, não o caso individual do Cássio, que
  já tem dois chamados. Evitei duplicar a fila de quem trabalha.
- O detector de "fechado que voltou a disparar" segue com o ponto cego do §1, e
  agora ele está **medido com denominador**, não suposto.
- A contagem de abertos depende do conjunto de status escolhido (§4). Declarei
  os dois números: **10** por `open/investigating`, **11** incluindo `fixing`.

## 9. O que eu NÃO fiz

Não fechei nem reabri incidente nenhum. Não respondi aluno nem escrevi rascunho,
**inclusive o Cássio, a Elizete, a Adriana e o Fabio Matsubara**, que são os
nomes concretos desta ronda; a medição foi para o chamado e para o Johnny, não
para eles. Não toquei em crédito, acesso, assinatura ou estorno — só **conferi**,
sempre por `ref_type` e casando por `ref_id`, nunca por `kind` nem por projeto.
Não toquei em e-mail não-lido (fila da Fast: 0). Não mexi em cron nem em ordem.
Não apliquei migration. Não escrevi código — li `incidents`, `credit_transactions`,
`studio_scenes`, `studio_projects`, `help_messages`, `generations`, `voices` e
parei aí. Não rodei GPU, whisper, `--curar` nem `--medir`. Não destravei projeto
nem refiz cena.
