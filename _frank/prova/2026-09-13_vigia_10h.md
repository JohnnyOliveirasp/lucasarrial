# Ronda do Vigia — 13/09/2026, 10hZ (07h BRT)

Sensor (14-A). Varri, medi, **abri 0** e **anotei 5** (`#373`, `#371`, `#369`,
`#370`, `#372`). Não respondi aluno, não escrevi rascunho, não mexi em crédito,
não fechei e não reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido,
escrito, classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**.

Janela coberta: **00:11:02Z → 10:11:16Z** (10 h — o turno da noite não roda,
ordem de 27/08). `now()` medido no banco = **2026-09-13 10:11:16Z**.

---

## 0. A dívida de instrumento de 10 rondas ACABOU — e ela era o furo

Dez rondas seguidas eu registrei *"continuo sem conseguir ler o Mission Board
desta máquina (`DB_ENCRYPTION_KEY` ausente)"*. **Nesta ronda eu consegui**,
rodando de `/home/johnny/FrankClaw`:

```
node dist/mission-cli.js list | result <id>
```

E a primeira coisa que ele mostrou é o achado da ronda. Eu estava cego
justamente para a camada onde o trabalho da casa vai morrer.

## 1. 🔴 O CONSERTO É DECLARADO NO CARTÃO, MORRE NO BOARD, E NINGUÉM É AVISADO

**Placar do board: 126 cards — 88 `completed`, 29 `failed`, 9 `cancelled`.**
Dos 38 ruins, **24** carregam o mesmo erro genérico
*"Worker não entregou saída — provável incapacidade pra essa tarefa ou erro
silencioso. **NÃO foi feito**"* e **5** são *"Timed out after 10 minutes"*.

Cruzei o board com a fila (`/tmp/board_x_fila.cjs`, leitura pura). **6
incidentes ainda abertos têm, como conserto declarado, um card que morreu:**

| incidente | estado | card | estado do card |
|---|---|---|---|
| `#369` mastroianni | investigating | `0b8e8e5e` @main | **failed** (timeout 10 min) |
| `#370` Fast cega a anexo | investigating | `b51e23a7` @coder | **failed** (não entregou saída) |
| `#371` alice / gate | investigating | `d0e246ef` @main · `243f4d4a` @coder · `e04bab57` @coder | **failed · failed · cancelled** |
| `#372` rastro do gate | investigating | `243f4d4a` @coder | **failed** (mas `2278158b` completou → PR #258) |
| `#234` palavra decapitada | investigating | `fa2c2f41` · `46f318a2` · `886aa9cb` | failed · cancelled · cancelled (há outros `completed`) |
| `#373` clone "travado" | **fixed** | `25a82beb` @coder | **failed** |

O cartão continua dizendo `investigating` e a nota continua dizendo *"cartão X
aberto pro coder"*. Lido da fila, parece trabalho em andamento. **Não está.**

## 2. 🔴 E o veredito "NÃO foi feito" é FALSO — provado pelo disco

O card `25a82beb` (*"#373: aluno preso atrás da própria geração"*) rodou
**12/09 21:27 → 21:43** (hora local, EDT) e foi marcado **failed / "NÃO foi
feito"**. A árvore de trabalho da `main` tem, **não commitada**, exatamente a
feature que ele pedia, com `mtime` **dentro da janela de execução dele**:

| mtime | arquivo |
|---|---|
| 21:35 | `scripts/109_video_clones_status_cancelado.sql` (novo) |
| 21:36 | `frontend/src/lib/support/failure-alert.ts` (M) |
| 21:36 | `frontend/src/lib/video-clone/runpod.ts` (M) |
| 21:37 | `frontend/src/app/api/v1/video-clone/[id]/cancel/route.ts` (**novo, 142 linhas**) |
| 21:39 | `frontend/src/components/video-clone/clone-studio.tsx` (M) |
| 21:41 | `frontend/src/lib/video-clone/cancelar-politica.ts` + `.test.ts` (novos) |
| — | `frontend/messages/{pt-BR,en,es}.json` (M) |

O trabalho **foi feito**. O que falhou foi o worker **devolver a saída** — e o
runner traduz "não devolveu" como "não fez", que é uma conclusão que ele não
mediu. O resultado é código pronto **sem commit, sem branch, sem PR e sem card
vivo**, em cima da árvore de trabalho da `main`. **Um `git checkout .` apaga.**
A ronda das 02hZ já tinha visto os arquivos (§8) e registrado como "achado
lateral de outro agente"; o que esta ronda acrescenta é **por que** eles estão
órfãos.

**Não toquei em nenhum desses arquivos, não commitei e não abri PR.**

### 2.1 De quebra: o número dos "20%" está 12× inflado

A nota de fechamento do `#373` reprovou (com razão) o teto de 1 h dizendo
*"4 de 20 (20%) passaram de 60 min"* — amostra de 20 clones, toda da madrugada.
O `cancelar-politica.ts` órfão traz medição própria e muito mais larga
(`_Bugs/2026-09-13_medir_entrega_video_clone.cjs`): **762 clones `ready` em 14
dias**, `created_at` contra o `LastModified` do MP4 no R2 —

> mediana **13,8 min** · p90 **34,2 min** · p99 **77,0 min** · máximo **97,0 min**
> acima de 1 h: **12 de 762 (1,6%)** — e as 12 **entregaram**.

A **direção não muda**: o teto de 1 h continua reprovado, porque mataria 12
entregas boas. Mas quem citar os "20%" daqui pra frente cita um número 12×
inflado. Anotado no próprio `#373`. **O teto segue reprovado pelas duas
medições** — isso não é objeção ao mérito, é correção de número.

## 3. 🔴 DOIS ALUNOS ESPERANDO AGORA, OS DOIS COM O CARD MORTO

**Alice Silveira (`alicearnaldo@gmail.com`, `#371`) — 9h18 sem resposta.**
Respondeu **00:53Z** (uid 603): *"Mas vocês mesmo disseram que a foto estava
correta. Eu já fiz a produção correta. Não é necessário outras fotos."*
Continua barrada no Vídeo Clone. Não-lidos no INBOX = **0**, ou seja o e-mail
dela **foi lido** e nada saiu. Os **três** cards do caso dela morreram (§1).

E isto **fecha uma ressalva que era minha**: na ronda das 00hZ eu escrevi que
não abriria as imagens dela e que o veredito dependia de olho humano. O olho
humano veio — a casa escreveu a ela às 00:27Z que **a mensagem do gate estava
errada**, ela não estava olhando para baixo, o que o verificador pega é o
**queixo recolhido**. Falso positivo **de mensagem**, confirmado pela casa, não
por mim. Meu `#372` não dependia disso e segue de pé.

**Mastroianni (`contato@mastroiannioliveira.com.br`, `#369`) — 10h14 sem a
entrega prometida.** A casa escreveu 23:55Z: *"vou pedir pra equipe subir o
vídeo no Drive e te mandar o link aqui"*; ele respondeu *"No aguardo"* 23:57Z
(uid 602). Nada saiu. Foram **8 e-mails dele seguidos** (uid 595→602) em 3 h —
não é aluno que sumiu, é aluno em cima. O card `0b8e8e5e` (*medir a voz, gerar
o teste prometido e responder*) **estourou timeout de 10 min**. O card irmão
`6ac5864c` completou e virou o **PR #257, aberto e não mergeado** — a
ferramenta para cumprir a promessa existe, parada num PR.

Os dois são **atendimento** (ordem 27/08 §2): vão pro **grupo**, não viram
chamado.

## 4. ⏳ Evelyn vence HOJE — 13,7 h

`garantia_na_fila.cjs` às 10:2xZ. **`evelyn.cheida@gmail.com` (`#341`): RESTAM
13,7 h** (janela 07/09 → **14/09 00:00Z**). É a decisão com relógio desta ronda.

Os outros 3 do bloco de 48 h: `mateusanalistadenegocios@gmail.com`,
`souzaprado@gmail.com` e **`drbrunoa@gmail.com`** — os três com 37,7 h. O
drbrunoa **entrou no bloco nesta janela** (eram 3 às 00hZ, são 4 agora).

**"Perderam a janela": 8, sem mudança.** Reconciliado nome a nome contra as
00hZ: `#254` ×2, `#282`, `#299`, `#306`, `#309`, `#350`, `#363` — os mesmos.
**Ninguém novo perdeu janela nesta janela.** O rodrigo (`#306`/`#363`,
R$ 1.194,90) continua perdido e continua sem resposta.

## 5. As checagens da ordem de 27/08 — e por que eu abri ZERO

| candidato | veredito | checagem |
|---|---|---|
| card morre sem avisar a fila (§1, §2) | **NÃO abri** | é **processo** pela tabela do §2 da ordem de 27/08 (*"ordem que não foi executada"*) → relatório + grupo, sem chamado. E o alvo é o runner do board (FrankClaw), não o produto FastCloner |
| código do cancelamento órfão na árvore (§2) | **NÃO abri** | mesma coisa: a causa é o card marcado failed, que é processo. Anotado no `#373` com `arquivo:mtime` pra não sumir |
| alice sem resposta (§3) | **NÃO abri** | **atendimento** → grupo. Já tem o `#371` |
| mastroianni sem a entrega (§3) | **NÃO abri** | **atendimento** → grupo. Já tem o `#369` |
| número "20%" errado (§2.1) | **NÃO abri** | **objeção** → nota no próprio `#373` (14-A) |
| `#370` com o conserto morto | **NÃO abri** | **reincidência do cartão que existe** → nota nele |
| clone de 62 min da ronda passada | **encerrado** | não era travado: entregou aos 95,3 min. Eu tinha registrado como "item de olho sem baseline" e **estava certo em não chamar de travado** |
| voz `d5ae61ac` `uploading` há 18 min | **NÃO abri** | em voo normal, aluno subindo agora |
| `marcelopersonalthe32` no 🚨 da varredura | **NÃO abri** | já é o `#128` (`ignored`), sem fato novo |

**Dinheiro:** nesta ronda **não fiz nenhuma conta de dinheiro**, não casei
`ref_id` com nada e não afirmei que alguém foi ou deixou de ser estornado.

## 6. Conferi o trabalho do Frank na janela

**`#313` fechado (`fixed`, 00:45Z)** — comprador de curso ganhava a plataforma
vitalícia. PR #242, commit `36886fa`, merge `b97e9f47`, com prova de produção na
nota. **Sem objeção.**

**`#373` aberto pela Fast 00:51Z e fechado 01:28Z** — 37 min. A medição dele
está certa e é boa: ele **recusou** executar a ação que o recado pedia (marcar
failed + estornar) porque mediu que o vídeo tinha **entregue 89 s depois da
reclamação**, e refutou o teto de 1 h com número. Isso teria custado o vídeo do
aluno e crédito. **Elogio registrado, e a objeção do §2 é a outra metade da
mesma história.**

**PRs abertos: 37** (36 às 00hZ + **#258** `feat/face-gate-rastro`, criado
00:52Z). `--limit 200` declarado.
O **PR #249** (`fix(sgp)`) segue **aberto e não mergeado — nona ronda seguida**.
Não re-medi a exposição dele e não afirmo número sobre ela.

**Commits em `origin/main` na janela: 0.** Dez horas, zero commit.

## 7. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0** (`--fila`, `EXAMINE`);
- **1 e-mail novo na janela**: uid **603**, a Alice, 00:53Z (§3);
- **aluno cobrando pela 2ª vez: 1** — a Alice, e ela é o item mais urgente do
  relatório, antes de qualquer número;
- **promessa vencida: 1** — o vídeo do mastroianni no Drive (§3);
- nenhum *"já pedi ajuda várias vezes"* novo.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). **Não toquei em não-lido.**
⚠️ Nota de instrumento: `ler_caixa.cjs --caixa "INBOX.Sent"` **ignora o
`--caixa` em silêncio** e devolve o INBOX assim mesmo. Quem for conferir
"Enviados" com essa flag vai ler a caixa errada achando que leu a certa.

## 8. Geração na janela: limpa

`created_at > 00:11:02Z`, quatro tabelas: **38 entregas prontas, ZERO falha** —
`image_generations` 22 · `video_clones` 7 · `generations` 7 · `voices` 2.

**Em voo neste instante: 1** — a voz `d5ae61ac` (`ericb.malzone@gmail.com`),
`uploading` há 18 min. Normal.
**`video_clones` `pending`/`generating` no sistema inteiro: 0.** Isto confirma
por medição minha o que o `#373` afirmou.
`training_jobs`: **1 linha obsoleta** (`ebf5cc56`, voz `f4b9b0f2` já `ready`) —
escrituração, ninguém esperando. Sem fato novo, não re-anotei.

## 9. Placar

- **79 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **44 de atendimento**, **35 técnicos**. Mais **10** em `aguardando_aluno`.
- **Delta contra os 80 das 00hZ, reconciliado inteiro:** `#313` fechado
  (técnico −1) · `#373` criado **e** fechado na janela (líquido 0). Atendimento
  **44** (sem mudança); técnicos 36−1 = **35**; total **79**.
- **Fechados que voltaram a disparar: 0.** Recorte de 72 h com
  `last_seen_at > resolved_at`: **vazio**.
- `garantia_na_fila.cjs`: **8** perderam a janela · **4** vencem em 48 h · **0**
  na renovação.
- **PRs abertos: 37.** Commits na janela: **0**.
- **Mission Board: 126 cards — 88 completed, 29 failed, 9 cancelled** (23% não
  entregaram). **Primeira medição desta série.**
- **Calibragem do Vigia** (ordem 27/08 §5): dos **35** cartões com
  `reported_by='vigia'` desde 27/08 — 23 `fixed`, 7 `investigating`, 4
  `ignored`, 1 `aguardando_aluno`. **`ignored` = 11,4%**, igual às 00hZ (não
  abri nada nesta ronda). Alvo ≤ 10%; linha de base 21%.

⚠️ O rótulo *"SEM LINHA NO NOSSO BANCO p/ este e-mail"* que a ferramenta imprime
continua **NÃO sendo veredito de pagamento** — é ausência de linha local.
**Décima quinta ronda** registrando.

## 10. Nota de instrumento — o que eu quase errei hoje

**Quase reportei "14 notas de incidente citam cartões que não existem".** Eu
tinha varrido `resolution_note` + `agent_notes` procurando `cartao <hex8>` e
conferido contra `incidents.id` — e 14 não batiam, incluindo o `25a82beb` do
`#373`. A manchete pronta era *"o Frank fecha cartão citando conserto que nunca
foi criado"*. **Estava errada, e era uma acusação grave.**

O que me segurou foi abrir o **contexto** de cada citação antes de escrever. O
próprio texto do `#234` dizia a resposta: *"achado que só vive em cartão do
**Mission Board** ninguém lê na ronda seguinte"*. Aqueles hex não são
incidentes: são cards de **outro sistema**, que eu não conseguia ler havia 10
rondas. Procurar um id do board na tabela de incidentes sempre daria zero.

É a mesma família do `#100` e do `#152` que a ordem de 27/08 mediu, e a mesma
que me mordeu ontem com as fotos de upload: **concluir de uma tabela sem
perguntar o que produziu aquela linha** — aqui, sem perguntar de que sistema
aquele identificador era. A defesa que funcionou foi barata: ler 300 caracteres
em volta da citação antes de afirmar o que ela significa.

E a ironia útil: foi essa mesma checagem que **me fez conseguir ler o board** —
e aí o achado real apareceu, maior do que o falso. O falso era "um cartão foi
citado e não existe". O verdadeiro é "**vinte e nove cards morreram e a fila não
sabe**".

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não reclassifiquei
cartão de ninguém, não abri chamado para o que é processo ou atendimento, não
fiz nenhuma conta de dinheiro, **não commitei, não mergeei e não toquei nos
arquivos órfãos do cancelamento**, não mergeei nenhum branch marcado STALE, não
acusei o Frank de citar cartão inexistente antes de conferir de que sistema o
identificador era, e não toquei em e-mail não lido.
