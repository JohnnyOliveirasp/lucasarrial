# Vigia — ronda de 04/09/2026, 00hZ (21h BRT de 03/09)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 2** (`702cc916`/#226,
`f8587cef`/#234). Não fechei, não reabri, não mudei status de nada, não respondi aluno, não escrevi
rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Medição feita às **00:12Z**. Ronda de vigia anterior: `22h` (22:10Z).

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 🟡 NOVO — a caixa do suporte está em SILÊNCIO TOTAL há 20 horas

**Zero e-mail de entrada desde 03/09 04:10Z.** O último que chegou foi o `uid 429` (Vinícius
Lorandi). Agora são 00:12Z de 04/09.

Base de comparação, contando o que entrou no INBOX por dia:

| dia | e-mails que entraram |
|---|---|
| 30/08 | 9 |
| 31/08 | 15 |
| 01/09 | 21 |
| 02/09 | 10 |
| **03/09** | **1** (às 04:10Z, e nada depois) |

**Não é desvio de pasta, isso eu já eliminei:** `Spam` = 0 mensagens, `Archive` = 0, `Trash` = 1.
Nada foi parar em outro lugar. E a fila de não-lidos da Fast é **0**, então também não é caso de
e-mail que chegou e ninguém abriu. Do lado de dentro, a caixa simplesmente não recebeu nada.

**Não consigo distinguir as duas hipóteses daqui, e não vou fingir que consigo:**
1. **Dia calmo de verdade.** Os três fios pesados que produziam quase todo o volume (Alana, Katia,
   Vinícius) se encerraram nas últimas 24h — a Alana disse "dia 07 te chamo", o Vinícius recebeu o
   desfecho. Sem eles, a caixa cai muito. Isso é plausível.
2. **A entrada de e-mail quebrou** e tem aluno escrevendo sem ninguém do outro lado. Isso seria
   grave e invisível: não gera erro, não gera incidente, não gera alarme. Só silêncio.

**O que distingue:** um e-mail de teste mandado de fora para `suporte@`. **Não fiz** — é ação para
o mundo externo e não é minha alçada; preciso do teu "pode".

Um sinal que reforça a dúvida, sem provar nada: o **chat do app continua produzindo normalmente**
(`#243`, `#244`, `#245` nasceram por volta das 21:15Z, há ~3h). Os dois canais caíram juntos seria
coincidência; um parado e outro vivo é exatamente o formato que uma entrada quebrada teria.

### 0.2 🔴 O `#226` desacelerou, mas **o motivo pode ser só a noite** — não leia como melhora

Uma entrega nova abaixo da régua desde as 22:10Z (`8c44488f`, 22:57:23Z, **0,800** contra 0,85).
Isso derruba o ritmo de ~2/hora para ~0,5/hora, e eu **não vou reportar isso como recuperação**: no
mesmo intervalo o volume total despencou — houve **1 única geração nas últimas 2 horas**. Menos
entrega ruim porque há menos entrega, não porque o defeito diminuiu.

A proporção, que é o número honesto, **não melhorou**: das 42 entregas pós-build, **22 têm
`exhausted > 0` (52,4%)** e **8 estão abaixo da régua**.

**A pergunta segue a mesma da ronda passada e ainda sem resposta: o `#226` continua na fila normal
ou vira prioridade?** O defeito de `loop.py:341-344` está inteiro, e o `3bc1535` não cobre este ramo.

### 0.3 Migration 102 — **décima quinta** ronda com a mesma pergunta

Conferido agora pelo banco: `select tgname from pg_trigger where not tgisinternal and
tgrelid='incidents'::regclass` → `[]`. `scripts/102_incidents_resolved_guard.sql` segue **não
aplicada**.

### 0.4 PRs abertos: **28**, parados

Igual à ronda das 22:10Z. Os `#41`/`#42` (teto de 2MB) estão no **décimo sexto dia** — e vale
lembrar que esse é exatamente o defeito que respondeu à Alana "seu anexo é grande demais" 2 minutos
depois dela mandar o print que a equipe tinha pedido.

---

## 1. O que eu ABRI

**Nenhum chamado.** Não entrou e-mail novo (§0.1), e nenhum achado desta ronda sobrevive ao teste
da ordem de 27/08 como chamado novo: os dois de série são ocorrência dos próprios `#226`/`#234`, e
o silêncio da caixa é **sinal não confirmado**, não defeito provado — vai como decisão, não como
chamado.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`702cc916` / `#226`** — série pós-build 8 → 9, nova `8c44488f` (0,800), e o aviso de que a
  queda de ritmo é volume de madrugada, não conserto. §4.
- **`f8587cef` / `#234`** — `coverage_rescue` segue **n=0** em 42 entregas, ~11h50 depois do build.
  §5.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de 19/08).

**5 abertos — 1 de atendimento, 4 técnicos.** Todos em `investigating`; `open` = 0.

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **372,0h** | katiasalvador32 | palavra "VOCÊ" no s34 — `categoria=atendimento` |
| `3ca22d47` `#222` | 56,3h | 5 no título, **14 medidos** | compra órfã casa só por e-mail |
| `702cc916` `#226` | 54,3h | **161 alunos** na série | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 31,9h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 27,5h | *(irrespondível)* | relato manual sem e-mail e sem anexo |

**Movimento desde as 22:10Z: saiu 1, entrou nenhum.** O `#245` (igorlramalho, realismo do Vídeo
Clone) **não foi fechado** — passou para `aguardando_aluno`, ou seja continua com gente do outro
lado, só saiu da coluna de abertos. **Não reescalei e não mudei status.**

**12 em `aguardando_aluno`** (eram 11): `#99` (**271,4h = 11,3 dias**), `#172` (152,6h), `#206`
(83,1h), `#207` (82,2h), `#214` (74,3h), `#216` (59,8h), `#218` (57,2h), `#224` (55,3h), `#229`
(51,4h), `#243` (2,9h), `#244` (2,9h), `#245` (2,7h).

**Placar honesto da ronda: 5 abertos + 12 esperando aluno = 17 cards com gente do outro lado.**
O total não mudou; o card só trocou de coluna.

### 3-A. Fechado que voltou a disparar: **nenhum**

`status in ('fixed','ignored','resolved') and last_seen_at > resolved_at` devolve **1 linha só**, o
`#8` (*"Treino de voz: áudio insuficiente"*, 57 ocorrências), parado desde **22/08** — 13 dias.
Nenhuma classe fechada está disparando hoje.

Conferi também os 15 fechados com `last_seen_at` das últimas 48h: **todos** têm `last_seen_at`
anterior ao `resolved_at`, que é o padrão normal de quem foi fechado depois de parar. O `#213`
(reembolso por insatisfação), fechado hoje às 20:08Z pelo suporte@, tem último disparo às 19:51Z —
antes do fechamento. Está correto.

### 3-B. Crédito: **zero estorno pendente**

61 gerações `failed` na base inteira: **11 nunca cobradas**, **50 cobradas e estornadas**,
**0 cobradas sem estorno**. A mais recente tem **149,9h**. Confere com as medições das 20:11Z e
22:10Z. Conferido por `ref_type` (`generation` × `generation_refund`), **nunca por `kind`**
(armadilha de 20/08). Não toquei em crédito.

### 3-C. Filas e sinal de vida

`generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. `voices/awaiting_training` = **17** (igual às 22:10Z e
20:11Z) — não é chamado, é o estado em que a bola está com o aluno; o PR **#15**, que daria dono a
essa lista, está **OPEN desde 20/08**.

**O pipeline está vivo, mas devagar:** 72 gerações nas últimas 24h, e apenas **1 nas últimas 2h**.
A última foi às **22:57:23Z**, 1h15 antes desta linha. Isso é compatível com madrugada (21h BRT em
diante) e não configura fila travada — mas é o motivo pelo qual o §0.2 não pode ser lido como
melhora do `#226`.

---

## 4. O `#226`: 8 → 9 na série pós-build

Abaixo da régua depois do build do `3bc1535` (`success`, 12:23:08Z). Todas `ready`, todas entregues
ao aluno, **nenhuma passou pelo resgate**:

| geração | quando | visto | régua | `exhausted` | `regens` |
|---|---|---|---|---|---|
| `5d5b7866` | 13:08:05Z | 0,667 | 0,85 | 4 | 14 |
| `84e3a400` | 14:44:26Z | 0,667 | 0,85 | 1 | 16 |
| `79309199` | 16:10:22Z | 0,800 | 0,85 | 1 | 2 |
| `49996c3e` | 19:52:45Z | 0,727 | 0,85 | 1 | 16 | ← **apagada**, medida na ronda anterior |
| `e87eaac2` | 20:42:57Z | **0,448** | 0,85 | 2 | 7 | ← pior de toda a série |
| `b7355097` | 20:50:29Z | 0,552 | 0,85 | 2 | 8 |
| `2876a712` | 21:05:48Z | 0,625 | 0,85 | 1 | 5 |
| `c6ec2bda` | 21:39:44Z | 0,750 | 0,85 | 2 | 13 |
| **`8c44488f`** | **22:57:23Z** | **0,800** | 0,85 | 1 | 8 | ← **nova desta ronda** |

**9 no total** (8 vivas na tabela + 1 apagada e medida). Mantenho a contagem com a apagada pelo
motivo registrado na ronda das 22:10Z: contar só o que está vivo **subnotifica o `#226`**, porque o
DELETE de histórico pelo aluno faz a série encolher sem nada ter sido consertado.

**Controle, agora com 51 + 56 entregas — o discriminador segue impecável:** desde 02/09 02:32Z,
régua por linha:

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 51 | 30 | **0 (0,0%)** | 0,857 |
| `exhausted > 0` | 56 | 30 | **22 (39,3%)** | **0,333** |

**52 entregas seguidas com `exhausted = 0` e nenhuma exceção.** O `exhausted` continua sendo o
melhor preditor único que temos de entrega reprovada.

⚠️ **Armadilha de instrumento desta ronda, e ela quase virou um "0 abertos" do `#226`.**
A minha primeira consulta filtrou `(qa->>'coverage_min')::numeric < 0.85` e devolveu **lista
vazia** — leitura que seria "nenhuma entrega ruim desde o build", contradizendo frontalmente a ronda
anterior. **A consulta é que estava cega:** no jsonb do `qa`, `coverage_min` é a **RÉGUA** (vale
0,85 em toda linha, então `< 0,85` nunca é verdade); o valor **medido** mora em
`coverage_min_visto`. Imprimi o `qa` cru antes de acreditar no zero, como manda a ordem, e o número
certo apareceu. Registro porque essa consulta, aceita sem conferência, fecharia o `#226` como
resolvido.

---

## 5. O `#234`: o resgate de cobertura continua sem existir na prática

Pós-build (42 entregas, ~11h50): **12 entregas com palavra decapitada entregue ao aluno (28,6%)**,
**20 palavras** no total, **10 alunos**.

E o campo `coverage_rescue` está ausente em **42 de 42** entregas pós-build — `n = 0`. É a terceira
ronda seguida com o mesmo número. O caminho de resgate não roda nem uma vez, então não há como ele
ter deixado de resgatar por qualidade: **ele não é chamado**. Anotado no `#234`, sem mudar status.

---

## 6. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500` e `--ultimos 60 --corpo 0`. Leitura pura (`EXAMINE` +
`BODY.PEEK`, só `SEEN`). **Não toquei em nenhum não-lido** — a fila da Fast está em **0**.

**Nenhum e-mail novo desde a ronda anterior.** O último da caixa é o `uid 429`, de 03/09 04:10Z —
ver §0.1, que é o achado desta ronda.

Reconferi os três fios que ainda aparecem nos últimos 25, e **nenhum precisa de chamado novo**:

- **Alana Pinho** (`uid 424`–`428`): fechou o próprio fio em 02/09 20:55Z com *"Dia 07 te chamo
  aqui e vc me diz no q já avançaram"*. Cancelamento resolvido, créditos intactos, incidentes
  `#223`/`#235` já fechados. **Tem data marcada com o aluno: 07/09.** Não é chamado, é compromisso
  — registro aqui para não cair no esquecimento.
- **Katia Salvador** (`uid 421`–`423`): *"É no segundo 34 do primeiro vídeo. A palavra VOCÊ, no
  final da frase está cortada."* É exatamente o `#47`, aberto e **na sua 372ª hora**. Já tem
  incidente, não abri outro.
- **Vinícius Lorandi** (`uid 429`): *"prefiro cancelar todos os cursos"* — respondido, incidente
  `#240` fechado hoje às 12:39Z. Não reabri.

Não encontrei **nenhum** aluno cobrando pela segunda vez sem incidente, e nenhuma promessa de
"a equipe vai verificar" pendurada sem dono.

---

## 7. O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei nem reabri incidente, não
mandei e-mail de teste para a caixa, não mergeei PR, não apliquei a migration 102, e não li,
escrevi, classifiquei nem reprocessei nada da planilha (ordem de 29/08).
