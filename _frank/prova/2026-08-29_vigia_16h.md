# 29/08 16h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → trouxe **2 commits**; a árvore
ficou em `23fa33b`. Índice de ordens lido antes de tocar em qualquer coisa. Ordem
vigente do assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C). Também valem
`2026-08-20_dono_da_fila_e_fila_zerada.md` (14-A), a `2026-08-20_REGRA_FINAL_CREDITO.md`
(ENCERRADA — não reabri) e a rotina `e-mail_tecnico.md`.

Ronda anterior: **14h UTC**. Esta: **16h UTC**. Intervalo de 2h, no ritmo.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **6** — **1 de aluno** (`99`), **5 técnicos** (`6`, `180`, `184`, `186`, `187`) |
| **Abertos por mim nesta ronda** | **1** — o `187` (§1) |
| **Anotados por mim** | **2** — o `6` e o `186` (§2) |
| **Fechados que voltaram a disparar** | **0** — só o `#8` de sempre, último disparo 22/08, há 6,9 dias |
| Fechados na janela | **3** — `181` (claude, 14:46Z), `185` (frank, 15:49Z), `183` (frank, 15:53Z) |
| Abertos por outros na janela | **2** — `6` voltou a disparar (detector) e `186` (detector, 16:09Z) |
| Merges na main na janela | **4** — PRs `118`, `121`, `114`, `122` |
| E-mails novos de aluno desde as 14h | **0** — o mais recente do INBOX segue o uid `373` (Johnathan, 04:15Z) |
| E-mails **enviados** na janela | **6** — uids `317`–`322` |
| Aluno cobrando 2ª vez ou mais | **0 novos** |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **111** — 108 `ready`, **2 `failed`**, 1 `pending` de 2 min (em voo) |
| Gerações presas (qualquer idade) | **0** |
| Estorno das falhas | **OK** — conferido por `ref_id`, sem filtrar por `kind` (§2) |
| Acesso vivo + crédito + nenhuma voz pronta | **3** (era 2) — a nova é a Francielly, voz `training` **há 3 min**, em voo |
| `training_jobs` preso >30min | **1** — o órfão de 25/08, voz já `ready`, escrituração pendente |
| Vozes com `lora_path` de outro dono | **2 de 975** — as duas são cópias de teste do admin (§2) |
| PRs abertos | **24** |
| Árvore de trabalho suja | **NÃO** — `git status --porcelain` vazio, HEAD == `origin/main` |
| Incidente que eu fechei, reabri ou reclassifiquei | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura** e **pelo complemento**
(`status not in ('fixed','ignored')`). Conferido pelo `group by status` da tabela
inteira: **141 `fixed`, 32 `ignored`, 3 `investigating`, 3 `open`** = 179 linhas.
Bate com as 177 das 14h + o `186` (detector) + o `187` (meu).

---

## 1. ABRI o `187` — a varredura do dia é cega a quem falhou ANTES de existir a voz

**Única coisa que abri nesta ronda.** Não é de dinheiro (§3 abaixo diz por quê), mas
segue o §3 da 14-C do mesmo jeito: `arquivo:linha`, tamanho medido, e as 3 checagens.

`_frank/ferramentas/varredura_travados.cjs:210`:

```js
if (minhas.length === 0) continue; // nunca tentou
```

A seção **"ACESSO VIVO, COM CRÉDITO E SEM NENHUMA VOZ PRONTA"** (linha 223) aplica
antes os dois filtros certos — acesso vivo (`:207`) e crédito ≥ `CUSTO_TREINO`
(`:208`) — e então descarta quem não tem **nenhuma** linha em `voices`, assumindo que
zero voz = nunca tentou.

**A premissa é falsa.** O registro de que o aluno tentou não é a linha de voz: é
`onboarding_runs`. Quando o import falha na etapa `audio`, ele falha **antes** de criar
a voz. O aluno tentou, está parado, e não tem linha nenhuma pra ser vista.

**O caso medido hoje:** `luanmarcal.com@gmail.com`, uid `6284e233`, conta 28/08 15:35Z.
`onboarding_runs` `4e61084e`, `criado_em` **05:42:44Z**, `ok=false`,
`etapa_falha='audio'`. `access_until` 29/09, saldo **98.425**. **Zero** linhas em
`voices`. Passa nos filtros `:207` e `:208` e cai no `:210`. Parado há **~10,5h**,
invisível na varredura das 10h, das 12h, das 14h **e desta**.

**Tamanho, medido e sem inflar:** 28 perfis no histórico têm `onboarding_run ok=false`
e zero voz. Aplicando os **próprios** filtros da seção (acesso vivo + crédito ≥ 10.000),
sobra **1**: o Luan. Os outros 27 caem fora por `access_until` nulo e/ou crédito abaixo
do custo — seriam excluídos de qualquer jeito. O custo **vivo** do ponto cego hoje é
**1 aluno**; o defeito é estrutural e volta em todo import que falhar cedo.

**A linhagem — e por que isto NÃO é o `#70` de novo:** o `#70` (fixed, 21/08) era *"a
classe pagante sem voz era medida só por `status=failed`"* — vozes num estado que o
detector não olhava. O conserto dele produziu a regra escrita no `:153` do próprio
arquivo (*"não enumere os estados ruins, afirme o estado bom"*), que virou o `:211`.
**Aquele conserto está certo e continua valendo.** Este chamado é a camada seguinte,
que aquele conserto não podia alcançar: não é um status novo, é a **ausência** da
linha. **Não reabri o `#70`.**

**As 3 checagens do §3, feitas antes de abrir:**

1. **Já existe?** Fila **aberta e fechada** por `varredura|ponto cego|sem voz|onboarding_runs`:
   5 chamados (`138`, `135`, `86`, `70`, `60`), **nenhum aberto**. Único parente é o
   `70`, fechado, mecânica diferente. Classe nova.
2. **Já foi corrigido?** `git log` de `varredura_travados.cjs`: último commit `c3ba274`
   (lista de estorno, do `#185`) — não toca nisto. Dos 24 PRs abertos, **só 1** mexe no
   arquivo, o **PR 15** (`feat/varredura-awaiting-training`); **li o diff**: ele adiciona
   `voices/awaiting_training` como alvo "aviso" e o recorte de pagante parado, e continua
   partindo de **linha de voz**. Não cobre zero-voz. Ninguém vai refazer trabalho pronto.
3. **Dinheiro?** **NÃO.** O chamado não acusa cobrança nem pede estorno. Para o registro,
   conferido **por `ref_id` e sem filtrar por `kind`**: o Luan tem 4 linhas em
   `credit_transactions` somando **+98.425**; o `-1.575` é o negativo de onboarding
   **autorizado** pelo Johnny em 21/08 (`debitCreditsOnboarding`, migration 88). Nada a
   devolver. **Não toquei em crédito.**

**O que eu NÃO afirmo:** (a) que o link do Drive do Luan é defeito nosso — **não é**,
re-medido às 14h: o arquivo devolve a página de login do Google (~905 kB) duas vezes com
2h de distância, e **não** é o `Quota exceeded` do `184` (2.009 B); a mensagem que ele
recebeu está correta, e o defeito deste chamado é só a **cegueira do instrumento**.
(b) que alguém além do Luan foi prejudicado hoje — medido: 1. (c) que a correção é óbvia;
deixei uma sugestão no chamado, não uma ordem.

## 2. ANOTEI o `6` e o `186` — os dois que dispararam nesta janela são o Johnny testando

Os dois eventos novos da janela caíram na conta de teste do dono, e **nenhum aluno foi
atingido**. Anotei em cada chamado, sem fechar nem reabrir.

**`#6` (open desde 21/07, disparou 15:50:30Z).** Geração `069563db`, user `6400fc05` =
`johnny.oliveirasp@gmail.com`, voz `2ba315cd` *"Lucas Arrial (cópia teste)"*. O lora que
falhou o download está em `92e895af/32fafd6b/lora.safetensors` — pasta do
`lucas.m.arrial@gmail.com`, ou seja **voz copiada cujo lora aponta pra pasta de outro
dono**. **Dinheiro, conferido por `ref_id` e sem filtrar por `kind`: a `069563db` não tem
NENHUMA linha em `credit_transactions`** — não houve débito, logo **não há estorno a
fazer**. Deixei isso escrito no chamado justamente pra ninguém "consertar" um estorno que
não existe (armadilha do `#152`). A outra falha das 24h é a `086970cd` de 28/08 18:16Z
(`executionTimeout`, pré-fix), já liquidada por `ref_id` (−400 `generation` / +400
`generation_refund`, com `kind=extra_purchase`, o `kind` que engana quem confere por ele).

**O que fecha o caso:** o **PR `122`** (*"contingência — não cobrar por voz com LoRA morto
e reenviar falha transitória"*) foi mergeado às **16:10:10Z**, 20 min depois da falha, e
há uma geração `270f5463` `pending` às 16:10:08Z na mesma voz. É **reprodução dirigida**,
não incidente de produção solto.

**`#186` (aberto pelo detector às 16:09:48Z).** Voz `97413a01` *"Pri Webinar (teste)"*, do
mesmo `johnny.oliveirasp@gmail.com`, mesma sessão. **Dimensionei a classe na base inteira:
975 vozes têm `lora_path`; 2 apontam pra pasta de outro dono; as 2 são estas cópias de
teste do admin. Exposição de aluno: ZERO.**

**A data estranha do `186` não é bug novo, e eu conferi antes de acusar.** O `created_at`
é 16:09:48Z de hoje mas o `first/last_seen_at` é **2026-06-15 23:41** — o detector data o
chamado pelo `created_at` da linha de origem. Varri a tabela: **25 incidentes** têm
`last_seen_at` anterior ao `created_at`, desde julho, vários do detector automático. É
comportamento conhecido, **não** uma regressão de hoje. **Não abri chamado por isso** —
teria sido um falso positivo.

## 3. Uma hipótese minha que eu derrubei antes de reportar

Johnathan (`johnathan.ppires@gmail.com`) está com `access_until` **nulo**, saldo
**−1.575** e **zero vozes** — e recebeu por escrito, às 13:50Z, a promessa *"nós mesmos
rodamos o processamento da sua voz"*. Ia reportar isso como risco: o retreino esbarraria
no gate de crédito e a promessa quebraria.

**Fui abrir o código antes de afirmar, e a hipótese está ERRADA.**
`frontend/src/lib/onboarding/treino.ts:11-16` diz, em comentário do próprio autor:
*"aqui NÃO há trava de saldo. O treino dispara mesmo com o aluno a zero e ele fica
NEGATIVO até assinar (`debitCreditsOnboarding`, migration 88)… Só o onboarding tem essa
exceção"*. A trava de saldo está no `start-training/route.ts` (caminho do app), **não** no
caminho do onboarding, que é o que vai ser usado. **Não há risco, e o `-1.575` é o negativo
autorizado.**

Registro porque é exatamente o modo de falha que a 14-C mede em mim (`#100`: acusar
dinheiro lendo o ledger sem abrir quem cobra). Desta vez o arquivo foi aberto **antes**, e
o resultado foi não reportar nada. Para o registro: `pagou_de_verdade.cjs` diz que o
Johnathan **nunca pagou** (0 assinaturas, 0 `PURCHASE_APPROVED`) — o que também não é
chamado, é dado.

## 4. Fechados que voltaram a disparar

**Zero novos.** A única linha com `last_seen_at > resolved_at` na tabela inteira continua
sendo o **`#8`** ("Treino de voz: áudio insuficiente/sem fala limpa"), fechado em 09/08,
último disparo **22/08 19:26** — 6,9 dias, mais uma janela sem se mexer. Segue como
registro, sem ação, e **não reabri**.

## 5. Caixa — nenhum e-mail novo de aluno, não-lido intocado

`ler_caixa.cjs --ultimos 25 --corpo 2500`: leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em não-lido.** Fila de não-lidos da Fast: **0**.

**Nenhum e-mail novo de aluno desde as 14h** — o mais recente do INBOX segue o uid `373`
(Johnathan, 04:15Z). Procurei os sinais de escalação silenciosa (*"a equipe vai
verificar"*, *"vou chamar alguém da equipe"*, aluno cobrando 2ª vez): **nenhum caso novo
sem chamado**. Antes de descartar qualquer um, conferi se já havia resposta enviada.

Nos Enviados, tudo trabalho do dono da fila:

- `317` 14:00Z **Fernando** — compra aprovada, falta criar conta. Fluxo normal.
- `318`/`319`/`320`/`322` — **Carolina** (`jmo.usa.007`), onboarding SGP correndo normal
  (código, "começamos a preparar", "sua plataforma está pronta").
- `321` 14:45Z **Max** (`manomax58`) — *"o Vídeo História mudou de nome"*. É o `181`,
  respondido e **fechado** na janela pelo claude.

**Marcelo** (`marcelopersonalthe32@gmail.com`, voz `f6f82819` multi-locutor, **19 dias**)
**continua sem retorno** — é a única das 3 da lista "sem voz pronta" que está de fato
parada esperando gente. O PR **50** é o que endereça o caso. **Kelinn** foi respondida às
13:55Z e a **Francielly** entrou na lista hoje com a voz em `training` **há 3 minutos**
(em voo, não presa).

**Luciano (`99`)**, para o registro: recebeu o retorno técnico às 10:47Z (uid `314`) e
**não respondeu** até agora. Última fala dele, de 28/08 20:53Z, é *"Fiz um agora com uma
foto do jeito que você sugeriu. Mesmo problema."* O chamado segue `investigating`; fechar
ou não é do dono, **não mexi**.

## 6. O que eu NÃO fiz

- Não fechei, não reabri e não reclassifiquei **nenhum** incidente.
- Abri **um só** chamado, o `187`, e ele **não é de dinheiro** — disse isso explicitamente
  em vez de deixar ambíguo.
- **Derrubei uma hipótese minha** (§3) abrindo o código antes de reportar, e o resultado
  foi **não** reportar. Se tivesse se sustentado, estaria aqui.
- **Não abri chamado** pela data invertida do `186` — conferi e é comportamento conhecido
  em 25 linhas desde julho.
- Não respondi aluno e **não escrevi rascunho de e-mail** (armadilha do lucvila, 20/08).
- Não toquei em crédito, acesso ou assinatura.
- Não toquei em e-mail não-lido.
- Não mexi em código.
