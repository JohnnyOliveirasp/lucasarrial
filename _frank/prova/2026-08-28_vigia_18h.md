# 28/08 18h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → fast-forward, árvore
limpa. Índice de ordens lido antes de tocar em qualquer coisa. Ordem vigente do
assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C) — só erro de SISTEMA
vira chamado. Também valem `2026-08-20_dono_da_fila_e_fila_zerada.md`, a
`2026-08-20_REGRA_FINAL_CREDITO.md` (ENCERRADA — não reabri) e a rotina
`e-mail_tecnico.md`.

Ronda anterior: **16h UTC**. Esta: **18h UTC**. Janela de 2h, no ritmo.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **6** — 2 `atendimento` (`120`, `153`), 4 `técnicos` (`11`, `168`, `174`, `176`) |
| Variação vs. 16h | **−3** (eram 9). O Frank fechou bastante: `fixed` foi de 117 → **125** |
| **Abertos por mim nesta ronda** | **0** |
| **Anotados por mim** | **1** — o `165` (§1) |
| **Fechados que voltaram a disparar** | **0** — nenhum incidente `fixed`/`ignored` tem `last_seen_at > resolved_at` na janela (§3) |
| Total de incidentes na tabela | **168** (era 166 às 16h) |
| Fora da contagem | 5 em `aguardando_aluno` (eram 10) |
| E-mails novos desde as 16h | **1** — uid 355 (§2) |
| Aluno sem resposta na caixa | **1** — José Ricardo, respondeu 14:36 BRT e o caso dele não está em fila nenhuma (§2) |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **99** — 99 `ready`, **0 `failed`** (§4) |
| Gerações/cenas presas >30min | **0 / 0** |
| `training_jobs` preso >30min | **1** — o órfão `ebf5cc56`, já medido no `108`, 3d16h em `queued` |
| Estornos em 12h | **0** (`ref_type='generation_refund'`, nunca por `kind`) |
| `awaiting_training` +24h | **16 vozes / 15 alunos / 45,6 dias** — **estável pela 4ª janela** |
| PRs abertos | **21** |
| Incidente que eu fechei, reabri ou reclassifiquei | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura** e **pelo complemento**
(`status not in ('fixed','ignored','aguardando_aluno')`), como fixei às 16h.
Nesta janela o status `fixing` sumiu (0 linhas), mas mantive o complemento —
é ele que me protege do próximo status novo. `status` distintos hoje:
`fixed` 125, `ignored` 32, `aguardando_aluno` 5, `investigating` 5, `open` 1.

---

## 1. O conserto do `165` FUNCIONA — e não alcança nenhum aluno que está reclamando

Isto é a coisa mais importante da ronda, e é metade boa notícia.

O `165` ("a régua do QA de ritmo nunca é gravada") foi fechado hoje 17:52Z.
Fui medir se pegou. **Pegou:** as vozes que terminaram treino depois de
~16:36Z estão com `voices.speech_rate_wps` preenchido — 3.11, 3.00, 2.26,
3.37 wps. Antes disso, tudo NULL. Conserto em produção e provado.

O que **não** saiu junto foi o backfill, e é aí que dói:

| recorte | preenchido | NULL |
|---|---|---|
| todas as vozes | 13 | **1.019** (de 1.032) |
| vozes que **geraram áudio nos últimos 7 dias** (as vivas) | 13 | **246** (de 259) |

**95% das gerações reais continuam caindo no fallback** que remede a
referência com whisper a cada job. E os alunos que estão reclamando de ritmo
e timbre **agora** — Victor (`176`), Carol (`170`), Telma (`156`), Vinicius
(`162`), Giovanna (`133`), José Ricardo (`169`/`171`) — todos têm voz treinada
**antes** do conserto. O fix não encosta em nenhum deles.

Não abri chamado por isto, pelas duas primeiras checagens do §3 da 14-C: a
classe **já existe** (é o próprio `165`) e **já foi corrigida** há 20 minutos.
Anotei no `165` (nota 3 do array, status intocado, `--confirmar` só depois do
ensaio) e trago aqui. **Se vale um backfill é decisão do Frank**, não minha.

## 2. José Ricardo respondeu e não está em fila nenhuma

Único e-mail novo da janela (uid 355, 14:36 BRT). Ele testou a voz
"José Ricardo — Voz Natural V2" depois da correção: melhorou a continuidade,
mas o timbre segue agudo/artificial e a cadência não é a dele. **Aceitou o
retreino sem custo** que a gente ofereceu e pediu as orientações de gravação.

O problema não é o que ele escreveu, é onde a mensagem caiu: os três chamados
dele (`169`, `171`, `172`) foram **todos fechados hoje**, entre 16:15 e 17:49Z.
A resposta dele chegou 17:36Z. **O caso continua e não tem chamado aberto.**

Duas pendências concretas nossas:
1. A correção do HeyGen entrou em produção ~16:20Z e a gente prometeu por
   e-mail *"te aviso assim que estiver no ar"*. Ele **não foi avisado** — já
   são ~2h.
2. Ele aceitou o retreino e está esperando as orientações técnicas.

Pela 14-C isto é **atendimento**, não chamado. Não abri incidente, não
respondi o aluno e não escrevi rascunho. Tentei o caminho previsto
(`avisar_grupo.cjs`) e **falhou** — ver §5.

## 3. Nada fechado voltou a disparar

Régua: `status in ('fixed','ignored') and last_seen_at > resolved_at`, sem
filtro de assinatura e sem recorte de tempo. Devolve **1 linha só**, e é velha:

- `#8` "Treino de voz: áudio insuficiente/sem fala limpa" — fechado 09/08,
  disparou pela última vez **22/08** (13 dias depois). Sem ocorrência nova há
  6 dias. Fica na anotação, não é achado desta ronda.

Conferi também o `8d370ef5` (`#34`) que a minha ordem manda vigiar: `fixed`,
`last_seen_at` 19/08 **anterior** ao `resolved_at` de 20/08. Parou de verdade.

A classe do `153` (chamado de aluno fechado 1s depois da mensagem dele, por
"carol (entregue ao time)") segue em **7 ocorrências** — as mesmas 7 de 16h,
**nenhuma nova nesta janela**. A última foi o `#173`, às 16:00Z. O campo
`occurrences` do `153` ainda marca 5; a subcontagem já está anotada lá de 16h,
não repeti.

## 4. As 6 falhas sumiram da janela — e o detector não está cego

Gerações em 24h: **99, todas `ready`, zero `failed`**. Antes de acreditar no
zero (armadilha da minha ordem), abri a janela pra 48h: aparecem **6 `failed`**,
todas entre 27/08 17:01 e 17:30Z — **as mesmas 6** já medidas e já estornadas
(6 de 6, 10.286 créditos, conferido por `ref_type`). Ou seja, o zero de 24h é
real e é só o envelhecimento daquelas 6, não detector quebrado.

## 5. Não consegui avisar o grupo — o canal não existe nesta máquina

`avisar_grupo.cjs` morreu com `WAHA_API_URL/WAHA_API_KEY ausentes nesta
máquina` (a WAHA só escuta em 127.0.0.1 no servidor). Montei a mensagem em
`--seco` e ela está correta, mas **não foi entregue**.

Consequência honesta: o §2 do José Ricardo **não tem dono** até alguém ler
isto. Não fico com a impressão de que avisei — não avisei. Pela 14-C isso é
**processo**, então vai como 1 linha aqui e 1 mensagem no Telegram, sem
chamado. Se o Johnny quiser que o Vigia consiga avisar o grupo sozinho, o
caminho é ter as credenciais da WAHA nesta máquina ou rodar a ronda no
servidor.

---

## O que eu não fiz (14-A / 14-C)

Não abri chamado nenhum, não fechei, não reabri, não reclassifiquei. Não
respondi aluno nem escrevi rascunho de e-mail. Não toquei em crédito, acesso
ou assinatura. Não toquei em e-mail não-lido. A única escrita da ronda foi a
nota no `165`, com ensaio antes.
