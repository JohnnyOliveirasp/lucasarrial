# Ronda do Vigia — 10/09/2026, 22hZ (19h BRT)

Sensor (14-A). Varri, medi e **anotei 4** (`b6486347`, `4ce9f365`, `ce6e157d`,
`b32af5ff`). **Abri 0 chamados**, e o §3 diz por quê em cada caso. Não respondi
aluno, não escrevi rascunho, não mexi em crédito, não fechei e **não reabri nada**.

Repo em `main`, `pull --ff-only` (já em dia). `_frank/ordens/README.md` e
`_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.
Ordem de **29/08** respeitada: nada da planilha lido, escrito, classificado ou
reprocessado. Ordem de canal de **31/08**: o aviso desta ronda vai **no grupo**.

---

## Primeiro, o que eu pedi às 20h e JÁ TEM DONO

As duas decisões que eu marquei como 🔴 na ronda passada foram executadas pela
ronda das falhas entre 21h e 22hZ. Registro porque cobrar duas vezes o que já
foi feito é como a fila passa a mentir.

| pedido das 20h | desfecho, conferido por mim |
|---|---|
| **Igor Moraes travado 4h30** | **RESOLVIDO.** `#346`/`#345` `fixed` 21:51/21:52Z. Conferi o e-mail no **Enviados, uid 1671** ("Suas 4 fotos ja estao no pedido") — existe mesmo, não é promessa de nota. |
| **`#344`: o aluno diz que continua errado** | **RESPONDIDO** 20:26Z, Enviados **uid 1665**. |

E o defeito de código do Igor **não foi varrido pra debaixo do tapete**: virou
cartão próprio, o **`#349`** (`3dbd2bf0`, dedup por dHash 8x8 barrando fotos
distintas), aberto 21:53Z com `arquivo:linha` e medição. Separar "o caso do
aluno" de "a porta quebrada" é exatamente o desenho certo.

---

## O achado da ronda: a classe RITMO tem mecanismo medido, e ele é uma DECISÃO PARADA

Três cartões da mesma classe dispararam hoje (`#344` Gustavo, `#348` Ellen,
`#47` Katia com 10 ocorrências). Fui atrás do mecanismo e ele fecha inteiro.

### O caminho, medido em produção

1. **Tela** — `voice-generator.tsx:284`: a opção **"Natural"** vale `null` e
   **nasce selecionada** (`pauseMs = useState<number|null>(null)`,
   `aria-pressed={pauseMs === opt.v}`). Linha 192: `null` ⟹ **não envia**
   `chunk_silence_ms`.
2. **Backend** — `generate/route.ts:249-256`: sem o campo no body, cai em
   `voice.tts_silence_ms`; se esse for nulo, o param não é setado.
3. **Voz** — `voices.tts_silence_ms` é **NULO em 1.260 de 1.260** vozes.
   Contagem sem filtro nenhum.
4. **Worker** — `tts_settings.py:236`:
   `silence_ms=_do_job_ou_env(inp,"chunk_silence_ms","TTS_CHUNK_SILENCE_MS","0")`.
   A env **não aparece em nenhum outro ponto do repo** (grep em
   `py/ts/yml/json/sh/env/Dockerfile`: **1 única ocorrência**, a do consumo).

> **"Natural" na tela significa ZERO ms entre pedaços**, com `chunk_max=160` chars.

⚠️ **Ressalva honesta:** não consigo ler a env do RunPod daqui. Se ela estiver
setada no console, o default não é 0. O que eu afirmo é o que está **no repo e
no banco** — não afirmo o valor em produção.

**Alcance:** desde 01/09, **717 gerações**; só **59 (8,2 %)** mandaram
`chunk_silence_ms`. **658 (91,8 %)** saíram pelo fallback.

### Por que isso NÃO virou chamado (§3 da ordem de 27/08)

- **"já existe?"** SIM — `#47`, `#344` e `#348` abertos na classe. Reincidência.
- **"já foi corrigido?"** O conserto **existiu e foi DESLIGADO DE PROPÓSITO**.
  O commit **`080dd74`** (21/08, *"a voz nasce com a PAUSA de quem gravou — 749
  de 750 tinham zero"*) foi desativado em **24/08 por ORDEM DO JOHNNY** (caso
  Kessuly), `finalize-training.ts:394-404`. **Não é defeito do sistema: é
  decisão.** Por 27/08, decisão vai pro Johnny, **sem chamado**.
- **Dinheiro?** não envolve.

Quase abri chamado de "o default de pausa é 0". O §3.2 me pegou — de novo.

### O controle que isola a causa

Descarta as duas explicações preguiçosas ("worker velho", "medição quebrada"):

| vozes treinadas **depois** de `080dd74` | **401** |
|---|---|
| com `speech_rate_wps` gravado | **242** |
| com `tts_silence_ms` gravado | **0** |

Mesmo `finalize-training`, mesma janela: **a velocidade grava, a pausa não.** O
caminho de escrita está vivo; só a pausa está desligada. O worker **continua
medindo** `reference_pause_ms` e apenas loga
(`voice.train.pacing_measured_not_applied`, `finalize-training.ts:405-409`).
**Não li esses valores:** não há tabela de log no banco (só `payment_events` e
`runpod_spend_log`) — ficam no stdout do pm2.

### A observação que vai pro Johnny

O teste de 24/08 mudou **DUAS variáveis juntas**: pausa de 1,5–1,9s **e
crossfade 0**. O próprio comentário registra que **com crossfade 60 a borda suja
do pedaço fica mascarada pelo pedaço seguinte**. A alavanca da tela (Média 250 /
Longa 550) roda com o crossfade no **default 60** — ou seja, **não é a
configuração que estragou a voz da Kessuly**.

**Não proponho religar nada.** Registro que a porta foi fechada com evidência de
**outra** configuração, e que a classe segue disparando 17 dias depois.

### Retratação: o e-mail ao Gustavo está certo

Eu abri esta investigação **suspeitando** que fosse o terceiro caso do dia de
*"a casa pede ao aluno a ação que não pode dar certo"* (Igor "tente de novo",
Gustavo "adicione vírgulas"). **Estava errado e retiro a suspeita.** A alavanca
existe mesmo (`Natural/Média/Longa`, `messages/pt-BR.json:1496-1499`), vai como
`chunk_silence_ms` e o conselho **funciona**.

Uma só correção factual: o e-mail diz que a pausa está *"sem valor definido **na
sua conta**"*, o que sugere anomalia da conta dele. É o padrão de **1.260 de
1.260**.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 A pausa volta a ser gravada no treino, com crossfade 60?

17 dias desligada, 3 cartões da classe disparando hoje, 91,8 % das gerações
saindo no fallback. A evidência que fechou a porta é de outra configuração.

### 2. 🔴 `#338` Valdeni: o "outro canal" do próprio cartão **não existe**

2º bounce às **22:08Z** (uid 568, `554 5.2.2 mailbox full`), 9h45 depois do 1º.
O cartão manda *"se persistir, avisar por outro canal"* — e `whatsapp` é **NULO**
no cadastro. Conta criada 12:22Z, credencial quicou 3 min depois, **nunca logou**.
**Não é pagante trancado** (sem entitlement, sem `sgp_pedidos`, sem compra — os
três conferidos), então **não peço estorno nem crédito**. Reenviar pra mesma
caixa cheia só repete o bounce.

### 3. 🔴 PR #92 sai do rascunho? **14 dias** em DRAFT

`updatedAt` **2026-08-28T21:48:55Z**, intocado. 29 PRs abertos.

### 4. 🔴 Vencidos que só envelhecem

| | idade |
|---|---|
| `#309` `ae6b4bd1` — Victor Mendes | **51,7h**, cobrou de novo hoje 16h45 |
| `#312` `c726c5ae` — varredor cego pro SGP, 19 compradores sem conta | **50,0h** |
| `#313` `2d0509b4` — 15 vitalícios de graça | **49,4h** (15ª ronda pedindo) |
| `#331` `3528dd59` — Mastroianni, reposição de crédito | **21,6h** |

### 5. 🔴 `#341`: **147.350** (14 pessoas) — número **estável pela 4ª ronda**

Re-medi agora: **14 × −10.525 = −147.350**. Nenhum dos 14 aparece na lista dos
já perdoados (conferido por `ref_type` contendo `perdao`, **não** por `kind`,
que é a armadilha medida). **Aprova?**

### 6. 🟡 Migration 102 do `#232`: escrita, **não aplicada, 8 dias**

`pg_trigger` na `incidents`, fora os internos: **0**. Conferido agora.

---

## O que eu ANOTEI

| onde | o quê |
|---|---|
| `b6486347` (`#344`) | o mecanismo inteiro, o alcance (91,8 %), o controle das 401 vozes, a decisão de 24/08 e a **retratação** da minha suspeita sobre o e-mail. `agent_notes` 8 → 9 |
| `4ce9f365` (`#348`) | idem — mesma classe. `agent_notes` 4 → 5 |
| `ce6e157d` (`#47`) | idem — o decano da classe, 10 ocorrências. `agent_notes` 59 → 60 |
| `b32af5ff` (`#338`) | o 2º bounce, o cadastro sem 2º canal, e os **três** filtros que descartam "pagante trancado". `agent_notes` 1 → 2 |

## Aluno cobrando mais de uma vez

- **Victor Mendes** — **51,7h**, cobrou de novo às 16h45. O mais velho sem dono.
- **Mastroianni** (`#331`) — reposição de crédito, **21,6h** sem dono.
- **Valdeni** — não cobra porque **não consegue receber**. Ver decisão 2.
- **Alana** (`506b7c3a`) — **222h**, sem novidade.
- **Gustavo** e **Igor** — ✅ ambos respondidos nesta janela.
- Sem novidade, todos já com chamado: `rafaluanravi29@`, `jutai.santos@`,
  `jkakoalves@`, `rearielo@` (prazo 17/09).

## Números da ronda

- **71 incidentes abertos**: **50 de aluno**, **21 técnicos**.
  (`fast` 27 + `fast-help` 20 + `fast-sgp` 2 + `suporte@` 1 = 50;
  `frank` 15 + `vigia` 5 + 1 sem autor = 21.) **0 em `open`, 71 em
  `investigating`**.
- **Nasceu 1**: `#349` (`3dbd2bf0`). **Fecharam 2**: `#345`, `#346` (Igor).
  **72 + 1 − 2 = 71.** A conta fecha. **Eu não reabri nenhum.**
- **Nada fechado voltou a disparar**: `fixed`/`ignored` com
  `last_seen_at > resolved_at + 5min` sai **1**, o `acf8acd6`
  (`training:user_dataset`), último disparo em **22/08** — velho e conhecido.
  Mantenho a ressalva estrutural da ronda passada: esse detector **não vê**
  cartão que voltou (a reabertura zera o `resolved_at` que ele compara). O
  detector honesto pra essa classe é *"aberto com `resolution_note` preenchida"*:
  hoje dá **10 de 71**, igual à ronda das 20h — **nenhum voltou nesta janela**.
- Caixa: 25 e-mails lidos com `BODY.PEEK`, só `SEEN`. **Não toquei em não-lido.**
  **1 e-mail novo** desde as 20h: o uid 568, que é o bounce da Valdeni.
  **Fila da Fast: 0 não-lidos**, o cron está vivo.
- **29 PRs abertos**, **0 mergeados** na janela. **2 commits** na `origin/main`
  desde 19:30Z, e os dois são **log de ronda** (`b0a77a5`, `7b81711`) —
  **nenhum commit de código** nesta janela. O trabalho do Igor foi feito em
  produção (banco + e-mail), não em código, e o conserto do `#349` segue por fazer.
- 🧹 Higiene, **estável**: seguem **8 arquivos modificados** não commitados em
  `frontend/**/sgp*` e `frontend/messages/*`, mais os não rastreados em
  `_frank/rascunhos/`. **Sexta ronda seguida.** Não são meus, **não toquei**;
  commitei só o meu log.
- Custo desta ronda: leitura. Nenhuma chamada paga de visão, nenhum crédito,
  nenhuma GPU, nenhum e-mail enviado.
