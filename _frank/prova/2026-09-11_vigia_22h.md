# Ronda do Vigia — 11/09/2026, 22hZ (19h BRT)

Sensor (14-A). Varri, medi, **abri 0 chamados** e **anotei 2** (`#349`, `#254`).
Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei e não
reabri nada.

Repo em `main`, `pull --ff-only` (2 commits novos: `dc3d941` + merge `243dc08`).
`_frank/ordens/README.md`, a ordem de **27/08** (só erro de sistema vira chamado)
e a de **29/08** (planilha desligada) lidas antes de tocar em qualquer coisa.
Nada da planilha foi lido, escrito, classificado ou reprocessado. Ordem de canal
de **31/08**: o aviso desta ronda vai **no grupo**.

Janela coberta: **20:25Z → 22:15Z**. `now()` medido no banco = **2026-09-11
22:11:02Z**.

---

## 1. 🔴 DECISÃO DO JOHNNY: restam **1,8h** do Leandro. Acaba hoje.

`leandro@aeroclubejf.com.br`, chamado **`#254`**. Medido pelo
`garantia_na_fila.cjs` às **22:12:11Z**: janela `2026-09-05 → 2026-09-12
00:00:00Z`, **RESTAM 1,8h**. Estorno: **zero**.

Progressão das rondas de hoje, sem nenhuma decisão no meio:

| ronda | restavam |
|---|---|
| 16hZ | 7,8h |
| 18hZ | 5,8h |
| 20hZ | 3,6h |
| **22hZ** | **1,8h** |

O par já foi medido e confirmado: **R$ 194 pagos pelo mesmo produto (7851642) no
mesmo ciclo** — `contato@aeroclubejf` (`HP3355066694`, COMPLETE, garantia venceu
04/09) e `leandro@aeroclubejf` (`HP0976568130`, APPROVED, garantia **12/09
00:00Z**).

Ele foi avisado 2× nos dois endereços e nunca respondeu; pela 9-C ninguém cancela
assinatura de titular sem pedido escrito, e essa parte está certa. **O que eu
registro como sensor é a consequência:** depois de 00:00Z a escolha deixa de
existir — não vira "não", vira "tarde demais", e ele entra como **6º** na lista
"perdeu a janela enquanto esperava na nossa fila". Anotado no próprio `#254`
(nota 27).

## 2. 🔴 DECISÃO DO JOHNNY: Wellington segue parado, e é o caso mais fácil da fila

`wgc.advogado@gmail.com`, chamado **`#360`**, aberto 19:35Z. **Nenhum movimento
em 2h40.** Nada mudou desde a ronda das 20hZ, então repito só o que decide:

- comprou **10/09**, pediu **11/09** — 1 dia, dentro da garantia com folga;
- o dinheiro é **R$ 297**, `ref_id` **`HP2231400018`** (Fábrica de Conteúdo
  Invisível, APPROVED);
- ⚠️ o código que ele mandou no assunto (`HP1224527470`) é a linha de **adesão de
  R$ 0**. **Quem estornar pelo código do assunto devolve zero** e fecha o caso
  achando que pagou.

## 3. O conserto do `#349` já está na main — e existe um PR duplicado que pode desfazê-lo

Este é o achado novo da ronda, e ele vem da checagem §3.2 da ordem de 27/08
("já foi corrigido?" — `git log origin/main` + `gh pr list`).

**O conserto entrou.** `dc3d941` — *"SGP: dHash 16x16 para de trancar aluno com
foto DIFERENTE (#349)"* — merge **`243dc08`** pelo **PR #243**, branch
`feat/349-dhash-16x16`. Entrou nesta janela.

**O cartão continua aberto.** `#349` está `investigating`, `resolution_note`
vazia e `resolved_commit` **NULO**. Pela 14-A eu não fecho: **anotei** o commit
no cartão pra quem fechar carimbar `dc3d941`.

⚠️ **E sobrou um PR concorrente, mesmo padrão do `feat/onedrive-401`.** O
**PR #234** (branch `feat/sgp-dhash-16x16`) continua **ABERTO** propondo o
**mesmo conserto do #349 por outro caminho** — extrai um
`frontend/src/lib/sgp/impressao-foto-pure.ts` novo. Medido:

- a branch está **33 commits atrás** da main (`git rev-list --count`);
- o que ela de fato adiciona são **3 arquivos, +395/−55** (three-dot diff);
- ela mexe em `frontend/src/lib/sgp/impressao-foto.ts` — **exatamente o arquivo
  que o `dc3d941` alterou.**

**Não mergear o #234.** É a terceira vez que este padrão aparece no repositório
(`feat/fix-image-upload-retry` em 19/08, `feat/onedrive-401` em 26/08) e nas
duas anteriores o risco era o mesmo: desfazer em cima do fix que já está em
produção. Se for descartar, fechar o PR e apagar a branch no origin.

> **Ressalva de método, pra não inflar o achado:** eu **não** afirmo que o merge
> apagaria as 4.799 linhas que aparecem no `git diff` de dois pontos — esse
> número é a divergência da branch em relação à main, não o efeito de um merge.
> O que está medido e é suficiente pra decidir: mesmo alvo, mesmo arquivo,
> branch velha, fix já na main.

## 4. O `#358` já tem conserto a caminho — não abri nada

`#358` (rajada do Vídeo Estúdio, Kie 429, `mcpaganatto@gmail.com`) segue aberto,
**sem ocorrência nova** desde 18:17Z. Checagem §3.2 da ordem de 27/08: existe o
**PR #241** aberto às 18:56Z — *"studio: teto de 4 no poll de cenas + throttle do
Kie não destrói cena (#358)"*. A causa que a ronda das 20hZ mediu (48 cenas em
84s sem fila do nosso lado) é exatamente o que o PR ataca. Nada a abrir.

Segue valendo a ressalva 14-A da ronda anterior: os 39.600 créditos voltaram, mas
**estornado não é resolvido** — ele continua sem as 22 cenas.

## 5. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0**;
- **1 mensagem nova** entre 20:25Z e 22:15Z: uid **584**, `angelacleomarfreitasdasilva@gmail.com`,
  **20:32:01Z**, texto inteiro: *"Qual tela??"* — resposta ao e-mail do código do
  SGP (*"Digite ele na tela pra continuar"*);
- **nenhum chamado novo precisou ser aberto**;
- nenhum aluno cobrando pela 2ª vez nesta janela.

**Por que a Angela NÃO virou chamado** — medi antes de decidir, porque o gatilho
óbvio ("aluna perdida no onboarding") tem cara de bug:

| medida | valor |
|---|---|
| `sgp_pedidos` dela, `criado_em` | 20:30:52Z |
| `email_verificado_at` | **20:32Z** |
| `status` hoje | `foto` |
| `codigo_tentativas` | 0 |

**Ela passou da tela do código no mesmo minuto em que mandou a pergunta.** Não
ficou presa, não errou tentativa nenhuma, e está no passo seguinte. Abrir cartão
disso infla a fila com um caso que se resolveu em 37 segundos.

**O que eu registro mesmo assim, porque é gap real e não vai aparecer sozinho:**
`frontend/src/lib/sgp/codigo.ts:29-36` monta o e-mail do código **sem nenhum link
para a tela** — o texto é literalmente *"Digite ele na tela"*, e a URL do `/sgp`
não aparece em lugar nenhum da mensagem. Quem abrir o e-mail no celular depois de
fechar a aba não tem caminho de volta. A Angela se virou; a próxima pode não se
virar. Não é chamado hoje (ninguém ficou preso), é **uma linha de relatório** —
se aparecer um aluno que efetivamente travou aí, aí sim vira cartão, e o
`arquivo:linha` já está medido.

Último movimento dela: **20:32:38Z**, parada em `foto` há ~1h40. Normal pra quem
está escolhendo foto; registro só pra próxima ronda ter a referência.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). Não toquei em não-lido.

## 6. Os relógios

`garantia_na_fila.cjs` às 22:12:11Z — **75 abertos** · 16 parecem pedido de
reembolso · 57 pessoas · **controle positivo OK (2/2)**.

**5 perderam a janela na fila** (estorno em todos: **zero**), inalterado desde as
20hZ, só o relógio andou: `contatoecocannabis@` (`#254`, virou há 46,2h),
`atendimento@dropweb.com.br` (`#282`, 70,2h), `contatoecocannabis@` (`#299`,
46,2h), `victor.inscriptio@` (`#309`, 22,2h) e (`#350`, 22,2h).

**2 vencem em 48h:** `leandro@aeroclubejf` (`#254`, **1,8h** — item 1) e
`rodrigo.limas.1978@` (`#306`, 25,8h).

⚠️ O "5" continua sendo **PISO, não total**: o script deriva a janela do
`warranty_date` do **nosso** banco, então quem não tem linha local sai como *"sem
compra paga"* — que **lê como veredito de pagamento** sendo só ausência de linha.
Teresa (`#356`), Wellington (`#360`), Simone (`#301`) e Nelson (`#354`) caem
todos nesse balde. Enquanto o rótulo não mudar, **ninguém deve ler a coluna
"janela" como "não pagou"**. Quinta ronda registrando isto.

## 7. Placar

- **75 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **41 de atendimento**, **34 técnicos**. Mais **10** em `aguardando_aluno`, que
  não entram nesses 75.
  (Registro medido: **zero** incidentes em `status='open'` — os 75 são todos
  `investigating`. Por isso a contagem tem que varrer os dois status.)
- **Fechados que voltaram a disparar: 0.** Cruzei `last_seen_at > resolved_at` na
  base inteira: sai **um só**, o `#8`, última ocorrência em **22/08** — velho,
  não é reincidência nova. Mesmo resultado das 20hZ.
- **Calibragem do Vigia** (ordem 27/08 §5): dos **30** cartões com
  `reported_by='vigia'` desde 27/08, **4** fechados como `ignored` = **13,3%**.
  Alvo ≤10%; linha de base 21%. Inalterado — **abri 0 nesta ronda**.

### 7-B. Divergência de 1 no placar anterior — registro, não acusação

O relatório das 20hZ publicou **73 abertos**. Medindo agora com o mesmo corte
(`created_at < 2026-09-11 20:23:19Z`, os dois status, sem filtro de assinatura),
saem **74**. Entre 20:23Z e agora entrou **1** cartão novo (`#361`, 21:28Z) e
**nenhum** foi fechado — daí os 75 de hoje.

Não consigo dizer **retroativamente** qual é o cartão da diferença: nenhum aberto
tem `resolved_at` preenchido (ou seja, **não houve reabertura**) e a tabela
`incidents` **não tem coluna de `updated_at`**, então mudança de status não deixa
rastro datado. A hipótese que sobra é um cartão que estava em `aguardando_aluno`
às 20:23Z e voltou pra `investigating` depois — mas **não tenho como provar**, e
não vou afirmar. Fica o registro: o placar das 20hZ estava **1 a menos**, e o
motivo não é auditável com o esquema de hoje.

## 8. Nota de processo — não é chamado

`main` continua com **arquivos do SGP modificados e não commitados**
(`frontend/messages/*.json`, `app/[locale]/sgp/page.tsx`,
`api/v1/sgp/codigo/route.ts`, `api/v1/sgp/inicio/route.ts`,
`components/sgp/step-dados-form.tsx`, `lib/sgp/sessao.ts`) mais os
`_frank/rascunhos/` não rastreados. **Quinta ronda seguida** registrando.

⚠️ **Detalhe desta ronda:** o `git pull --ff-only` **abortou** por causa dessas
alterações. Eu usei `git stash push -u` e **devolvi com `git stash pop` logo
depois do pull** — conferido: as 8 modificações e os arquivos não rastreados
estão de volta na árvore, nada foi perdido. Mas o risco ficou explícito: trabalho
não commitado em `main` agora **bloqueia a ronda seguinte**, não só a deixa cega.

## 9. Disciplina de commit — a lição das 20hZ, aplicada

A ronda anterior commitou sem pathspec e levou junto 2 arquivos do index de outra
rotina. Este log foi commitado com **caminho explícito**
(`git commit <arquivo>`), sem confiar no index.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado de
dinheiro sem `ref_id` + `arquivo:linha`, e não toquei em e-mail não lido.
