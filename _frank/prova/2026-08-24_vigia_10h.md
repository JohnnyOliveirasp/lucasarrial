# 24/08 10:10Z — Ronda do Vigia (sensor, regra 14-A)

`git checkout main && git pull --ff-only origin main` → **atualizado, nada preso**
(`origin/main..HEAD` vazio na entrada, ao contrário da ronda das 00h). Índice de
ordens lido. Valem a `2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐), a
`2026-08-20_REGRA_FINAL_CREDITO.md`, a `2026-08-21_passagem_incidentes_para_claude.md`
e a rotina `e-mail_tecnico.md`.

---

## Placar

| | |
|---|---|
| Incidentes abertos | **14** — 10 de aluno, 4 técnicos (6 `open`, 8 `investigating`) |
| Abertos por mim nesta ronda | **1** — `120` (`6e94acc6`), pré-venda sem dono |
| Anotados por mim | **3** — `d3d8d1b2` (objeção + correção), `47ffd002`, `b2651a6f` |
| Fechados que voltaram a disparar | **1** pelo detector (`acf8acd6`) — **e o detector é cego, §2** |
| E-mails lidos | 25 (INBOX, `BODY.PEEK`, só `SEEN`) |
| E-mail novo desde a ronda das 00h | **3** — uids 270, 271, 272 |
| Fila não-lida da Fast | **0** (medido por `--caixas`, STATUS read-only) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito que eu mexi | **nenhum** |

---

## 1. O ACHADO DA RONDA: o incidente mais velho da fila sumiu dos dois relatórios anteriores

**Incidente `d3d8d1b2` = `numero 15`.** Criado **30/07 13:24Z — 596,8h, 24,9 dias**.
**12 alunos** em `affected_emails`. É o mais antigo do quadro inteiro.

Ele não aparece em **nenhum** dos dois relatórios anteriores:

- A **Rotina das Falhas de 01h** o lista na triagem como *"`15` | 12 alunos |
  `ignored` por decisão do Johnny (aceite de risco)"* e por isso **não o pegou**.
  O `status` real no banco é **`investigating`**.
- A **ronda do Vigia das 00h** afirmou ter contado *"sem filtro nenhum"* e listou
  **20 abertos**. O `15` **não está na lista dela**. A conferência de §6 fecha a
  aritmética e mostra que a ausência é dele.

### O estado da linha é contraditório — e eu errei ao descrever isso

Anotei primeiro que o incidente *"nunca foi fechado, `resolved_at` está NULL"*.
**Isso estava errado.** Só vi porque a ferramenta relê a linha depois de gravar.
O real:

```
resolved_at = 2026-08-20 03:36:36Z · resolved_by = agent · resolved_commit = 60fb061
resolution_note = "FECHADO POR DECISAO DO JOHNNY (20/08) - NAO E UM FIX,
                   e aceite de risco consciente."
status = investigating
```

**Corrigi a nota no próprio incidente, na mesma ronda** (nota 34). A Rotina das
Falhas estava **certa no conteúdo** — o Johnny encerrou isto como aceite de risco.
Eu errei ao dizer que não tinha sido fechado. Retirado.

**O que sobra depois da correção, e é o ponto:** os campos de fechamento estão
todos preenchidos, mas o `status` nunca virou `ignored`. A linha escapa das **duas**
redes ao mesmo tempo:

| rede | filtro que ela usa | por que não pega o `15` |
|---|---|---|
| contagem da fila | `status in ('open','investigating')` | **pega** — e infla o quadro com assunto já encerrado |
| detector de "fechado que voltou" | `status in ('fixed','ignored')` | **não pega** — o status não é nenhum dos dois |
| triagem humana | lê a `resolution_note` | **pula** — lê como fechado |

Resultado: é contado como aberto pelo contador e tratado como fechado pela pessoa.
Hoje ele responde sozinho por **1 dos 14 abertos** e por **12 dos alunos afetados**
do quadro.

### E a classe voltou a disparar sem deixar rastro

`signature = generation:capacity:executiontimeout exceeded`. Contei a classe
**direto em `generations`**, sem confiar no incidente:

| dia | falhas `executionTimeout` |
|---|---|
| 18/08 | 2 |
| **23/08** | **1** → `2e2938b7`, 23:41:27Z, `janetecasarotto2@gmail.com` |

O `last_seen_at` do incidente segue **congelado em 18/08 20:46Z**. A falha de 23/08
**não incrementou nada**. O *"dormente desde 18/08, não voltou"* da triagem é chute
com cara de medição — e é a **segunda confirmação** do defeito do §2, agora num
incidente **aberto**.

Se o aceite de risco foi assumido sobre *"~2 por semana"*, quem for reavaliar
precisa saber que **a contagem do incidente não acompanha a realidade**.

**Não reabri, não fechei, não mudei status.** Regra 14-A.

---

## 2. O detector de "fechado que volta a disparar" continua cego

Rodado nesta ronda: devolveu **um só caso**, o `acf8acd6` (fechado 09/08, último
disparo 22/08 19:26Z, **+330,3h depois do fecho**, 57 ocorrências).

**Quieto há 38,8h** — eram 28,8h às 00h, e a diferença bate exatamente com o
relógio → **zero disparo novo**. Décima primeira ronda sem alguém confirmar
produção de `74ae65a`/`1e5a893`.

O detector segue **não enxergando** classe que dispara sem atualizar `last_seen_at`.
Nesta ronda isso ficou provado **duas vezes**: o `37bacb68` (qa_coverage, medido na
ronda das 00h) e agora o `d3d8d1b2` (§1). O defeito não é de um incidente — é do
campo. Qualquer raciocínio apoiado em `last_seen_at` é frágil.

---

## 3. O qa_coverage não estabilizou: a taxa SUBIU e chegaram 3 alunos novos

Incidente `47ffd002` (`112`), aberto na ronda das 00h. **Anotado, não fechado.**

| janela | gerações | falhas | taxa |
|---|---|---|---|
| 23/08 00:00Z → 15:09Z | 34 | 0 | 0,0% |
| 23/08 15:09Z → 24:00Z | 70 | 4 | 5,7% |
| **24/08 00:00Z → 10:10Z** | **28** | **3** | **10,7%** |

Três janelas consecutivas subindo. As 3 falhas de hoje são **100% das falhas de
hoje** — nenhuma outra classe falhou em 24/08. Erro cru idêntico nas três:
`qa_coverage: audio gerado nao contem o texto completo apos esgotar regeneracoes`.

| geração | hora | aluno | desfecho |
|---|---|---|---|
| `fbfd0e2d` | 00:27:16Z | renelopes170 | **recuperou** — `ready` 1min depois |
| `5da0cd91` | 01:35:38Z | drelvislandi | **não recuperou** — foi a última ação dele, 8,6h atrás |
| `615a99f3` | 03:41:23Z | j2sproducoes | **recuperou** — `ready` 7min depois |

**Estorno conferido por `ref_type='generation_refund'`, nunca por `kind`:** os três
receberam de volta (959 / 763 / 433 créditos), todos em menos de 3min. Nenhum
repetiu os 30min01s registrados na ronda anterior. **Dinheiro certo. Entrega, não.**

**A classe tem 6 alunos agora, não 3.** O `affected_emails` do incidente só tem
`tulliojeronimo`, `janetecasarotto2` e `danicale`. Faltam os três de hoje.
**Não alterei o campo** — registrei que está desatualizado.

### Quem acabou em falha e não voltou (vale mais que a taxa)

| aluno | última falha | sem voltar há |
|---|---|---|
| **janetecasarotto2** | 23/08 23:38Z | **10,5h** |
| danicale | 23/08 23:39Z | 10,5h |
| drelvislandi | 24/08 01:35Z | 8,6h |

**A Janete é o caso mais duro do quadro.** Tinha 8 gerações `ready`, a última em
**07/08**. Voltou depois de **16 dias parada**, tentou **duas vezes** em 23/08 —
`c5eb5cb4` 23:38Z (qa_coverage) e `2e2938b7` 23:41Z (executionTimeout, a classe do
§1) — e **as duas falharam**. Não voltou mais. Foi estornada. Ela não pediu
crédito de volta; ela queria o áudio.

**O que eu não medi, e não disfarço:** continuo sem abrir os áudios gerados pra
saber se o `qa_coverage` está **certo** (áudio realmente incompleto) ou **errado**
(áudio bom sendo reprovado). Sem isso não dá pra dizer se o defeito é do **gerador**
ou do **conferidor** — e é essa diferença que decide se o aluno perdeu áudio bom.
Próximo passo de quem pegar o `112`. Também **não acusei o PR #16**: 2 das 4 falhas
de 23/08 são anteriores ao merge.

---

## 4. FRENTE 1 — todos os abertos, sem filtro de assinatura

`status in ('open','investigating')`, **sem cláusula de `signature`**. Total: **14**
(13 antes do que eu abri).

| id | nº | quem espera | idade |
|---|---|---|---|
| `d3d8d1b2` | 15 | **12 alunos — §1, encerrado pelo dono mas contado como aberto** | **596,8h** |
| `ce47c3b9` | — | Rafael Paganini | 60,7h |
| `07af5758` | — | Luciano de Pinho | 48,9h |
| `7963388e` | — | Kessuly + Kátia | 36,0h |
| `60f3e9e2` | — | João Rezende | 22,7h |
| `bc8f234a` | — | Luciano (mesmo caso do `07af5758`) | 21,7h |
| `03495040` | — | Rafael — drift de rosto no Vídeo Clone | 18,3h |
| `b2651a6f` | 101 | técnico — entrega de e-mail, 17 bounces | 13,3h |
| `8311b02f` | 102 | Martin Mendez — **agora 1 linha só** | 12,3h |
| `73b9f772` | — | técnico — backfill da referência | 12,3h |
| `2adf0848` | — | aluno via Carol/zap | 12,2h |
| `a9de22d5` | 111 | técnico — 2.625 MP3 antigos sem Xing | 11,3h |
| `47ffd002` | 112 | técnico — qa_coverage (§3), 6 alunos | 9,9h |
| **`6e94acc6`** | **120** | **Sandra Diniz — §5, aberto por mim** | **aberto agora** |

**10 de aluno, 4 técnicos.** Se o `15` for para `ignored` como o dono já decidiu,
o quadro cai para **13 (9 de aluno)** e perde 12 dos alunos afetados de uma vez.

### O Martin: a rajada foi consolidada corretamente

As 6 linhas viraram **1**. Os `numero` 103–107 foram marcados **`ignored` como
duplicata**, todos no mesmo timestamp (01:10:33Z) — dedup em lote, feito certo — e
o `102` (`8311b02f`) ficou como a linha real. **Nada foi apagado.** O `141b034f`
(o incidente que descrevia a própria burst-rule) foi fechado em seguida.

---

## 5. FRENTE 2 — a caixa (25 últimos)

Não toquei em não-lido. **Fila não-lida da Fast: 0** em todas as pastas, medida por
`--caixas` (`STATUS (MESSAGES UNSEEN)`, read-only).

**3 e-mails novos desde a ronda das 00h** (que parou no uid 269):

| uid | quem | o que é |
|---|---|---|
| **270** | **Sandra Diniz** | **pré-venda, 7 perguntas de política — §5.1** |
| 271 | bounce `luctec@gmail.com` | caixa cheia — anotado no `b2651a6f` |
| 272 | Kátia Salvador | *"vou ouvir no meu intervalo e te respondo"* — **positivo, sem ação** |

### 5.1 Abri o `120`: pré-venda sem dono

`sandradiniz86@outlook.com`. Enfermeira e **perita judicial**. Chegou pelo curso
*"Fábrica de Conteúdo Invisível"*. **Não é aluna** — não há compra dela no banco.
É **prospect declarada do plano Pro**: *"antes de contratar o plano, gostaria de
esclarecer..."*.

Escreveu **24/08 01:21Z**. Sem resposta até 10:10Z = **8,8h**. Nenhum incidente
para o endereço dela. A caixa está com **0 não-lidos** — ou seja, a mensagem **já
foi vista** e mesmo assim não gerou nem resposta nem chamado. É exatamente o buraco
que a FRENTE 2 existe pra cobrir.

As 7 perguntas são **todas de política, nenhuma de bug**: acesso ao histórico após
cancelar; se o clone de voz/avatar sobrevive ao cancelamento; se cancelar assinatura
encerra a conta; se crédito vence por inatividade; disclaimer obrigatório para uso
em saúde/jurídico; o que é reembolsável se cancelar após treinar o clone; e **qual
é a versão final dos três documentos que ela viu marcados como rascunho**.

**Por que isso precisa de pessoa e não da Fast:**

1. As perguntas 1, 2 e 4 batem em cheio na **regra de crédito de 20/08**. Essa
   regra existe como **ordem interna** — não está escrita nos Termos que ela leu.
   Quem responder está **escrevendo política pública**, não consultando documento.
2. A pergunta 6 é **reembolso** — decisão de pessoa por ordem permanente.
3. A pergunta 7 pede a **versão final** de documento que a própria aluna viu
   marcado como rascunho. **Não existe versão final pra entregar.**
4. **Risco medido, não hipotético:** a Fast foi pega inventando resposta neste
   mesmo período. No caso do Luciano (uids 262/266, 23/08) afirmou *"a equipe já
   está cuidando do seu treino de voz"* (**não havia treino rodando**) e *"sua voz
   foi feita com o gravador da plataforma"* (**foi feita com arquivo que o próprio
   aluno enviou**). O aluno desconfiou — *"mas como vocês estão treinando se eu não
   enviei vídeo?"* — e as duas afirmações tiveram que ser corrigidas por escrito.
5. A destinatária é **perita judicial**. Resposta escrita sobre retenção de dado,
   reembolso e uso em saúde/jurídico vira **compromisso que a plataforma honra**.

**Não respondi, não escrevi rascunho.** Abrir e anotar é o meu papel.

### Já tratados — não abri duplicata

Cruzei os 25 contra `incidents` por `affected_emails`. Os dois casos de
`exceed_egress_quota` de 22/08 **já têm incidente e já foram fechados**:

| aluno | incidente | estado |
|---|---|---|
| Michell Baía (`michellufpa`) | `e3c5e445` = **92** | `fixed` |
| Daniel Leles (`danielltozello`) | `85ffef6b` = **93** | `fixed` |
| o apagão em si | `ca8edb0b` = **91** | `fixed` |

**Cobrando pela segunda vez:** Rafael (*"6 dias, muito decepcionado com o descaso"*)
e Luciano. **Os dois já têm incidente aberto e dono** — `ce47c3b9`/`03495040` e
`07af5758`/`bc8f234a`. Nada novo por eles nesta janela. O Rafael segue sendo o caso
de aluno mais velho do quadro: **60,7h**.

Os 3 alunos novos do §3 (renelopes170, drelvislandi, j2sproducoes) **não
escreveram** — nenhum e-mail, nenhum incidente prévio. É o mesmo buraco de sempre:
a falha não gera alerta e o aluno não reclama.

---

## 6. A aritmética do quadro fecha (e é assim que o §1 apareceu)

A ronda das 00h reportou **20 abertos**. Agora são **13** (antes do meu). Isso
poderia parecer perda de dado — **não é**. Entre 00:51Z e 01:19Z houve **8
fechamentos**, todos com `resolved_at` gravado:

```
98  531b6529  fixed    00:51:49Z
103 c9681460  ignored  01:10:33Z ┐
104 68000759  ignored  01:10:33Z │ dedup da rajada
105 ead550e7  ignored  01:10:33Z │ do Martin
106 9c3dad36  ignored  01:10:33Z │ (5 duplicatas)
107 f339f18b  ignored  01:10:33Z ┘
110 141b034f  fixed    01:15:25Z
113 bb530ff8  fixed    01:19:48Z
```

**20 − 8 = 12. O quadro tem 13.** A linha que sobra é o **`d3d8d1b2`** — aberto
naquela hora, aberto agora, **ausente da lista das 00h**. Foi essa diferença de 1
que me fez ir atrás dele.

**Nada foi apagado.** Conferi os `numero` de 1 a 120: **1–113 estão todos
presentes**. Só faltam **114–119**, seis números contíguos no topo da sequência,
imediatamente antes do meu insert — são `nextval` consumidos por inserts que
falharam ou sofreram rollback, não incidentes removidos. O meu saiu como **120**.

---

## 7. O que eu NÃO fiz, de propósito (regra 14-A)

Não fechei incidente, **não reabri nenhum**, não mudei status de nada. Não respondi
aluno, **não escrevi rascunho de e-mail**, não toquei em crédito, não mexi no gate,
não gastei GPU, não mexi em cron, **não toquei em e-mail não lido** (0 não-lidos
preservados), não alterei ferramenta nem ordem, não mexi em `affected_emails`.

**O que eu NÃO medi, e não vou disfarçar de saudável:**

1. **Não abri os áudios** das falhas de `qa_coverage` (§3). É a medição que decide
   se o bug é do gerador ou do conferidor, e segue sem ser feita — terceira ronda.
2. **Não medi** se o rate limit da Kie (429) atinge alunos por via que não
   `studio_scenes`. Herdado das rondas de 22h e 00h. **Segue sem medir.**
3. **Não investiguei** por que o `last_seen_at` não é atualizado. Já é o segundo
   incidente onde isso morde (§2). É bug nosso, e é do Frank decidir se vira card.
4. **Não conferi** as 11 branches sem PR que a Rotina das Falhas nomeou às 01h —
   inclusive as duas de aluno (`fix/trava-foto-nova-8379549c`,
   `feat/valida-conteudo-audio-ingestao`) que **não estão em produção**. Não é
   trabalho de sensor mergear código de outro agente, mas registro que **continua
   de pé** e que enquanto estiverem fora da `main` os 6 alunos do `8379549c` seguem
   com foto ignorada e cobrada.

---

## 8. Herdado e ainda de pé

1. **Os 4 pagantes sem voz** (jRF, Leandro, Ivanilde, Marcelo). **jRF tem acesso
   até 25/08 — amanhã.**
2. **Luciano** — acesso vence **26/08** (2 dias), e o caso é promessa de venda
   (vídeo de 45min), não bug. Ele disse em 23/08 14:14Z que vai preparar e enviar.
3. **Rafael** (`79`, 24.360 cr acima do teto da 9-B) e **Luciano** (`82`/`95`,
   comercial) seguem **parados em decisão do dono**, e a 9-B diz que o Johnny está
   dirigindo. O do Luciano **vence sozinho**.
4. **Turno da noite** (~21:40 → 06:40 local) segue descoberto com a ordem afirmando
   que está coberto. **Esta ronda caiu no fim dele.**
5. **A armadilha dos 20 minutos** (`MIN_DURATION_SECONDS = 20*60` em
   `voice-creator.tsx:11`) contra a mensagem que diz "mínimo 10min" segue de pé.

Commitei **apenas este arquivo**, por caminho explícito, na `main`.
