# 29/08 22h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → atualizado até `dff332e`. Índice de
ordens lido antes de tocar em qualquer coisa. Ordem vigente do assunto:
`2026-08-27_vigia_so_erro_de_sistema.md` (14-C). Também valem
`2026-08-20_dono_da_fila_e_fila_zerada.md` (14-A), a `2026-08-20_REGRA_FINAL_CREDITO.md`
(ENCERRADA — não reabri) e a rotina `e-mail_tecnico.md`.

Ronda anterior: **20h UTC**. Esta: **22h UTC**. Intervalo de 2h, no ritmo.

---

## Placar

| | |
|---|---|
| Não-fechados no fim | **7** — **2 de aluno** (`99`, `192`), **5 técnicos** (`180`, `184`, `189`, `190`, `193`) |
| **Abertos por mim nesta ronda** | **0** — e a §1 explica por que o achado *não* virou chamado |
| **Anotados por mim** | **3** — `193` (§1), `184` (§2), `192` (§3) |
| **Fechados que voltaram a disparar** | **0** — só o `#8` de sempre, último disparo 22/08, há **170,8h (7,1 dias)** |
| Fechados na janela 20h→22h | **2** — `187` (20:47Z) e `191` (21:47Z), ambos pelo Frank |
| Abertos por outros na janela | **2** — `192` (fast-help, 21:23Z) e `193` (frank, 21:55Z) |
| E-mails novos de aluno desde as 20h | **0** — o mais recente do INBOX segue o uid `373` (Johnathan, 04:15Z) |
| Aluno cobrando 2ª vez ou mais | **0 novos** |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **79** — **79 `ready`, 0 `failed`, 0 presas** |
| Acesso vivo + crédito + nenhuma voz pronta | **2** — Marcelo 19d, Kelinn 16d (inalterado) |
| Tentou e o import quebrou antes da voz | **1** — Luan, 16,6h (§2) |
| `training_jobs` preso >30min | **1** — o órfão de 25/08 (voz `f4b9b0f2` "Ellen 3" já `ready`, escrituração pendente) |
| Lista de estorno | **em dia** — 10 tipos, 2.488 linhas varridas, nenhum tipo desconhecido |
| PRs abertos | **25** (era 24) |
| Incidente que eu fechei, reabri ou reclassifiquei | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura** e **pelo complemento**
(`status not in ('fixed','ignored')`). `group by status` da tabela inteira: **145 `fixed`,
33 `ignored`, 6 `investigating`, 1 `aguardando_aluno`** = 185 linhas (183 da ronda anterior +
`192` + `193`). A `varredura_travados` fechou pelo mesmo número por outro caminho: 6 abertos +
1 aguardando aluno = **7**.

> O alerta de processo das rondas anteriores **continua valendo**: hoje a fila tem **0** `open`,
> **6** `investigating` e **1** `aguardando_aluno`. Uma varredura escrita como
> `status in ('open','investigating')` reportaria **6** e esconderia o `#99`. Pior: hoje ela
> nem sequer acharia um `open`, porque **não existe nenhum**. É por isso que conto pelo
> complemento.

**Fechamento que eu conferi e endosso:** o `187` (que eu abri às 16h) foi fechado às 20:47Z, e
nesta ronda a `varredura_travados` **passou a enxergar o Luan** no bloco novo "TENTOU E O IMPORT
QUEBROU ANTES DE EXISTIR A VOZ". Era exatamente o cego que eu tinha medido. Conserto conferido
funcionando, não só declarado.

---

## 1. ANOTEI o `193` — a auditoria do Frank tinha um furo, eu fechei, e a notícia é BOA

O `193` (aberto 21:55Z) mede que `conferir_transcript_referencia.cjs --curar` grava cauda
alucinada do whisper quando o `ref/auto.wav` corta no meio da frase. Auditoria honesta e boa: o
Frank achou o próprio estrago, consertou duas vozes e deu o placar de **2 de 5 curas pioraram
o dado (40%)**.

**O furo:** a nota diz *"4b0e315d voz nao existe mais"* e tira essa cura da auditoria. **A voz
existe** — "VOZ CLONE", `ready`, treinada 26/08 14:50Z, `reference_audio_path` válido,
`updated_at` 26/08 15:48Z (a cura, 1h depois do treino). Das 6 curas com backup, **uma ficou
sem auditar por premissa errada**, não por impossibilidade.

**Auditei agora, com o discriminador do próprio `193`** (leitura pura, nada gravado):

```
ref/auto.wav = 28,8s
clipe INTEIRO (o que --curar usa hoje):  "…aeroporto de San Diego COMIGO."
só os últimos 5s, 3 chamadas:            "…aeroporto de San Diego com…"   (3 de 3 IDÊNTICAS)
ESTÁVEL: SIM
texto ANTES da cura (backup):  "…aeroporto de San Diego com minha filha e meu filho."
texto DEPOIS da cura (hoje):   "…aeroporto de San Diego."
```

**Veredito: essa cura foi CORRETA.** O áudio corta no meio da palavra "com"; o texto original
seguia por mais 5 palavras que o áudio **não diz** — cauda fantasma de verdade, da família do
`#124`/`#191`, e a cura a removeu. De quebra, o clipe inteiro **alucinou "comigo"** a partir do
"com" truncado: confirma que o discriminador do `193` funciona e que transcrever o clipe inteiro
não é confiável.

**Placar corrigido: 2 de 6 (33%)**, e **não sobra cura sem auditar**. Conferi as duas danificadas
por fora, sem confiar na nota: `1d332ef0` = 244 chars idêntico ao backup (restauração OK);
`f0738839` = 406 chars terminando em "Me deixe aprender com você." (conserto OK). **Nenhum aluno
pendente disso.**

**Por que NÃO virou chamado novo:** a classe já tem o `193` aberto. Objeção e complemento vão na
nota do próprio chamado (14-A), e abrir um segundo seria o erro do `#112`.

### Dois alertas de processo (14-C §2 → relatório + Telegram, sem chamado)

**(a) O registro forense desta ferramenta só existe numa pasta gitignored.** Os 6 backups de
transcript vivem em `frontend/_Bugs/chamado_108_referencias/`, que `git ls-files` confirma fora
do versionamento. **Procurei um detector do lado do banco e não existe:** `voices.updated_at`
muda com *qualquer* coluna, não só com `reference_transcript`. A prova de que não serve: **25
vozes compartilham o `updated_at` idêntico `2026-08-24 15:43:12.720705Z`** — um UPDATE em massa,
que a ferramenta de cura não faz (ela grava uma voz por vez). Logo `updated_at > trained_at`
**não separa** "transcript reescrito" de "campo qualquer tocado". Consequência honesta: **se essa
pasta local sumir, ninguém sabe quais vozes foram reescritas nem consegue restaurar as que
foram.** Às 20h isso era só uma nota minha sobre o `--medir`; com **2 vozes de aluno já
corrompidas e consertadas**, virou o único rastro de um estrago real.

**(b) A ferramenta continua armada.** Reli o `curar()` nesta ronda: ele ainda transcreve o
**clipe inteiro** e grava o que vier, **sem** o discriminador de estabilidade — a linha 92 é o
único portão e só checa `--confirmar`. Enquanto o `193` estiver `investigating`, qualquer ronda
que rodar `--curar --confirmar` pode corromper mais uma voz de aluno pelo mesmo caminho. O
conserto já está especificado dentro do próprio `193`. **Não implementei** (14-A).

## 2. ANOTEI o `184` — ele tem um SEGUNDO aluno, e esse é pagante e está parado

O `184` tem só `johnathan.ppires@gmail.com` em `affected_emails`. Medi um segundo caso com a
assinatura idêntica, em `onboarding_runs`:

```
luanmarcal.com@gmail.com · 2026-08-29 05:42:44Z · ok=false · etapa_falha "audio"
motivo: "Arquivo 1uIeww… não está público no Drive (veio página HTML, não o arquivo)"
```

É a frase exata do defeito do `184` (`drive.ts:101` trata **todo** `text/html` como privado).
Estado: acesso **ativo até 29/09**, 98.425 créditos, **zero voz**, parado há **16,6h**.

**O ponto que importa é o próprio defeito:** eu **não consigo dizer**, olhando o nosso log, se o
arquivo do Luan é realmente privado ou se bateu no "Quota exceeded". Os dois produzem a **mesma
mensagem**. Essa indistinguibilidade é o que o `184` descreve, e o custo dela aqui é concreto: o
aluno recebe a ordem de "consertar o compartilhamento" que talvez já esteja certo.

Registrei como **reincidência** do `184` (14-C §2). Não entrou sozinho pela `signature` porque a
falha veio por `onboarding_runs`, não pelo detector de incidente — ficaria invisível se eu não
anotasse. **Não escrevi pro aluno nem rascunho** (14-A); não consegui confirmar se ele já foi
avisado porque `--enviados` volta **vazio** (o SMTP da Fast não salva cópia).

## 3. ANOTEI o `192` (Robert Ros) — a classe da cauda fantasma **não** explica a queixa dele

Linha do tempo medida: treino 15:14/15:23Z (60min de áudio, `ready`) → queixa 21:23Z → transcript
**reescrito pela cura 21:51Z** → **restaurado** do backup pelo Frank, que abriu o `193`.

**Conferi a restauração por fora:** o texto no banco tem 244 chars e é caractere a caractere
igual ao backup pré-cura. **Ele não ficou com dado estragado.**

E o que isso significa: o `193` mediu que na voz dele o texto original **já estava certo** (a
cauda "Então, essa…" é estável em 3 de 3; quem inventava era o whisper no clipe inteiro). Então
a hipótese óbvia — *"é o mesmo bug do Augusto (`191`)"* — está **medida e descartada** para este
aluno. **Não sei** qual é a causa da insatisfacão dele, e digo que não sei em vez de chutar.

Observação sem virar veredito: ele mandou 60min e a referência escolhida é um clipe de ~30s que
corta no meio da frase. Se isso degrada o timbre depende de **ouvir**, e ouvido humano não é meu
(14-C §4).

**Dinheiro (14-C §3), conferido por `ref_id`/`ref_type` e NUNCA por `kind`:** −10.000 em 29/08
15:17, `ref_type` "voice", clonagem/treino; **nenhum estorno**. **Não afirmo cobrança indevida**
— o treino rodou e entregou uma voz `ready`. Cortesia por insatisfação é **decisão do Johnny**.
Acesso ativo até 03/09, 86.412 créditos.

Por 14-C §2, insatisfação com qualidade é atendimento/decisão, **não** erro de sistema: por isso
**não abri chamado técnico**.

## 4. Checagem obrigatória do 14-C §3 item 2 — "já foi corrigido?"

`git log origin/main` desde as 20h: 4 commits novos, incluindo o merge do PR #127 (`187`).
`gh pr list --state open`: **25** (era 24) — novos o **#128** (botão de Ajuda) e o **#126**
(download do Drive em pedaços).

Olhei o **PR #54** ("transcript que o áudio não diz", aberto desde 25/08) porque pelo título ele
parecia ser o conserto da classe do `193`. **Não é:** `gh pr view 54 --files` mostra que ele toca
só `_frank/ferramentas/fabricar_referencia.cjs` e `refazer_audio_conta_da_casa.cjs` — ferramentas
manuais, **não** o caminho de treino em produção nem o `curar()`. Registro para ninguém fechar o
`193` achando que já existe PR resolvendo.

## 5. Saúde — 79 gerações, ZERO falha

79 gerações nas últimas 24h, **todas `ready`**, nenhuma `failed`, nenhuma presa >30min. Job órfão
de 25/08 continua sendo só escrituração (voz "Ellen 3" já `ready`, ninguém esperando). Lista de
estorno em dia: 10 tipos, 2.488 linhas, nenhum tipo desconhecido.

## 6. Fechados que voltaram a disparar

**Zero novos.** Varri a tabela inteira por `last_seen_at > resolved_at`: a única linha continua
sendo o **`#8`**, fechado em 09/08, último disparo 22/08 19:26 — **7,1 dias** parado. Também
varri os fechados com `last_seen_at` nas últimas 48h (26 linhas): **todas** têm `resolved_at`
posterior ao último disparo, ou seja, foram fechadas *depois* de pararem. Nenhuma classe fechada
segue disparando. Sem ação, e **não reabri**.

## 7. Caixa — nenhum e-mail novo, não-lido intocado

`ler_caixa.cjs --ultimos 25 --corpo 2500`: leitura pura (`BODY.PEEK`, só `SEEN`). Fila de
não-lidos da Fast: **0** (via `--fila`, só contagem). **Não toquei em não-lido.**

**Nenhum e-mail novo de aluno desde as 20h** — o mais recente do INBOX segue o uid `373`
(Johnathan, 04:15Z). Procurei os sinais de escalação silenciosa (*"a equipe vai verificar"*,
*"vou chamar alguém da equipe"*, aluno cobrando 2ª vez): **nenhum caso novo sem chamado**. Antes
de descartar qualquer um, conferi se já havia resposta enviada.

**Luciano (`99`)**, para decisão do Johnny, **não como chamado técnico**: segue em
`aguardando_aluno`, **6,2 dias**. A própria varredura avisa que *"parado há 7d+ sem resposta pede
SEGUNDA tentativa, não silêncio"* — ele **crava o prazo amanhã**. Insatisfação/espera é
decisão/atendimento (14-C §2), então **não abri chamado**.

## 8. O que eu NÃO fiz

- Não fechei, não reabri e não reclassifiquei **nenhum** incidente. Os 7 seguem como estavam.
- **Não abri chamado nenhum.** Os dois candidatos (o furo da auditoria e o 2º aluno do `184`)
  eram complemento de chamado existente — deixei escrito por que abrir seria duplicata (`#112`).
- **Não rodei `--curar`**, nem em simulação-que-grava. Toda a medição da §1 foi leitura pura, em
  script próprio no `/tmp`, sem escrever no banco nem no repositório de bugs.
- Não inflei nem desinflei o número: corrigi o placar do `193` de 2/5 para **2/6**, o que
  **melhora** o número do Frank em vez de piorar — e disse isso explicitamente.
- Não respondi aluno e **não escrevi rascunho de e-mail** (armadilha do lucvila, 20/08).
- Não toquei em crédito, acesso ou assinatura. Não toquei em e-mail não-lido.
- Não afirmei causa da queixa do Robert. Disse **"não sei"** onde não sei.
- Não mexi em código de produção.
