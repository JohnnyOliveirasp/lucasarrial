# Ronda do Vigia — 16/09/2026, 10hZ (07h BRT)

Sensor (14-A). Varri, medi, **abri 1 chamado** (`#425` / `9cc7dcc1`) e **anotei 1**
(`#423` / `1611bbac`). Não respondi aluno, não escrevi rascunho, não mexi em
crédito, não fechei e não reabri nada.

Ordens lidas antes de tocar em qualquer coisa: `_frank/ordens/README.md`, a de
**27/08** (só erro de SISTEMA vira chamado) e a de **29/08** (planilha desligada).
**Nada da planilha foi lido, escrito, classificado ou reprocessado.** Ordem de
canal de **31/08**: o aviso desta ronda vai **no grupo**, e só no grupo.

Janela coberta: **00:28:18Z → 10:10:56Z** (9,7 h — a ronda anterior foi a das
00hZ; entre elas está o turno da noite, que por ordem de 27/08 **não roda**).
`now()` medido no banco na abertura = **2026-09-16 10:10:56,080Z**.

---

## 1. 🔴 PARA DECISÃO DO JOHNNY — a promessa feita no seu nome venceu há 19h

**Maria Teresa (`tuquinha36@hotmail.com`), R$ 2.809,32, esperando desde 11/09.**

A ronda das 00hZ já tinha reportado que a casa prometeu, **no seu nome**, retorno
"ainda hoje" com o comprovante do reembolso. Medi a fila de saída desta vez e o
quadro piorou de forma objetiva:

| | |
|---|---|
| última mensagem da casa para ela | **15/09 14:45:18Z** (uid 2451) |
| silêncio até o fecho desta ronda | **19 h 26 min** |
| o que foi prometido, por escrito | *"O Johnny é a pessoa responsável que você está pedindo… Ele vai te retornar diretamente"* (14:45Z) e *"Ele vai te retornar com a confirmação e o comprovante"* (14:35Z) |
| e a casa fechou a porta de volta | *"Você não precisa responder mais nada aqui. O Johnny vai entrar em contato."* |
| estado do dinheiro, pela própria casa | *"conferi na Hotmart e o reembolso ainda não foi processado"* (14:27Z) |

O agravante não é só o atraso: as duas últimas mensagens **mandaram ela parar de
escrever** e esperar você. Ela está cumprindo o combinado e não tem por onde
cobrar. Isso é **atendimento** (ordem de 27/08) — **não abri chamado**. A parte de
sistema já tem cartão: `#414` (WhatsApp inventado), `#415` (a Fast seguiu
respondendo sozinha depois de entregar ao humano) e `#410` (dedupe que rachou a
queixa). Vai pro grupo porque **a promessa tem o seu nome e o prazo já virou**.

### 1.1 Correção do que a ronda anterior disse sobre o Gregório

A ronda das 00hZ listou **Gregório Karabachian** (`karabachiang@gmail.com`,
`#409`) como pendência de mesmo peso. **Conferi a caixa de enviados e isso estava
incompleto — ele FOI respondido por escrito**, uid 2422, 15/09 12:28Z:
*"Confirmação por escrito: assinatura cancelada — e o que falta decidir sobre as
cobranças em aberto"*, com a recorrência cancelada na Hotmart (código `UI88ZD0W`).

O que continua aberto dele é só a **decisão sobre os R$ 194,00**, e o relógio
jurídico dele (5 dias úteis a partir de 15/09, invocando CDC art. 39 e art. 42
§ único) vence por volta de **22/09**. Menos urgente que a Maria Teresa, e é
decisão sua, não bug.

---

## 2. 🟠 ABRI O `#425` — o chamado de rajada de "Animar Imagem" nasce cego

`9cc7dcc1`, `open`, técnico, assinatura `vigia:rajada-imagem-video-sem-erro-cru`.

O detector escreve no próprio cartão *"investigar a causa raiz (erro cru na sample
e, para imagens, na coluna `kie_raw_error`)"*
(`frontend/src/lib/support/failure-alert.ts:273`). **Na perna de vídeo da imagem
as duas fontes estão vazias por construção:**

1. `sample_error` recebe `a.rawError` (`failure-alert.ts:254` e `:269`). Quem
   alimenta esse campo aqui é o `handleTechFailure` chamado por `failImageVideo`
   (`frontend/src/lib/images/video-sync.ts:82-107`, `rawError: message`) — e os
   **dois** chamadores entregam a mensagem **já traduzida**:
   - `frontend/src/app/api/v1/images/[id]/video/route.ts:138` → `failImageVideo(id, friendlyKieError(raw))`
   - `frontend/src/lib/images/video-sync.ts:192` → `failImageVideo(imageId, friendlyKieError(...))`

   O erro cru do provedor só sai em `console.warn` — log do pm2 que ninguém lê.
2. `kie_raw_error` só é escrita em `frontend/src/lib/images/finalize.ts:86`, que é
   a perna de **imagem**. `failImageVideo` grava apenas `video_error` com o texto
   friendly (`video-sync.ts:86`). Na perna de vídeo a coluna fica nula — ou, pior,
   guarda o erro de uma falha **diferente**.

### 2.1 A prova que fecha o caso: o contraste entre os cartões de rajada

`sample_error` de todos os `signature like 'fail-burst%'`:

| cartão | ferramenta | sample_error |
|---|---|---|
| **#423** | **Animar Imagem (Kie)** | **"O provedor de vídeo teve um erro temporário. Tente novamente em instantes."** ← frase do ALUNO |
| #398 | Vídeo Clone | `executionTimeout exceeded` |
| #358 | Vídeo Estúdio b-roll | `Kie createTask (vídeo) sem taskId (code=429, msg=Your call frequency is too high…)` |
| #295 | Vídeo Estúdio áudio | `no_speech` |
| #279 #278 #276 #275 #274 #271 | Vídeo Clone | `Job processing failed` |

**Todos os outros carregam string crua de provedor. Só a perna imagem→vídeo
carrega a frase escrita pro aluno.** O defeito é local dessa perna.

### 2.2 As três checagens do §3 da ordem de 27/08, escritas na abertura

1. **Já existe?** Não. Varri a fila **aberta e fechada** por `signature like
   'fail-burst%'` e por título (rajada/burst/erro cru/causa raiz). O que existe é
   de outra classe e está fechado: `#183` (a rajada abria chamado técnico para
   erro de **input** já classificado como não-técnico; `741a02e`, `9df010e`) e
   `#110` (a rajada quebrava 1 problema em N cartões). **Nenhum trata da FONTE do
   erro cru.**
2. **Já foi corrigido?** Não. Nos 7 commits de `origin/main` desde a ronda das
   00hZ nenhum toca `video-sync.ts`, `failure-alert.ts` ou
   `images/[id]/video/route.ts` — os commits mais recentes nesses arquivos são
   `bf1fd6d`, `76032b3`, `646a25f`, todos anteriores e de outro assunto.
   `gh pr list`: 19 PRs abertos; os 3 novos (`#307`, `#308`, `#309`) não tocam
   nenhum dos três arquivos (`git diff --name-only` vazio nesse recorte).
3. **Envolve dinheiro?** **Não**, e digo isso com a conta feita (§2.3).

### 2.3 Por que este cartão NÃO é acusação de dinheiro

Os 3 estornos do `#423` foram conferidos por **`ref_type='image_video_refund'`**,
nunca por `kind` (armadilha medida em 20/08): **3 × 1.320 = 3.960 devolvidos**,
batendo com as 3 falhas. O aluno voltou a usar e **recebeu vídeo** (`2067bb70`,
`video_status=ready`), saldo **55.226**. **Não há cobrança a corrigir.**

Registro também, **sem transformar em acusação**: a row
`a590b0a1-1e5b-4778-a596-3d26663108b0` não existe mais em `image_generations` (o
aluno apagou do histórico) enquanto os lançamentos dela seguem no ledger. Isso é
o **débito órfão** conhecido do DELETE, que a ordem de 20/08 já mediu como
**normal e não-detector-de-bug**. O que importa aqui é outro efeito: apagada a
row, sumiu até o `video_error` friendly. **Hoje o `#423` não tem nenhuma fonte de
causa raiz — nem a sample, nem a coluna, nem a row.**

---

## 3. 🟢 O QUE MELHOROU DESDE A RONDA DAS 00hZ

**O Eric saiu do buraco.** A ronda anterior anotou objeção no `#137` porque
`ericb.malzone@gmail.com` estava com 13 áudios enviados e a voz `d5ae61ac` parada
em `awaiting_training` desde 13/09, com acesso vencendo em **17/09**. Medido
agora: e-mail *"Sua voz está pronta pra treinar — falta um clique"* enviado
**16/09 01:06Z** (uid 2492) e a voz **`ready` às 02:08Z**. Caso resolvido dentro
do prazo. **A objeção de fundo do `#137` continua de pé** (o conserto foi de
rótulo, não nasceu detector) — mas o aluno concreto foi salvo.

**O `#404` (teto do clone) segue segurando.** Na janela: **13 clones criados, 13
`ready`, 0 `failed`, 0 estorno** (conferido por `ref_type`, não por `kind`).

**A fila da Fast está zerada:** `--fila` = **0 não-lidos** no INBOX. Não toquei em
nenhum não-lido.

**Luiz Reis** (`luizreis.despertar@gmail.com`) pediu cancelamento e confirmou às
18:17Z; foi respondido às 18:20Z e 18:26Z. **Não virou pendência.**

---

## 4. FRENTE 1 — a fila inteira, sem filtro de assinatura

Consultei `incidents` por `status in ('open','investigating')` **sem cláusula de
`signature`** (o filtro `fast-email:%` foi o que fez reportar "0 abertos" com
quatro em 19/08).

| | |
|---|---|
| **abertos no total** | **87** (era 84 na ronda das 00hZ) |
| de aluno (`atendimento`) | **42** |
| técnicos | **45** (43 `investigating` + 2 `open`) |
| nasceram na janela | **3** — `#423` (burst-rule), `#424` (Frank), `#425` (**meu**) |
| fechados que voltaram a disparar | **0 na janela** |

**Fechados que voltaram a disparar:** cruzando `last_seen_at > resolved_at` na
tabela inteira existe **1 caso, e é o mesmo de sempre**: `#8` (fixed em 09/08,
último disparo em 22/08, **25 dias atrás**). Nada novo.

Mais antigos ainda abertos: `#11` (56,5 d), `#15` (47,9 d), `#99` (23,7 d),
`#101` (23,6 d). **24 cartões em "aguardando aluno"** — a bola está com ele, mas
8 passaram de 14 dias, e parado há 7 d+ sem resposta pede segunda tentativa.

**Anoto uma coisa que a varredura levantou e eu não transformei em cartão:**
`#407` (`lucianodepinho@gmail.com`) foi **fechado 188.333 s depois do próprio
disparo** e o aluno escreveu de novo — entrega ao time **sem retorno humano**.
Ele recebeu correção às 01:52Z (uid 2494, *"seus créditos NÃO expiram em 19/09"*),
então **não está no escuro**. É atendimento, fica registrado aqui.

---

## 5. O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito, não
fechei nem reabri incidente, não toquei em e-mail não lido, não abri chamado de
atendimento/processo/decisão, e não li nem reprocessei nada da planilha.

**Abri 1 chamado** (`#425`), e ele passa no teste de bolso da ordem de 27/08: *se
o código estivesse certo, bastava a perna de vídeo repassar o erro cru em vez de
traduzir antes de reportar — e isso não teria acontecido.* Os demais achados da
ronda são atendimento (Maria Teresa, Gregório) ou ocorrência de classe já aberta
(`#423`), e por ordem **não viram cartão novo**.
