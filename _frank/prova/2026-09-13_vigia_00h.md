# Ronda do Vigia — 13/09/2026, 00hZ (12/09, 21h BRT)

Sensor (14-A). Varri, medi, **abri 1** (`#372`) e **anotei 2** (`#371`, `#335`).
Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei e não
reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido,
escrito, classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**.

Janela coberta: **22:12:46Z → 00:11:02Z**. `now()` medido no banco =
**2026-09-13 00:11:02Z**.

---

## 1. 🔴 ACONTECEU — a janela do Rodrigo fechou. Não é mais previsão, é fato medido

Sete rondas seguidas avisando. **Fechou às 00:00:00Z de hoje**, e eu medi
**0,3 h depois**.

`rodrigo.limas.1978@gmail.com`, cartões `#306` e `#363` (**a mesma pessoa**,
duas linhas na ferramenta). Saída do `garantia_na_fila.cjs` 00:16:24Z, os dois
agora no bloco **"PERDEU A JANELA ENQUANTO ESPERAVA NA NOSSA FILA"**:

| cartão | pediu em | janela | esperou de prazo DENTRO da fila |
|---|---|---|---|
| `#306` | 08/09 | 06/09 → **13/09 00:00Z** | **106,1 h** |
| `#363` | 12/09 | 06/09 → **13/09 00:00Z** | **22,3 h** |

O valor em nome dele é **R$ 1.194,90** (SGP R$ 741,00 + Fábrica R$ 356,90 +
mensalidade R$ 97,00). Ele pediu **duas vezes** (uid 586 *"Quero reembolso"*,
uid 587 *"Devolução total por favor!"*), as duas lidas, e o próprio e-mail da
casa admitiu por escrito que ele **pagou e nunca teve conta criada**.

**O contador subiu de 6 para 8, e os 2 novos são exatamente ele.** Reconciliado
linha a linha: os outros 6 (`#254` ×2, `#282`, `#299`, `#309`, `#350`) são os
mesmos das 22hZ, com as mesmas datas. **Ninguém mais perdeu janela nesta
janela** — e esta é a diferença entre o número de hoje e o das rondas
anteriores: as outras subidas eram rótulo, esta é uma pessoa.

**O que eu NÃO faço:** não respondo, não escrevo rascunho, não toco em crédito.
14-A, e não muda. O que mudou é só que isto saiu da coluna "decidir hoje" e
entrou na dos que **já perderam**.

O próximo da fila é `evelyn.cheida@gmail.com` (`#341`), agora com **23,8 h** —
vira **amanhã**. Mais 3 no bloco de 48 h (`#341` mateus e souzaprado, 47,7 h
cada; e a evelyn).

## 2. 🔴 ABRI — `#372`: quando o gate de rosto barra o aluno, o sistema não grava NADA

Único cartão que abri. Nasce do `#371` (aberto pela Fast às 23:57Z), mas o
defeito não é do caso da aluna.

**O caminho, com `arquivo:linha`** (`frontend/src/app/api/v1/video-clone/route.ts`):

| linha | o que faz |
|---|---|
| `150-160` | o gate roda **ANTES** de cobrar e **ANTES** de qualquer INSERT |
| `155` | `const gate = await checkFrontalFace(gateUrl)` |
| `157` | `console.log("[video-clone] face-gate bloqueou", {...})` ← **único registro que existe** |
| `158` | `return jsonError("face_not_frontal", ..., 400)` ← devolve e acaba |

Quando o gate barra: **não nasce linha em `video_clones`, não sobe contador, não
existe assinatura de incidente, não há tabela de auditoria.** O bloqueio mora só
no stdout do pm2. Nenhuma consulta ao banco responde *"quantos alunos o gate
barrou hoje"* — nem *"quantas vezes o mesmo aluno bateu na mesma parede"*.

**A medição.** Ferramenta nova,
`_frank/prova/2026-09-13_medir_face_gate_falso_positivo.cjs`, leitura pura (não
escreve, não cria clone, não cobra). Ela **lê `MODEL` e `SYSTEM` do próprio
`face-gate.ts` em tempo de execução** em vez de copiá-los, pra medição e produção
não poderem divergir. Nas 6 imagens `ready` mais novas da aluna do `#371`, URL
assinada do R2 igual à produção, 2 passadas cada:

- **5 de 6 BLOQUEIAM**, veredito **estável** (mesmo resultado nas duas passadas
  de cada imagem), motivo sempre da mesma família (*"a pessoa está olhando para
  baixo"*);
- **1 de 6 passa**;
- janela das imagens: **18:07Z → 23:54Z**. Ela abriu o chat às **23:57Z**, três
  minutos depois da última.

**A ressalva, e ela é dura: eu NÃO abri as 5 imagens e NÃO afirmo que o gate
errou.** Se ela estiver mesmo olhando pra baixo, o gate **acertou** e o caso dela
é atendimento, não bug. Isso depende de **olho humano** (ordem 27/08 §4), vai pro
grupo e nunca vira veredito meu. **O `#372` não depende disso**: o defeito é a
casa não enxergar a recusa, esteja o gate certo ou errado.

**Tamanho da classe: NÃO MENSURÁVEL — e essa é a própria falha.** O teto
grosseiro que consigo tirar: nos últimos 7 dias **184 alunos** têm imagem `ready`
**e** voz `ready`, e **48** nunca criaram uma única linha em `video_clones`.
**Não afirmo que os 48 foram barrados** — a maioria provavelmente só não tentou.
O ponto é que o banco **não permite separar** "foi barrado" de "não tentou".

**Dinheiro: nenhuma afirmação.** Conferido no arquivo — o gate roda antes da
cobrança; o INSERT e o débito vêm depois. Não cruzei `ref_id` com nada e não
afirmo estorno devido.

## 3. 🟡 A premissa do `#371` está INVERTIDA — anotei, pra ninguém caçar bug que não existe

O título do `#371` diz *"nem as geradas pela própria plataforma"*. **Medido, é o
contrário.**

A **única** imagem dela que veio do Gerador de Imagem com prompt e custo
(`4100fc07`, 20:35Z, **525 cr**) é justamente a **única que PASSA** no gate. As 5
que bloqueiam têm `credits_cost = 0` e `prompt`/`idea` **vazios** — assinatura de
**upload**, não de geração. Conferido na base inteira desde 05/09: **153 de 958**
linhas de `image_generations` são cost 0 + prompt vazio, e entre elas há
resolução **3024×4032**, que é foto de câmera de celular.

E o detalhe que dói: o prompt que ela escreveu na única que gerou foi
**"Olhar fixo para a câmera, boca visível rosto ereto sem estar inclinado"**.
Ela leu a mensagem de recusa e **pagou 525 créditos pra escrever os requisitos do
gate à mão**. Funcionou. O produto fez a aluna descobrir a regra por tentativa e
erro, e a casa só soube porque ela abriu chat.

**Não mudei o status do `#371` e não respondi a aluna.**

## 4. 🟡 Anotei o `#335` — sem o `#372`, calibrar o gate é chutar duas vezes

O `#335` diz que o gate é **permissivo demais**. O `#372` mede que a **recusa é
invisível**. Os dois lados do mesmo balcão, e só um deixa rastro: o clone cobrado
que saiu ruim. Quem o gate barra some.

Isso importa na prática porque a correção proposta no `#335` é mexer num limiar
(`face_height_pct`) — e em 12/09 21hZ **o Frank refutou por medição o meu patch**
(`81438b60`): nas 9 imagens reais nenhuma pontuou abaixo de 35, o piso 25 seria
NO-OP, e a mesma imagem varia ±10 pontos entre passadas. **Ele estava certo e eu
errado.** Mexer no limiar sem registro de recusa é chutar sem poder ver quem o
limiar novo passaria a barrar. Sugestão registrada na nota (**não é ordem** —
quem decide e fecha é o Frank): gravar a recusa **antes** de calibrar.

Pista lateral, registrada **como pista e não como prova**: rodando o
`face-gate.ts` de produção, os booleanos `frontal`/`mouth_visible` vieram
**estáveis** nas 2 passadas de cada uma das 6 imagens — diferente do ±10 que o
Frank achou no `face_height_pct`. Sugere que a oscilação é do campo **numérico
que o patch introduziria**, não dos booleanos que o gate já usa. São alunas,
imagens e campos diferentes; 2 passadas em 6 imagens **não provam estabilidade**.

## 5. As checagens da ordem de 27/08, feitas antes de abrir e de NÃO abrir

| candidato | veredito | checagem |
|---|---|---|
| **recusa do gate sem rastro (§2)** | **ABRI (`#372`)** | **1:** fila aberta E fechada, por título (`face`/`gate`/`rosto`/`rastro`/`invisív`) e por `signature` (`%gate%`/`%rosto%`/`%face%`). Vizinhos: `#335` (investigating) é a direção **oposta**; `#131` (fixed) foi quem **criou** o gate; `#371` é o cartão de **atendimento** da aluna. Nenhum trata a recusa não deixar rastro. **2:** `git log origin/main` da janela = **2 commits, ambos log de ronda** (`_frank/prova`), nenhum toca código; `gh pr list --state open --limit 200` = **36**, e os que citam face/rosto/gate/clone/vídeo são **#254** (gate de job velho no reenvio, **outro** gate), **#200** (prévia de transcrição) e **#185** (teste) — **nenhum** toca `face-gate.ts` nem o ramo da linha 157. **3:** nenhuma afirmação de dinheiro |
| a aluna do `#371` está travada | não abri | **atendimento** → grupo. O pedaço de sistema virou o `#372`; o veredito sobre as imagens dela precisa de **olho humano** |
| premissa invertida do `#371` (§3) | não abri | **objeção/correção** → nota no **próprio** cartão (14-A) |
| `#335` precisa do `#372` (§4) | não abri | **reincidência/objeção** → nota no cartão que existe |
| `marcelopersonalthe32@gmail.com` (§7) | não abri | **1 (já existe)** — é o `#128` (`ignored`), e a causa já está escrita na própria linha da voz |
| clone `pending` há 62 min (§8) | não abri | **não tenho baseline** pra chamar de travado, e o detector da casa não o marcou. Fica como item de olho |

**Dinheiro:** nesta ronda **não fiz nenhuma conta de dinheiro**, não casei
`ref_id` com nada e não afirmei que alguém foi ou deixou de ser estornado.

## 6. Conferi o trabalho do Frank na janela — nada fechou, então não há o que objetar

**Zero incidentes `fixed` ou `ignored`** entre 22:12:46Z e 00:11Z. Nenhum
fechamento pra conferir, nenhuma objeção a registrar. Os 2 commits da janela são
os logs de ronda (`4a7c3f0` rotina das falhas 23hZ, `dd96fe4` vigia 22hZ).

**PR novo:** **#257** (*conta da casa: gerar com TEXTO NOVO (`--voz`)*), criado
22:41Z, aberto. 35 → **36 PRs abertos**, reconciliado.

O **PR #249** (`fix(sgp): limpa o erro na recuperação`) segue **aberto, não
mergeado** — **oitava ronda seguida**. **Não re-medi** a exposição em
`sgp_pedidos` e **não afirmo número** sobre ela. Afirmo só o que conferi: o PR
continua aberto, e o custo de decidir é mergear um PR já escrito.

## 7. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0** (`ler_caixa --fila`, `EXAMINE`);
- **4 e-mails novos na janela** (uid 599→602), **de 1 aluno só**: o
  **mastroianni**, entre 20:29 e 20:57 BRT. Todos sobre o vídeo de teste que a
  casa ofereceu por conta da casa; ele pediu o arquivo **em link do Drive** pra
  validar quando voltar de um aniversário. **Já tem cartão aberto pela Fast**
  (23:55Z, 6 ocorrências). **Nada a abrir de minha parte**;
- **nenhum aluno cobrando pela 2ª vez** dentro desta janela;
- **nenhum "já pedi ajuda várias vezes"** novo;
- a promessa dos créditos do mastroianni (*"assim que o dono retornar"*, dita por
  escrito **duas vezes** em 12/09) continua de pé e **continua sendo decisão do
  Johnny**. Reportada na ronda anterior, sem fato novo: **não re-anotei**.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). **Não toquei em não-lido.**

## 8. Geração na janela: limpa, com 1 item de olho

`created_at > 22:12:46Z`, quatro tabelas: **43 entregas prontas, ZERO falha** —
`image_generations` 29 · `generations` 10 · `video_clones` 2 · `voices` 2.

**Em voo neste instante: 6** — 4 `video_clones` `pending` (62, 22, 20 e 1 min) e
2 `image_generations` `generating` (< 1 min).

⚠️ **O de 62 min** (`1e3b89a3`, `rafapaga@uol.com.br`, tier 480p-v3, 945 cr) é
**3× mais velho** que os outros três em voo. Ele **tem `runpod_job_id`**, então
foi despachado. **Não abri cartão** por dois motivos honestos: (a) o
`varredura_travados.cjs` **não o marcou** como preso, e (b) **eu não tenho
baseline de duração** — `elapsed_seconds` está **nulo em 100 % dos `ready` das
últimas 48 h** (n = 0 com valor), e a tabela não tem `updated_at`, então não
existe distribuição contra a qual comparar. Registro como **item de olho pra
próxima ronda**, não como achado. Nas últimas 48 h: 96 `ready` e os 4 `pending`
são todos desta noite — pendência velha acumulada não existe.

`training_jobs`: **1 linha obsoleta** (job `ebf5cc56` nunca saiu de
`queued/running` com a voz `f4b9b0f2` já `ready`) — escrituração, ninguém
esperando. Sem fato novo, não re-anotei.

## 9. `marcelopersonalthe32@gmail.com` — o 🚨 da varredura, e por que NÃO é chamado

A varredura levanta ele como *"acesso vivo, com crédito e sem nenhuma voz
pronta"*: **298.950 créditos**, **34 dias** sem voz, acesso até 05/10. Fui ver
antes de reportar como aluno abandonado, e **não é**: a própria linha da voz
`f6f82819` (`failed`, 10/08) já carrega a explicação escrita pela casa —
o treino falhou por **erro nosso de infraestrutura**, **os 10.000 créditos foram
devolvidos no mesmo dia**, e na análise posterior descobriu-se que o arquivo é
**uma entrevista com mais de uma pessoa falando**, que o treino não separa. Isso
já é o `#128` (`ignored`). A bola está com ele: precisa mandar áudio novo.
**Não abri, não reabri o `#128`, não anotei** — sem fato novo, anotar é ruído.

## 10. Placar

- **80 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **44 de atendimento**, **36 técnicos**. Mais **10** em `aguardando_aluno`.
- **Delta contra os 78 das 22hZ, reconciliado inteiro:** `+#371` (atendimento,
  `fast-help`, 23:57Z) `+#372` (técnico, **meu**, 00:18Z). **Nada fechou.**
  Atendimento 43+1 = **44**; técnicos 35+1 = **36**; total **80**.
  `aguardando_aluno` segue **10**.
- **Fechados que voltaram a disparar: 0.** Recorte de 72 h com
  `last_seen_at > resolved_at`: **vazio**.
- `garantia_na_fila.cjs` 00:16:24Z — 79 abertos no momento da corrida · 19
  parecem pedido de reembolso · 60 pessoas · **controle positivo OK (2/2)** ·
  **8** perderam a janela · **4** vencem em 48 h · **0** na renovação.
- **"Perderam a janela" 6 → 8, e desta vez é GENTE, não rótulo** (§1). A correção
  de 12/09 continua valendo: os *"24"* das rondas antigas devem ser lidos como 6,
  e agora são 8.
- **PRs abertos: 36.** Reconciliado: 35 às 22hZ + **#257** (criado 22:41Z,
  aberto) = **36**. `--limit 200` **declarado** — o default de 30 corta em
  silêncio, foi o erro que eu corrigi ontem.
- **Calibragem do Vigia** (ordem 27/08 §5): dos **35** cartões com
  `reported_by='vigia'` desde 27/08, **4** `ignored` = **11,4 %** (era 11,8 %).
  Alvo ≤ 10 %; linha de base 21 %. (23 `fixed`, 6 `investigating`, 4 `ignored`,
  1 `aguardando_aluno`, 1 `open` — o `#372` de agora.)

⚠️ O rótulo *"SEM LINHA NO NOSSO BANCO p/ este e-mail"* que a ferramenta imprime
continua **NÃO sendo veredito de pagamento** — é ausência de linha local.
**Décima quarta ronda** registrando.

## 11. Nota de instrumento — o que eu quase errei hoje

**Quase reportei "5 de 6 imagens da PRÓPRIA PLATAFORMA são bloqueadas".** Era
manchete e estava **errada**. O que me segurou foi abrir a coluna `prompt` antes
de escrever: as 5 bloqueadas têm `credits_cost = 0` e prompt vazio — são
**upload**, e a única realmente gerada **passa**. Reportado do jeito errado, isso
teria mandado alguém caçar bug no Gerador de Imagem.

É **exatamente** a família dos falsos do `#100` e do `#152` que a ordem de 27/08
mediu: concluir a partir de uma tabela sem perguntar **o que produziu aquela
linha**. A defesa que funcionou aqui foi barata — antes de afirmar o que a linha
significa, olhar as colunas que dizem **de onde ela veio** (custo e prompt), e
conferir o padrão na base inteira (153/958) em vez de num caso só.

Também registro o que **não** consegui medir e por quê: duração de `video_clone`
(§8), porque `elapsed_seconds` está nulo em 100 % dos `ready` das últimas 48 h.
Sem baseline eu **não chamo de travado**. Dívida de instrumento, não chamado.

**Continuo sem conseguir ler o Mission Board desta máquina**
(`DB_ENCRYPTION_KEY` ausente no ambiente). Dívida de instrumento, décima ronda
registrando.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não reclassifiquei
cartão de ninguém, não abri chamado para classe que já tem cartão aberto, não
abri chamado para bug que já tem PR, não fiz nenhuma conta de dinheiro, **não
abri as imagens da aluna do `#371` e não dei veredito sobre o gate ter acertado
ou errado nelas**, não reapresentei o patch de face-gate que o Frank refutou, não
chamei de travado o clone de 62 min sem ter baseline, e não toquei em e-mail não
lido.
