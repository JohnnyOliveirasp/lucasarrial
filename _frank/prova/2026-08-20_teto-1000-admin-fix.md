# Fix do teto-1000 nas métricas do /admin (churn, GPU, cortesia, dedupe)

Continuação do incidente `72a4c9db` (teto silencioso de 1000 do PostgREST).
Varredura de origem: `_frank/prova/2026-08-20_teto-1000-varredura-e-fix.md`.
Branch: `feat/teto-1000-admin`. Padrão de conserto: o mesmo do PR #13
(commit `c5f67bd`) — paginar com ordem estável, `.in()` em blocos, guarda que
falha ABORTA.

## O que foi corrigido

Novo helper compartilhado **`frontend/src/lib/db/paginate.ts`** (`fetchAllPages`):
pagina com `.range()` até a página vir incompleta e lança em erro (nunca
degrada pra lista vazia).

| Arquivo | Query | Conserto |
|---|---|---|
| `lib/admin/churn.ts:53` | `payment_events PURCHASE_APPROVED` — **JÁ TRUNCAVA (1099 > 1000)** | paginado `.order("id")` |
| `lib/admin/churn.ts:49` | `payment_events SUBSCRIPTION_CANCELLATION` (198) | paginado `.order("id")` |
| `lib/admin/churn.ts:54` | `subscription_cancellations` (175) | paginado `created_at desc + id` (mantém "1ª vista = mais recente") |
| `lib/admin/churn.ts:69` | `profiles .in(id, surveyUserIds)` | `.in()` em blocos de 500 + throw em erro |
| `lib/admin/totals.ts:77` | `runpod_spend_log balance_usd` (623, ~1/dia) | paginado `.order("at")` (PK) |
| `lib/admin/totals.ts:78` | `courtesy_grants amount` — **extra, mesmo padrão no mesmo `Promise.all`** | paginado PK `(campaign_id, email)` |
| `lib/admin/queries.ts:130` | `runpod_spend_log` por período | paginado `.order("at")` |
| `lib/admin/queries.ts:137` | `courtesy_grants` do período — **extra, idem** | paginado PK |
| `lib/courtesy/service.ts:45-48` | `courtesy_campaigns` / `courtesy_grants` (painel) | paginado `created_at desc + id` / PK |
| `lib/incidents/ingest.ts:40-43` | `incident_occurrences .in(ref_id)` (dedupe Sentinela) | `.in()` em blocos de 200 (pior caso 600 linhas < teto) + throw |

**NÃO tocado (de propósito):**
- `totals.ts:65-68` (`firstReadAt`): já correto com `.limit(1)` — conferido na varredura, ordem do card.
- `courtesy/service.ts` `processCourtesyCampaigns` (linhas ~245/271) e
  `claimCourtesyOnLogin`: também são selects sem range, mas alimentam AÇÃO de
  crédito (território da regra 9-A) e se auto-drenam entre sweeps de 5min
  (truncar atrasa, não corrompe). Ficam pra card próprio se o Frank decidir.

## Prova (réplica read-only contra produção, 20/08 — script em `_Bugs/teto-1000-admin/prova.mjs`, pasta local/gitignored; a saída bruta está abaixo)

Nenhum write, nenhum e-mail, nenhum crédito. Saída bruta:

```
== TAREFA 1: churn.ts / payment_events PURCHASE_APPROVED ==
linhas vistas   VELHA(sem range): 1000
linhas vistas   NOVA(paginada)  : 1099
pagantes(paid)  VELHA: 280 | NOVA: 309
trial(freeOnly) VELHA: 414 | NOVA: 456
canceledPaid    VELHA: 30 | NOVA: 31
canceledFree    VELHA: 158 | NOVA: 165
churnPaidPct    VELHA: 10.71% | NOVA: 10.03%
churnFreePct    VELHA: 38.16% | NOVA: 36.18%
e-mails que a VELHA não enxergava/classificava errado: 96

== TAREFA 2: bombas-relógio (contagem exata hoje vs teto 1000) ==
payment_events SUBSCRIPTION_CANCELLATION: 198 linha(s) (abaixo do teto hoje)
subscription_cancellations: 175 linha(s) (abaixo do teto hoje)
runpod_spend_log: 623 linha(s) (abaixo do teto hoje)
courtesy_grants: 1 linha(s) (abaixo do teto hoje)
courtesy_campaigns: 1 linha(s) (abaixo do teto hoje)
incident_occurrences: 164 linha(s) (abaixo do teto hoje)
profiles: 1295 linha(s) ⚠️ ACIMA DO TETO
```

Leitura: o /admin mostrava churn pago **10,71%** quando o real é **10,03%**, e
churn trial **38,16%** vs **36,18%** — a lógica velha não enxergava 99 compras
e classificava errado (ou nem via) **96 assinantes**, 29 deles PAGANTES. As
demais tabelas estão abaixo do teto hoje, então nelas o antes/depois é idêntico
por construção — o fix é a bomba desarmada, provada pelo caso que já estourou.

## Qualidade

`NODE_ENV=development npx tsc --noEmit` → exit 0.
`NODE_ENV=development npx eslint <6 arquivos tocados>` → exit 0.
