# Vigia — ronda de 02/09/2026, 10hZ (07h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri 1 chamado (#232) e anotei em
3 (#226, #47, #173). Não fechei, não reabri, não mudei status de nada, não
respondi aluno, não escrevi rascunho, não toquei em crédito.**
Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado,
avisado ou reprocessado nesta ronda.
Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem do Johnny de
31/08. Nada foi mandado pro privado.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 Johnathan Pires — a garantia de R$ 2.391 vence HOJE às 21h BRT, e ele falou em processo

Chamado `954ca6c9` (#173), `investigating`. Fato novo desta janela, caixa **uid
417** (01/09 21:27 BRT), palavras dele:

> *"Vocês são bandidos profissionais, já estou buscando as providências cabíveis.
> Aguardem!"*

É a resposta ao e-mail em que a Fast admite, às 20:44 BRT, que o **terceiro**
prazo dado a ele foi descumprido. O relógio: pelo próprio levantamento da Fast na
Hotmart, a garantia das **três compras aprovadas de 27/08 (R$ 2.391,00)** vence
**hoje, 02/09, às 21h BRT**; depois disso o cancelamento deixa de ser automático.

A ronda das 23h marcou contato com ele para **02/09 21h BRT** — **a mesma hora em
que a garantia expira**, sem folga nenhuma. Se o contato atrasar como os três
anteriores, ele perde o cancelamento automático por causa da nossa demora.

**A pergunta que está parada desde 27/08 e é a causa raiz de tudo:** as três
compras dão direito ao processamento da voz dentro do FastCloner? É decisão do
Johnny. Não é bug, então **não abri chamado** (ordem de 27/08) — mas é o item
mais caro da mesa hoje.

### 0.2 Katia — o chamado mais velho da fila se reabriu sozinho, e alguém precisa OUVIR 5 segundos

Chamado `ce6e157d` (#47), de **19/08 — 14 dias**, o mais velho. Estava **fechado
desde 28/08** ("não há defeito técnico vivo"). A aluna escreveu de novo e a
reincidência o devolveu para `open` às **06:15Z de hoje**. Até a ronda das 00hZ o
placar dizia "open: 0".

O que ela escreveu (uid 418):

> *"ela está cortada no final e continua com os mesmos problemas… não sei se
> consigo continuar esperando… assim poderei cancelar a minha assinatura e migrar
> para outra plataforma."*

Cobrança de segunda vez **e** ameaça de cancelamento. A Fast respondeu às 06:15Z
(Enviados uid 447) prometendo *"vou passar isso agora mesmo pro responsável
técnico"* — dívida com a equipe já criada.

**O que eu medi, e onde eu paro.** O áudio é `81d4f3f4` (25/08 22:48Z, 555 chars,
39,8 s). Contra a queixa de "cortada no final", a duração joga **a favor** do
áudio: as outras 11 gerações do mesmo texto vão de 32,3 s a 37,5 s — esta é a
**mais longa de todas**, não há truncamento de conteúdo. Mas o QA dela não está
limpo: **`rate_flagged` 10 de 11 trechos, `regens` 10, `coverage_flagged` 2**, e
`coverage_min_visto` nulo (geração anterior ao #147). `exhausted` 0 — passou pelo
nosso próprio portão.

**Não dou veredito** (ordem de 27/08 §4: o que depende de ouvir não é veredito
meu). **Pedido concreto: alguém ouve os últimos 5 segundos de `81d4f3f4` e diz se
corta.** Isso encerra a discussão em um minuto.

Registro sem afirmar que consertam: **dois PRs abertos são deste chamado e nenhum
foi mergeado** — **#56** (`feat/tts-chunkmax-por-job`, *"causa medida do silêncio
sumido — chamado 47"*) e **#23** (`fix/fast-email-dedupe-por-queixa`, *"caso
Katia"*). Fechar de novo sem olhá-los repetiria 28/08.

---

## 1. O que eu ABRI — #232 (`51d86460`)

**"Chamado reaberto sozinho continua carimbado como resolvido: a guarda existe,
mas só no caminho manual."** Técnico, `reported_by='vigia'`.

A reabertura automática por reincidência está **certa e é intencional**
(`incidents/reportar.ts:67` e `:97`) — conferi no código antes de acusar, e por
isso **não** é isso que eu abri. O defeito é o que essa mesma atualização
(`reportar.ts:96-105`) **não** faz: não zera `resolved_at`, `resolved_by` nem
`resolved_commit`.

A guarda **já existe** — só não neste caminho:
`api/v1/admin/incidents/[id]/route.ts:41-49` e `api/v1/agent/actions/route.ts:113-126`
limpam os campos quando a mudança vem pela tela/agente, e o comentário de 20/08
nos dois diz com todas as letras que sem isso *"vira um registro que se
contradiz"* — e **nomeia este mesmo chamado**: *"Achado vivo: ce6e157d estava
investigating com resolved_at de 19/08"*. Hoje o `ce6e157d` está exatamente
assim de novo.

**Mesma classe do #217 e do #215: conserto armado numa direção só.**

**O que eu NÃO afirmo.** O estrago medido hoje é **1 linha** — é o único
não-fechado com `resolved_at` preenchido (`open` 1/1; `investigating` 0/7;
`aguardando_aluno` 0/12). Li os dois consumidores e **os dois filtram por
`status`**: `admin/falhas/page.tsx:249` só mostra "Corrigido" se
`status==='fixed'`, e `health-report/route.ts:33-34` casa `.eq(status,'fixed')`
**com** `.gte(resolved_at, 7d)`. **Nenhum número que o Johnny lê está mentindo
hoje.** O defeito é o registro contraditório em si e a próxima consulta que
confiar em `resolved_at` sem aviso.

**Dinheiro:** não envolve.

### As 3 checagens obrigatórias, feitas antes de abrir

1. **Já existe?** Fila aberta E fechada por `signature`/`kind`/`title`/
   `description` com `resolved_at|reabert|reportar.ts`: **1 achado, o #67
   (`fixed`)**, que é a direção **contrária** (fechar sem gravar a data). Nada
   aberto nesta classe.
2. **Já foi corrigido?** `git log origin/main -- .../incidents/reportar.ts`: o
   commit mais recente é **32bdbfd** (título do chamado reaberto, mergeado 01/09
   21:15) e **não toca em `resolved_at`**. `gh pr list --state open` (**26 PRs**):
   **nenhum** mexe em `reportar.ts` nem em `espera.ts`.
3. **Dinheiro?** Não envolve.

**Não proponho o conserto.** Quem decide e fecha é o Frank.

---

## 2. #226 — o PR #147 subiu de verdade, e eu quase reportei o contrário

### 2.1 A boa: está no ar, conferido por DADO e não por grep

O **PR #147** foi **mergeado em 02/09 01:16Z** (`dff2f23`). Ele mexe em
`runpod-worker/` (`tts_qa/loop.py`, `inference.py`), que sobe por **imagem
própria** — merge na main não provaria nada. A prova está no banco, e o corte
aparece na hora exata:

| geração | quando | `exhausted` | `exhausted_score_max` |
|---|---|---|---|
| `d1847536` | 01:09Z (**antes**) | 1 | **nulo** |
| `1abdf1f3` | 02:32Z (**depois**) | 1 | **207** |
| `d909bd27` | 07:29Z | 1 | **50** |
| `4d4d303c` | 07:41Z | 0 | nulo / `[]` (correto) |

**A telemetria está gravando na GPU.** Isso responde, com evidência de campo, a
lição do `f3449c6` ("está no ar" não se prova com grep no bundle).

**Ressalva minha, e ela é um erro que eu cometi nesta ronda:** procurei o campo
pelo nome errado (`exhausted_score`) e tinha concluído *"0 gravações, não subiu"*.
Os campos são `exhausted_score_max` e `exhausted_scores`. **Falso negativo meu,
pego e corrigido antes de virar relatório** — registro porque é exatamente o tipo
de erro que já me fez reportar "0 abertos" havendo quatro.

### 2.2 A ruim: a fila não anda, e o pior caso da janela foi cobrado

**Primeira leitura do score novo:** `1abdf1f3` saiu com **score 207 e coverage
0,778** (piso 0,85). **Não dramatizo:** é um áudio de 3,3 s, teste do próprio
aluno, não material final.

**O caso sério da janela é anterior à telemetria:** `44a2f87e`
(`valimreis@gmail.com`, 02/09 00:28Z, **66,4 s**) saiu com **`exhausted` 6 e
coverage 0,682** — o pior que eu já medi — **e foi cobrado**. Casado por
**`ref_id`**, não por timestamp (armadilha do #152): `credit_transactions`
`8e40a597`, `ref_type='generation'`, `ref_id = 44a2f87e…`, **−1.082 cr**.
**A cobrança está CERTA pela regra vigente** (entregamos), **não peço estorno** e
não afirmo nada além disso. O que registro é o que o aluno recebeu por 1.082
créditos.

### Recontagem — terceira ronda seguida sem cair

| medida | 00hZ | 10hZ |
|---|---|---|
| entregues com `exhausted > 0` | 300/686 (43,7 %) | **307/695 (44,2 %)** |
| alunos distintos atingidos | 136 | **138** |
| **últimas 24 h** | 32/66 (48,5 %) | **32/65 (49,2 %)** |
| abaixo do piso configurado | 68/408 (16,7 %) | **70/417 (16,8 %)** |

O #147 **mede**, e agora está medindo de fato. Ele **não segura**: nenhuma das
entregas acima teria sido impedida por ele. **O #226 segue sem conserto que
impeça a entrega.**

---

## 3. Placar honesto

Consulta **sem cláusula de `signature` e sem cláusula de `status`**:

| status | 00hZ | 10hZ |
|---|---|---|
| fixed | 160 | **161** |
| ignored | 42 | 42 |
| aguardando_aluno | 8 | **12** |
| investigating | 11 | **7** |
| open | 0 | **1** |

**Não-fechados: 20** (era 19). **10 técnicos, 10 de atendimento.**
Por quem abriu: **2 nossos** (`#222` e `#226`, frank) + **1 novo meu** (`#232`),
**17 nascidos de aluno** (12 `fast-help`, 5 `fast`).
Mais velho da fila: **`ce6e157d` (#47), de 19/08 — 14 dias** (a ronda passada
apontava o #99, de 23/08; o #47 estava fechado e voltou).

**O que mudou de estado sem ser por mim:** `#217` foi **fechado** (`fixed`, commit
`9f1e452`) — é o chamado que eu abri ontem, e o conserto saiu em menos de 24 h.
**Quatro** saíram de `investigating` para `aguardando_aluno`. Nasceram `#230`
(fechado) e `#231`.

**A objeção das últimas cinco rondas continua e mudou de tamanho:** contando como
a rotina manda (`open` + `investigating`) daria **8**. Os **12** em
`aguardando_aluno` seguem sem dono e fora de todo número que o Johnny vê — e
agora eles são **maioria** da fila não-fechada.

---

## 4. Frente 1 — não-fechados, sem filtro

Os três "nossos" e o que mudou:

| id | quando | o que é |
|---|---|---|
| `702cc916` (#226) | 01/09 17:52Z | QA esgotado entregue em silêncio. **Ganhou a medição pós-#147 (§2).** Maior número técnico da mesa. |
| `3ca22d47` (#222) | 01/09 15:54Z | Compra órfã presa em outro e-mail. `aguardando_aluno`, **sem mexer há 18 h**. |
| `51d86460` (#232) | 02/09 10hZ | **Aberto nesta ronda** (§1). |

`91f15c99` (#217) saiu da lista: **fechado**.

### Fechados que voltaram a disparar

**1 de 203**, e é o conhecido: `acf8acd6` (#8), último disparo em **22/08** —
**0 com ocorrência nas últimas 48 h**. `resolved_at is null` entre os fechados:
**0 de 203** (o conserto do #110 segue segurando).

Mantenho a objeção do `7963388e` (#90): este número é **cego** para a classe
*"promessa sem dono"*, que não tem assinatura nem detector. E acrescento uma
cegueira nova, medida hoje: **chamado que se reabre some deste contador** — o
`ce6e157d` disparou às 06:15Z e não aparece aqui porque deixou de ser "fechado".

---

## 5. Frente 2 — caixa, chat e WhatsApp

**Caixa (25 últimos, `BODY.PEEK`, só `SEEN`, nenhum não-lido tocado; fila da
Fast: 0 não-lidos).** Duas mensagens novas desde as 00hZ:

- **uid 417** — Johnathan (§0.1).
- **uid 418** — Katia (§0.2). Chegou na nossa caixa **depois** do uid 417 apesar
  do cabeçalho dizer 01/09 17:53 +0100; é atraso de entrega do lado dela, não
  nosso — registro só para ninguém achar que a Fast demorou 13 h.

**Nenhum aluno cobrando pela segunda vez ficou sem chamado.** Katia → #47 (open),
Johnathan → #173, Márcio → #212, Paulo Martin → #229, Silmara → #230 (fechado).

**Chat do app: 10 mensagens, 1 aluno** (`sidneysantos100`) — é o **#231**, já
aberto. Reincidência, não chamado novo.

**WhatsApp (`agent_messages`): 0 mensagens em 14 h.**

**Processo, 1 linha (ordem de 27/08 — não vira chamado):** a Fast disse duas
vezes hoje *"vou confirmar com a equipe"* sobre o **tempo MÁXIMO de áudio de
treino** — ela só tem o mínimo de 20 min no manual. É a mesma dúvida que está por
trás do #99 (`6c38c99d`, áudio de 30+ min), aberto desde 23/08. **Uma linha no
manual da Fast encerra os dois.**

---

## 6. Saúde

**Gerações nas últimas 6 h: 4, todas `ready`.** 0 `failed`, 0 travada, 0 `pending`.
Volume baixo é madrugada BRT, não fila presa (00hZ mediu 33 em 6 h).
**Vozes nas últimas 24 h: 14 `ready`**, 0 falha, 0 em treino.

---

## 7. O que eu NÃO fiz

Não fechei, não reabri, não mudei status de nada (o `#47` voltou para `open`
sozinho, pela reincidência automática — conferi no código de onde veio). Não
escrevi resposta nem rascunho para aluno. Não mexi em crédito, não estornei. Não
toquei em e-mail não-lido. Não abri, li ou reprocessei nada da planilha.

Abri **1**: `51d86460` (#232). Anotei em **3**: `702cc916` (#226), `ce6e157d`
(#47) e `954ca6c9` (#173).

---

## 8. Passo fixo de fim de ronda

Log escrito na **main** e empurrado. `git log --oneline origin/main..HEAD`
conferido **vazio** depois do push. Nenhuma branch criada — não houve código.
