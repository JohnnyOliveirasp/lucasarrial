# Ronda do Vigia — 08/09/2026, 00h UTC (07/09, 21h BRT)

Sensor. Varri, medi, **abri 1 chamado** e **anotei 1 objeção**. Não respondi
aluno, não escrevi rascunho, não mexi em crédito, não fechei e não reabri nada
(14-A).

Repo em `main`, `pull --ff-only` (trouxe até `b55db26`), `_frank/ordens/README.md`
e `_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 NOVO — abri o `#303`: o sistema entrega ao agente uma data sem dizer que ela é RENOVAÇÃO, e a casa transforma isso em prazo de vencimento

`#303` / `a0bc1f7e`, técnico, `reported_by='vigia'`.

O Frank mediu isto na ronda das 23hZ e **decidiu não abrir chamado** (item 12.3
do log dele), por receio justificado de fazer `INSERT` na mão em `incidents`.
Concordo com a precaução dele e discordo do desfecho: **abrir é função minha**
(14-A), e achado de código com `arquivo:linha` e vítima medida que fica só num
log de ronda é achado que some na ronda seguinte. Abri.

**O defeito**, `frontend/src/lib/agent/account.ts:258-263`:

```
const acesso = profile.access_until
  ? `ativo até ${dtBR(profile.access_until)}`
  : profile.access_source ? "ativo" : "SEM assinatura ativa";
```

A string entregue ao agente **não carrega o status da assinatura**. Para
assinatura `ACTIVE`, `access_until` **é a próxima cobrança** — a data em que ela
RENOVA. Quem ler aquilo como prazo inventa urgência que não existe. O bloco
vizinho `linhaGarantiaHotmart()` (`account.ts:266-268`) resolveu exatamente esse
tipo de cegueira, e existe porque a ausência dele produziu o `#198`.

**Teste de bolso da ordem de 27/08:** se o código entregasse o status junto da
data, a casa não teria escrito o prazo falso. → erro de **SISTEMA**.

**Vítima confirmada: 1.** Conta `49110dde`
(`leonicemleandrosociedadeadvoca@gmail.com`). O e-mail que saiu daqui em 07/09
**14:27Z** (Enviados uid 1243) disse *"o seu acesso está indo até 09/09, ou
seja, mais dois dias"* e recomendou regravar *"antes do prazo"*. Na Hotmart
viva: `HCIA7GIM`, status **ACTIVE**, `date_next_charge` **2026-09-09T12:00Z** —
bate exatamente com o `access_until`. É renovação. Ela não ia ser trancada e não
ia perder crédito nenhum. Saldo citado no próprio e-mail: **149.049**; oito
horas depois, **121.613** — **27.436 créditos** queimados depois de a casa ter
criado a pressa. **Nexo causal não está provado**, e não afirmo que está: o que
está medido é a **sequência** e o **tamanho**.

**Exposição — 265, e isso NÃO são vítimas.** Medi por conta própria
(`access_source='hotmart'`), sem herdar o número do Frank: **661** com acesso
vivo, **265** com `access_until` dentro de 7 dias, **123** dentro de 3 dias.
Bate com a medição independente dele às 23hZ (661/264/123; a diferença de 1 é o
relógio andando). São quantos **podem** receber a frase errada. Mensagem errada
de fato, **1**. Faço a distinção de propósito: é este tipo de número que já
virou achado inflado três vezes neste repositório.

**As três checagens do §3 (ordem de 27/08), feitas antes de abrir:**

1. **"Já existe?"** — não na fila aberta (varri os 34, sem filtro de
   assinatura). A **classe** já apareceu 3× e sempre foi fechada, nunca
   consertada: `#48` (`4ce5b24c`, `ignored` 19/08 — Josilene achou que perdia
   185.969 créditos "no vencimento de 22/08"), `#136` (`fixed`), `#198`
   (`fixed`). É reincidência da classe, e é o argumento de por que desta vez
   precisa virar conserto.
2. **"Já foi corrigido?"** — não. `git log origin/main` até `b55db26` não tem
   nada disso. `gh pr list --state open`: 20 PRs; o único que toca
   `lib/agent/account.ts` é o **PR #186** (Message-ID `#259` + `ref_type`
   `#260`), que não encosta nestas linhas. O `#265` também vive em `account.ts`,
   mas nas linhas **171-188** (janela de garantia) — defeito diferente.
3. **Dinheiro** — **não peço estorno e não afirmo cobrança indevida.** O dia
   inteiro da conta foi conferido por `ref_type` (não por `kind`, armadilha de
   20/08): 5× `studio_audio` −550 com 5× +550 `studio_audio_refund` **casados
   por `ref_id`** (líquido zero), 5 `video_clone` todos `ready`, 1 `image_video`
   −9000 entregue com `video_path` gravado. O crédito foi gasto em entrega que
   aconteceu. O problema é a pressa que a casa inventou, não o débito.

**O conserto é card + PR, não patch de madrugada.** Duas saídas, ambas em
código: (a) passar status/renovação junto do `access_until` em `account.ts`, no
desenho do `linhaGarantiaHotmart()`; ou (b) proibir o agente de citar a data
como prazo. Quem decide e fecha é o Frank.

### 2. 🟠 Simone — a promessa por escrito passou de 8 horas e virou o dia

`#301`/`86de22c6`. A Fast prometeu em nome da casa às **16:45Z**; agora é
**00h5xZ do dia 8**. Sem novidade desde a ronda das 22h e sem nenhum movimento
em três rondas. Compra em 31/08 (`HP2163322038` R$ 672,00 + `HP3504926128`
R$ 303,40 = **R$ 975,40**, ambas APPROVED), conta `b85a6c34` sem acesso,
0 créditos, 0 vozes, 0 gerações — **nunca usou nada**. E o prazo de garantia
**não é conferível no nosso banco** (0 linhas em `payment_events`; produto
avulso não alimenta o webhook), então o `#265` não alcança este caso.

### 3. 🟠 Lucila — nada novo, e é isso que incomoda

`#299`/`eec81565` + `#300`/`a10723ae`. Nenhum e-mail dela desde 13:41 BRT.
Duas assinaturas pagas ativas, **R$ 291**, **200.000 créditos intactos em cada**,
**zero voz criada** em 46 dias. A Fast já encaminhou e já disse que não processa
devolução. Daqui só anda com o Johnny.
⚠️ Endereço vivo pra resposta: `lucilablanco75` (o `blancolucila539` está com
caixa cheia — `#291`).

### 4. 🟡 Continuam na fila de decisão, sem mudança nesta ronda

- **`#290`** — o "pode" dos 8, pendente desde 04/09, ~800k créditos parados.
- **`#15`** — `migration 82`, 39 dias.
- **`#254` / Diego** — relógio em **08/09 12:00Z**, ou seja **hoje**.
- **`marcelopersonalthe32`** — prazo de reembolso vence **11/09**.
- **6 em `aguardando_aluno` com 7d+** (`#47`, `#99`, `#172`, `#206`, `#207`,
  `#214`) pedindo SEGUNDA tentativa, não silêncio.

---

## Anotei (objeção, sem reabrir)

**`#48`** / `4ce5b24c` (`ignored` desde 19/08). Nota gravada às 01hZ, `agent_notes`
3 → 4, status **inalterado**. O chamado tratou a confusão da Josilene como
dúvida de aluna; a mesma confusão voltou em 07/09 com causa nossa medida. A nota
aponta pro `#303` pra que a próxima leitura do `#48` saiba que havia sistema
atrás da dúvida. **Não reabri** — 14-A.

---

## A contagem de 22h fechou (e a resposta veio do Frank)

Eu tinha registrado às 22h que `ignored` caiu 46→45 com `investigating` subindo
2, e que **não conseguia dizer qual chamado tinha voltado pra fila**. O Frank
respondeu no log das 23hZ: **era o `#296`**, reaberto deliberadamente pelo
EXECUTOR às ~21:25Z porque a 2ª ocorrência mudou de assunto. Minha sonda voltou
0 linhas **corretamente**, porque `limparFechamento()` apaga `resolved_at` de
propósito.

**A sonda que funciona, pra próxima ronda não refazer o caminho errado:**

```sql
select numero, status, last_seen_at, resolution_note
from incidents
where status in ('open','investigating') and resolution_note is not null;
```

`resolution_note` é o único vestígio que a reabertura não apaga.

Nesta ronda a aritmética fecha sozinha: `ignored` 45 → **46** (o Frank fechou o
`#302` como duplicata do `#296`), `investigating` 34 → **33**, e o meu `#303`
entra como `open`. Total **292**.

⚠️ **O que continua sem conserto é a auditoria:** `incidents` não tem histórico
de mudança de status (conferido no `information_schema`), então reabertura
legítima segue indistinguível de silenciosa. Isso é anotação, não chamado.

---

## Números

- **34 chamados abertos** (`open` + `investigating`), contados **sem filtro de
  assinatura**: **17 técnicos**, **17 de atendimento** (campo `categoria`).
  Eram 33 na entrada da ronda; o +1 é o meu `#303`.
- **Chamados abertos por mim: 1** (`#303`).
- **Fechados que voltaram a disparar: 0.** O único da base inteira com
  `last_seen_at > resolved_at` continua sendo o `acf8acd6` (`#8`,
  `training:user_dataset`, `fixed`), último disparo em **22/08** — 17 dias. Não
  é regressão viva.
- **12 em `aguardando_aluno`**, fora da contagem de propósito. 6 com 7d+.

## Saúde do sistema agora

Limpo:

- **gerações últimas 4 h: 29 — as 29 `ready`.** Nenhuma pendente.
- **`failed` nas últimas 24 h: 0.**
- **Vídeo Clone últimas 4 h: 25 — todos `ready`.**
- **Imagens últimas 4 h: 29 — 28 `ready`, 1 `pending` com 1 minuto de vida**
  (`62a23658`, criada 00:11Z). Não é presa.
- **vozes presas** (`training`/`uploading` há mais de 90 min): **0**.
- Varredura marcou 5 "presos"; nenhum é defeito novo, e digo um a um:
  - `plastiassist@gmail.com`, voz `774a63c2` em `training` — **criada 00:12Z,
    atualizada 00:13Z**. É desta ronda, não é travamento.
  - `tania-araujo` (`awaiting_training`) — a aluna subiu o áudio e não clicou em
    treinar; nada tentou rodar e falhar. Já triado nas rondas anteriores.
  - `hellengrasso` (`rejected_too_short`) — mensagem já é a corrigida pelo PR
    #52; ela precisa gravar mais. O Frank mediu a classe às 23h: ~20 vozes em 2
    meses, não é surto.
  - `marcelopersonalthe32` — coberto pelo `#65` (`fixed`), contatado, créditos
    intactos, depende de gravação nova dele. Prazo de reembolso em 11/09.
  - `luanmarcal.com@gmail.com` — import quebrado em 29/08 por arquivo do
    **Drive** não-público. **Fora do meu escopo por ordem de 29/08** (onboarding
    antigo por Drive/WeTransfer): não abri, não classifiquei, não avisei.
  - Mais 1 `training_jobs` obsoleto (`ebf5cc56` → voz `f4b9b0f2` já `ready`):
    escrituração pendente, ninguém esperando.

## A rajada de `no_speech` de 14h — o sistema acertou, e registro por quê

5 falhas de `Vídeo Estúdio (limpeza de áudio)` entre 14:00 e 14:29, todas com
`error_message='no_speech'`, todas da Leonice. Fui conferir se era defeito nosso
e **não é**:

- os 5 débitos de −550 têm os 5 estornos de +550 **casados por `ref_id`**
  (`studio_audio` ↔ `studio_audio_refund`), 9 a 12 segundos depois de cada
  falha. Líquido **zero**;
- o `#295`/`9acd4ede` nasceu **`ignored`** de propósito: `finalize.ts:95-110`
  marca `userInputError: isInputError(out)`, e o `openBurstIncident` lê **esse**
  campo pra decidir. Erro de entrada do aluno não acorda o suporte;
- **`occurrences=2` para 5 falhas não é subcontagem**: a regra conta **rajadas**,
  e as falhas caem em duas janelas (14:00–14:02 e 14:29). Fui atrás disso
  achando que era bug de telemetria e não era. Registro pra ninguém gastar a
  próxima ronda no mesmo caminho.
- depois das 14:29, `studio_audio` rodou 7× (15:34, 16:29, 17:17, 22:17, 22:56,
  23:17, 23:26) **sem nenhum estorno**. Não ficou quebrado.

**Nenhum chamado aberto por isso.** Era o desfecho certo do sistema.

## A Leonice parou de queimar às 22:45

Último lançamento dela é **22:45Z** (−4.830, `video_clone`). Nada depois disso —
**1h30 sem gastar** até o fim desta ronda. Saldo agora: **116.783**.
**Não atribuo isso ao e-mail de correção do Frank** (uid 1285, ~22:5xZ): ela
parou **antes** dele sair. Digo o que está medido — parou — e não invento a
causa.

## Caixa (`BODY.PEEK`/`SEEN` — não toquei em não-lido)

**Nenhum e-mail novo desde a ronda das 18h.** Último uid segue **481** (Jackson
Lopes, 14:26 BRT) — quarta ronda seguida no mesmo ponto. Fila da Fast
(não-lidos): **0**.

Refiz a conferência de órfãos mesmo sem novidade — todo remetente recente tem
chamado ou desfecho:

| uid | quem | onde está |
|---|---|---|
| 470/471 | Jackson Nogueira (`jkakoalves`) | `#247`/`b229e491` — respondeu agradecendo |
| 473/474 | Iran Moura (`iran@ogr.com.br`) | `#298`/`1095bb4b`, técnico, aberto |
| 475/477 | Lucila (`contatoecocannabis`) | `#299`/`eec81565` |
| 476 | Lucila (`lucilablanco75`) | `#300`/`a10723ae` |
| 478 | Simone | `#301`/`86de22c6` |
| 479/481 | Jackson Lopes (`jkdeliberatus93`) | sem chamado, **correto** — cancelou sozinho; motivo no `#108` |
| 480 | Elane (`elaneyani`) | `#289`/`4ab7e04d`, aberto |

Ninguém cobrando pela **segunda vez sem chamado**. Nenhum *"já pedi ajuda várias
vezes"* órfão.

⚠️ Uma linha da Elane (uid 480) que vale a pena não perder: *"Como faço para não
gastar muitos créditos tentando… Quero gerar um vídeo mas fiquei com receio."*
É a mesma coisa que o `#303` produz pelo outro lado — aluno com medo de gastar
por não saber o que a conta dele custa/vale. Está dentro do `#289`, não abri
nada por isso.

## O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito ou
acesso, não fechei chamado, não reabri chamado, não mergeei PR, não escrevi
código de produção, não gastei GPU nem whisper.

Escritas da ronda: **1 chamado aberto** (`#303`, `INSERT` único, número pela
sequence `incidents_numero_seq` — não escrevi número na mão, e a `signature`
foi conferida como inédita antes de gravar), **1 nota de objeção** (`#48`, via
`anotar_incidente.cjs --confirmar`, que concatena e não sobrescreve), **1 aviso
no grupo** e **1 arquivo no git** (este log).

## Escopo respeitado

Nada da **planilha** foi lido, escrito, classificado, avisado ou reprocessado —
ordem de 29/08. O caso `luanmarcal` (import por Drive do onboarding antigo) foi
explicitamente deixado de fora por essa ordem. Nenhum chamado de causa-planilha
aberto ou reaberto. Aviso da ronda postado **no grupo** (`notify-grupo.sh`),
ordem de canal de 31/08 — nada no privado do Johnny.
