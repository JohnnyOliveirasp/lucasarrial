# Rodada 02h — 19/08 ~02:05 UTC

Rodada da rotina das falhas, 26 min depois da rodada 01h35. Não repeti o que
já estava medido: fui atrás do que a rodada anterior **afirmou sem conferir**.

Achei uma coisa séria, e ela é minha.

---

## 1. O achado: o fix do incidente nunca saiu da minha máquina

A rodada 01h35 escreveu, no §4 do relatório dela:

> "O fix está no bundle mas ainda não foi exercitado — não houve falha de
> timeout depois que subiu."

**Isso estava errado.** Conferi commit por commit:

| commit | o que faz | estava onde |
|---|---|---|
| `1c09508` (18/08 18:10) | grava `elapsed_seconds` na falha | **em `origin/main`** ✅ |
| `4418b0e` (18/08 21:12) | grava `delay_seconds` + `execution_seconds` | **só local** ❌ |
| `ddc0a4f` (18/08 21:16) | teto de 5s antes do estorno | **só local** ❌ |

O repositório estava **3 à frente e 3 atrás** de `origin/main`: enquanto eu
commitava, outra frente empurrou 3 commits de vendas/cenas, o `git pull`
abortou por divergência e os meus ficaram parados. Pela regra 1 (deploy é
`git push` na `main`), commit que não foi empurrado **não existe em produção**.

O teste decisivo descrito no §4 da rodada anterior — distinguir cold start de
hang comparando `delay` × `execution` — depende do `4418b0e`. Ele não estava
no ar. Se um timeout tivesse acontecido entre 01h e 02h, **não teria medido
nada** e a próxima rodada leria "o teste está armado" de novo.

### E tem uma segunda camada, que sobrevive ao push

Conferi as colunas no banco em vez de acreditar no DDL:

```
delay_seconds:     NAO EXISTE
execution_seconds: NAO EXISTE
elapsed_seconds:   existe
```

A **migration 82 não foi aplicada** (aguarda aval do Johnny, regra 21). Ou
seja: mesmo com o código no ar a partir de agora, `recordRunpodTiming` vai
cair no `logger.warn` e não grava nada até a 82 subir.

**Conclusão honesta: o teste decisivo continua NÃO armado.** O que está armado
é só o `elapsed_seconds` do `1c09508` — relógio de parede, que diz "morreu
perto do teto" ou "morreu cedo", mas **não separa fila de execução**. É metade
do diagnóstico. A outra metade depende de uma decisão do Johnny, não de código.

---

## 2. O que eu fiz com isso

- `git rebase origin/main` — os 3 commits reaplicados limpos, sem conflito.
- `npx tsc --noEmit` → **0 erros**.
- `npx eslint` nos 5 arquivos tocados → **0 erros**, 1 warning
  (`'path' is defined but never used`, em `webhookUrlFor`) que **já existia em
  `831b4c3`**, antes de eu encostar no arquivo. Não é meu e não inventei fix
  pra ele.
- Antes de empurrar, reli o código em vez de confiar na mensagem do commit:
  `recordRunpodTiming` faz **UPDATE separado**, tipado, e nunca lança; o
  `runpodGetStatus` novo no caminho da falha tem `AbortSignal.timeout(5000)`
  cobrindo inclusive o fallback pro endpoint B. **O estorno do aluno não
  depende da telemetria em nenhum caminho** — confirmado lendo as duas rotas,
  não a descrição.
- `git push` → deploy.

Custo real disso em produção enquanto a 82 não sobe: uma linha de `warn` por
falha e, no pior caso (webhook sem `delayTime`), até 5s a mais antes do
estorno. São ~2 falhas/dia. Aceito de propósito, e está escrito aqui.

---

## 3. Estado da fila (conferido agora, não herdado)

- **Incidentes abertos: 1** — `d3d8d1b2`, `investigating`, 13 ocorrências.
  Última segue **18/08 20:46:05 UTC** = **5h15 limpas**. Nenhuma nova.
- **Varredura de travados: 0 itens presos**, sem linha de erro de tabela.
- **generations com `failed` nas últimas 12h: 2** — as duas do incidente
  (18:05 e 20:46), ambas com `elapsed_seconds = null`, ambas **anteriores** ao
  deploy do `1c09508`. Coerente, não é regressão.
- **Movimento normal**: 20 gerações desde 22:10 UTC, 16 com `elapsed`
  preenchido, última `ready` às 01:52. A plataforma está produzindo.
- **Aluno pagante parado**: mantido o 0 medido às 01h35 (a régua não muda em
  26 min e nada novo entrou na fila). Não remedi pra não fingir trabalho.

## 4. Produção

- `pm2 aiverse` **online**, uptime 35 min, `unstable_restarts: 0` — bate com o
  deploy do `831b4c3` às 01:26, não é crash loop.
- Sweep **vivo** de 5 em 5 min, último às **02:00:02**.
- Segue o `expiração de trial FALHOU` a cada 5 min: é a trava de 18/08
  (`DESATIVADA MANUALMENTE ... zerou 14 pagantes`) funcionando e se registrando
  como falha. Normal, e continua ruim que o normal apareça como alarme.

---

## 5. O que precisa do Johnny (uma coisa só)

**Aplicar a migration 82** (`scripts/82_generations_runpod_timing.sql`): duas
colunas novas em `generations`, `delay_seconds` e `execution_seconds`, ambas
anuláveis. Não reescreve linha existente, não mexe em saldo, não mexe em
crédito. Sem ela, o incidente `d3d8d1b2` fica exatamente onde está há 19 dias:
a única causa que sobrou (hang do worker) não tem como ser provada, porque o
log do RunPod evapora ~30min depois do job e toda investigação posterior bate
em 404 — isso já foi provado com HTTP 404 na rodada das 17h.

## 6. O que eu mexi

Push dos 3 commits que já existiam (nenhum código novo escrito nesta rodada),
nota 24 no incidente `d3d8d1b2`, e estes dois documentos. **Nada de crédito,
nada de GPU, nada de e-mail pra aluno, nada apagado, nenhuma migration
aplicada.**

## 7. A lição, que é de processo e não de código

O playbook **P** já dizia "'o fix está pronto' não é 'o fix está no ar'". Eu
tinha o playbook e mesmo assim escrevi um relatório afirmando que o fix estava
no bundle — porque conferi que o **commit existia**, não que ele estava
**empurrado**. `git log` local responde a pergunta errada.

A verificação certa é `git log origin/main..HEAD`: se voltar qualquer coisa,
tem código meu que não está em produção. Isso vira passo fixo de fim de rodada,
antes de escrever qualquer frase sobre estado de fix. Registrado como **P2** no
playbook P (que já era da família certa — não abri letra nova pra não inchar o
arquivo com dois playbooks dizendo a mesma coisa).
