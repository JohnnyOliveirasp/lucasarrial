# Rodada 02h — 19/08 ~02:05 UTC

Rodada da rotina das falhas, 26 min depois da rodada 01h35. Não repeti o que
já estava medido: fui atrás do que a rodada anterior **afirmou sem conferir**.

Achei uma coisa séria, e ela é minha.

---

## 1. O achado: o fix do incidente nunca saiu da minha máquina

A rodada 01h35 escreveu, no §4 do relatório dela:

> "O fix está no bundle mas ainda não foi exercitado — não houve falha de
> timeout depois que subiu."

**Isso estava errado.** Conferi commit por commit:

| commit | o que faz | estava onde |
|---|---|---|
| `1c09508` (18/08 18:10) | grava `elapsed_seconds` na falha | **em `origin/main`** ✅ |
| `4418b0e` (18/08 21:12) | grava `delay_seconds` + `execution_seconds` | **só local** ❌ |
| `ddc0a4f` (18/08 21:16) | teto de 5s antes do estorno | **só local** ❌ |

O repositório estava **3 à frente e 3 atrás** de `origin/main`: enquanto eu
commitava, outra frente empurrou 3 commits de vendas/cenas, o `git pull`
abortou por divergência e os meus ficaram parados. Pela regra 1 (deploy é
`git push` na `main`), commit que não foi empurrado **não existe em produção**.

O teste decisivo descrito no §4 da rodada anterior — distinguir cold start de
hang comparando `delay` × `execution` — depende do `4418b0e`. Ele não estava
no ar. Se um timeout tivesse acontecido entre 01h e 02h, **não teria medido
nada** e a próxima rodada leria "o teste está armado" de novo.

### E tem uma segunda camada, que sobrevive ao push

Conferi as colunas no banco em vez de acreditar no DDL:

```
delay_seconds:     NAO EXISTE
execution_seconds: NAO EXISTE
elapsed_seconds:   existe
```

A **migration 82 não foi aplicada** (aguarda aval do Johnny, regra 21). Ou
seja: mesmo com o código no ar a partir de agora, `recordRunpodTiming` vai
cair no `logger.warn` e não grava nada até a 82 subir.

**Conclusão honesta: o teste decisivo continua NÃO armado.** O que está armado
é só o `elapsed_seconds` do `1c09508` — relógio de parede, que diz "morreu
perto do teto" ou "morreu cedo", mas **não separa fila de execução**. É metade
do diagnóstico. A outra metade depende de uma decisão do Johnny, não de código.

---

## 2. O que eu fiz com isso

- `git rebase origin/main` — os 3 commits reaplicados limpos, sem conflito.
- `npx tsc --noEmit` → **0 erros**.
- `npx eslint` nos 5 arquivos tocados → **0 erros**, 1 warning
  (`'path' is defined but never used`, em `webhookUrlFor`) que **já existia em
  `831b4c3`**, antes de eu encostar no arquivo. Não é meu e não inventei fix
  pra ele.
- Antes de empurrar, reli o código em vez de confiar na mensagem do commit:
  `recordRunpodTiming` faz **UPDATE separado**, tipado, e nunca lança; o
  `runpodGetStatus` novo no caminho da falha tem `AbortSignal.timeout(5000)`
  cobrindo inclusive o fallback pro endpoint B. **O estorno do aluno não
  depende da telemetria em nenhum caminho** — confirmado lendo as duas rotas,
  não a descrição.
- `git push` → deploy.

Custo real disso em produção enquanto a 82 não sobe: uma linha de `warn` por
falha e, no pior caso (webhook sem `delayTime`), até 5s a mais antes do
estorno. São ~2 falhas/dia. Aceito de propósito, e está escrito aqui.

---

## 3. Estado da fila (conferido agora, não herdado)

- **Incidentes abertos: 1 ao começar a rodada** — `d3d8d1b2`, `investigating`,
  13 ocorrências. Última segue **18/08 20:46:05 UTC** = **5h15 limpas**.
  Nenhuma nova. (Terminei a rodada com **3**: abri 2 à mão, item 3-A.)
- **Varredura de travados: 0 itens presos**, sem linha de erro de tabela.
- **generations com `failed` nas últimas 12h: 2** — as duas do incidente
  (18:05 e 20:46), ambas com `elapsed_seconds = null`, ambas **anteriores** ao
  deploy do `1c09508`. Coerente, não é regressão.
- **Movimento normal**: 20 gerações desde 22:10 UTC, 16 com `elapsed`
  preenchido, última `ready` às 01:52. A plataforma está produzindo.
- **Aluno pagante parado**: mantido o 0 medido às 01h35 (a régua não muda em
  26 min e nada novo entrou na fila). Não remedi pra não fingir trabalho.

## 3-A. O achado maior: escalação por e-mail não avisa ninguém

Puxei o log do `mail-sweep` e vi `escalated: 1` às 01:35. Fui atrás de qual
incidente ele tinha aberto. **Não tinha aberto nenhum.** Isso abriu o buraco.

### A assimetria, nas duas linhas que decidem

| canal | arquivo | condição | o que acontece |
|---|---|---|---|
| **chat do site** | `agent/respond.ts:308` | `if (reason)` | pausa a IA **e chama a equipe** (WhatsApp → e-mail) |
| **e-mail** | `agent/mail-respond.ts:377` | `if (reason && technical)` | **só** abre incidente, e **só** se for técnica |

`technical` é `true` apenas quando a Fast escreve o marcador
`[ESCALAR-TECNICO: ...]`. A escalação comum — `[ESCALAR: ...]`, que é o formato
de **cobrança, cancelamento, reembolso e dúvida de conta** — pelo e-mail:

- **não** abre incidente,
- **não** manda WhatsApp pra equipe,
- **não** manda e-mail pra equipe (`notifyTeamEscalation` é importado só pelo
  `respond.ts`; o `mail-respond.ts` importa apenas o `extractEscalation`).

Ela existe como **uma linha de log do pm2**, que ninguém lê. E a Fast já
respondeu ao aluno *"a equipe vai verificar"*. Prometemos um humano e não
chamamos humano nenhum.

**Isto é exatamente o silêncio que fez a Viviana explodir**, e é uma variante
diferente da que a ordem `2026-08-19_ler_caixa.md` descreve. Lá o furo é *"a
Fast acha que resolveu e respondeu mal, ninguém revisa"*. Aqui é pior: **ela
admite que não resolveu, pede humano, e mesmo assim ninguém é chamado.**

### Quem isso pegou (nas ~22h de log que existem)

O log do pm2 só alcança até **18/08 04:01** — antes disso não sei, e não vou
fingir que sei.

| aluno | escalou | estado da conta (conferido) | é travamento? |
|---|---|---|---|
| `itamar.vanzin@` | 18/08 12:30 | acesso até 23/08, 100.525 cr, 4 vozes ready, tudo funcionando | **não** — mas pediu humano há ~14h e ninguém soube |
| `jolenesaraiva@` | 19/08 01:35 | acesso até 22/08, 85.969 cr — e **gerou com sucesso 01:41, 01:46, 01:52 e imagem 01:56** | **não** — pelo jeito se resolveu sozinho |

**Ninguém está sangrando** — nenhum dos dois está bloqueado, os dois têm
crédito e acesso. Por isso não acordei o Johnny. Mas os dois pediram gente e
não tiveram.

Abri os dois na fila à mão (`0dc3e0fe` e `a620a782`), `open`, cada um com nota
explicando que o incidente foi aberto por mim porque o sistema não abre, o
estado da conta, e o que falta. Não marquei `fixed`: não resolvi nada, só
tornei visível.

**Não li a caixa do suporte@** pra saber o que os dois escreveram — a ordem
`2026-08-19_ler_caixa.md` manda esperar o `ler_caixa.cjs` (`BODY.PEEK` + `SEEN`),
porque entrar pelo caminho da Fast marca como lido e pode deixar aluno sem
resposta. O conteúdo está no `suporte@`, e o Johnny tem cópia.

### O falso alarme que quase virei manchete (registro honesto)

No meio disso achei `tecnologylegacy@gmail.com` com **`access_until` nulo,
0 crédito e `-195.800`** numa transação, tendo escalado 3× no mesmo dia. Ia
reportar "aluna pagante trancada" — seria a manchete errada.

O campo que decide não é o valor, é o `ref_type`/`note`:

```
kind=adjustment  amount=-195800  ref_type=estorno
note="Estorno da compra pedido pela aluna e processado (dinheiro devolvido).
      O credito acompanha o dinheiro. Decisao do Johnny 18/08/2026."
```

É a **Viviana**, e está **correto**: ela pediu estorno, o dinheiro voltou
(`HP3851239009`, documentado em `_frank/prova/2026-08-18_viviana_cobranca_22.md`)
e o crédito acompanhou o dinheiro. Saldo zerado é a consequência certa, não um
bug. Nada a fazer, e nada pra avisar o Johnny com urgência.

Vale o registro porque **errar pra mais custa igual a errar pra menos**: o
`-195.800` chama atenção, o `note` decide.

## 4. Produção

- `pm2 aiverse` **online**, uptime 35 min, `unstable_restarts: 0` — bate com o
  deploy do `831b4c3` às 01:26, não é crash loop.
- Sweep **vivo** de 5 em 5 min, último às **02:00:02**.
- Segue o `expiração de trial FALHOU` a cada 5 min: é a trava de 18/08
  (`DESATIVADA MANUALMENTE ... zerou 14 pagantes`) funcionando e se registrando
  como falha. Normal, e continua ruim que o normal apareça como alarme.

---

## 5. O que precisa do Johnny (duas coisas)

### 5.1 — Escalação por e-mail chamar humano (a que eu acho mais urgente)

A correção é pequena e tem duas formas. **Não fiz sozinho de propósito**, e a
razão é concreta: a versão "avisa a equipe" **dispara WhatsApp e e-mail pra
pessoas**, e mandar mensagem em nome da casa às 23h40 sem ele saber não é meu
lugar. Além disso o Johnny já tem um card desenhado nessa área (a tabela onde
"a Fast grava tudo", na ordem `ler_caixa.md`) e eu ia mexer em cima dele.

- **(a) Mínima e sem mandar mensagem pra ninguém:** tirar o `&& technical` do
  `mail-respond.ts:377`, para que **toda** escalação por e-mail vire incidente.
  Só escreve na nossa fila. É reversível e é o que eu recomendo pra hoje.
- **(b) Simétrica com o chat:** chamar também o `notifyTeamEscalation` no
  caminho do e-mail, para escalação por e-mail avisar a equipe como a do chat
  já avisa. Aí sim sai WhatsApp/e-mail — precisa da palavra dele.

Enquanto nenhuma das duas sobe, **toda escalação por e-mail não-técnica
continua sumindo**, e eu só vou achar as próximas se o log do pm2 ainda
alcançar — e ele só guarda ~22h.

### 5.2 — Aplicar a migration 82 (`scripts/82_generations_runpod_timing.sql`): duas
colunas novas em `generations`, `delay_seconds` e `execution_seconds`, ambas
anuláveis. Não reescreve linha existente, não mexe em saldo, não mexe em
crédito. Sem ela, o incidente `d3d8d1b2` fica exatamente onde está há 19 dias:
a única causa que sobrou (hang do worker) não tem como ser provada, porque o
log do RunPod evapora ~30min depois do job e toda investigação posterior bate
em 404 — isso já foi provado com HTTP 404 na rodada das 17h.

## 6. O que eu mexi

- Push dos 3 commits que já existiam (**nenhum código novo escrito** nesta
  rodada — só empurrei o que estava preso).
- Nota 24 no incidente `d3d8d1b2`, corrigindo por escrito a nota 23.
- Dois incidentes abertos à mão (`0dc3e0fe`, `a620a782`), cada um com nota.
- Estes documentos e a seção P2 no playbook.

**Nada de crédito, nada de GPU, nada de e-mail pra aluno, nada apagado,
nenhuma migration aplicada, nenhuma mensagem enviada a ninguém.**

## 7. A lição, que é de processo e não de código

O playbook **P** já dizia "'o fix está pronto' não é 'o fix está no ar'". Eu
tinha o playbook e mesmo assim escrevi um relatório afirmando que o fix estava
no bundle — porque conferi que o **commit existia**, não que ele estava
**empurrado**. `git log` local responde a pergunta errada.

A verificação certa é `git log origin/main..HEAD`: se voltar qualquer coisa,
tem código meu que não está em produção. Isso vira passo fixo de fim de rodada,
antes de escrever qualquer frase sobre estado de fix. Registrado como **P2** no
playbook P (que já era da família certa — não abri letra nova pra não inchar o
arquivo com dois playbooks dizendo a mesma coisa).
