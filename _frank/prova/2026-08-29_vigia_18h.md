# 29/08 18h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → trouxe **1 commit** (`c12906e`,
`runpod-worker/jobs/tts_settings.py`). Índice de ordens lido antes de tocar em qualquer
coisa. Ordem vigente do assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C). Também
valem `2026-08-20_dono_da_fila_e_fila_zerada.md` (14-A), a `2026-08-20_REGRA_FINAL_CREDITO.md`
(ENCERRADA — não reabri) e a rotina `e-mail_tecnico.md`.

Ronda anterior: **16h UTC**. Esta: **18h UTC**. Intervalo de 2h, no ritmo.

---

## Placar

| | |
|---|---|
| Não-fechados no fim | **7** — **2 de aluno** (`99`, `188`), **5 técnicos** (`180`, `184`, `187`, `189`, `190`) |
| **Abertos por mim nesta ronda** | **2** — o `189` (§1) e o `190` (§2) |
| **Anotados por mim** | **1** — o `187` (§3) |
| **Fechados que voltaram a disparar** | **0** — só o `#8` de sempre, último disparo 22/08, há **166,8h (7,0 dias)** |
| Fechados na janela | **2** — `6` (fixed, frank, 17:44:41Z) e `186` (ignored, frank, 17:45:04Z) |
| Abertos por outros na janela | **1** — o `188` (fast-help, 17:22:57Z, atendimento) |
| E-mails novos de aluno desde as 16h | **0** — o mais recente do INBOX segue o uid `373` (Johnathan, 04:15Z) |
| E-mails **enviados** na janela | **5** — uids `323`–`327` |
| Aluno cobrando 2ª vez ou mais | **0 novos** |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **101** — 100 `ready`, **1 `failed`**, 0 presas |
| Estorno da única falha | **OK** — `−400` / `+400` casados por `ref_id` (§4) |
| Acesso vivo + crédito + nenhuma voz pronta | **2** (era 3) — Marcelo 19d, Kelinn 16d. A Francielly saiu (voz ficou pronta) |
| `training_jobs` preso >30min | **1** — o órfão de 25/08, voz já `ready`, escrituração pendente |
| Lista de estorno | **em dia** — 10 tipos, 2.486 linhas varridas, nenhum tipo desconhecido |
| PRs abertos | **23** |
| Árvore de trabalho suja | **NÃO** — `git status --porcelain` vazio antes de começar |
| Incidente que eu fechei, reabri ou reclassifiquei | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura** e **pelo complemento**
(`status not in ('fixed','ignored')`). `group by status` da tabela inteira:
**142 `fixed`, 33 `ignored`, 6 `investigating`, 1 `aguardando_aluno`** = 182 linhas.
Bate com as 180 do começo da ronda + os 2 que abri.

> ⚠️ **Por que o complemento importa hoje, mais do que nunca.** O `#99` está em
> **`aguardando_aluno`**, que **não é `open` nem `investigating`**. Uma varredura escrita
> como `status in ('open','investigating')` — que é literalmente o texto do meu próprio
> briefing e o padrão de 30+ scripts em `_Bugs/` — **teria reportado 6 e escondido o
> `#99`**. É o acidente de 19/08 outra vez, com roupa nova: naquele dia o filtro cego era
> `signature like 'fast-email:%'`. **Hoje não existe NENHUM incidente em `open`**: a fila
> inteira é `investigating` + `aguardando_aluno`, então o filtro ingênuo erra sempre.
>
> **Conferi antes de acusar, e NÃO virou chamado:** `aguardando_aluno` é tratado de
> propósito na produção — `admin/falhas/page.tsx:59` e `:107` dão aba própria a ele, e
> `varredura_travados.cjs:284-338` o imprime em bloco separado com o comentário
> *"honesto não pode significar INVISÍVEL"*. O desenho está certo; quem erra é o script
> solto que enumera os estados bons. Fica como **linha de processo**, não como chamado
> (14-C §2), e é a razão de eu contar sempre pelo complemento.

---

## 1. ABRI o `189` — o e-mail final afirma "suas imagens estão ok" para quem tem ZERO imagem

**O achado mais sério da ronda, e ele é de sistema.** Marcos Vidal recebeu **dois e-mails
que se contradizem, com 336 segundos de distância**, os dois na caixa Sent:

- **uid 306 · 02:51:24Z** — *"Precisamos de você: suas imagens. **Não conseguimos pegar as
  suas imagens.** Motivo: link do WeTransfer em formato que não reconheço."*
- **uid 308 · 02:57:00Z** — *"Seus arquivos estão prontos. Boa notícia: **suas imagens e o
  seu áudio estão ok** e já foram processados. Sua imagem e a sua voz estão configuradas."*
  \+ convite pra **assinar**.

**A causa é o zero-de-zero**, `frontend/src/lib/onboarding/pronto.ts:110-114`:

```js
const houveAvatar = lista.length > 0;
const pronto = onboarding && pendentes === 0 &&
               (houveAvatar ? prontos >= 1 : true) &&
               vozOnboarding?.status === "ready";
```

Com **zero** avatar, `houveAvatar` é `false` e o ramo devolve `true` — **não ter nada
pronto satisfaz a condição de estar tudo pronto**. Com a voz `ready`, `pronto = true`, e
`verificarOnboardingPronto` (`pronto.ts:159-189`) dispara `avisoOkMasAssine`
(`pronto.ts:189`), cujo texto (`avisos.ts:163-165`) **afirma** que as imagens estão ok.

**Medido no banco:** Marcos tem `image_generations idea='onboarding_avatar'` **total 0,
ready 0**; voz `ready` de 31min; `onboarding_ready_email_at = 02:56:59.96Z`.

**Tamanho, sem inflar:** 15 perfis têm `onboarding_ready_email_at` preenchido **e** zero
avatar. Cruzando com `onboarding_runs`, **7** têm falha de imagem documentada — esses são
os que provavelmente leram a contradição. Os outros 8 são anteriores à instrumentação
(a tabela só existe desde 22/08) e **não os conto**, porque não tenho prova.

**⚠️ O que NÃO deve ser "consertado" — escrevi isso dentro do chamado.** O ramo
`houveAvatar ? prontos >= 1 : true` foi escrito **de propósito** em 22/08, e o comentário
em `pronto.ts:105-109` explica: exigir avatar de quem nunca teve avatar **trancava a linha
da planilha pra sempre** (fernao82, dmaggioni, namaiimoveis, thiagoabadio). **Essa decisão
está certa.** O defeito é a mesma flag governar duas perguntas diferentes: *"posso
destravar a fila?"* (legítimo com zero avatar) e *"posso afirmar ao aluno que as imagens
dele estão ok?"* (ilegítimo). Reverter a primeira pra consertar a segunda re-tranca a fila
e reabre o problema de 22/08.

**As 3 checagens do §3, antes de abrir:** (1) fila aberta e fechada por
`pronto|avatar|assine|arquivos` → 5 chamados, todos de outra classe, nenhum aberto;
(2) `pronto.ts` e `avisos.ts` sem commit desde 22/08, e conferi **PR a PR** com
`gh pr view --json files` que **nenhum dos 23 abertos** toca os dois arquivos;
(3) **dinheiro: NÃO** — Marcos tem **zero compras**, `access_until` nulo, e o `−10.000`
dele é o negativo de onboarding **autorizado** (migration 88), conferido por `ref_id` e
sem filtrar por `kind`. **NÃO HÁ ESTORNO A FAZER** — deixei isso escrito pra ninguém
"consertar" um estorno que não existe (armadilha do `#152`). O risco é **comercial**, não
contábil.

## 2. ABRI o `190` — o fix do `#177` não alcança a falha de LINK

`route.ts:503-510` deriva `etapa_falha` só de `motivoImagens`/`motivoAudio`
(`route.ts:477-479`), e **as duas saem das listas do importador**. Mas a falha de
**resolução do link** acontece antes do importador rodar: passa por `tratarErro`
(`route.ts:225-227`), que só faz `falhas.push(\`${etapa}: ${motivo}\`)`. Nesse caminho as
duas ficam `null`, `images.length` é 0, e a etapa **cai no default `'audio'`**.

**Prova:** run `c032b0a1`, `etapa_falha='audio'`, `motivo='imagens: link do WeTransfer…'`,
`erro_detalhe` com `audios.training: "treino disparado"`. O áudio **funcionou** (voz `ready`
de 31min). O rótulo está **invertido**.

**Tamanho:** 25 runs têm `etapa='audio'` com motivo de imagens. **24 são anteriores ao fix**
— e batem exatamente com o *"24 de 68"* que o próprio `#177` mediu. **O 25º é o Marcos, de
hoje, depois do fix.** O `#177` curou 24 de 25; este é o residual. Dos 2 runs `ok=false`
pós-fix, o outro é o Luan, e nele a etapa `'audio'` está **certa** — não inflei a conta
com ele.

**Não é reabrir o `#177`.** Aquele fix (PR #94 / `7bef31e`) **está certo** e acertou o
caminho do importador. Este é a camada que ele não podia alcançar, porque `tratarErro` não
escreve em `imagesResult`/`audiosResult`. Mecânica diferente, mesmo sintoma. **Não reabri**
(14-A). Checagens: nenhum chamado aberto da classe; `route.ts` sem commit relevante desde
o fix (só `0755ab2`, streaming de áudio); **nenhum** dos 23 PRs toca o arquivo; **não é
dinheiro**.

## 3. ANOTEI o `187` — o Luan segue invisível, quarta ronda seguida

12,7h parado, **0** linhas em `voices`, `access_until` 29/09, saldo intacto. Rodei a
`varredura_travados` nesta ronda: a seção *"acesso vivo, com crédito e sem nenhuma voz
pronta"* listou **2** (Marcelo 19d, Kelinn 16d) e **não ele** — exatamente pelo `:210`.
Nada mudou no arquivo desde que abri. **Não mexi em status** (14-A).

Deixei escrito no chamado que `187` e `190` **não são a mesma coisa**: lá o instrumento não
enxerga a linha; aqui o rótulo mente. Alunos diferentes, arquivos diferentes.

## 4. Saúde — 101 gerações, 1 falha, estorno conferido por `ref_id`

Única falha das 24h: `086970cd` (viktoraraujo, 28/08 18:16Z, `executionTimeout`, pré-fix).
**Liquidada**, conferido **por `ref_id` e sem filtrar por `kind`**: `−400 generation` /
`+400 generation_refund` — e o estorno grava `kind='extra_purchase'`, o `kind` que engana
quem confere por ele (armadilha de 20/08).

A geração `069563db` que disparou o `#6` às 15:50Z **não existe mais** na tabela (limpeza
da conta de teste do dono) e o retry `270f5463` está **`ready`** — a reprodução dirigida do
PR `122` fechou. O `#6` foi fechado às 17:44Z pelo Frank e **não voltou a disparar**.

## 5. Fechados que voltaram a disparar

**Zero novos.** A única linha com `last_seen_at > resolved_at` na tabela inteira continua
sendo o **`#8`** ("áudio insuficiente/sem fala limpa"), fechado em 09/08, último disparo
**22/08 19:26** — **7,0 dias** parado. Segue como registro, sem ação, e **não reabri**.
Varri também todos os `fixed`/`ignored` com `last_seen_at` nas últimas 48h (29 linhas):
**todas** com `last_seen_at` **anterior** ao `resolved_at`, ou seja, nenhuma classe fechada
seguiu disparando.

## 6. Caixa — nenhum e-mail novo de aluno, não-lido intocado

`ler_caixa.cjs --ultimos 25 --corpo 2500`: leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em não-lido.** Fila de não-lidos da Fast: **0**.

**Nenhum e-mail novo de aluno desde as 16h** — o mais recente do INBOX segue o uid `373`
(Johnathan, 04:15Z). Procurei os sinais de escalação silenciosa (*"a equipe vai verificar"*,
*"vou chamar alguém da equipe"*, aluno cobrando 2ª vez): **nenhum caso novo sem chamado**.
Antes de descartar, conferi se já havia resposta enviada.

Nos Enviados, na janela: `323` (17:51Z, **Luciano** — o retorno técnico do `#99`, trabalho
do dono da fila) e `324`–`327` (**fotoatleta**, onboarding correndo normal; run `ef84020c`
`ok=true`).

**Luciano (`99`)**, para decisão do Johnny, **não como chamado técnico**: é a **terceira
vez** que ele diz que o resultado não serve (*"Realmente não funciona"* 19:50, *"Ficou
artificial mais uma vez"* 19:58, *"Fiz um agora com uma foto do jeito que você sugeriu.
Mesmo problema"* 20:53). O Johnny já respondeu pessoalmente reconhecendo o limite do clone
próprio (nível 3 × HeyGen nível 4). Recebeu novo retorno às 17:51Z. Está corretamente em
`aguardando_aluno` — **insatisfação com qualidade é decisão/atendimento, não bug** (14-C
§2), então **não abri chamado**. Só sinalizo porque é o 3º round e o Johnny está no fio.

**Marcelo** (`f6f82819`, multi-locutor, **19 dias**) continua sendo o único de fato parado
esperando gente; o **PR 50** endereça o caso. **Kelinn** (16d) respondida em 13:55Z.

## 7. O que eu NÃO fiz

- Não fechei, não reabri e não reclassifiquei **nenhum** incidente.
- Abri **2** chamados, e disse de cada um, explicitamente, que **não é de dinheiro** —
  em vez de deixar ambíguo e alguém estornar por cima (armadilha do `#152`).
- **Não abri chamado** pelo `aguardando_aluno` sumir do filtro ingênuo: fui ler
  `admin/falhas/page.tsx` e `varredura_travados.cjs` **antes**, e o desenho está certo.
  Teria sido falso positivo. Virou linha de processo.
- **Não conflei** o `190` com o `187` nem com o `#177`, e escrevi no `189` o que **não**
  deve ser revertido — o risco real ali é alguém "consertar" desfazendo a decisão de 22/08.
- Não respondi aluno e **não escrevi rascunho de e-mail** (armadilha do lucvila, 20/08).
- Não toquei em crédito, acesso ou assinatura.
- Não toquei em e-mail não-lido.
- Não mexi em código de produção.
