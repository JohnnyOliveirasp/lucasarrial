# Vigia — ronda de 03/09/2026, 18hZ (15h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 2** (`702cc916`/#226,
`f8587cef`/#234). Não fechei, não reabri, não mudei status de nada, não respondi aluno, não escrevi
rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Última ronda de vigia: a nomeada `16h` (escrita 12:16Z). Ronda das falhas mais recente (`20h`,
escrita 14:00Z) lida antes de tocar em qualquer coisa.

⚠️ **Nota de nomenclatura, para a próxima ronda não se perder:** os nomes de arquivo desta série
**não batem com o relógio**. `vigia_16h` foi escrito às 12:16Z, `rotina_falhas_20h` às 14:00Z. Eu
nomeei o meu pelo **UTC real** (`18h`, agora 18:11Z), que é o mesmo relógio usado dentro dos textos
(`12:23:08Z`, `16:10:22Z`). Quem for cruzar rondas: confie no carimbo `Z` dentro do texto, não no
nome do arquivo.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 🔴 9 pagantes órfãos sem contato real — o "pode" pedido às 13hZ continua sem resposta

Não é achado meu, é do `#222`, e eu registro porque **continua parado e tem prazo correndo**.

**O funil eu reproduzi sozinho, não herdei** — `entitlements`, agora:

| etapa | n |
|---|---|
| órfãs (`user_id is null`) | **92** |
| `status='active'` | **46** |
| janela vigente (`access_until > now()`) | **26** |
| com `price.value > 0` (pagante de verdade) | **15** |

Bate exatamente com o que as rondas das 17h/18h/20hZ mediram. **O que eu não remedi** (e digo que
não remedi): quantos dos 15 já receberam a carta do órfão. A ronda das 20hZ mediu **6 contatados →
9 restantes**, e mostrou que o critério antigo ("tem qualquer coisa nos Enviados") contava mala
direta de crédito como contato — o que não conta o problema pra quem lê.

**A pergunta, em 1 linha: posso escrever para os 9 de uma vez?** Enquanto a resposta não vem, sai 1
por ronda e as janelas de garantia vencem uma a uma.

### 0.2 Migration 102 — **décima segunda** ronda com a mesma pergunta

Conferido agora pelo banco, não pelo registro anterior:

```sql
select tgname from pg_trigger where not tgisinternal and tgrelid='incidents'::regclass;  -- (NENHUM)
```

`scripts/102_incidents_resolved_guard.sql` segue **não aplicada**.

### 0.3 PRs abertos: **26** (eram 24) — o mais antigo é de 18/08

Entraram dois hoje (`#167`, `#168`, ambos do SGP). Nenhum saiu. Os `#41`/`#42` (teto de 2MB) estão
no **décimo sexto dia** — o mesmo buraco que fez a Alana receber *"não consegui ler o que você
mandou"* para um anexo de 2,2MB.

---

## 1. O que eu ABRI

**Nenhum chamado.** Nenhum e-mail novo na caixa (§4), nenhuma classe nova nas medições (§5, §6),
nenhum incidente novo nasceu na janela (`max(numero)` segue **242**). Abrir qualquer coisa aqui
seria duplicar fila ou gastar chamado com processo — teste de bolso da ordem de 27/08 aplicado em
todos os achados desta ronda.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`702cc916` / `#226`** — nota 24: a série andou **16 → 17** e a nova é **posterior ao build**. §5.
- **`f8587cef` / `#234`** — nota 12: **n=0** persiste, agora 5h48 depois do build. §6.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de 19/08,
que fez reportar "0 abertos" existindo quatro).

**5 abertos — 1 de aluno, 4 técnicos.** Todos em `investigating`; `open` = 0.

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **366,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) — `categoria=atendimento` |
| `3ca22d47` `#222` | 50,3h | 5 no título, **26 medidos** | compra órfã casa só por e-mail |
| `702cc916` `#226` | 48,3h | **157 alunos** | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 25,9h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 21,5h | *(irrespondível)* | relato manual sem e-mail e sem anexo |

**Composição desde a ronda das 16hZ: saíram 2, não entrou nenhum.** O `#241` e o `#242` (os dois
`fast-help` do chat do app) foram fechados pelo **Frank** às **16:27Z**, ambos com nota de conteúdo
— *"dúvida de uso + falha de comunicação, não bug"* (#242) e *"dúvida de uso, não bug"* (#241). São
fechamentos do dono da fila, com justificativa; **não tenho objeção a registrar**.

**Mais 9 em `aguardando_aluno`**, que o número acima não mostra: `#99` (265,4h — **11,1 dias**),
`#172` (146,6h), `#206` (77,1h), `#207` (76,2h), `#214` (68,3h), `#216` (53,8h), `#218` (51,2h),
`#224` (49,3h), `#229` (45,3h). Sem mudança desde as 16hZ.

**Placar honesto da ronda: 5 abertos + 9 esperando aluno = 14 cards com gente do outro lado.**

### 3-A. Crédito conferido, um a um — e uma correção ao placar da ronda anterior

Por `ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por `kind`, armadilha de
20/08), nos **16** e-mails citados nos abertos — todos os 16 têm `profile`.

**Nenhum estorno pendente. Zero gerações em curso presas.**

⚠️ **Mas "zero gerações `failed`" estava errado.** A ronda das 16hZ registrou zero; eu medi **três**.
Conferi uma a uma antes de chamar qualquer uma de problema, e **nenhuma é dívida com aluno**:

| geração | aluno | idade | transações no `ref_id` | leitura |
|---|---|---|---|---|
| `e0de4212` | kessulyl | 239,3h | **0** | nunca foi debitada — não há o que estornar |
| `678267fe` | kessulyl | 239,4h | **0** | idem |
| `18a25568` | catarinacouras | 671,3h | 2, sendo **1 refund** | já estornada |

O que faz a diferença aqui é `trans_total = 0`, não a ausência de refund: **cobrança que não
existiu não gera estorno pendente**. Registro a divergência de propósito — não sei se as 16hZ usaram
recorte diferente ou se o zero era instrumento cego, e prefiro a correção dita à conta que fecha
sozinha.

### 3-B. Filas

`generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. **`voices/awaiting_training` = 17** (era 18) — não é
chamado, é o estado em que a bola está com o aluno. Registro porque o PR **#15**, que daria dono a
essa lista, está **OPEN desde 20/08**.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 700`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Nenhum e-mail novo desde a ronda das 16hZ:** o uid mais alto continua **429** (Vinícius, 03/09
04:10Z), cujo card `#240` já foi fechado por humano às 12:39Z.

Nenhum aluno cobrando pela segunda vez **sem resposta**, nenhum *"já pedi ajuda várias vezes"* novo,
nenhum *"a equipe vai verificar"* sem card correspondente. Os quatro alunos da janela (Katia, Alana,
Silmara/Rufino, Johnathan) seguem com o card que as rondas anteriores mapearam; **não reescalei
nenhum**, inclusive o Johnathan (ameaça jurídica), porque já tem dono e repetir seria reapresentar
o velho como novidade (regra 27).

---

## 5. A série do `#226`, 13ª medição — e ela andou DEPOIS do build

**Instrumento primeiro, porque eu quase errei aqui.** Minha primeira consulta comparou
`coverage_min_visto` com um limiar **chutado** de 0,80 e devolveu **11 abaixo da régua** — um número
que teria contado uma **queda** de 16 → 11 e sugerido que o build tinha ajudado. Está errado: a
régua não é chute, é o campo `qa.coverage_min` da própria linha, **0,85 em 79 de 79**. Com a régua
certa o número é **17**, e o sentido se inverte.

Registro porque este é o terceiro instrumento cego seguido nesta mesma série (`coverage_min` × 
`coverage_min_visto` nas 12hZ, agora limiar chutado × régua da linha) e porque um limiar
optimista aqui **produz exatamente a conclusão que o time quer ouvir**.

Janela instrumentada desde 02/09 02:32Z, régua por linha:

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 38 | 25 | **0 (0,0%)** | 0,857 |
| `exhausted > 0` | 41 | 22 | **17 (41,5%)** | **0,333** |

Às 16hZ era 37/0/0,857 e 38/**16**/0,333. O controle **continua nunca** caindo abaixo da régua,
agora com 38 entregas. O outro grupo ganhou **mais uma: 16 → 17**.

**A nova é POSTERIOR ao build**, e é isso que importa:

| geração | quando | cobertura | `exhausted` | `regens` | `coverage_rescue` |
|---|---|---|---|---|---|
| `79309199` | 03/09 **16:10:22Z** | **0,800** | 1 | 2 | **ausente** |

Isso é **3h47 depois** do `Build RunPod Worker` do `3bc1535` fechar (`success`, 12:23:08Z).

**A leitura honesta, com o limite dito:** o `3bc1535` conserta o gate de fim de arquivo **no caminho
do resgate**. Esta entrega não passou por resgate, então o conserto **nunca teve chance de agir
sobre ela** — não é que ele falhou, é que ele **não cobre este ramo**. O defeito do `#226`
(`loop.py:341-344` esgota as tentativas e entrega assim mesmo) segue inteiro e segue disparando
depois do build.

Série acumulada: **357 de 779** entregues com `exhausted>0` (**45,8%**), **157 alunos** distintos
(era 354/775 e 156 às 16hZ). Últimas 24h: **30 de 61 (49,2%)**.

---

## 6. O `#234`: o conserto está na main e **continua sem um único teste em produção**

1. **`3bc1535` está na main de verdade** — `git merge-base --is-ancestor 3bc1535 origin/main` (sim),
   não leitura de `git log`.

2. **A predição da ronda das 12h30Z continua com `n=0`, agora 5h48 depois.** A hipótese testável
   era: *"se o conserto entrar, a próxima geração com `coverage_rescue=1` tem que sair na faixa do
   controle"*. Medido: **15** gerações com `jsonb qa` desde 12:23:08Z — a primeira às 12:30:36Z, a
   última às **18:07:30Z** — e **zero** carregam a chave `coverage_rescue`. O conserto está no ar e
   **ainda não foi exercitado nem uma vez**. Não há resultado a favor nem contra.

3. ⚠️ **O limite de instrumento que eu reconferi em vez de herdar.** *"Build success"* **não é**
   *"imagem nova rodando no endpoint do RunPod"*. Varri as chaves do `jsonb qa` da janela procurando
   um marcador que distinguisse imagem velha de nova: as 49 chaves existentes são todas anteriores
   ao build. Então eu **não tenho, no banco, como provar** que o worker adotou o `3bc1535`.
   *"Merge não é entrega"* foi exatamente o que produziu o fechamento falso do `#226`; a distinção
   seguinte — *"build não é adoção"* — também segue **sem prova**.

4. **O que a próxima ronda precisa medir** (inalterado): a primeira geração com
   `coverage_rescue=1` posterior a **12:23:08Z**. Até ela existir, o `#234` **não pode** ser fechado
   por *"o fix subiu"*.

---

## 7. A ordem de 27/08 vence HOJE — e ela bateu o alvo

A própria ordem marcou **03/09** como a data de medição. Rodei a consulta dela, e depois a corrigi,
porque a consulta literal engole os casos que a ordem cita como o "antes".

**Consulta literal da ordem** (`created_at >= '2026-08-27'`): 18 chamados do vigia — 15 `fixed`,
**2 `ignored` (11,1%)**, 1 `investigating`; **2 com `categoria='atendimento'`**.

**Mas os três que estragam o número foram criados às 12:17 e 14:15 de 27/08** — `#151`, `#152` e
`#153` são **exatamente os exemplos que a ordem usa como o problema a corrigir**. Contá-los como
resultado da ordem é medir o remédio pela doença.

**Recorte honesto — só o que nasceu DEPOIS da ordem** (`#158` em diante), **15 chamados**:

| métrica | alvo | medido | |
|---|---|---|---|
| `ignored` | ≤ 10 % | **1 de 15 = 6,7 %** | ✅ (era 21 %) |
| `categoria='atendimento'` | zero | **0** | ✅ |
| `kind` começando com `processo`/`atendimento` | zero | **0** | ✅ |

**A ordem funcionou.** O único `ignored` é o `#190` (*"o fix do #177 não alcança a falha de link"*).

⚠️ **E aqui está o furo, que é de processo e não vira chamado:** a ordem de 27/08 §5 manda que, ao
fechar um chamado do vigia como `ignored`, a nota diga **qual das três checagens do §3 faltou** — é
o mecanismo pelo qual a ordem se corrige sozinha. O `#190` foi fechado por `claude` em 30/08 00:01Z
com `resolution_note` **VAZIA**. O mecanismo de autocorreção **não rodou nem uma vez**, e como só
houve um `ignored`, isso significa que ele **nunca rodou**. Uma linha no relatório, sem chamado.

---

## 8. Fechados que voltaram a disparar

`status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` `#8` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
**286,8h atrás**. Não é reincidência ativa. **Nada fechado voltou a disparar nesta janela.**

⚠️ **O ponto cego continua.** O `#226` tem `last_seen_at` congelado em **01/09 17:52:45Z** e
`occurrences` em **290** contra **357** reais. Ele está `investigating`, então sai desta consulta
por **reabertura, não por conserto**. Se alguém fechar o `#226` de novo, a consulta volta a mentir
na hora. O PR **#163** faz o detector **declarar** o ponto cego; ele **não descongela** o contador.

---

## 9. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada — em particular **não
toquei nos 9 de `aguardando_aluno`** nem nos dois que o Frank fechou às 16:27Z. Não respondi aluno
nem escrevi rascunho: **não escrevi aos 9 pagantes órfãos** (comunicação em massa, precisa do
"pode"), **não escrevi ao Johnathan** (ameaça jurídica, já tem dono) e **não escrevi à Katia** (o
`#47` é do Frank). Não mexi em crédito, acesso, voz ou áudio. Não mergeei PR (26 abertos) nem
apliquei migration (102, **12ª ronda**). Não apliquei o patch do `#237` — quem aplica e revisa é o
Frank; a chave `patch_92b1cc85` segue no `agent_state` sem caminho para o repositório. Não ouvi
nenhum áudio e não vi nenhuma imagem: toda afirmação aqui é de telemetria, `gh`, leitura de código,
leitura de caixa e cronologia. Nada da **planilha** foi lido, escrito, classificado, avisado ou
reprocessado (ordem 29/08).
