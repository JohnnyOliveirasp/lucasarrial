# Ronda do Vigia — 05/09/2026, 00hZ

Sensor (regra 14-A + ordem de 27/08). **Varri e anotei. Não abri chamado novo,
não fechei nada, não reabri nada, não mexi em crédito e não escrevi para aluno.**

Ronda curta de propósito: a de 22hZ foi há 2h, a caixa não recebeu nada novo e a
fila não mudou de tamanho. O que esta ronda tem de próprio é **uma correção do
alarme que eu mesmo levantei na ronda anterior**.

---

## O que precisa de DECISÃO do Johnny

### 1. Marcelo — **eu estava errado. Não existe precipício às 12:00Z.**

Na ronda das 22hZ eu escrevi *"Marcelo perde a janela amanhã (05/09)"*. **Retiro,
com prova de código.** Fui conferir o que de fato acontece quando o `access_until`
vence, porque afirmar urgência sem abrir o arquivo é exatamente o erro que a
ordem de 27/08 me manda parar de cometer (§3).

O que o gate faz hoje:

- `frontend/src/app/[locale]/app/voice-cloning/page.tsx:66` —
  `canTrain = team || creditsTotal >= TRAINING_CREDIT_COST`. **`access_until` não
  entra na conta.** O comentário nas linhas 45-55 é explícito: esta tela era a
  última que exigia assinatura vigente por cima do saldo e **foi corrigida** (caso
  `erwintst@`, 58.089 créditos e botão travado).
- `frontend/src/app/api/v1/voices/[id]/start-training/route.ts:102-120` — a API
  **nunca** exigiu assinatura. `access_until` é lido só para escolher o texto do
  CTA quando falta crédito (`/planos` × `/app/credits`).
- Nenhuma varredura zerou crédito: em 7 dias, **zero** débitos de
  `trial_expiry`/`courtesy` — os 2.267 débitos do período são consumo real
  (imagem, geração, vídeo, treino). As funções `expire_trial_credits` e
  `expire_courtesy_credits` existem no banco mas não dispararam.

Marcelo tem **198.950 créditos** e o treino custa 10.000. Às 12:00Z de hoje o
acesso vence e **ele continua podendo treinar a voz** — é a regra do Johnny de
20/08 funcionando na prática ("usa os que tem até acabar"). Confirmei que ele
**pagou de verdade** (`pagou_de_verdade.cjs`: rec#2 R$97 COMPLETE em 12/08, mais
avulsa de R$368,64 em 27/07), então nem é caso de trial.

**O que sobra de verdade, e é isto que peço decisão:** o e-mail que nós enviamos
a ele em 29/08 (Enviados, uid 341) diz *"o seu acesso atual vai até 5 de setembro
(...) não quero que você perca a janela"*. Pelo código acima, **essa frase assusta
com um prazo que não existe**. Ele está calado há 6 dias, sem voz há 26 (a falha
de 10/08 foi infra nossa, já estornada). A pergunta não é mais "corre antes das
12h" — é **se vale uma segunda tentativa com ele e se a próxima mensagem
desmente o prazo** que a nossa própria deu. Não escrevi para ele: não é alçada
minha.

Não virou chamado, de propósito: o teste do §3 ("se o código estivesse certo,
isto não teria acontecido?") dá **não** — o código está certo, quem errou foi o
texto que uma pessoa escreveu. É processo, não bug.

### 2. SOLON — R$97 errados entram em ~36h (06/09 12:00Z). `#254` — **sem resposta**
Segue exatamente como na ronda das 22hZ, e o relógio andou: o "pode" foi pedido
no grupo às ~20hZ e continua sem resposta. Não repito a prova aqui (está no card
e no log das 22hZ); repito só o prazo, porque cancelar é ação de dinheiro e para
o mundo externo, e não é alçada de agente.

### 3. Jackson quer trocar o reembolso por crédito. `#247` — **sem resposta**
Proposta comercial dele de ontem 21:19Z (uid 440, a mensagem mais nova da caixa).
Nada mudou desde as 22hZ. Decisão do Johnny.

---

## O que ABRI

**Nada.** Varri as três frentes e não achei classe de erro de sistema que já não
tenha chamado. Registro o que **quase** virou chamado e por que não virou — é o
que a ordem de 27/08 pede que eu deixe escrito:

**SGP: 3 pedidos parados no meio do formulário.** `b294fa45` (schiavojr77, 9h41),
`068f5987` (iran@ogr, 4h20) e `0fd2845a` (wallanadaphiny, 4d10h) estão em
`status='foto'` com `enviado_em` NULO e **zero fotos** enviadas. Três alunos no
mesmo passo é padrão suspeito, então testei a hipótese "o upload quebrou" antes
de acusar: **está funcionando**. Dois pedidos fecharam o ciclo inteiro hoje —
`francaanampf` (11:09 → pronto 13:16) e `valdianycoelho83` (08:14 → pronto 09:33).
Upload que entrega 2 de 2 no mesmo dia não é upload quebrado. É aluno que abriu o
formulário e não terminou — funil, não defeito. A `wallanadaphiny` já tem dono
(`#206`, aguardando aluno). **Sem chamado.**

## O que ANOTEI

**`#47` Katia — a objeção das 22hZ segue de pé, e piorou de idade.** O
`last_seen_at` do card continua em **09-04 09:25**, mas ela escreveu **três vezes**
depois disso (10:15Z com `Video.mov`, 10:20Z e 10:22Z com print). O status diz
`aguardando_aluno` — "a bola está com ela" — há 15h de um caso onde ela já
respondeu. Não anotei de novo no card para não empilhar nota repetida: a anotação
das 22hZ cobre o mesmo fato e nada mudou desde então. Registro aqui que **nada
mudou**, que é a informação nova.

**`#15` — não há achado meu nesta ronda, e digo isso de propósito.** As duas
falhas de ontem (`a07e9278` 20:36Z, `86254b30` 20:47Z, ambas `executionTimeout`)
me chamaram atenção porque uma gravou `[fase: inference.chunk.generate]` e a
outra `[fase: (sem fase instrumentada)]` — parecia instrumentação nova cobrindo
só metade. **Fui conferir antes de reportar e a leitura estava errada:** o commit
`2bd3c3f` (main, 23:42Z) é *posterior* às duas falhas e **já analisa exatamente
essas duas gerações** no corpo da mensagem. A diferença de fase é o achado do
Frank, não meu, e é o motivo do commit existir. Não há nada novo aqui.
Ressalva honesta que **não** verifiquei: `2bd3c3f` mexe em
`runpod-worker/jobs/inference.py`, que só passa a valer com imagem do worker
reconstruída. **Não confirmei se o worker foi redeployado** — se não foi, a
próxima ocorrência ainda não vai nomear o passo do setup.

---

## Frente 1 — a fila, sem filtro nenhum

**12 abertos: 9 técnicos, 3 de atendimento.** Contados por
`status in (open, investigating, suporte_necessario)`, **sem cláusula de
signature**.

Comparado com as 22hZ: mesmo total, composição diferente. Saiu o `#255` (o card
descartável de QA, que se autodeclarava apagável e não existe mais em nenhum
status) e entrou o `#256`, que eu abri às 22:16Z. **Nenhum chamado novo nascido
nestas 2h.** A mudança de "8 técnicos / 4 atendimento" para "9 / 3" é só o `#255`
saindo do balde de atendimento e o `#256` entrando no de técnico — não é fila
piorando.

| # | idade | o que é |
|---|---|---|
| `#15` | 36d | timeout de geração — 19 ocorrências, 18 alunos, as 2 de ontem estornadas |
| `#222` | 3d | 5 alunos com acesso ativo presos fora da própria conta |
| `#226` | 3d | entregamos áudio que o nosso próprio QA reprovou — 290 de 659 (44%) |
| `#234` | 2d | palavra decapitada no áudio entregue — 609 gerações (14,3%) |
| `#237` | 2d | "não conta nada na plataforma" |
| `#251` | 6h | upload de áudio de 11s travado na transcrição |
| `#253` | 4h | 1h de áudio acumulado e botão Treinar morto |
| `#254` | 4h | cobrança em dobro — o do Solon, acima |
| `#256` | 2h | a varredura conta menos abertos do que existem (meu, das 22hZ) |
| `#246` `#247` `#249` | — | os 3 de atendimento |

**Fechados que voltaram a disparar em 48h: 8 — os mesmos 8 da ronda anterior,
nenhum com ocorrência nova.** Sete são `kind=reported` de atendimento (`#252`,
`#218`, `#213`, `#242`, `#241`, `#240`, `#239`), cujo `last_seen_at` sobe quando o
aluno escreve. O oitavo é o `#248`, fechado com medição. **Zero classe técnica
fechada disparando escondida.**

**Nota sobre o `#256`, contra o meu próprio chamado:** a varredura imprimiu **12**
nesta ronda, batendo com a consulta sem filtro. Isso **não** significa que o
defeito sumiu — conferi o código, `varredura_travados.cjs:377-379` segue
`.in("status", ["open","investigating"])` sem `suporte_necessario`. Os números
batem hoje porque o único ocupante daquele status (`#255`) deixou de existir. O
defeito é o mesmo, o dano segue **zero**, e o risco segue sendo o próximo aluno
que cair ali. Registro para ninguém ler "bateu, então estava errado".

**Aguardando aluno: 13.** Mais velhos: `#47` (16d, acima), `#99` (12d) e `#172`
(7d) — os três passaram do prazo em que a varredura pede segunda tentativa.

## Frente 2 — a caixa

**Nenhum e-mail novo.** A mensagem mais recente da caixa continua sendo a do
Jackson (uid 440, 21:19Z), a mesma que a ronda das 22hZ já tinha lido. 439 no
total, 25 lidas com `BODY.PEEK`, só `SEEN`.

**Fila de não-lidos: 0.** A Fast está em dia — não há aluno esperando primeira
resposta na caixa. Conferi um por um os 25 e **não há ninguém abandonado nem
cobrando pela segunda vez**. Ressalva: a fila zerada é evidência de que o cron de
5 min da Fast está vivo (ela criou o `#253` às 19:39Z e mexeu nele às 19:49Z),
mas **não conferi o `crontab` do Hetzner** nesta ronda.

## Presos na varredura

3 itens, os mesmos: Marcelo (acima), `tania-araujo` (`awaiting_training` de hoje,
normal) e Luan (import travado por link do Drive — **ordem de 29/08 manda não
abrir chamado disso**, e ele já foi avisado em 30/08). Mais 1 `training_jobs`
obsoleto com a voz já `ready` — escrituração, ninguém esperando.

## Produção nas últimas horas

Limpa. Gerações: 14 `ready`, 2 `failed` (as duas do `#15`, **as duas estornadas**
— conferido por `ref_type='generation_refund'`, nunca por `kind`: R$1.307 às
20:56Z e R$749 às 21:04Z) e 1 `pending` de 1 minuto atrás. Imagens: 24 de 24
`ready`. Cenas do estúdio: 53 de 53 `ready` em 48h. Vozes: 10 `ready`.

Um detalhe que sigo de perto sem alarmar: **Renan** falhou 20:47Z, tentou de novo
21:03Z e **saiu pronto**. **Debbie** falhou 20:36Z e só voltou a tentar
**00:12Z** — 3h36 sem o áudio dela, e a tentativa nova está `pending` há 1 minuto.
Estornada, mas estorno não é o que ela queria. Se esta segunda tentativa falhar,
vira ocorrência do `#15` com o mesmo aluno duas vezes, e aí muda de figura.

## Registro

`git pull --ff-only` na main entrou limpo desta vez (trouxe `2bd3c3f`); o bloqueio
por arquivos não rastreados que a ronda das 22hZ relatou não se repetiu. Estorno
em dia (10 tipos, 2.767 linhas, nenhum tipo desconhecido). Log commitado na
**main**.
