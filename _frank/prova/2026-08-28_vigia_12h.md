# 28/08 12h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → já estava atualizado
(veio 1 commit novo desde as 10h: o merge do PR #79). Índice de ordens lido
antes de tocar em qualquer coisa. Ordem vigente do assunto:
`2026-08-27_vigia_so_erro_de_sistema.md` (14-C) — só erro de SISTEMA vira
chamado. Também valem `2026-08-20_dono_da_fila_e_fila_zerada.md`, a
`2026-08-20_REGRA_FINAL_CREDITO.md` (ENCERRADA — não reabri) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **10h UTC**. Esta: **12h UTC**. Janela de 2h, no ritmo
certo pela primeira vez hoje (as 4 janelas puladas da madrugada estão no log
das 10h, §7 item 1).

---

## ⚠️ ABRO PELO MEU PRÓPRIO ERRO: eu arranquei o Frank da branch dele

Isto vem antes de qualquer número porque **o estrago foi meu e mexeu no
trabalho de outro agente**, não numa medição.

O meu passo obrigatório de abertura (`git checkout main`) rodou enquanto o
Frank estava na branch `fix/inc166-url-presignada-expirada` **com o conserto
do `166` escrito e não commitado**. Prova no reflog:

```
f0fba18 HEAD@{0}: checkout: moving from fix/inc166-url-presignada-expirada to main
```

O trabalho dele **não foi perdido** — as 3 modificações vieram junto na troca e
seguem íntegras (`download-file.ts`, `clone-history.tsx`, `clone-studio.tsx`,
163 linhas, mtime 11:52–11:53Z). Mas ele ficou **em `main` com o fix por cima**,
e se voltasse e commitasse sem olhar, o conserto caía **direto na main** —
exatamente a disciplina de branch que o repositório proíbe.

**O que eu fiz:** commitei só o meu log na main (nunca os arquivos dele) e
**devolvi a árvore para `fix/inc166-url-presignada-expirada`**, restaurando o
estado em que ele estava. Reverter o meu próprio estrago não é decidir nada
que não me cabe; deixar como estava, sim, seria escolher em silêncio.

**A causa é estrutural e vai acontecer de novo:** o Vigia e o Frank dividem a
**mesma árvore de trabalho**, e a minha ordem de abertura manda trocar de
branch incondicionalmente. Toda ronda minha que cair no meio de um conserto do
Frank vai arrancá-lo da branch. Isso é **processo**, não bug de código — pela
14-C vai como 1 linha aqui e 1 mensagem pro Johnny (§7 item 1), sem chamado.
O conserto óbvio (o Vigia rondar num `git worktree` próprio) é decisão dele.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **8** — 4 `atendimento` (`120`, `151`, `153`, `167`), 4 `técnicos` (`11`, `108`, `165`, `166`) |
| Variação vs. 10h | **+1** (eram 7) — entraram `166` e `167`, saiu o `164` |
| **Abertos por mim nesta ronda** | **0** — os 2 novos foram abertos pelo `fast-help`, não por mim |
| **Anotados por mim** | **1** — nota de medição no `166` (§3) |
| **Fechados que voltaram a disparar** | **0** — por 2 réguas independentes (§4) |
| Fechados na janela | **1** — o `164`, às 12:04Z, pelo Frank |
| Total de incidentes na tabela | **159** (era 157 às 10h) |
| Fora da contagem | 8 em `aguardando_aluno` |
| E-mails novos desde as 10h | **1** — uid 346 (João Rezende, 12:04Z) |
| Aluno sem resposta | **0** — respondido em 6 min, mas com ressalva séria (§5) |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **85** — 79 `ready`, 6 `failed` **todas de 27/08 17:01–17:30Z** (§2) |
| Gerações/cenas/clones presos >30min | **0 / 0 / 0** |
| Vídeo Clone em 24h | **44 `ready`** + 1 `generating` de 6,7 min (normal) |
| Cenas do estúdio 24h | **59 — todas `ready`** |
| Imagens 24h | **178 — todas `ready`** |
| Vozes 24h | **21 — todas `ready`** |
| Estornos em 12h | **0** (`ref_type='generation_refund'`, nunca por `kind`) |
| `awaiting_training` +24h | **16 vozes / 15 alunos / 45,4 dias** — **estável** vs. 10h (§6) |
| PRs abertos | **21** (eram 22; o #79 foi mergeado) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**, incluindo `open`, `investigating` e
`fixing`. `status` distintos: `fixed` 112, `ignored` 31, `aguardando_aluno` 8,
`investigating` 6, `open` 2.

---

## 1. Os 2 incidentes novos da janela — nenhum aberto por mim

| # | quem abriu | categoria | o que é |
|---|---|---|---|
| `166` | `fast-help` | **técnico** | `novaeraperformance@gmail.com`: "gerei um vídeo clone que não está reproduzindo ne possibiloitando baixar". 11:37Z. |
| `167` | `fast-help` | **atendimento** | `braboblindagem@gmail.com`: quer saber como controlar/adicionar expressões corporais no Vídeo Clone. 11:55Z. |

Os dois nasceram do chat do app, **não da minha varredura**. O `167` é dúvida de
uso — atendimento, não é bug; pela 14-C não viraria chamado meu, mas quem abriu
foi o `fast-help` e eu **não reclassifico nem fecho** (14-A).

## 2. Produção: nada de sistema novo nesta janela

As 6 falhas de geração das últimas 24h são **todas** de 27/08, entre **17:01Z e
17:30Z**, todas com o mesmo erro (`qa_coverage: audio gerado nao contem o texto
completo apos esgotar regeneracoes`) — a classe do **`52`**, fechado às 19:23Z
de 27/08. **Todas anteriores ao fechamento.** Depois do fix: **79 gerações,
zero falha**. Imprimi o `error_message` cru antes de acreditar no zero.

O único item "em voo" é 1 Vídeo Clone `generating` há 6,7 min — dentro do
normal, e a régua de presos (>30min) devolveu **0 em todas as três tabelas**.

## 3. O `166`: medi, anotei, **não investiguei causa e não fechei**

A queixa é "não reproduz nem permite baixar". A primeira hipótese barata seria
"o arquivo se perdeu" — **medi e é falsa**. Os dois vídeos do aluno estão no R2,
íntegros (`HeadObject` no bucket do worker `voices-clone-ai-verse`):

| clone | criado | tier | custo | bytes | gravado no R2 |
|---|---|---|---|---|---|
| `29d5ebaa` | 27/08 21:51Z | 480p-v2 | 6.160 cr | **11.035.200** `video/mp4` | 22:15:17Z |
| `e84dc74b` | 27/08 21:08Z | 480p-v3 | 8.085 cr | **8.487.892** `video/mp4` | 21:43:24Z |

O "18:51" do título é **BRT** = 21:51Z, então o vídeo da queixa é o `29d5ebaa`.

**Armadilha que registro para as próximas rondas:** `video_clones.video_path` é
gravado **no disparo** do job (`api/v1/video-clone/route.ts:287` — é uma saída
determinística planejada, `s3Key`), **não na entrega**. Campo preenchido
**não prova** que o arquivo existe. Aqui eu não acreditei no campo: provei pelo
R2. É a mesma família de erro do débito órfão — o banco descrevendo intenção,
não fato.

Também **não** afirmo que o bucket é a causa: `imagesBucket()`
(`lib/r2/client.ts:44`) resolve `R2_BUCKET_IMAGES || voices`, e no `.env.local`
que eu li o `R2_BUCKET_IMAGES` não está setado, então cai no bucket onde o
arquivo está. **Não conferi o env de produção** e não afirmo o que não medi.

**Não abri chamado** — checagem §3.1 da 14-C ("já existe?") segurou: o `166` já
existe, está `investigating` e **já tem nota do Frank**. E a §3.2 ("já foi
corrigido?") mostrou o conserto **já escrito** na branch
`fix/inc166-url-presignada-expirada`, atribuindo a causa à URL presignada de 1h
que a tela não renova. Minha contribuição foi só a **prova independente pelo
R2**, gravada como nota no próprio incidente.

## 4. Fechado que voltou a disparar: zero

Duas réguas, nenhuma com cláusula de assinatura:

1. **Fechado com `last_seen_at` nas últimas 48h** — **18** incidentes, e em
   **todos os 18** o `last_seen_at` é **anterior** ao `resolved_at`
   (`h_depois` ≤ 0). Fechados **depois** da última ocorrência, que é o desenho
   certo. (Correção minha: a 1ª versão deste log dizia 19 — contagem a olho
   sobre a saída da consulta. Recontei com `count(*)`: são **18**, e o
   `count(*) filter (where last_seen_at <= resolved_at)` também dá 18, então a
   conclusão não muda. Fica registrado porque número de prova não se corrige
   em silêncio.)
2. **Fechado que disparou 10min+ DEPOIS do fechamento** — devolve **1 linha
   só**, o `#8` (`acf8acd6`), visto pela última vez em **22/08 19:26**, ou seja
   **5,7 dias frio**. Não está disparando; resíduo histórico já registrado.

O `8d370ef5` (`#34`), que a minha ordem manda vigiar de nome, não aparece em
nenhuma das duas. **Encerrado de verdade.**

## 5. A caixa: 1 e-mail novo, respondido em 6 min — e o buraco do `[ESCALAR:]` mordendo de novo

**uid 346, 28/08 12:04Z — João Rezende** (`JR@jfbmarketsupply.com`, JFB Market
Supply). Diz que **vai prosseguir com a assinatura**, manda o primeiro vídeo
dele (link iCloud) e faz duas perguntas: como extrair cortes/fotos do vídeo, e
pede avaliação dos movimentos e do áudio.

Conferi os enviados **antes** de acusar fila perdida: a Fast respondeu às
**12:10Z, 6 minutos depois** (uid 238). **Não é aluno esperando, não é aluno
cobrando pela segunda vez, não é erro de sistema.** Não abri chamado e **não
escrevi rascunho de resposta** (14-A).

**Mas a resposta dela termina assim:**

```
[ESCALAR: João mandou o primeiro vídeo dele (link iCloud) pedindo avaliação
dos movimentos/áudio ... a equipe precisa abrir o link ...]
```

`[ESCALAR:]` — o marcador **comum**, que pela rotina `e-mail_tecnico.md`
**não abre nada**: vira uma linha de log do pm2 que ninguém lê. Ou seja: a Fast
**prometeu ao aluno** que "a equipe vai abrir o link do iCloud e analisar o
vídeo pra te dar um retorno específico", e **ninguém foi avisado**. É a mesma
mecânica que produziu o caso Viviana.

**Não abri chamado por isso, e a razão é a checagem §3.2 da 14-C:** o conserto
**já existe e está parado** — **PR #11, `feat/escalacao-email-avisa-grupo`,
"escalação por E-MAIL avisa a equipe no WhatsApp", aberto em 19/08, há 9 dias**.
Abrir chamado aqui seria pedir a alguém que refizesse trabalho pronto. Vai como
decisão pro Johnny (§7 item 2).

Registro a divergência de ordens, em vez de escolher em silêncio: a rotina
`e-mail_tecnico.md` (19/08) manda **abrir incidente** quando a Fast escreve "a
equipe vai verificar" e não existe chamado. A **14-C (27/08)** manda o oposto
para este caso — aluno esperando gente é **atendimento**, vai pro grupo, **sem
chamado**. Pela regra do índice ("a ordem mais nova vence"), segui a 14-C. Se o
Johnny quiser o comportamento antigo para o `[ESCALAR:]` especificamente, é uma
linha de ordem nova e eu passo a abrir.

Fila de não-lidos da Fast: **0**, e **não toquei em nenhum não-lido**.

## 6. `awaiting_training`: parou de subir nesta janela

| | 00h | 10h | **12h** |
|---|---|---|---|
| vozes `awaiting_training` +24h | 14 | 16 | **16** |
| alunos distintos | 13 | 15 | **15** |
| a mais velha | 44,9 d | 45,3 d | **45,4 d** |

**Estável nas últimas 2h** (o +2 do log das 10h não continuou). Segue sendo
desenho, não defeito — pela 14-C não vira chamado. O acúmulo de 15 alunos
parados num botão continua de pé como decisão de produto (§7 item 3).

O `training_jobs` órfão segue **1 linha só**: job `ebf5cc56` em `queued` desde
25/08 01:33, com a voz `f4b9b0f2` já **`ready`** (escrituração pendente,
ninguém esperando). **Não abri chamado** — está medido na **nota [15] do `108`**,
que está aberto e com dono. Chamado novo seria duplicata de classe.

## 7. Pro Johnny — o que precisa de decisão

1. **Eu arranco o Frank da branch dele.** Hoje aconteceu de verdade
   (§ do topo): dividimos a mesma árvore de trabalho e a minha ordem de
   abertura troca de branch incondicionalmente. Devolvi a branch e nada se
   perdeu, mas **vai repetir a cada 2h**. O conserto seria eu rondar num
   `git worktree` só meu. Decisão dele.
2. **O buraco do `[ESCALAR:]` mordeu de novo hoje** (§5) e o conserto está no
   **PR #11 parado há 9 dias**. Enquanto ele não sobe, toda promessa de "a
   equipe vai analisar" feita por e-mail depende de eu passar aqui e ver.
3. **16 vozes / 15 alunos parados num botão**, a mais antiga há 45,4 dias.
   Parou de crescer nesta janela, mas o estoque continua. O fluxo passa a
   cutucar esse aluno, ou o treino dispara sozinho?
4. **21 PRs abertos**, o mais velho de 18/08. O gargalo segue sendo o aval, não
   o diagnóstico — dois subiram nas últimas horas (#75, #79) e cada um deles
   fechou item que eu vinha reportando.

## 8. O que eu NÃO fiz (14-A)

- **Não abri chamado nenhum** — os 2 novos são do `fast-help`.
- **Não fechei nem reabri incidente.** O `164` foi fechado pelo Frank às 12:04Z;
  eu só registrei.
- **Não respondi aluno e não escrevi rascunho.**
- **Não toquei em crédito, acesso ou assinatura.**
- **Não toquei em e-mail não lido** (fila da Fast: 0).
- **Não mergeei PR, não apliquei migration, não gastei GPU.**
- **Não commitei arquivo do Frank** — o meu commit leva só este log.
