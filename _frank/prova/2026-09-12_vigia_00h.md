# Ronda do Vigia — 12/09/2026, 00hZ (11/09 21h BRT)

Sensor (14-A). Varri, medi, **abri 0 chamados** e **anotei 2** (`#254`, `#360`).
Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei e não
reabri nada.

Repo em `main`, `pull --ff-only` limpo (2 commits novos: `1f4397a`+`e6868ca` e
`011bd89`/`5623934`+`7bcf686`). `_frank/ordens/README.md`, a ordem de **27/08**
(só erro de sistema vira chamado) e a de **29/08** (planilha desligada) lidas
antes de tocar em qualquer coisa. Nada da planilha foi lido, escrito,
classificado ou reprocessado. Ordem de canal de **31/08**: o aviso desta ronda
vai **no grupo**.

Janela coberta: **22:15Z → 00:15Z**. `now()` medido no banco = **2026-09-12
00:10:59Z**.

---

## 1. 🔴 A JANELA DO LEANDRO FECHOU. Não tem mais decisão a tomar.

`leandro@aeroclubejf.com.br`, chamado **`#254`**. Medido pelo
`garantia_na_fila.cjs` às **00:11:36Z**: janela `2026-09-05 → 2026-09-12
00:00:00Z`, **virou há 0,2h**. Ele esperou **172,2h de prazo dentro da nossa
fila**. Estorno: **zero**.

Conferido agora em `entitlements`: as duas linhas do par seguem `status='active'`,
`updated_at` de **05/09**, nenhuma devolução — `contato@aeroclubejf`
(`HP3355066694`, R$ 97) e `leandro@aeroclubejf` (`HP0976568130`, R$ 97), R$ 194
pelo mesmo produto `7851642` no mesmo ciclo.

A progressão do dia inteiro, sem nenhuma decisão no meio:

| ronda | restavam |
|---|---|
| 16hZ | 7,8h |
| 18hZ | 5,8h |
| 20hZ | 3,6h |
| 22hZ | 1,8h |
| **00hZ** | **VENCIDO** |

Ele foi avisado 2× nos dois endereços e nunca respondeu; pela 9-C ninguém
cancela assinatura de titular sem pedido escrito, e essa parte está certa — não
é o que eu registro. **O que eu registro como sensor é a consequência:** a
escolha deixou de existir. Não virou "não", virou "tarde demais". Ele é o **6º**
nome da lista "perdeu a janela enquanto esperava na nossa fila" — eram **5** nas
quatro rondas anteriores de hoje. Anotado no próprio `#254` (nota 28).

## 2. 🔴 OBJEÇÃO DE MEDIÇÃO: o R$ 297 do Wellington não existe na nossa base

Este é o achado novo da ronda, e ele é contra **o meu próprio relatório
anterior**.

Os relatórios do Vigia das **20hZ e 22hZ** disseram ao Johnny que o dinheiro do
`#360` (`wgc.advogado@gmail.com`) é **R$ 297 no `ref_id` `HP2231400018`**
(Fábrica de Conteúdo Invisível, APPROVED). **Eu não consigo reproduzir esse
número.** Medido agora:

| medida | resultado |
|---|---|
| `payment_events where payload::text ilike '%2231400018%'` | **0 linhas** |
| `entitlements where raw_event::text ilike '%2231400018%'` | **0 linhas** |
| `pagou_de_verdade.cjs wgc.advogado@gmail.com` (Hotmart **viva** + Stripe) | **SEM PAGAMENTO ENCONTRADO** · avulsas pagas 0 · stripe pago 0 |
| `payment_events` por `buyer.name ilike '%wellington%'` | 1 linha — a mesma `HP1224527470` |
| `entitlements` deste e-mail | 1 linha, produto `7851642`, `canceled`, **sem `raw_event.purchase`** |

O **único** lançamento que existe para esse e-mail é `HP1224527470`, **valor 0**,
FastCloner, APPROVED, recebido **10/09 03:12:55Z**.

**A parte do aviso anterior que eu confirmo:** quem estornar pelo código do
assunto (`HP1224527470`) **devolve zero**. Isso está medido e está certo.

**A parte que eu contesto:** não há, nos nossos registros, uma compra de R$ 297
alternativa para estornar no lugar. Ou ele pagou com **outro e-mail** (classe do
`#222`, que já voltou 7 vezes e nunca foi achada por instrumento), ou não há
nada a devolver além de zero. **Não sei qual dos dois, e não vou afirmar.**

Pela ordem de 27/08 §3.3, afirmação de dinheiro exige `ref_id` que **case** +
`arquivo:linha`. Este `ref_id` não casa com nada. Registro para que ninguém
estorne R$ 297 em cima de um id que a nossa base não conhece — foi exatamente
assim que o `#152` quase pagou R$ 3.600 em dobro. O próximo passo correto é
humano: pedir o comprovante (valor + data) ou procurar por CPF/nome na Hotmart,
**antes** de qualquer estorno. Anotado no próprio `#360` (nota 3).

Estado da fila: aberto desde 19:35Z, **sem nenhum movimento há 4h40**.

## 3. 🔴 Entrou no relógio de 48h: Evelyn

`garantia_na_fila.cjs` às 00:11:36Z, bloco "vence em até 48h" — **2 nomes**, e a
composição mudou (o Leandro saiu porque venceu):

- `rodrigo.limas.1978@gmail.com` (`#306`) — **restam 23,8h** (era 25,8h às 22hZ);
- `evelyn.cheida@gmail.com` (`#341`) — **restam 47,8h**. **Nova no bloco.**

## 4. O `#349` continua com o conserto na main e o PR duplicado aberto

Sem mudança desde as 22hZ, registro porque o risco continua de pé:

- o conserto **está na main**: `dc3d941`, merge `243dc08` (PR #243);
- o cartão `#349` segue `investigating`, `resolution_note` vazia e
  `resolved_commit` **NULO** — pela 14-A eu não fecho, já anotei o commit;
- ⚠️ o **PR #234** (`feat/sgp-dhash-16x16`) continua **ABERTO** propondo o mesmo
  conserto por outro caminho, mexendo em `frontend/src/lib/sgp/impressao-foto.ts`
  — o mesmo arquivo que o `dc3d941` alterou. **Não mergear.** Terceira ronda
  registrando; é o quarto caso do mesmo padrão no repositório
  (`feat/fix-image-upload-retry`, `feat/onedrive-401`,
  `fix/referencia-fronteira-de-frase-por-palavra`).

## 5. O `#358` segue sem ocorrência nova

Nenhuma ocorrência desde **18:17Z**. `resolved_commit` já carimbado com
`4204762` (perna 1, o 429 do Kie). O **PR #241** (perna 2, teto de 4 no poll)
continua aberto desde 18:56Z. Nada a abrir.

Segue valendo a ressalva 14-A: os 39.600 créditos voltaram, mas **estornado não
é resolvido** — o `mcpaganatto@gmail.com` continua sem as 22 cenas.

## 6. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0**;
- **nenhuma mensagem nova** entre 22:15Z e 00:15Z. A última da caixa continua
  sendo o uid **584** (Angela, 20:32Z), já reportado na ronda das 22hZ;
- **nenhum chamado novo precisou ser aberto**;
- nenhum aluno cobrando pela 2ª vez nesta janela.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). Não toquei em não-lido.

## 7. Placar

- **75 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **41 de atendimento**, **34 técnicos**. Mais **10** em `aguardando_aluno`, que
  não entram nesses 75. Inalterado em relação às 22hZ.
  (Registro medido: **zero** incidentes em `status='open'` — os 75 são todos
  `investigating`. Por isso a contagem tem que varrer os dois status.)
- **Zero incidentes criados** na janela (`created_at > 22:15Z` → vazio) e **zero**
  com `last_seen_at` na janela.
- **Fechados que voltaram a disparar: 0.** Cruzei `last_seen_at > resolved_at` na
  base inteira: sai **um só**, o `#8`, última ocorrência em **22/08** — velho,
  não é reincidência nova. Recortando as últimas 48h: **0**.
- `garantia_na_fila.cjs` 00:11:36Z — 75 abertos · 16 parecem pedido de reembolso ·
  57 pessoas · **controle positivo OK (2/2)** · **6** perderam a janela (era 5) ·
  **2** vencem em 48h · **0** na perna da renovação.
- **Calibragem do Vigia** (ordem 27/08 §5): dos **30** cartões com
  `reported_by='vigia'` desde 27/08, **4** fechados como `ignored` = **13,3%**.
  Alvo ≤10%; linha de base 21%. Inalterado — **abri 0 nesta ronda**.

⚠️ O "6" continua sendo **PISO, não total**: o script deriva a janela do
`warranty_date` do **nosso** banco, então quem não tem linha local sai como
*"sem compra paga"* — que **lê como veredito de pagamento** sendo só ausência de
linha. Teresa (`#356`), Wellington (`#360`) e Simone (`#301`) caem todos nesse
balde. Enquanto o rótulo não mudar, **ninguém deve ler a coluna "janela" como
"não pagou"**. Sexta ronda registrando isto — e o item 2 desta ronda mostra
exatamente o estrago que esse balde faz quando alguém tenta preencher o vão com
um número que não foi medido.

## 8. Nota de processo — não é chamado

`main` continua com **arquivos do SGP modificados e não commitados**
(`frontend/messages/*.json`, `app/[locale]/sgp/page.tsx`,
`api/v1/sgp/codigo/route.ts`, `api/v1/sgp/inicio/route.ts`,
`components/sgp/step-dados-form.tsx`, `lib/sgp/sessao.ts`) mais os
`_frank/rascunhos/` não rastreados. **Sexta ronda seguida** registrando.

Detalhe desta ronda: diferente das 22hZ, o `git pull --ff-only` **passou sem
stash** — os commits que entraram não encostaram nesses arquivos. Isso não
resolve nada, só quer dizer que a mina não foi pisada desta vez.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado de
dinheiro sem `ref_id` que case, e não toquei em e-mail não lido.
