# Ronda do Vigia — 11/09/2026, 14hZ (11h BRT)

Sensor (14-A). Varri, medi, **abri 0 chamados** e **anotei 3** (`#309`, `#350`,
`#265`). Não respondi aluno, não escrevi rascunho, não mexi em crédito, não
fechei e não reabri nada.

Repo em `main`, `pull --ff-only` (já em dia). `_frank/ordens/README.md`, a ordem
de **27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. Nada da planilha foi lido,
escrito, classificado ou reprocessado. Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**.

Janela coberta: **12hZ → 14hZ**. `now()` medido = **2026-09-11 14:10Z**.

---

## 1. 🔴 DECISÃO DO JOHNNY: o Victor é maior e mais urgente que os dois R$ 97 — e o prazo dele **já venceu**

A ronda das 00hZ registrou que a garantia do Victor tinha vencido 14 min antes.
Passaram-se **14 horas** e nada se moveu. Nesta ronda o caso mudou de tamanho:
**não é R$ 397, é R$ 662,74 — e metade disso é invisível para nós.**

### 1.a — O relógio já estourou

`victor.inscriptio@gmail.com`, `#309`:

| o quê | medido agora |
|---|---|
| compra | `HP0250902700`, **R$ 397,00**, *Sistema de Geração Pronto*, 04/09 21:52Z |
| garantia | `2026-09-11T00:00:00Z` → **venceu há 14,2h** |
| estorno / chargeback / protesto | `payment_events`: 1 linha, **zero** |
| entrega | **nenhuma** — sem conta em `profiles`, sem linha em `sgp_pedidos`, sem voz, sem clone |
| pedido | 08/09; cobrou em 09, 10 e 10/09; **três** promessas nossas de retorno |

Ele recebeu o e-mail de boas-vindas (a thread *"Seu Sistema de Geração Pronto:
comece por aqui"* é dele) e nunca abriu o portal. Isso explica a não-entrega;
não muda o fato de ter pago e pedido devolução dentro do prazo.

### 1.b — A promessa que ficou sem lastro

Em **10/09 19:44Z** a casa escreveu a ele, textual:

> *"o seu pedido está registrado com a data em que você fez, 08/09, que está
> dentro do prazo de garantia da compra. É essa data que vale. A nossa demora em
> responder é problema nosso e não vai ser cobrada de você de jeito nenhum."*

Com a janela da Hotmart fechada, **não existe mais caminho automático**. Ou
alguém executa por decisão comercial, ou a frase acima fica sem lastro. É
exatamente a forma que a ronda das 14h de hoje já registrou no caso do Marcelo
(*promessa não-cumprível*), aqui com o maior valor da fila.

### 1.c — Estamos vendo METADE do que ele pede (provado, não suposto)

Ele escreveu (uid 569): *"seriam a de Fábrica de Conteúdo e o Sistema Pronto"* e
anexou o extrato do cartão. Baixei o anexo (`IMG_6153.jpeg` →
`_Bugs/anexos/569/`, flags e fila da Fast conferidas intactas). O extrato mostra,
em **4 de setembro**:

| lançamento | valor | está em `payment_events`? |
|---|---|---|
| `HTM *clonepronto` | **R$ 397,00** | ✅ `HP0250902700` |
| `HTM*fabricaconteudo` | **R$ 132,87 — Parcela 1 de 2** (= R$ 265,74) | ❌ **não existe** |

**Total do pedido dele: R$ 662,74.** Quem for executar olhando só os nossos
registros devolve R$ 397,00 e fecha o caso achando que acabou.

**A causa, lida no fonte — não adivinhada.** `route.ts:109-111` devolve 200 e faz
`return` para a rota `de_fora` **antes** do `upsert` em `payment_events`;
`sgp-boas-vindas.ts:330-341` (`roteamentoDoProduto`) manda todo produto que não é
`HOTMART_PRODUCT_ID` nem `HOTMART_SGP_PRODUCT_ID` para `de_fora`; e
`sgp-boas-vindas.test.ts:393` **trava** `7283335` (Fábrica de Conteúdo Invisível)
nesse ramo. É a mesma classe que cegou o `7283229` entre 09/06 e 04/09 (commit
`4688e40`).

⚠️ **Não afirmo "a Hotmart não manda".** O próprio comentário do `route.ts:96-104`
avisa que procurar o produto em `payment_events` depois daquele commit só
reencontra o efeito do `return` — a medição seria circular. O que afirmo é o que
o código diz: **nós descartamos antes de gravar.** Medido: `7283335` tem 55
eventos, **todos** em 06/09 entre 16h11 e 16h19, e nenhum depois; o `7851642` tem
6.339 até hoje e o `7283229`, 171.

### 1.d — A pergunta que é decisão, não chamado

> **A Fábrica de Conteúdo (7283335) é nossa de servir — como o SGP passou a ser
> em 03/09 — ou é legitimamente `de_fora`?**

Enquanto for `de_fora`, todo comprador dela é invisível para o varredor de
garantia, para o aviso de compra órfã e para o `pagou_de_verdade.cjs`. Pela
ordem de 27/08 isso é **decisão**, não chamado — por isso **não abri cartão**,
anotei no `#309` e trago a pergunta aqui em uma linha.

## 2. 🔴 Os dois R$ 97 da mesma meia-noite: **9,8h**, zero movimento

Remedido agora, não herdado da ronda anterior:

| aluno | transação | garantia | resta | estorno |
|---|---|---|---|---|
| `marcelopersonalthe32@` (`#265`) | `HP0618766977` | 12/09 00:00Z | **9,8h** | zero |
| `contato@fotoatleta.com` (`#207`/`#212`) | `HP2131614976` | 12/09 00:00Z | **9,8h** | zero |

A decisão segue represada há ~27h. A objeção sobre o `#212` (fechado como `fixed`
sobre premissa que morreu em 05/09) continua registrada no próprio cartão, com o
status intacto.

## 3. A varredura D-1, sem filtro de título

Sobre todos os cartões `open` / `investigating` / `aguardando_aluno`:

- **no limite de ~9,8h (12/09 00:00Z):** `contato@fotoatleta` (`#226`/`#207`,
  R$ 97), `marcelopersonalthe32@` (`#265`, R$ 97), `contatogrupoavip@` (`#288`,
  R$ 633,81) e `leandro@aeroclubejf` (`#254`, R$ 97 — já avisado 2×, bola com o
  aluno);
- **já vencidos há 14,2h:** o Victor (`#309`) e os **19 e-mails do `#312`**
  (`aguardando_aluno`, todos contatados na ronda das 02h — a bola é do aluno,
  mas a partir de agora sem caminho automático se algum voltar pedindo dinheiro).

**Limites declarados** (para ninguém ler a lista como fila de pendência):
(i) cruzei por `affected_emails` — aluno citado só no corpo não aparece;
(ii) usei `max(warranty_date)`, enquanto a produção usa de propósito a janela que
**fecha primeiro** (`garantia.ts:88`), então minha lista é mais generosa que a
regra da casa; (iii) não descontei quem já foi respondido; (iv) não abri a
Hotmart viva; (v) um valor sai absurdo (`orthobor@gmail.com`, 733699) — é
artefato do payload, **não é dinheiro**.

## 4. Frente 2 — a caixa: nada novo nesta janela

- não-lidos no INBOX (fila da Fast): **0**;
- mensagem `SEEN` mais recente: **uid 571**, 11/09 **12:09Z** (Emanuel, `#304`) —
  já coberta na ronda das 12hZ;
- entre 12:18Z e 14:10Z: **nenhuma mensagem nova**, **nenhum incidente criado**,
  nenhum dos cartões de prazo mexido.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). O download do anexo do uid 569
conferiu e imprimiu a prova de que flags e fila continuaram intactas.

## 5. Placar

- **71 abertos** (`open` + `investigating`), sem filtro de assinatura: **39 de
  atendimento**, **32 técnicos**. Mais **10** em `aguardando_aluno`, que não
  entram nesses 71.
- **Fechados que voltaram a disparar: 0.** Nenhum incidente `fixed`/`ignored` tem
  `last_seen_at > resolved_at` nas últimas 48h. O único da base inteira é o `#8`,
  cuja última ocorrência é de **22/08** — velho, não reincidência nova.
- Dos 7 fechados com `last_seen_at` recente, todos dispararam **antes** de serem
  fechados (`#351`, `#346`, `#345`, `#342`, `#337`, `#267`, `#323`).

## 6. Nota de processo — não é chamado

`main` tem **8 arquivos modificados e não commitados** do SGP
(`step-dados-form.tsx`, `sessao.ts`, rotas `/api/v1/sgp/*`, mensagens i18n) mais
`destino.ts`, `retomada.ts` e os testes deles como não rastreados. Não toquei em
nada disso. Registro porque trabalho não commitado em `main` é invisível para a
próxima ronda — exatamente o risco que a regra do registro em `main` existe para
evitar.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado de
dinheiro sem `ref_id` + `arquivo:linha`, e não toquei em e-mail não lido.
