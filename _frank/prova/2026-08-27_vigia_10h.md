# 27/08 10h UTC — Ronda do Vigia (sensor, regra 14-A)

`git checkout main && git pull --ff-only origin main` → veio `c426ab7..3ca9eb1`
(sweep de cenas do Estúdio). Índice de ordens lido. Valem a
`2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐), a
`2026-08-20_REGRA_FINAL_CREDITO.md`, a
`2026-08-20_decisoes_55_trancadas_e_turno_noite.md` (item 2) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **00h UTC**. Janela desta: **~10h** (00h14Z → 10h10Z).
**Essa janela É o buraco do `#143`** — e ela não passou vazia. Ver §1.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **10** — 3 `atendimento` (`97`, `99`, `120`), 7 `técnicos` (`11`, `52`, `143`, `146`, `147`, `148`, `149`) |
| Variação vs. 00h | **+4** — eram 6. Entraram `11`, `147`, `148`, `149`. Nenhum fechado |
| **Abertos por mim nesta ronda** | **0** — os 4 novos nasceram sozinhos (`burst-rule`, `fast`, reabertura automática). Justificativa no §5 |
| **Anotados por mim** | **2** — `147` (nota 2) e `11` (nota 5) |
| Fora da contagem | **6** em `aguardando_aluno` (`47`, `65`, `72`, `124`, `133`, `139`) — inalterado |
| Fechados que voltaram a disparar | **1** (`acf8acd6` / #8), última ocorrência **110,7h** atrás — **0 vivos** em 72h |
| E-mails novos desde as 00h | **11** (uid 318–328): 8 alertas automáticos de falha + 3 do Cássio |
| Fila de não-lidos da Fast | **0** (`--fila`). **Não toquei em não-lido** |
| Gerações na janela | **21 em 12h, 21 `ready`**, 0 `failed`, 0 presa |
| Cenas do Estúdio presas | **0** · projetos em `generating`: **0** |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**, `status in ('open','investigating')`.
Detector de "fechado que voltou" por `last_seen_at > resolved_at + 2min`.

---

## 1. O `#143` deixou de ser argumento e virou medição

A ronda das 00h escreveu que eu era a última do dia e que depois de mim ficavam
~9h sem ninguém. Essa ronda é a primeira DEPOIS do buraco, e o buraco rendeu:

| entrou | hora | quem abriu | ficou sozinho |
|---|---|---|---|
| `147` | 01h11Z | `burst-rule` | **9,0h** |
| `148` | 01h12Z | `burst-rule` | **9,0h** |
| `149` | 02h10Z | `fast` | **8,0h** |
| `11` | 03h04Z | reabertura automática | **7,1h** |

**Quatro incidentes, 3 alunos com e-mail de falha e 1 aluno sem a voz, todos
dentro da janela que o `#143` descreve.** Nenhum é catástrofe isolada; o ponto é
que os quatro esperaram de 7 a 9 horas porque não havia ninguém, exatamente como
o chamado previu. Registro e não tomo pra mim: ligar turno é decisão do Johnny.

---

## 2. O achado da ronda: estornamos e mandamos "falha técnica" para trabalho ANTIGO que está ENTREGUE

### 2.1 O que os e-mails dizem

8 alertas automáticos em **71 segundos** (uid 318–325, 01:11:19 → 01:12:30Z),
todos com o mesmo erro cru:

> `salvar cena: No value provided for input HTTP label: Bucket.`

**São 3 alunos, não 2**: pizolatip 3, allysoncruz 4, **priscillarosseti 1**.
Os chamados `147` e `148` cobrem os dois primeiros. A **priscillarosseti não tem
chamado nenhum** — a regra de rajada pede 3-em-15min e ela teve 1, então caiu no
vão. Não abri um terceiro chamado pra mesma classe (§5); botei o nome dela na
nota do `147` pra ela não ficar invisível.

### 2.2 O crédito está quitado — conferido do jeito certo

8 × `studio_scene_refund` = **+14.400**. Conferi por **`ref_type`, nunca por
`kind`** (o estorno grava `kind='extra_purchase'` — é a armadilha que quase pagou
em dobro pra 13 alunos). Casei os **8 `ref_id`** um a um: cada um tem 2 linhas,
débito + estorno, **saldo 0**. Ninguém foi cobrado.

### 2.3 O que não fecha

Aí começa o problema de verdade:

- Os débitos dos 8 são de **13/08, 18/08 e 19/08** — de **8 a 14 dias** antes do
  estorno de hoje.
- Os 3 alunos **não têm nenhuma cena com esse erro**. As cenas deles estão todas
  **`ready`**.
- `studio_scenes` tem **ZERO** linha com erro de Bucket em **qualquer período** —
  e eu **imprimi o campo cru antes de acreditar no zero**, como a ordem manda.

Ou seja: **mandamos e-mail de "falha técnica" e estornamos trabalho antigo que já
tinha sido entregue e que continua `ready` na conta do aluno.** O aluno recebeu de
madrugada um aviso de que algo quebrou, sobre um vídeo que está lá, funcionando.

**Produção está sã**: 13 cenas `ready` na janela, inclusive às **03:24Z**, depois
da rajada, pelo mesmo `PutObject`. Não há defeito aberto sangrando agora.

### 2.4 Causa provável — e eu declaro que é inferência

`imagesBucket()` (`frontend/src/lib/r2/client.ts:28`) tem uma cadeia de fallback
que **termina em string vazia em vez de estourar**. Nome de bucket vazio produz
exatamente aquela mensagem do SDK. Isso é **defeito latente real**,
independentemente de quem disparou: o certo era falhar alto no boot, não entregar
`""` pro cliente S3 em runtime.

**Quem disparou eu não consigo provar pelo banco.** O que eu sei:

- O commit `c426ab7` (sweep de cenas presas, *"a mais velha 13 dias"* — bate com
  os débitos de 13/08) é de **01:13:03Z**, **dois minutos DEPOIS** da rajada.
  Portanto **o cron deployado está descartado** — cheguei a apostar nele e a
  conferência derrubou a aposta antes de virar relatório.
- O quadro é compatível com alguém **rodando o sweep local contra o banco de
  produção** logo antes de commitar, num ambiente sem a configuração de bucket.
  É **hipótese**. Quem confirma é o Johnny ou o coder, não eu.

O backlog que o sweep existia pra drenar está **zerado agora**: 0 cena presa,
0 projeto em `generating`.

---

## 3. `#11` reabriu sozinho, e a condição escrita em 10/08 foi cumprida

Reaberto pelo `system` às **03:04:56Z** (*"REINCIDÊNCIA: falha voltou após status
fixed"*). A nota de 10/08 tinha deixado a régua pronta: *"se reincidir DEPOIS do
deploy do fix de disco, aí sim é falha própria e precisa dos logs do RunPod"*.
Passaram **17 dias** sem reincidência e voltou.

Aluno: **franwd82@gmail.com**, voz `26553fa0`, criada 02:51:45Z, morta 03:04:48Z
— **13 minutos** de treino antes de cair.

**Fiz o primeiro passo da armadilha antes de qualquer palpite, e deu LIMPO**:
listei `raw_audio_paths` antes de olhar worker/ffmpeg. São **6 arquivos, os 6
`.m4a`**. **Não há foto na lista de áudio** — a causa de 20/08 (`910ea757` /
`8d370ef5`) está **descartada aqui**. Não é o defeito conhecido, e o motivo real
não está no banco: `trainer failed` é o que o pipeline devolve quando o subprocess
morre, sem carregar o stderr.

Variável que destoa (e eu **não** afirmo que seja a causa): `duration_seconds =
7889` (**131 min**), o maior da janela. As vozes que passaram no mesmo período
tinham 2758s, 7386s e 2100s.

Crédito quitado (`voice` −10.000 → `voice_train_refund` +10.000, saldo 0). **Mas
estorno não é caso resolvido**: o franwd82 subiu 131 minutos de áudio em 6
arquivos e continua **sem a voz**, sem ninguém ter falado com ele.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500` (uid 304–328, `BODY.PEEK`, só `SEEN`).
Fila de não-lidos: **0**. **Não toquei em não-lido.**

11 e-mails novos desde as 00h: **8 são os alertas automáticos** do §2 e **3 são do
Cássio Fialho** (uid 326–328), que já viraram o `#149`. Varri pelos sinais da
rotina (*"a equipe vai verificar"*, *"vou chamar alguém da equipe"*, aluno
cobrando pela segunda vez):

- **Cássio** cobra pela segunda vez (*"Favor responder a minha pergunta
  anterior"*) sobre cancelamento/cartão. A Fast respondeu no uid 328 que não há
  assinatura ativa no e-mail dele. A parte técnica (vídeo de exemplo do site sem
  áudio) está no `#149`. **Tem dono.**
- **Luziélia** (uid 316, 26/08 08h41): ultimato — *"Está chato já isso! (…) ou eu
  cancelarei a minha solicitação"*, recusando refazer o envio. O OneDrive foi
  corrigido (`#144`, PR #60). **O que não consigo ver é se alguém respondeu o
  ultimato dela** — o `ler_caixa` lê a entrada, não a saída, e não houve e-mail
  novo dela desde então. Reporto como pendência de decisão, sem cravar que ficou
  sem resposta. Vai no §6.
- Sidney resolveu sozinho (uid 313, *"Consegui!"*). Carlos/ycarlosk está gripado e
  vai regravar. Giovanna → `#133`. Sandra → `#120`. Luciano → `#99`.

**Nenhum caso sem dono** além da priscillarosseti do §2.1.
**Nenhum aluno em `aguardando_aluno` respondeu** — a bola não voltou em nenhum
dos 6.

---

## 5. Por que eu não abri chamado nesta ronda

Abrir é a minha função e eu não abri — por medição, não por omissão:

- Os 4 casos novos **já nasceram com chamado** (`burst-rule` ×2, `fast` ×1,
  reabertura automática ×1). Abrir de novo seria duplicar.
- A **priscillarosseti** é a única pessoa sem chamado, e é **a mesma classe,
  mesmo erro, mesmos 71 segundos** do `147`/`148`. Um terceiro chamado idêntico
  suja a fila de quem trabalha; o nome dela foi para a nota do `147`. **Se o
  Johnny preferir chamado próprio pra ela, eu abro na próxima** — é a única
  decisão desta ronda em que eu escolhi anotar onde a regra literal diria abrir,
  e por isso ela está explícita aqui e no §6.
- A caixa não trouxe caso sem dono.
- O motor está limpo: 21 gerações, 21 `ready`.

---

## 6. O que precisa de decisão do Johnny

1. **Os 3 alunos do §2 receberam aviso de falha sobre vídeo que está entregue.**
   Alguém fala com eles ou deixa quieto? E **quem rodou o quê contra produção às
   01h11Z** — isso eu não consigo apurar do banco.
2. **`priscillarosseti@hotmail.com` sem chamado** — anotada no `147`. Abro um
   próprio se ele quiser.
3. **`franwd82` sem a voz** e `#11` reaberto: precisa do **stderr do RunPod** do
   job de 02:51Z, que é trabalho de investigação (coder), não meu.
4. **`#143`** — o buraco de 9h agora tem 4 casos medidos dentro dele (§1).
5. **`#120` Sandra** — reembolso com CDC, prazo que ela alega: **30/08**.
6. **`#99` Luciano** — pede nominalmente posicionamento do Lucas e do Johnny há
   >90h. Prazo conhecido: **02/09**.
7. **Luziélia** — ultimato de ~25h atrás; não consigo ver da caixa de entrada se
   foi respondido (§4).

---

## 7. Objeções e limites desta ronda

- **Não tenho objeção a fechamento nenhum.** Nenhum incidente foi fechado na
  janela.
- O detector de "fechado que voltou a disparar" segue com o mesmo ponto cego
  registrado nas rondas anteriores: não pega incidente fechado **depois** da
  falha que o motivava.
- O §2.4 é **hipótese declarada**, não causa cravada. Já cravei errado duas vezes
  nesta base (foto no áudio), e nesta ronda a primeira aposta — o cron do sweep —
  **caiu na conferência do horário**, 2 minutos.

## 8. O que eu NÃO fiz

Não fechei nem reabri incidente nenhum (o `#11` reabriu sozinho pelo `system`;
eu só anotei). Não abri chamado — e explico no §5 em vez de deixar o zero falar
sozinho. Não respondi aluno nem escrevi rascunho — **inclusive o franwd82, a
priscillarosseti e a Luziélia**, que são os três nomes mais concretos desta
ronda; a medição foi para o incidente e para o Johnny, não para eles. Não toquei
em crédito, acesso, assinatura ou estorno — só **conferi** que os 14.400 + 10.000
fecham em zero. Não toquei em e-mail não-lido (fila da Fast: 0). Não mexi em cron
nem em ordem, **inclusive tendo medido o buraco do `#143` com 4 casos dentro**.
Não apliquei migration. Não escrevi código — li `scenes.ts`, `r2/client.ts` e
`sweep-stuck-scenes.ts` para saber o que a mensagem de erro significa antes de
reportar, e parei aí. Não rodei GPU, whisper, `--curar` nem `--medir`.
