# Ronda do Vigia — 14/09/2026, 22hZ (19h BRT)

Sensor (14-A). Varri, medi, **abri 1 chamado** (`#402`) e **anotei 2** (`#398`,
`#402`). Não respondi aluno, não escrevi rascunho, não mexi em crédito, não
fechei e não reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido,
escrito, classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**, e só no grupo.

Janela coberta: **20:10:34Z → 22:10:33Z** (2,0 h exatas). `now()` medido no
banco na abertura = **2026-09-14 22:10:33.818525Z**. Fechamento das contas
finais às **22:18:18Z** (depois de eu abrir o `#402`, e a diferença está
declarada no placar).

Duas coisas nesta ronda. A primeira é uma previsão minha das 20hZ que **se
cumpriu em duas horas** e que eu tenho de contar mesmo sendo contra o meu
próprio instrumento. A segunda apareceu num canto que ninguém estava olhando: um
aluno entrou na casa **hoje às 21:41Z**, está mexendo na tela **agora**, e a
casa **não consegue mandar e-mail pra ele de jeito nenhum** — nem pro endereço
que bounceou, nem pro que está no cadastro.

---

## 1. 🔴 A PREVISÃO DAS 20hZ SE CUMPRIU EM 2 HORAS: MAIS 2 ALUNOS DERRUBADOS NO TETO, E **ZERO** CARTÕES

**Anotado no `#398`. Não abri chamado novo** (27/08 §3.1: a classe **é** o
`#398`, que está aberto). Não mexi em crédito, não estornei nada.

Às 20hZ eu escrevi: *"se o teto derrubar dez pessoas uma vez cada, a fila mostra
zero e a casa jura que está tudo bem"*. Isso era previsão. Agora é medição.

**As duas novas, e o teto que o nosso próprio código manda** —
`frontend/src/lib/video-clone/config.ts:96-101`, `cloneExecutionTimeoutMs()`:
`(20*60 + billed*30)*1000` para `flow != v1`.

| aluno | áudio / frames | teto calculado | `elapsed` medido | erro |
|---|---|---|---|---|
| `danicale` (20:45:52Z) | 28,44 s / 725 | **2070 s** | **2074,9 s** (+4,9 s) | `executionTimeout exceeded` |
| `claytonpc10` (20:48:13Z) | 74,30 s / 1875 | **3450 s** | **3459,4 s** (+9,4 s) | `executionTimeout exceeded` |

Mesma assinatura das três de hoje mais cedo: **morrem no orçamento**, com o
overhead de rede em cima. Não antes, não depois.

**O placar do dia, corrigido na fonte:**

| dia | jobs | falhas | alunos |
|---|---|---|---|
| 06/09 → 13/09 (8 dias) | **439** | **0** | 0 |
| **14/09 (hoje)** | 70 | **5** | **4** |

**E aqui está o item que é sobre a MINHA fila, não sobre o produto:** o único
gatilho da classe é `fail-burst:video_clone_refund:<email>` — **2 em 6 h, por
aluno**. `danicale` falhou uma vez. `claytonpc10`, uma vez. `leonice`, uma vez.
**Nenhum dos três gerou cartão.** De **4 alunos** derrubados hoje pelo mesmo
defeito, a fila mostra **1** — o `welrisson`, que teve o azar de tentar duas
vezes.

**CORREÇÃO DE UM NÚMERO MEU:** às 20hZ eu disse que em 05/09 a classe abriu
**seis** cartões individuais. Conferi na fonte agora: são **NOVE** (`#264`,
`#266`, `#268`, `#271`, `#274`, `#275`, `#276`, `#278`, `#279`), todos fechados
um a um. Eu subnotifiquei o meu próprio argumento em 33 %.

**O dinheiro, conferido por `ref_id` e lido por `ref_type` — nunca por `kind`,
nunca por timestamp** (armadilha do `#152`):

| `ref_id` | débito | estorno | intervalo |
|---|---|---|---|
| `d3cf02ba` | −3.045 `video_clone` 20:45:52,78 | **+3.045 `video_clone_refund` 21:21:38,92** | **35,8 min** |
| `dbeb63dc` | −7.875 `video_clone` 20:48:14,41 | **+7.875 `video_clone_refund` 21:46:19,60** | **58,1 min** |

**As duas estornadas.** Aviso pra quem for auditar: o `kind` das duas linhas de
estorno é **`extra_purchase`**, não `refund` — quem filtrar por `kind` conclui
que não houve estorno e paga em dobro.

**O que o estorno não cobre:** os dois tentaram **de novo às 22:00Z**, com o
mesmo insumo, e estavam em voo no fechamento. Crédito de volta não é tempo de
volta — `danicale` já tinha gasto 34,6 min e `claytonpc10` 57,7 min só na
primeira tentativa.

**Em voo no fechamento, registrado como ESTADO e não como previsão:**
`daniel@dlima.adv.br` (2196 frames / 86,82 s) estava com **52,1 min** contra um
teto calculado de **63,5 min**. **Não afirmo que ele vai falhar** — as duas
maiores entregas de hoje (2225 e 2200 frames) terminaram `ready`, e às 20hZ eu
marquei um job como "4ª falha provável" e ele ficou pronto. Fica só o registro.
O `#244` (`help:tec:daniel@dlima.adv.br`) voltou a disparar às **22:04:08Z**,
com esse job rodando.

**O que eu NÃO afirmo:** não digo que a correção é subir o teto (a medição do
`#15` diz o contrário). Não li o worker, não medi a fila do RunPod, não sei qual
GPU pegou cada job.

---

## 2. 🔴 UM ALUNO ENTROU HOJE ÀS 21:41Z, ESTÁ NA TELA AGORA, E A CASA **NÃO CONSEGUE FALAR COM ELE POR E-MAIL** — NEM PELO ENDEREÇO CERTO

**Este é o achado da ronda.** Abri o `#402` para a parte que é **código**, e
mandei pro **grupo** a parte que é **atendimento** (27/08: aluno esperando gente
não vira cartão).

### 2.1 O chamado que eu abri — `#402`, e é defeito por UMA PALAVRA

Dois bounces na janela (INBOX uid **620** às 21:52:51Z e uid **621** às
22:08:43Z), o mesmo endereço. O sistema abriu o `#401` às 21:55:04Z e somou a 2ª
ocorrência às 22:10:04Z — **o agrupamento funcionou; a CLASSIFICAÇÃO errou.**

Texto exato que o código viu (`sample_error` do `#401`):

> `smtp; DNS Error: DNS error occurred while resolving the Mail Exchange (MX) server for the specified domain (pradocomunicacao.com). No MX server found`

`frontend/src/lib/agent/mail-bounce.ts`, `classificarDiagnostico()`. O bloco
`inexistente` (**:176-183**) já tem **dois** padrões pra falha de MX:

- `:180` — `/failed to resolve any ip addresses for the mail exchange/i`
- `:181` — `/\bno mx (record|hosts?)\b/i`

O servidor escreveu **"No MX server found"**. **`server` não está na alternância
`(record|hosts?)`.** Falha por **uma palavra**. Conferi as saídas seguintes na
ordem em que o código testa, e nenhuma casa (`:188` sem `5.7.x`/`S\d{4}`/
`blocked|banned|blacklist|not authorized|policy`; `:191` e `:192` sem código
numérico nenhum). Cai no `return` final → **`desconhecida`**. Bate com o que está
gravado no banco — **confirmado por leitura do código E por observação**, não só
por leitura.

**Por que não é cosmético:** o comentário em **`mail-bounce.ts:167-175`**,
escrito na correção de **13/09** (caso Sheila, `gmail.com.br`), diz que domínio
sem MX é **permanente** e que a orientação de `inexistente` — *"reenviar NUNCA
vai funcionar, confirme o e-mail real no cadastro/Hotmart"* — é exatamente a
certa. Como caiu em `desconhecida`, **o cartão não carrega instrução nenhuma**
pra quem for tratar.

**Terceira vez a mesma ferida:** o arquivo já foi corrigido **duas** vezes em
13/09 pelo mesmo motivo — variante de redação que o regex não previa (uid 608, o
`high probability of spam` sem o código JFE; uid 605/606/607, a prosa do Gmail
sem o `550-5.1.1`). Os dois comentários estão no próprio arquivo. Esta é a
**terceira** redação. O padrão não é "faltou um caso": é que a classificação
depende de **adivinhar a frase exata de cada provedor**.

**O que eu NÃO afirmo:** não digo qual é a correção certa. Ampliar o regex pra
`(record|hosts?|servers?)` resolve **este** texto e deixa a ferida aberta pro
próximo provedor. Não li o código que consome a classe, então não sei o que muda
de comportamento entre `desconhecida` e `inexistente` além do texto de
orientação. Não abri PR e não mergeei nada.

### 2.2 O que apareceu DEPOIS de abrir o cartão, e é maior: **falta um `.br`**

| | endereço |
|---|---|
| o que **bounceou** (uid 620/621, `#401`) | `guitaschetti@pradocomunicacao.com` — **sem `.br`** |
| `profiles` (desde 06/09 17:04:46Z) | `guitaschetti@pradocomunicacao.com.br` — **com `.br`** |
| `sgp_pedidos` (hoje 21:41:03Z, "Guilherme Taschetti Prado") | `guitaschetti@pradocomunicacao.com.br` — **com `.br`** |

Procurei a versão `.com` (sem `.br`) em `profiles` e em `sgp_pedidos`: **não
existe em lugar nenhum.** O `.br` se perdeu entre o cadastro e o envio.

**⚠️ O QUE EU NÃO AFIRMO, DE PROPÓSITO:** **não digo que o nosso código
truncou.** Não abri o código que monta o destinatário e **não tenho
`arquivo:linha`**. As outras explicações continuam vivas e não foram
descartadas: o aluno pode ter digitado `.com` em algum lugar que eu não
consultei; o relay (jellyfish/Namecheap) pode ter mangleado; ou o próprio texto
do DSN pode ter cortado. Afirmar "nosso código trunca" sem ler quem envia é
**exatamente** o erro medido nos `#100`/`#152`. Isto é **pista pro Frank**, não
veredito.

### 2.3 O que ESTÁ provado, e não depende de qual dos dois endereços é o certo

Medido com `dig` nesta ronda, os **dois** domínios:

| domínio | MX | leitura |
|---|---|---|
| `pradocomunicacao.com` | **nenhum MX, nenhum registro A** | não existe servidor de e-mail |
| `pradocomunicacao.com.br` | **`0 .`** | **MX NULO (RFC 7505)** — o domínio declara que **não aceita e-mail** |

**Mesmo que o `.br` não tivesse sumido, a mensagem não chegaria.** É a mesma
assinatura do caso Sheila/`gmail.com.br` de 13/09, já documentada no próprio
`mail-bounce.ts:167-175`.

### 2.4 E ele está dentro da casa **agora**

`sgp_pedidos`, no fechamento: status **`foto`**, `erro` **vazio**, criado
**21:41:03Z**, atualizado **22:15:55Z**. **Tem WhatsApp no cadastro** (13 chars).

Linha do tempo: entra no SGP **21:41:03Z** → 1º bounce **21:52:51Z** (11,8 min
depois) → 2º bounce **22:08:43Z** → segue mexendo **22:15:55Z**.

Existe caminho pra falar com ele (WhatsApp). Isso é **atendimento** → **grupo**,
sem cartão.

### 2.5 Contradição que eu **não** consigo fechar, e não vou fingir que fecho

`sgp_pedidos.email_verificado_at` = **2026-09-14 22:10:19,874Z**. Ou seja: o
cadastro consta como **e-mail verificado** às 22:10Z, **depois dos dois
bounces**, num domínio que publica MX nulo e portanto **não pode ter entregue
código nenhum**.

Não li o código da verificação. **Não sei** dizer se (a) ele recebeu por um
caminho que eu não enxergo, (b) a verificação foi contornada, ou (c) o campo é
carimbado por outra coisa que não a digitação do código. Registro como
**pergunta aberta**. Se for (b), o defeito é bem maior que o `#402` e **não é** o
`#402`.

*(Registro de método, porque muda número: as duas leituras de `fotos` que fiz com
~5 min de diferença deram **5** e depois **0**. Ele está mexendo na tela durante
a medição. Não trato nenhum dos dois como número estável.)*

---

## 3. 🟢 A MARY SAIU DA MINHA LISTA — DEPOIS DE 3 RONDAS, A CASA AGIU

Por três relatórios seguidos (16hZ, 18hZ, 20hZ) eu repeti que a `mary.020220`
estava invisível pra todo instrumento da casa e com cobrança marcada pra 20/09.
**Resolvido na ronda das falhas das 21hZ**, commit `ac7a47c`:

- assinatura **cancelada na mão** e reconferida na fonte viva
  (`ACTIVE` → `CANCELLED_BY_SELLER`; entitlement `active` → `canceled`);
- acesso e **62.063 créditos intactos**;
- aluna respondida, com a casa assumindo o erro.

E a causa raiz virou o **`#400`** (aberto 20:51:04Z, `tecnico`,
`frank:manual-manda-aluno-cancelar-sozinho-contra-regra-9c`): o
`manual.ts:396-403` manda a Fast passar o passo a passo da Hotmart pro aluno se
cancelar sozinho, contra a regra 9-C de 21/08, que diz que **a casa cancela a
pedido do titular**. Enquanto isso valer, o pedido morre como "atendido" e nunca
vira cartão — que é exatamente por que ela era invisível. PR **#282** aberto pra
isso.

**Registro honesto do que isso diz de mim:** o `#400` explica o achado que eu
levei 3 rondas repetindo sem conseguir nomear a causa. Eu descrevi o sintoma
certo (cegueira de instrumento, entrada da varredura são *cards*) e **não** achei
a camada de cima. Quem achou foi a ronda das falhas.

---

## 4. 🟠 MASTROIANNI — O ACESSO VENCE **AMANHÃ**, 5ª RONDA COM O MESMO SALDO

**Já anotado no `#329` às 18hZ. NÃO re-anotei** — estado idêntico, e nota
repetida a cada 2 h vira ruído no cartão de quem for tratar. Reconferido na
fonte agora:

| | 14hZ | 16hZ | 18hZ | 20hZ | **22hZ** |
|---|---|---|---|---|---|
| saldo | 874 | 874 | 874 | 874 | **874** |
| acesso | 15/09 | 15/09 | 15/09 | 15/09 | **15/09 — AMANHÃ** |
| última linha do extrato | 10/09 12:20 | idem | idem | idem | **10/09 12:20** |

**Quinta ronda seguida, nenhum lançamento positivo.** A casa prometeu decisão
*"hoje"* em **12/09**.

**RESSALVA QUE EU REPITO DE PROPÓSITO:** os **48.025** créditos são número **da
nota da casa, não meu**. Não reconferi `ref_id` por `ref_id` e **não endosso**
esse valor. Conta olhada de longe quase pagou 3.600 em dobro no `#152`.

---

## 5. As checagens da ordem de 27/08 — por que abri UM e só um

| candidato | veredito | checagem |
|---|---|---|
| regex de bounce não classifica MX (§2.1) | ✅ **ABRI — `#402`** | as 3 checagens abaixo, escritas na nota de abertura |
| 2 timeouts novos de Vídeo Clone (§1) | **NÃO abri** | §3.1: a classe **é** o `#398`, aberto. Virou nota com `arquivo:linha` |
| detector por aluno cega a classe (§1) | **NÃO abri** | limite do mesmo `#398`; nota no próprio cartão |
| `.br` que sumiu do endereço (§2.2) | **NÃO abri** | §3: **sem `arquivo:linha` eu não acuso.** Virou nota no `#402`, como pista |
| `email_verificado_at` num domínio MX-nulo (§2.5) | **NÃO abri** | não li o código da verificação — **pergunta**, não achado. Nota no `#402` |
| aluno inalcançável por e-mail, ativo agora (§2.4) | **NÃO abri** | 27/08: **atendimento** → grupo. Ele tem WhatsApp |
| Mastroianni vencendo amanhã (§4) | **NÃO abri** | §2: **decisão de dinheiro** → Johnny. Causa técnica já é o `#335` |
| 10 fichas de bounce paradas | **NÃO abri** | aval de telefone é **decisão** → Johnny. 10ª ronda |
| 6 SGP parados na tela de foto | **NÃO abri** | §3.2: **PR #265, #274 e #277** abertos |

**As 3 checagens do `#402`, feitas antes de abrir:**

1. **Já existe?** **Não.** Procurei na fila aberta **e** fechada por classe de
   classificador de bounce: aparecem só o `#308` (endereço interno inexistente,
   detector **descarta** o retorno — outro defeito), o `#183` (`fixed`, rajada
   abrindo cartão técnico pra erro de input) e o `#101` (`open`, não existe
   registro do que foi enviado). **Nenhum é este.** Os 10 cartões `fast-bounce:*`
   abertos são **ocorrências de alunos**, não a classe do classificador.
2. **Já foi corrigido?** **Não.** Li o código na `main` de hoje (o regex está
   como descrito). **Nenhum dos 48 PRs abertos toca `mail-bounce.ts`.** Conferi
   também os 3 branches do origin com nome parecido —
   `feat/triagem-de-bounce`, `feat/mx-entregabilidade-sgp` e
   `feat/laco-bounce-message-id`: `git diff origin/main...FETCH_HEAD`,
   **nenhum dos três toca `mail-bounce.ts`**.
3. **Envolve dinheiro?** **Não.** Nenhum crédito, débito, estorno ou cobrança.
   Por isso não há `ref_id` a casar.

**Dinheiro nesta ronda:** um único caso (§1), conferido por `ref_id` e
`ref_type`. **Não estornei nada e não afirmei cobrança sem prova.**

---

## 6. Placar — cada linha reconciliada contra as 20hZ

- **82 abertos** no fechamento da janela (22:10:33Z), **sem filtro de
  assinatura**: **44 atendimento**, **38 técnicos**. Mais **19** em
  `aguardando_aluno`. Nas 20hZ: 80 (44 + 36) + 19.
  **Depois de eu abrir o `#402`** (22:15:48Z), a contagem final às 22:18:18Z é
  **83 (44 + 39) + 19 = 102**. Contra 99 nas 20hZ. **A fila SUBIU 3**, e um
  desses 3 fui eu.
- **Criados na janela: 2** — `#400` (frank, 20:51:04Z, técnico) e `#401` (fast,
  21:55:04Z, atendimento). Mais o `#402`, meu, **fora da janela** (22:15:48Z) —
  contado à parte de propósito.
- **Fechados na janela: 1** — `#247` (21:47:59Z), o Jackson, com o aluno
  confirmando por escrito (*"Está tudo certo"*, uid 613).
- **Fechados que voltaram a disparar: 0.** Varri os **12** `fixed`/`ignored` com
  `last_seen_at` nas últimas 48 h: em **todos** o `last_seen_at` é **anterior**
  ao `resolved_at` (de −0,22 h a −13,06 h; o `#375` é o mais apertado, 0,00 h).
  **O furo declarado continua o mesmo de três rondas:** este teste só enxerga
  classe com `signature` que dispare.
- **Reincidência por aluno (48 h): 4 pares.** `amanda.rosaleal` `375`→`382`
  (verdadeiro), `evelyn.cheida` `385`→`389` (falso, coorte), `raulcssavatar`
  `396`→`397` (falso, cartão partido) — e o novo par
  **`guitaschetti` `401`→`402`, que é FALSO e a culpa é MINHA**: o `#402` é da
  classe do classificador e só carrega o e-mail dele porque foi o caso que
  revelou o defeito. **Meu próprio cartão inflou esta métrica**, e quem ler sem
  esta nota vai contar um aluno reincidente que não existe.
- **Produção na janela** (`created_at > 20:10:34Z`):

  | tabela | prontas | falhas | em voo no fechamento |
  |---|---|---|---|
  | `generations` | 2 | 0 | 0 |
  | `image_generations` | 20 | 0 | 0 |
  | `video_clones` | 8 | **2** | 4 |
  | `voices` | 1 | 0 | 0 |

  **31 entregas prontas contra 2 falhas** em 37 jobs. Volume bem menor que a
  janela anterior (84 contra 1), o que é esperado pro horário.
- **Presos de verdade: 0.** `generations` e `image_generations` com **nada** em
  voo. Os 4 `video_clones` em voo estavam todos **dentro** do próprio teto (o
  mais velho, 52,1 min contra 63,5 min). As `voices` fora de `ready`/`failed`
  estão em `awaiting_training` e `rejected_too_short` — **estados terminais de
  espera do aluno, não fila presa**; não conto como travado.
- **E-mails novos na janela: 2** — uid **620** (21:52:51Z) e uid **621**
  (22:08:43Z), e os **dois são o mesmo bounce** do `guitaschetti` (§2).
  **0 e-mails de aluno. 0 aluno cobrando pela 2ª vez.**
  **O agrupamento acertou:** o 2º bounce **não** virou cartão novo — entrou como
  ocorrência do `#401` (`last_seen_at` 22:10:04Z). Quem errou foi a
  classificação, não o agrupamento.
- **Não toquei em e-mail não lido.** Leitura com `BODY.PEEK`, critério `SEEN`.
- **Fichas de bounce: 10 abertas** + 1 em `aguardando_aluno` (`#338`) = **11**.
  Eram 10 nas 20hZ; entrou o `#401`. A mais velha é a `#249`, de 04/09
  (**245,5 h · 10,2 d**). O aval de telefone está parado pela **10ª ronda**.
  Uma delas segue sendo endereço **da própria casa** (`victor@lucasarrial.com`,
  `#308`). **Três** das abertas estão no balde `desconhecida` (`#374`
  `horta.pericias@gmail.com.br`, `#379` `luctec@gmail.com`, `#401`) — e pelo
  menos **duas** (`#374` e `#401`) são falha de domínio, o defeito do `#402`.
- **SGP parados na tela de foto: 6** (eram 5). Os 5 de sempre, contadores
  **1/6/3/4/4**, nenhum se mexeu; o 6º é o `guitaschetti`, que entrou hoje.
  A `wallanadaphiny` está parada há **344,2 h (14,3 d)**.
- **Fila do SGP por status:** `dados` 103 · `pronto` **75** · `foto` **57** ·
  `audio` 16 · `processando` **0**. Contra as 20hZ (103 · 75 · 55 · 16 · 0):
  **`foto` +2**, resto parado. **Aviso de instrumento repetido:** este corte é
  **mais largo** que o "6" da linha acima, que conta só quem já subiu foto.
  **Os dois números são verdadeiros e não devem ser comparados entre si.**
- **Casa na janela:** **3 commits** na `main` — `4ce820b` (meu log das 20hZ),
  `ac7a47c` (ronda das falhas 21hZ, a Mary) e `6586677` (ronda 22hZ, fecha o
  `#247`). **PRs abertos: 48** (eram 47; entrou o **#282**, o do cancelamento
  pela regra 9-C). O mais antigo segue o **#4**, de 18/08 (**27,4 d**).
- **Mais velho aberto:** `#15`, de 30/07 — **46,4 dias**.

### Limite da minha própria reconciliação, declarado

A aritmética da fila **não fecha sozinha**: 80 (20hZ) + 2 criados − 1 fechado =
**81**, e eu medi **82**. O `+1` que sobra é, pela minha reconstrução, o
**`#244`** (`help:tec:daniel@dlima.adv.br`), que disparou às **22:04:08Z** e está
em `open` — provavelmente voltou de `aguardando_aluno`. **Isso é reconstrução,
não prova**, e o motivo é o mesmo das três rondas anteriores: a tabela
`incidents` **não tem `updated_at`** e não existe histórico de status, então
**mudança de status não deixa carimbo de tempo**. Prefiro escrever que a conta
não fecha a apresentar um `+1` arredondado como se eu tivesse medido.

---

## 7. O que esta ronda diz

O primeiro item é sobre o meu instrumento, e por isso eu não tinha escolha senão
escrever. Duas horas atrás eu disse, como hipótese, que um defeito que derruba
cada aluno **uma vez** fica invisível pra fila. Duas horas depois são **4 alunos
derrubados hoje pelo mesmo teto e 1 cartão**. Não é mais hipótese, e o número de
05/09 que eu usei pra sustentar o argumento estava **subnotificado em 33 %** —
eram nove cartões individuais, não seis.

O segundo é o tipo de coisa que só aparece quando se cruza caixa com cadastro.
Um aluno entrou às 21:41Z, está mexendo na tela **agora**, e a casa escreveu pra
um endereço **sem o `.br`** que não existe — mas o endereço **com** `.br`, o que
está no nosso cadastro desde 06/09, **também não aceita e-mail** (MX nulo, RFC
7505). Então a pergunta "quem comeu o `.br`" é interessante e **eu não vou
respondê-la sem ler o código que envia**; a pergunta que importa já está
respondida: **e-mail nenhum chega nesse aluno**, e ele tem WhatsApp no cadastro.
O cartão que o sistema abriu sozinho diz `desconhecida` porque o regex procura
`no mx record` e o Google escreveu `No MX server found` — **uma palavra**, no
mesmo arquivo que já foi remendado duas vezes em 13/09 pelo mesmo tipo de
motivo. Abri o `#402` pra isso, e **só** pra isso.

E ficou aberta uma contradição que eu prefiro entregar por escrito a esconder:
esse cadastro consta como **e-mail verificado às 22:10:19Z**, num domínio que
declara não aceitar e-mail, **depois** dos dois bounces. Não sei explicar, não
li o código da verificação, e se a explicação for "a verificação foi
contornada", isso é maior que o cartão que eu abri.

O que andou: a **Mary** saiu da minha lista depois de três rondas — cancelada na
mão, créditos intactos, causa raiz virou o `#400` e o PR **#282**; e o `#247`
fechou com o aluno confirmando por escrito. O que não andou: o **Mastroianni**
perde o acesso **amanhã** com os mesmos 874 créditos de cinco rondas atrás, e as
**10 fichas de bounce** seguem paradas esperando um aval de telefone pela
**décima** ronda.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não estornei nada, não afirmei cobrança sem prova, não endossei
o valor de 48.025, não afirmei que a correção do timeout é subir o teto, **não
afirmei que o nosso código truncou o endereço** (não tenho `arquivo:linha`), não
afirmei que o job do `daniel` vai falhar, não cancelei assinatura de ninguém,
não fechei incidente, não reabri incidente, não mudei status nem título de card
nenhum, não abri chamado para o que é processo, atendimento ou decisão, não abri
chamado para classe que já tem chamado ou PR aberto, não toquei em
`sgp_pedidos` (só li), **não mergeei e não abri PR**, não toquei nos branches
marcados STALE (`feat/fix-image-upload-retry`, `feat/onedrive-401`,
`fix/referencia-fronteira-de-frase-por-palavra`,
`feat/fabricar-referencia-fronteira-por-palavra`), **não li nem reprocessei nada
da planilha** (ordem 29/08), não liguei nem mandei WhatsApp pra ninguém, e **não
toquei em e-mail não lido**.
