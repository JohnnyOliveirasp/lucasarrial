# VIGIA — ronda de 26/09/2026, 00hZ

Sensor (regra 14-A): varre, abre e anota. Não fecha, não reabre, não responde
aluno, não mexe em crédito. Calibrado pela ordem de 27/08 (só erro de SISTEMA
vira chamado). Ordem de 29/08 respeitada: **nada da planilha** foi lido,
escrito, classificado ou reprocessado. Canal (ordem de 31/08): o aviso desta
ronda sai **no grupo** (`notify-grupo.sh`), nada no privado do Johnny.

**Janela coberta:** 22:20Z → 00:35Z (2,2h). Atravessa a meia-noite UTC; o turno
da noite (ordem de 27/08, Executor 08h–23h BRT) segue respeitado porque em BRT
ainda é 21h de 25/09.

**Chamado aberto nesta ronda: 1** (`#587`).
**Anotações em cartão existente: 2** (`#586` e `#581`).

**O que esta ronda tem de próprio:** fui atrás de um `buyer_email` NULL numa
linha de cancelamento e achei que **518 de 518 cancelamentos entram no banco sem
o e-mail do aluno**, por um erro de posição que o cabeçalho do próprio arquivo
documenta e que a função vizinha já tinha aprendido. Dano em produção hoje:
**ZERO** — e eu medi isso consumidor por consumidor, contra a minha própria
acusação. Detalhe em §1.1.

---

## 1. Para DECISÃO do Johnny

### 1.1 🟠 Todo cancelamento entra no banco sem o e-mail do aluno — 518 de 518

Vai primeiro porque é código nosso, é medido nos dois sentidos, e porque a
pergunta *"esse aluno cancelou?"* feita pela coluna óbvia responde **sempre
"não"**.

O achado nasceu de um detalhe da própria ronda: ao conferir os eventos de
pagamento da janela, a linha de cancelamento apareceu com `buyer_email` **NULL**
e eu só descobri de quem era abrindo o `raw_event` do entitlement.

**Medido nos dois sentidos — é a população inteira, não amostra:**

| | nº |
|---|---|
| eventos `SUBSCRIPTION_CANCELLATION` | **518** |
| com `data.buyer.email` (**onde o código procura**) | **0** |
| com `data.subscriber.email` (**onde o e-mail está**) | **518** |
| linhas com `payment_events.buyer_email` preenchido | **0** |
| todos os `PURCHASE_*`, para controle | **100% preenchido** |

O e-mail sempre chegou. Ele nunca foi lido. **518/518 → 0/518.**

**Arquivo:linha:**

```
frontend/src/lib/payments/hotmart-payload.ts:20-21
  export function extractBuyerEmail(data: Record<string, unknown>): string | null {
    const email = asRecord(data.buyer).email;

frontend/src/app/api/v1/webhooks/hotmart/route.ts:121 e :132
  const buyerEmail = extractBuyerEmail(data);
  ... upsert em payment_events { buyer_email: buyerEmail }
```

**Por que é código nosso e não surpresa da Hotmart:** o cabeçalho **do próprio
arquivo**, linhas 7–13, já documenta a armadilha — *"a POSIÇÃO do código do
assinante muda conforme o evento… Bug de 18/08/2026: sem o `data.subscriber.code`
na cadeia, TODO cancelamento caía no fallback e a revogação era um no-op 100%
silencioso (185 cancelamentos, zero entitlements canceled)"*. A lição foi
aplicada em `extractExternalId` (linhas 90–94, que hoje lê `data.subscriber.code`)
e **não** em `extractBuyerEmail`, duas funções acima, no mesmo arquivo, para o
mesmo evento e a mesma troca de posição. **Uma função aprendeu, a vizinha não.**

**Dano em produção HOJE: ZERO — e isto é contra a minha própria acusação.** Fui
ler os três leitores de `payment_events` por e-mail, um por um:

| consumidor | veredito |
|---|---|
| `admin/churn.ts:56-64` e `:121` | **imune** — não usa a coluna: seleciona só `(payload, received_at)` e lê `d?.subscriber?.email \|\| d?.buyer?.email`, ou seja **já contorna** o NULL por dentro do payload |
| `winback/targets.ts:94` | **imune** — `.ilike(buyer_email)` só com `.eq(event_type,'PURCHASE_APPROVED')`, que tem 100% de cobertura |
| `agent/account.ts:236` | **imune** — mesma coisa, só `PURCHASE_APPROVED` |

Nenhum aluno prejudicado, nenhum crédito, nenhum acesso. O defeito é
**LATENTE**, e registrei isso na abertura do cartão pra ninguém tratar como
incêndio.

**Onde o dano é real hoje: na investigação.** `select * from payment_events where
buyer_email = '<aluno>'` não devolve cancelamento nenhum e **não avisa que
filtrou**. É exatamente a família de erro que a casa já pagou três vezes: o
filtro `signature like 'fast-email:%'` que reportou *"0 abertos"* existindo
quatro (19/08); o estorno casado por `kind` em vez de `ref_type`, que quase pagou
em dobro pra 13 alunos; e o `profiles.last_seen_at` em vez de
`auth.last_sign_in_at`, que produziu o falso *"nunca logou"* do Rafael **hoje**.

**Parentesco, pra não ser tratado como duplicata:** primo do `#481` (a consulta
`/sales/history?buyer_email` da Hotmart devolve só COMPLETE e esconde 17/17
estornos) — **mesma classe**, perguntar por `buyer_email` esconde uma categoria
inteira em silêncio. Mas é **outro defeito e outra superfície**: o `#481` é a API
da Hotmart filtrando na resposta; este é o **nosso insert gravando NULL**.
Consertar um não conserta o outro. E é a mesma forma do `#554`/`#585`: defeito
nomeado no genérico, consertado em **um** consumidor, irmão deixado com o buraco.

**O que isto custa decidir:** nada do Johnny. Tem dono (`#587`), o conserto é
ensinar a posição alternativa do mesmo jeito que a função vizinha já faz, e o
backfill das 518 linhas antigas é decisão de produção (o dado está todo no
payload — nenhuma linha precisa ser adivinhada).

⚠️ **Registrado como pergunta, não como acusação:** `CLUB_FIRST_ACCESS` (684),
`CLUB_MODULE_COMPLETED` (138), `checkout.session.completed` (67),
`UPDATE_SUBSCRIPTION_CHARGE_DATE` (9), `SWITCH_PLAN` (2) e `ORDER_FULFILLMENT`
(2) **também** estão 100% sem `buyer_email`. **Não medi** onde o e-mail mora no
payload deles, então não afirmo que seja o mesmo defeito.

### 1.2 🔴 A promessa ao Rafael passa a correr às 03:00Z — e a decisão que a destrava está em dois cartões que nenhuma contagem enxerga

O relógio, com a conta feita: a carta da casa (uid 765, 25/09 **17:27Z** = 14:27
BRT) diz por escrito *"SE PASSAR DE AMANHÃ sem notícia minha, responde aqui"*.
Escrita em 25/09 BRT, **"amanhã" = 26/09 BRT, que começa às 03:00Z**. Agora são
00:3xZ = 21:3x BRT de 25/09. **Faltam ~2h30 pro dia do prazo começar** — ele não
venceu, entra em vigor na próxima ronda. Digo com o número porque *"vence
amanhã"* repetido três rondas perde sentido: a partir das 03:00Z a frase certa
passa a ser **"o prazo é HOJE"**.

**Nada entrou — terceira medição idêntica em 6h.** `plan=free ·
credits_subscription=0 · credits_extra=0 · access_until NULL · entitlements
ACTIVE=0 · profiles.last_seen_at NULL`. Igual às 18hZ e às 20h40Z. E
`emails_enviados` confirma que **nenhuma carta saiu pra ele depois das
18:26:19Z**. O estado não andou **e** a casa não escreveu.

**O fato novo é de estrutura.** A nota do Frank das ~20hZ diz *"o próprio #505
registra o conflito de ordem **aberto** há 4 dias"*. Fui conferir:

```
#505 · status = IGNORED · resolved_at 2026-09-21 10:55:46Z · resolved_by frank
```

**Não está aberto — está fechado há 4,5 dias.** O conflito sobrevive só dentro da
nota de fechamento dele: *"Pendente é comercial, não técnico: decisão do Johnny
entre a regra do Lucas de 31/08 e o weekly dele de 14/09 (SGP=30 dias), cuja
migration 115 segue não aplicada."*

Então o estado é este: **a única pergunta que destrava a carta do Rafael mora em
dois lugares — `#505` (ignored desde 21/09) e `#581` (aguardando_aluno) — e os
dois estão fora de toda contagem de aberto.** Os 130 abertos que eu publico toda
ronda **não contêm essa pergunta**. Ela não está perdida: está escrita, com
clareza, nos dois cartões. Está escrita **onde a casa não procura**.

Não é birra de nomenclatura — é o custo medido do `#207`, citado na correção de
21/09 da ordem de 17/09: o Vigia avisou que a garantia vencia em ~11,7h, ninguém
viu porque o cartão estava em `aguardando_aluno`, a garantia venceu e o aluno
ficou sem devolução. Aqui a forma é a mesma **com um agravante: são dois cartões
invisíveis, e o de fora (`#505`) é o que carrega a decisão.**

**O que eu não contesto:** concordo com o Frank em **não** escrever hoje. A
medição por `product_code` dele (7283229 R$597 + 7283335 R$297 = curso; 7851642 =
plataforma, que ele não tem) está certa. Terceira versão do mesmo fato em 24h
seria pior que esperar.

**A pergunta pro dono, inalterada e agora datada:** a compra avulsa do SGP dá ou
não dá acesso/crédito na plataforma — e o que a casa faz com a promessa de
crédito **já enviada por escrito** ao Rafael, cujo prazo passa a correr às
03:00Z de hoje?

### 1.3 🟠 O cartão novo do Walter manda processar um reembolso que não existe

`#586`, nascido **23:39:34Z**, aberto pelo sistema (`fast-help`), aluno
`walter_rapuano@hotmail.com`. O texto do próprio cartão instrui: *"cancelar,
processar reembolso pela Hotmart (garantia não confirmada no sistema, verificar
janela)"*. **As duas pernas estão erradas em cima do dado**, e anotei a medição
no cartão **antes** que alguém escreva a carta.

**Perna 1 — o cancelamento já está feito.** Ele mesmo cancelou na Hotmart e a
casa processou em **1,6 segundo**:

```
raw_event.cancellation_date ... 2026-09-25T23:28:28.386Z   (ação dele, na Hotmart)
payment_events received_at .... 2026-09-25T23:28:29.689Z   (webhook chegou)
entitlements.updated_at ....... 2026-09-25T23:28:30.035Z   status = canceled
processed_at .................. 23:28:30.376Z   error = NULL
```

O pedido dele no chat é das **23:39:34Z — onze minutos depois de já estar
cancelado**. Ele não está pedindo que a gente cancele: **ele não sabe que já
cancelou.** Ninguém mentiu pra ele — ele ficou sem notícia *nossa* (por desenho:
`mapRevokeStatus`, `hotmart-payload.ts:175`, revoga e não escreve carta) e voltou
a pedir.

**Perna 2 — não existe reembolso a processar.** `actual_recurrence_value: 0`,
"Plano Founder" assinado **hoje** 18:57Z, `date_next_charge 02/10` que nunca
chegou. **Ele nunca foi cobrado pela assinatura.** Quem escrever *"vamos
processar seu reembolso"* promete devolução de dinheiro que não saiu da conta
dele — a mesma família de erro que custou credibilidade com o Rafael.

**O dinheiro real dele é outro e é de seis meses atrás:** `HP3626375286` R$ 497 +
`HP3075414861` R$ 297 = **R$ 794 em 18/03/2026** (~6,2 meses), produto de
**curso** — a mesma classe do `#505`/`#581`. **Antes de escrever, decidir de qual
dos dois ele está falando**: o resumo da Fast não distingue.

Contexto que muda o tom da carta (fato, sem juízo — mérito é do dono): conta
criada 16:52Z, voz treinada, 2 áudios, 2 imagens e um **Vídeo Clone às 22:20Z**
(78s, −8.190 créditos), **68 min antes de cancelar**; 10.615 créditos consumidos
dos 100.000 do ciclo. Acesso segue ativo até 02/10 (regra do Johnny: cancelou,
usa o que tem até acabar).

**Ninguém falou com ele ainda:** a última carta é de 17:01Z (onboarding), **zero**
sobre cancelamento ou reembolso. O pedido tem ~55 min — **não é atraso ainda**, e
é por isso que a nota chega antes da carta e não depois.

**Não virou chamado** (ordem de 27/08): o webhook fez exatamente o que está
escrito, em 1,6s, sem erro. O que virou chamado foi o `#587`, que eu só encontrei
**olhando este evento**.

### 1.4 🟠 `#249` + `#250`: R$ 1.427,60 em dois pagantes parados por um "pode"

Repasse sem número novo — **nenhum movimento na janela**.
`glaubermed@ig.com.br` (endereço inexistente, 550 5.1.1) e
`andy.silvestre@icloud.com` (caixa cheia, 552 5.2.2, bounce novo às 20:35:05Z).
*A pergunta continua: pode chamar esses dois no WhatsApp?*

---

## 2. O que ABRI e ANOTEI

| ação | cartão | o quê |
|---|---|---|
| **ABRI** | `#587` `b8e7321c` | `vigia:buyer-email-nulo-em-todo-cancelamento-hotmart` — 518/518 cancelamentos com `buyer_email` NULL; e-mail presente em 518/518 em `data.subscriber.email` e em 0/518 em `data.buyer.email`; `arquivo:linha` dos dois pontos; **dano em produção hoje 0**, com os 3 consumidores conferidos um a um (§1.1) |
| anotei | `#586` `176251dd` | medição que derruba as duas pernas do texto do cartão (cancelamento já feito em 1,6s; reembolso inexistente porque a assinatura é R$ 0) + o R$ 794 de curso de 18/03 + o consumo no trial. **Status não mudado** (§1.3) |
| anotei | `#581` | o prazo passa a correr às 03:00Z, com a conta em BRT; 3ª medição idêntica de estado zerado; e a correção de que o `#505` **não** está aberto — está `ignored` desde 21/09, então a decisão mora em dois cartões invisíveis. **Status não mudado** (§1.2) |

**1 chamado, 2 anotações.** Não fechei nem reabri cartão nenhum.

---

## 3. Frente 1 — todos os abertos, SEM filtro de assinatura

Consulta sem cláusula de `signature`, como manda a regra (o filtro
`fast-email:%` já fez reportar "0 abertos" existindo quatro, em 19/08).

```
130 abertos = 15 open + 115 investigating      (22hZ: 129 = 14 + 115)
 37 aguardando_aluno   (contados à parte)       (22hZ: 37)
167 vivos no total                              (22hZ: 166)
por categoria (abertos): 84 técnicos + 46 atendimento   (0 com categoria nula)
404 fechados = 337 fixed + 67 ignored           (22hZ: 403)
```

Fecha nos dois eixos: 84 + 46 = 130 = 15 + 115. ✔

**Movimento da janela, conciliado linha por linha:** 129 − `#584` (fechado)
+ `#586` + `#587` = **130**. ✔

**Nasceram na janela: 2.** `#586` (sistema, 23:39Z — §1.3) e `#587` (meu — §1.1).

**Fechados na janela: 1** — `#584`, pelo Frank, 23:28:34Z. **Fui conferir e o
fechamento se sustenta:** a nota mede as duas assinaturas do aluno na Hotmart
viva (`211AHFMF` INACTIVE e `5RZFL369` CANCELLED_BY_SELLER, ambas trial), diz que
não havia relógio de cobrança rodando, e o aluno **foi respondido** — carta
`"Confirmado: seu cancelamento esta feito"` às **23:27:52Z**, `origem
ronda-manual`, **sem bounce**, e ela **entrou no livro** (§4). Promessa com
relógio que nasceu na ronda anterior foi cumprida dentro da janela. **Sem
objeção.**

### ✅ Conferência que evita um falso alarme na próxima ronda

`#552` (*"o botão de cancelar do app diz CANCELADA antes de tentar cancelar"*)
foi fechado às **17:47Z**, e `#584` — mesma área — nasceu às **22:15Z**, 4,5h
depois. Isso **parece** reincidência e **não é**. A nota de fechamento do `#552`
diz que o conserto **criou** a assinatura
`assinatura:cancelamento-no-app-nao-saiu:<userId>`, que é **exatamente** a
`signature` do `#584`, e trocou a carta falsa de "cancelada" por "recebemos o seu
pedido". Ou seja: o `#584` é **o conserto do `#552` funcionando como projetado** —
o `catch` vazio virou chamado em vez de engolir o erro. Registro aqui pra ninguém
ler isso como regressão.

### Cartões duplicados por aluno

```
21  alunos com mais de um cartão vivo   (22hZ: 21)
43  cartões envolvidos                  (22hZ: 43)
22  EXCEDENTES                          (22hZ: 22)
```

**Parado — nenhum dos três se moveu na janela.** Régua: `array_length(affected_emails,1)=1`
(um cartão, um aluno), que é a que reproduz a série — `unnest` devolve número
maior e **não comparável**, porque cartão de CLASSE carrega dezenas de e-mails.

### Idade dos abertos (régua canônica: `first_seen_at`)

| faixa | nº | 22hZ |
|---|---|---|
| < 24h | 9 | 10 |
| 1–3d | 18 | 16 |
| 3–7d | 26 | 25 |
| 7–14d | 42 | 42 |
| **14d+** | **34** | **34** |

Soma 129 no instante da medição (antes do `#587` nascer). **O 14d+ não se move há
quatro rondas.**

### Percepção travada (ordem de 17/09)

`percepcao_travada.cjs` — instrumento canônico, não o SQL:

```
controle positivo OK (#310) · controle negativo OK (#518 descontado)
570 incidentes varridos
👁  SO PARAM POR FALTA DE VER/OUVIR/ASSISTIR: 0
>>> 0 card(s) travado(s) em percepcao · mais velho parado ha 0d
```

**0 cartões, mais velho 0d. Nada a despachar pro `olho`/`qa`.**

⚠️ **Limite mantido do `#585`:** este 0 é apurado por um detector que lê
`agent_notes[-1]` cru e por isso **não enxerga os 22 cartões (13%) que têm
carimbo de nota em lote por cima**. Hoje ele acerta **pelo conteúdo, não pelo
alcance**. Chamado aberto na ronda das 22hZ, segue `investigating`, **não
mexido** (é de dono, não meu).

### Fechados que voltaram a disparar

`2026-08-20_fechados_que_disparam.cjs`:

- **Família A** (disparo *depois* do fechamento): **1 de 404** — `acf8acd6`
  (`#8`, *"Treino de voz: áudio insuficiente/sem fala limpa"*, `fixed`),
  `last_seen` **22/08**. **Não dispara há 34 dias.** `0 com ocorrência nas
  últimas 48h.` **Não reabri** (14-A).
- **Família B**: 16 fechados em cima do disparo, **1 com sinal nas últimas 48h**
  → `8da87d52` (`#544`), conferido nas duas rondas anteriores:
  `first_seen_at = last_seen_at = resolved_at`, `occurrences 1` — **não é
  reincidência**. Nada mudou na janela.
- **8 reincidentes com aluno** (o aluno escreveu de novo e o chamado re-fechou):
  `#154 #130 #82 #167 #126 #141 #375 #407`. Número estável, **anotação, não
  reabertura**.

⚠️ **Limite declarado do instrumento, mantido:** dos 404 fechados, **186 são
auditáveis** e **217 são CEGOS** (`last_seen_at` congelado desde o insert). Sobre
os cegos o script **não** diz "nenhum disparou", diz **"não dá pra saber por
aqui"** — foi um cego que produziu o falso fechamento do `#226`. E **30 deles
carregam `occurrences > 1` digitado no insert**, número que nunca mais anda.

### Nenhum pagante novo ficou sem conta nesta janela

Checagem própria, porque o `#582` (*"três pagantes sem conta nenhuma e
invisíveis pra fila"*) está **vivo**: cruzei os eventos de compra da janela
contra `profiles`.

| aluno | evento | hora | produto | perfil |
|---|---|---|---|---|
| `erick@pro-contaty.com.br` | PURCHASE_COMPLETE | 22:20Z | 7851642 | ✔ |
| `pri_portes@hotmail.com` | PURCHASE_COMPLETE | 22:44Z | 7851642 | ✔ |
| `jessicasilvamarcelo@gmail.com` | PURCHASE_APPROVED | 23:01Z | 7851642 | ✔ |
| `tales.arnaldo@gmail.com` | PURCHASE_APPROVED | 23:09Z | 7851642 | ✔ |

**4 entradas novas, 4 contas, 0 órfãos.** Todas do produto da plataforma a
`value 0` (adesão/trial). **A classe do `#582` não reincidiu na janela.**

**E os webhooks não erraram:** 6 eventos nas últimas 3h, **`error` NULL em todos
os 6**, `processed_at` preenchido em todos.

---

## 4. Passo fixo da ronda — reconciliar os envios

```bash
node _frank/ferramentas/2026-09-18_reconciliar_envios_da_pasta.cjs \
     --corte=2026-09-14T14:06:31Z --confirmar
```

```
1314  lidas da pasta "Sent"       (22hZ: 1313 · +1 na janela)
   0  + registro local (#210 — gitignored, não existe nesta máquina)
1314  = TOTAL
1237    já tinham linha            (22hZ: 1236 · +1)
   0    repetidas entre as duas fontes
  77    FORA DA JANELA (decisão: --corte)
   0    RECUSADAS (defeito da carta)
   0    DENTRO DA JANELA — escrituráveis
✔ 1314 = 1314: nenhuma carta sumiu na classificação
🕳️ CARTAS SEM LINHA, DENTRO DA JANELA: 0     → nada a fazer
```

Irmão de leitura, instrumento independente:

```bash
node _frank/ferramentas/2026-09-18_enviados_x_tabela.cjs
>>> VEREDITO: o buraco é PASSIVO. Depois do corte, 0 carta da pasta
    ficou fora da tabela.
```

**Os dois batem: 0.** E o `+1` tem nome: a carta
`"Confirmado: seu cancelamento esta feito (e o que continua seu)"` pro
`dionatas@outlook.com`, **23:27:52Z**, `origem ronda-manual`, `bounce_em NULL`.
**Uma carta saiu na janela e ela entrou no livro** — o controle compensatório do
`#101` fez o trabalho dele com carga real, não só com zero.

⚠️ As **77 cartas** anteriores a 14/09 14:06:31Z seguem sem decisão. Não é
recusa, é o `--corte`. Escriturá-las é decisão de produção (`cobreDesde` em
`contato-tentativas.ts`), não de ronda.

---

## 5. Frente 2 — a caixa

```bash
node _frank/ferramentas/ler_caixa.cjs --ultimos 12 --corpo 700
```

Leitura pura (`BODY.PEEK`, só `SEEN`). **O não-lido segue intacto** — é a fila da
Fast.

**NENHUM e-mail novo na janela.** Total `SEEN` segue **765**, último uid segue
**766** (bounce do `andy.silvestre@icloud.com`, 20:33:45Z), exatamente onde as
rondas das 20hZ e 22hZ pararam. **Nenhum aluno cobrando pela 2ª vez que já não
estivesse reportado.**

**O pedido novo da janela não chegou por e-mail, chegou pelo chat do app** — é o
`#586` (§1.3). Foi por isso que a Frente 2 não o viu: a caixa não é o único canal
de entrada, e uma ronda que só olhasse a caixa teria dito "nada novo" com um
pedido de reembolso de 55 min em aberto.

**Cartões abertos com carta já enviada:** `65` (`aberto_mas_ja_respondido.cjs`).
É **ordem de visita, não veredito** — e `24` abertos estão sem carta depois de
nascer, o que **não é prova de silêncio** (carta de worktree que não registrou,
WhatsApp e chat não aparecem ali).

**Pedidos de cancelamento/reembolso ainda sem resposta** (atendimento, **não**
chamado — ordem de 27/08), com o relógio corrigido: Danilo Paganucci (uid 752,
24/09 16:58Z, cita CDC e CPF — **31,6h**), combofav (uid 759, 13:30Z — **11h**),
Graziela (uid 751, 24/09 15:22Z), e agora **Walter** (`#586`, chat do app,
23:39Z — **~55 min**). Magda Reis (uid 756) segue sem cartão e **não deve ter**
— é pré-venda.

---

## 6. Checagem 2 do §3 (ordem de 27/08) — "já foi corrigido?"

`git log origin/main -- frontend/src/lib/payments/hotmart-payload.ts`: os commits
mais recentes que tocam o arquivo são o `07767682` (estorno zera crédito),
`a49f432e` (aviso de compra órfã só quando entrou dinheiro), `93d8f87a` e
`6e3f981b`. **Nenhum deles mexe em `extractBuyerEmail`** — a função segue como
está em produção.

`gh pr list --state open`: **69 PRs abertos**. Filtrei por `buyer|email|hotmart|
payload|cancel` e os candidatos (`#437`, `#406`, `#369`, `#354`, `#343`, `#340`)
são de envio de e-mail, thread, guarda de bounce e retomada do SGP. **Nenhum PR
aberto toca `extractBuyerEmail` nem `payment_events.buyer_email`.**

Busca por cartão pré-existente antes de abrir o `#587`: `#314`, `#317` e `#481`
casam por texto. Li o `#481` inteiro — é a **API** da Hotmart filtrando a
resposta, não o nosso insert. **Não é duplicata**, e o parentesco está nomeado
dentro do `#587` pra ninguém fechar um pelo outro.

---

## 7. O que eu NÃO fiz (14-A)

Não fechei cartão. Não reabri cartão. Não mudei status de nada — inclusive o
`#581` (`aguardando_aluno`), o `#586` (`investigating`) e o `#505` (`ignored`),
onde registrei observação sem tocar no rótulo. Não respondi aluno. Não escrevi
rascunho de carta — inclusive no `#586`, onde a medição está pronta e a carta é
de quem tem alçada. Não gerei link de acesso. Não mexi em crédito, saldo, acesso
ou assinatura. Não toquei em e-mail não lido. **Não mexi no `hotmart-payload.ts`
nem no `percepcao_travada.cjs`** — medi o defeito, abri o `#587` e deixei o
conserto com o dono. Não li, escrevi, classifiquei nem reprocessei nada vindo da
planilha (ordem de 29/08).
