# Ronda do Vigia — 09/09/2026, 22hZ (19h BRT)

Sensor. Varri, medi e **anotei 2** (`76e68853`, `16510171`). **Não abri chamado
novo** — as duas coisas novas desta ronda caíram na checagem §3.1 da ordem de
27/08 (classe já aberta = ocorrência, não chamado). Não respondi aluno, não
escrevi rascunho, não mexi em crédito, não fechei e não reabri nada (14-A).

Repo em `main`, `pull --ff-only` (já estava em dia). `_frank/ordens/README.md` e
`_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.
Ordem de **29/08** respeitada: nada da planilha lido, escrito, classificado,
avisado ou reprocessado; nenhum chamado de causa-planilha aberto ou reaberto.
O caso do `welrisson` abaixo é do **/sgp em produção**, que substituiu a
planilha — não é a planilha.
Ordem de **canal de 31/08**: o aviso desta ronda vai **no grupo**, com
`notify-grupo.sh`.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 Um pagante de **R$ 894** está travado na porta de entrada do SGP, e o plano de destravar não bate com o dado

**welrisson@gmail.com / (62) 99233-0525** (`76e68853`, aberto hoje 20:54Z pela
Fast). Ele **pagou R$ 894 em 29/08** — duas avulsas COMPLETE, medidas com
`pagou_de_verdade.cjs`: `HP0496009128` R$ 597 *Sistema de Geração Pronto* e
`HP0402899895` R$ 297 *Fábrica de Conteúdo Invisível*. São **11 dias** entre o
dinheiro entrar e ele estar parado na tela 1.

O Executor já diagnosticou às 21:25Z e deixou plano. **Conferi a premissa dele e
ela não fecha:** a nota diz que o e-mail trava porque *"já existe na base (conta
**ou pedido SGP anterior**)"*. Medi as duas pontas — **`sgp_pedidos` não tem
linha nenhuma dele** (`email ilike '%welrisson%'` → 0; `whatsapp like '%99233%'`
→ 0; e a tabela tem 50 linhas, 48 nos últimos 7 dias, então não é tabela vazia).
O que existe é o **profile**, criado em 04/09 15:40Z.

Consequência prática: o bloqueio vem do lado da **conta**, não de pedido SGP
anterior — e o plano (a), *"reassumir o pedido existente / reenviar código"*,
**não tem pedido pra reassumir**. Se alguém executar o plano como está, o aluno
continua travado.

Sobre dinheiro, explicitamente (lição do `#152`): **não afirmo defeito de
cobrança, não proponho estorno, nenhum `ref_id` foi casado.**

**A parte que é sua, em 1 linha:** quem destrava o welrisson hoje à noite, já
que o caminho previsto não existe pra ele?

### 2. 🔴 A decisão dos 15 vitalícios (`#313`) faz **25,4h** parada — sétima ronda pedindo

Nada mudou desde as 20h. O `2d0509b4` espera você dizer se **honra ou revoga** os
15 entitlements vitalícios de produto de curso (12 pessoas). Enquanto não houver
resposta, os **19 alunos** do `c726c5ae` (26,0h) não podem ser tratados, e o
Victor (`victor.inscriptio@gmail.com`) segue esperando reembolso — ele cobrou
pela segunda vez às 18:42Z de hoje e **não houve resposta desde então**.

**Pergunta em 1 linha:** honra ou revoga os 15 vitalícios?

### 3. 🟡 O PR duplicado continua aberto — terceira ronda avisando

O **PR #221** (`feat/fast-sabe-a-data-de-hoje`) segue **OPEN**, fazendo o mesmo
que o **PR #222**, que já está em produção. Avisado às 18h e às 20h, nada mudou.
Se for mergeado, passa por cima do conserto que está no ar — mesmo padrão já
registrado no índice de ordens para `feat/onedrive-401` e
`feat/fix-image-upload-retry`. **Não mergeio nem fecho PR (14-A).** É processo,
não virou chamado.

### 4. 🟡 O DDL segue sem aval — terceiro dia

Sem novidade: `104_avisos_enviados.sql` não aplicado, e com ele as **8 colunas**
que o `ddl_aplicado.cjs` acusa como ausentes (`106_sgp_cobranca`,
`82_generations_runpod_timing`, `96_training_jobs_cura_transcricao`). Quem
escreve nessas colunas cai no catch em silêncio: a telemetria **parece** ligada e
não mede nada. Escolha registrada, não defeito — **não virou chamado**.

---

## O que eu ANOTEI

| | onde | o quê |
|---|---|---|
| **anotei** | **`76e68853`** | nota 2: **o aluno é pagante de R$ 894 (29/08) e a premissa do plano não fecha** — `sgp_pedidos` vazio pra ele, o que existe é o profile de 04/09; logo o bloqueio é da conta, não de pedido anterior. Registrado também que **`affected_emails` deste chamado está VAZIO** (o e-mail só aparece no `title`, o telefone no `sample_error`): quem varrer por `affected_emails` **não enxerga este aluno**. `agent_notes` 1 → 2. |
| **anotei** | **`16510171`** | nota 5: **o caminho de cancelamento que a Fast repetiu 3x não existe, e o texto é nosso** — `frontend/src/lib/agent/manual.ts:403` manda *"Minhas compras → FastCloner → Cancelar assinatura"*. A aluna percorreu e respondeu 4x que só há *"Acessar"* e *"Mostrar detalhes"*. `agent_notes` 4 → 5. |

### A classe do `manual.ts:403`, com a fonte separada do medido

A aluna **Mara Maia** (`mmbabona@gmail.com`) escreveu **9 vezes em 45 min**
(20:21Z–21:05Z) tentando cancelar dentro da garantia. A Fast deu o mesmo caminho
errado **três vezes**. Ela achou sozinha e **nos ensinou** (uid 524): *três
pontinhos → gestão de pagamento → cancelar assinatura*.

- **Medido e nosso:** a linha 403 do `manual.ts`, com o caminho que ela não achou.
- **Relato dela, uma fonte só:** o caminho certo. Não conferi na Hotmart e **não
  afirmo como medido**.
- **Precedente da mesma classe, já aceito como chamado e já fechado:**
  `6437aed9` (01/09) — *"A FAST MANDA O ALUNO GRAVAR NA TELA
  /app/voice-cloning/new, ONDE NÃO EXISTE BOTÃO DE GRAVAR"*.
- **Por que não abri chamado:** a classe está aberta no próprio `16510171` (§3.1).
- **Desfecho:** ela cancelou sozinha e confirmou o e-mail da Hotmart às 21:05Z.

### Um alarme que eu conferi e **descartei** — não é o `#531b6529` voltando

Os 3 prints que a Mara mandou pra provar o problema bateram no teto de anexo
(4,1 MB cada) e a Fast não conseguiu ler nenhum. Antes de chamar de reincidência,
li a `resolution_note` do `531b6529`: o defeito de lá era **silêncio de 38h**, e
a resolução prova que `mail-respond.ts:266` trata oversized **antes** da checagem
de texto e **responde sempre**. Hoje ela foi respondida na hora. **O fechado não
voltou a disparar — o que aparece é o teto funcionando como projetado.** O que
sobra é que a prova visual do aluno não chega até nós, com os **PRs #41, #42 e
#187** abertos justamente nisso. Processo, não chamado.

---

## Aluno cobrando mais de uma vez

- **Victor** (`victor.inscriptio@gmail.com`) — segunda cobrança às 18:42Z de
  hoje, **sem resposta desde então**. Travado atrás da decisão do `#313`.
- **Mara** (`mmbabona@gmail.com`) — 9 mensagens em 45 min; resolveu sozinha.
- Sem novidade minha, todos já com chamado aberto: **Alana** (`506b7c3a`),
  `rafaluanravi29@gmail.com`, `jutai.santos@gmail.com`, `jkakoalves@gmail.com`.

## Números da ronda

- **55 incidentes abertos**: **37 de aluno**, **18 técnicos**. Todos em
  `investigating`; **zero** em `open`.
- **Nasceram 3** desde as 20h, todos de origem aluno: `16510171` (20:25Z, Mara),
  `76e68853` (20:54Z, welrisson), `f1025570` (21:06Z, `jack35marinho@gmail.com`,
  quer apagar a voz e treinar do zero). **52 + 3 = 55**, o total fecha, e
  **nenhum foi reaberto**.
- ⚠️ **Divergência honesta de classificação com a ronda das 20h**, que reportou
  32/20. O **total** bate; o **corte** não. Meu critério aqui é a **origem**
  (`reported_by`): `fast` 19 + `fast-help` 16 + `fast-sgp` 1 + `suporte@` 1 = 37
  de aluno; `frank` 11 + `vigia` 6 + 1 sem autor (o `d3d8d1b2`, técnico antigo) =
  18 técnicos. Não é dado divergente, é régua diferente — fica registrado em vez
  de arredondado.
- **Nada fechado voltou a disparar.** Varri `fixed`/`ignored` com
  `last_seen_at > resolved_at + 5min`: sai **1** registro, o `acf8acd6`
  (`training:user_dataset`), que disparou pela última vez em **22/08** — velho,
  conhecido, nenhum caso novo em 48h. Mesmo resultado das 18h e das 20h.
- **Nenhum merge na `origin/main`** desde 19:30Z. **32 PRs abertos.**
- Caixa: 25 e-mails lidos com `BODY.PEEK`, só `SEEN`. **Não toquei em não-lido.**
  Os **9 novos** (uids 518–526) são todos da mesma aluna.
