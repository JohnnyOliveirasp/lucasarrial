# Vigia — ronda de 03/09/2026, ~00hZ (02/09 21h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 3** (`3a703b12`,
`d48e6a45`, `702cc916`). Não fechei, não reabri, não mudei status de nada, não respondi aluno,
não escrevi rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 O `#235` foi fechado às 22:44Z e o buraco continua na main — o conserto está no PR #160, aberto 4 minutos DEPOIS do fechamento

Achado desta ronda. **Fui ler a main eu mesmo**, não repeti o corpo do PR:

| onde | o que está lá hoje |
|---|---|
| `voice-recorder.tsx:147` | `if (leuLocal) setLeituraConfiavel(true);` — a trava é decidida **só** pelo sucesso da leitura **local**. A leitura da conta (linha 121) não entra na decisão. |
| `clip-sync.ts:36-45` | `listServerClips(): Promise<ServerClip[]>` devolve `[]` em **três** situações que o chamador não distingue: conta vazia, `!r.ok` (l. 39) e exceção de rede (l. 42-43). |

Somando as duas: IndexedDB local vazio + soluço de rede na conta → a lista chega vazia, a leitura
é marcada confiável assim mesmo, e a marca de gravação é apagada — a prova que permitiria a tela
seguinte dizer *"você gravou e eu não achei"*. É o `#235` por outro gatilho.

**A pergunta, em 1 linha:** merga o **PR #160** ou não?

⚠️ **O que eu NÃO afirmo:** não reproduzi em runtime (exigiria forçar falha de rede no
`/api/v1/voice-clips` com IndexedDB vazio) e **não afirmo que algum aluno perdeu gravação por este
caminho depois do deploy** — não medi isso. A Alana marcou retorno para o dia 07, por escolha dela.

Não virou chamado novo: a classe é **este mesmo card** (checagem 1) e o conserto **já existe
escrito** (checagem 2). Anotado no `#235`, nota 9.

### 0.2 A medição da ordem de 27/08 vence HOJE (03/09) e **não bateu o alvo, por um chamado**

```sql
select status, count(*) from incidents
 where reported_by='vigia' and created_at >= '2026-08-27' group by 1;
```

| status | n |
|---|---|
| fixed | 15 |
| ignored | 2 |
| investigating | 1 |

**`ignored` = 2 de 18 = 11,1%.** Alvo **≤10%**. **Não bateu** — falta um chamado no denominador ou
um `ignored` a menos. Os dois `ignored` são o `#152` (a acusação de dinheiro por *timestamp*, que a
própria ordem dissecou) e o `#190`.

Segundo alvo — **zero chamado com `categoria='atendimento'` ou `kind` `processo`/`atendimento`**:
filtrando por `reported_by='vigia'` **e** posterior às 13:40 de 27/08 (hora da ordem), sobra
**exatamente 1**: o `#153`, de 14:15Z. Os outros dois que rondas anteriores citavam (`#150`,
`#151`) são de **12:17Z**, *antes* da ordem — não contam contra ela. A leitura que mantenho é a das
rondas anteriores: no `#153` o errado é o **campo `categoria`**, não a decisão de abrir; o conteúdo
é defeito de sistema legítimo. Registro as duas leituras em vez da que me favorece.

### 0.3 PRs #41/#42 (teto de 2MB) — décimo primeiro dia parados

Sem mudança. Os dois seguem **OPEN**. Ninguém está esperando por eles agora (a Alana marcou o dia
07), mas o buraco continua aberto para o próximo aluno.

### 0.4 Migration 102 — **oitava** ronda com a mesma pergunta de 1 linha

**Aplica a migration 102?** O PR **#150 foi mergeado** (`c26d260`) e o `#232` fechou — mas
`scripts/102_incidents_resolved_guard.sql` segue **não aplicada**, e a nota de fechamento diz isso
com todas as letras.

---

## 1. O que eu ABRI

**Nenhum chamado.** Os três achados desta ronda são prova de entrega, objeção e medição de série —
os três destinos que a ordem de 27/08 define fora da fila.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`3a703b12` / `#236`** — §3 abaixo, a prova de entrega. Nota 4.
- **`d48e6a45` / `#235`** — §0.1, a objeção. Nota 9.
- **`702cc916` / `#226`** — §4, a nona medição. Nota 18.

---

## 3. EXECUTEI A RECEITA QUE A RONDA ANTERIOR DEIXOU — o `#236` agora tem prova de ENTREGA

A rotina das 23hZ fechou o `#236` com prova de **deploy** e registrou, honestamente, que faltava
prova de **entrega**: nenhum aluno tinha animado imagem depois das 22:01Z. Deixou a receita no
card. **Rodei.**

| passo | resultado |
|---|---|
| `image_generations` com `video_path` não nulo e `created_at > 22:01Z` | **1 linha** (era 0 às 23hZ) |
| a geração | `2d0c9cbc`, **23:42:09Z** — 1h41 depois do deploy — `status=ready`, `video_status=ready` |
| tier | **`bronze`** — exatamente o que a receita pedia (*"Gold já saía mudo antes, Gold mudo não prova nada"*) |
| modelo | `grok-imagine-video-1-5-preview` · aluno `evertonk95@gmail.com` |
| objeto do R2 | bucket `voices-clone-ai-verse`, `1c991f29-…/images/2d0c9cbc-…/video.mp4`, 2.724.673 bytes |
| `ffprobe` | `index=0 codec_name=h264 codec_type=video` · **faixas de áudio: ZERO** |

O fechamento do `#236` deixa de valer por prova de merge e passa a valer por **prova de entrega** —
que é exatamente a distinção que produziu o fechamento falso do `#226` às 16:59Z.

**Limites, ditos na cara:** **n = 1**. Um tier (bronze), um modelo. Não provei silver/gold nesta
janela e **não provei o clipe de CENA** (2ª metade, PR #158) — este objeto é do caminho *Animar
Imagem*. Não ouvi o vídeo: `ffprobe` lê o container, o que basta para *"não existe faixa de áudio"*
e não bastaria para nada sobre conteúdo.

---

## 4. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature`.

**4 abertos — 1 de aluno, 3 técnicos.** (eram 7 às 22hZ: `#232`, `#235`, `#236` e `#238` fecharam
às 22:44Z; o `#226` foi reaberto às 23hZ)

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **348,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) |
| `702cc916` `#226` | 30,3h | 151 alunos | QA esgota e entrega assim mesmo — **reaberto às 23hZ** |
| `f8587cef` `#234` | 7,9h | **237 alunos** | palavra decapitada em fronteira interna (609 gerações) |
| `92b1cc85` `#237` | 3,5h | *(irrespondível)* | `affected_emails` **vazio**, `signature` é um uuid aleatório |

**Crédito conferido** por `ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por
`kind`, armadilha de 20/08; **não** só por `generation_refund`, armadilha do `#129`):

```
katiasalvador32       estornos 4 · falhas 0 · em curso 0
contato@fotoatleta    estornos 0 · falhas 0 · em curso 0
catarinacouras        estornos 1 · falhas 1 · em curso 0
kelinnavelar          estornos 0 · falhas 0 · em curso 0
digital@semente       estornos 0 · falhas 0 · em curso 0
```

**Nenhum aluno travado, nenhum estorno pendente.** Filas: `generations` presas há +2h = **0**;
`voices` em `training` paradas há +2h = **0**.

**`voices/awaiting_training` = 17**, sem mudança. **Não é chamado** — é o estado em que a bola está
com o **aluno**. Registro porque o PR **#15**, que daria dono a essa lista, está **OPEN desde 20/08**.

---

## 5. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Nenhum e-mail novo desde a ronda das 22hZ.** O uid mais alto continua sendo o **428**
(02/09 20:55:23Z, a Alana marcando o retorno para o dia 07) — o mesmo que a ronda anterior já
reportou. Três horas e quinze sem mensagem nova.

Nenhum aluno cobrando pela segunda vez sem resposta. Nenhum *"já pedi ajuda várias vezes"* novo.
Nenhum *"a equipe vai verificar"* sem incidente correspondente.

---

## 6. Fechados que voltaram a disparar

`status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` `#8` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
22/08 — **268,7h atrás**. Não é reincidência ativa.

✅ **O buraco que eu reportei por 4 rondas seguidas parou de ser buraco — mas não porque foi
consertado.** A consulta escondia o `#226` porque o `last_seen_at` dele estava congelado em 01/09,
anterior ao `resolved_at`. **Ele agora está `investigating`**, então saiu do escopo desta consulta
por reabertura, não por conserto. O `last_seen_at` **continua congelado em 01/09 17:52Z** e
`occurrences` **continua em 290** contra **329** reais. **Se alguém fechar o `#226` de novo, o
detector volta a mentir na hora.** Fica registrado: *"nenhum fechado disparou"* vale para o que a
`signature` consegue contar, não para tudo.

---

## 7. A série do `#226`, nona medição — primeira com o chamado já reaberto

Mesma consulta em todas (`generations status='ready'`, `qa` não nulo, **sem** cláusula de assinatura):

| medida | 18hZ | 20hZ | 22hZ | **00hZ 03/09** |
|---|---|---|---|---|
| entregues com `qa.exhausted>0` | 316/708 (44,6%) | 317/715 (44,3%) | 324/728 (44,5%) | **329/735 (44,8%)** |
| alunos distintos atingidos | 144 | 145 | 148 | **151** |
| últimas 24h | 28/52 (53,8%) | 23/46 (50,0%) | 29/55 (52,7%) | **29/52 (55,8%)** |

**Depois do fechamento das 16:59Z:** 36 entregas `ready`, **17** com `exhausted>0`. A última é de
**00:05:36Z** — quatro minutos antes da consulta. **7h06 seguidas** de entrega após aquele `fixed`.

As **5 novas** desde a nota das 22hZ: `2d68284f` (22:30:10Z, exh 1, 7,5s) · `4f35bef3` (22:43:06Z,
exh 2, 22,9s) · `ff867fc5` (23:10:39Z, exh 5, 142,9s) · `97464f01` (23:23:40Z, exh 2, 153,3s) ·
`cbd71872` (23:55:42Z, exh 1, 20,2s).

### 7-A. O controle de cobertura da rotina das 23hZ se sustenta — e piorou

Mesma janela instrumentada (desde 02/09 02:32Z, o `d11394c`), mesma régua (0,85):

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 22 | 15 | **0 (0,0%)** | 0,867 |
| `exhausted > 0` | 25 | 15 | **13 (52,0%)** | **0,333** |

Ela mediu 0/20 e 11/22 (50,0%), pior 0,571. Duas horas depois: o controle **continua nunca** caindo
abaixo da régua, e o pior caso do outro grupo caiu de **0,571 para 0,333**.

**O pior caso da série inteira é novo e é de um aluno que não aparecia antes:** `97464f01`,
23:23:40Z, `sidneysantos100@gmail.com`, 153,3s, cobertura entregue **0,333** — cerca de **dois
terços** do texto daquele trecho não aparece na transcrição do áudio que ele recebeu. Ele tem
**duas** entregas esgotadas hoje, as duas abaixo da régua (`ff867fc5`, 142,9s, 0,667), as duas
**depois** do fechamento falso. `estornos 0 · falhas 0 · em curso 0` — não está travado e **não
reclamou**.

### 7-B. UMA HIPÓTESE MINHA QUE A MEDIÇÃO DERRUBOU

Olhando o 0,333 e o 0,667 — as **duas entregas mais longas** da janela — eu ia escrever que texto
longo esgota mais. Fui medir antes de afirmar:

```
abaixo da régua (13 entregas): duração média 46,1s
na régua ou acima (34):        duração média 39,0s
```

**7 segundos de diferença com n=47 não sustenta a afirmação.** Os dois piores serem os dois mais
longos é coincidência até prova em contrário, e eu não tenho a prova. Registro a hipótese derrubada
em vez de guardar a versão que parecia mais inteligente.

**Ressalva que eu mantenho:** `coverage_min_visto` diz que a **nossa própria conferência** reprovou
aquele trecho. Continua **não** sendo prova de que o áudio está *audivelmente* defeituoso —
afirmar isso exige **ouvir**, e por ordem de 27/08 ouvir não é veredito meu.

---

## 8. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada. Não respondi aluno nem
escrevi rascunho. Não mexi em crédito, voz ou áudio — em particular **não estornei** o
`sidneysantos100@gmail.com` nem os outros dois abaixo da régua, e **não escrevi para eles**
(avisar quem não reclamou é decisão de produto, não efeito colateral de uma medição). Não mergeei
PR (`#15`, `#41`, `#42`, `#160`, `#161`, `#162`) nem apliquei migration (102). Não ouvi nenhum
áudio: toda afirmação aqui é de telemetria, `ffprobe` de container, leitura de código e cronologia.
Nada da planilha foi tocado (ordem 29/08). O download do R2 no §3 foi **GET**, somente leitura.
