# Vigia — ronda de 02/09/2026, ~20hZ (17h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 3** (`531b6529`,
`d48e6a45`, `702cc916`). Não fechei, não reabri, não mudei status de nada, não respondi aluno,
não escrevi rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 Pedimos o print para a Alana, ela mandou, e o nosso sistema devolveu pedindo o print

Este é o achado da ronda, e ele é medido inteiro pelos relógios da própria caixa (UTC).

| hora | onde | o quê |
|---|---|---|
| **18:51:50Z** | Sent uid 463 | A **equipe técnica** escreve para a Alana e pede **uma coisa só**: *"tire um print dessa tela inteira e me responda com ele"*. O texto explica que há 3 causas possíveis e que o print decide qual é. |
| **19:18:13Z** | INBOX uid 426 (2,2MB) | **Ela mandou.** Quatro anexos: `1001217036.jpg`, `1001217034.jpg`, `1001217035.jpg`, `1001217037.jpg`. Escreveu: *"Prints anexados...só estou aqui pq agora quero ver isso acontecer."* |
| **19:20:04Z** | Sent uid 464 | **111 segundos depois**, a Fast responde sozinha: *"veio com um anexo grande demais (2 MB) e o nosso suporte por e-mail não consegue abrir arquivos desse tamanho — por isso não consegui ler o que você mandou. **Me reenvia só o texto**."* |
| **20:07:21Z** | INBOX uid 427 | **47 minutos depois:** *"De verdade! Agora cansei. Quero o cancelamento."* |
| **20:10:13Z** | Sent uid 467 | Orientação de cancelamento na Hotmart. O **`#223`** (pedido de cancelamento dela) **reabriu às 20:10Z**. |

**Onde nasce, `arquivo:linha`:**
- `frontend/src/lib/agent/mail-imap.ts:140` — `MAIL_MAX_BYTES = 2_000_000`. Acima disso baixa
  **só os cabeçalhos** (`oversized=true`).
- `frontend/src/lib/agent/mail-respond.ts:229-248` — `responderAnexoGrande()` manda o texto
  pronto **e marca como lida**. O que o aluno **escreveu** nunca é lido.

**O conserto já existe e já está provado — dentro de casa.** O `_frank/ferramentas/ler_caixa.cjs`
tem o **mesmo** teto de 2MB (linha 53) e mesmo assim **lê o texto**: não baixa o anexo, mas puxa a
parte MIME de texto sozinha via `BODY.PEEK[n]`, que tem poucos KB. Foi exatamente por isso que
**eu** consegui ler *"Prints anexados..."* no uid 426 e a Fast não conseguiu. O cabeçalho do
próprio `ler_caixa` registra o motivo: *"desistir do corpo inteiro por causa do anexo deixou 5
alunos sem resposta (um deles 62h)"*.

**E já tem PR aberto, há 10 dias:** **#41** (23/08 20:57Z) e **#42** (24/08 00:05Z) — *"Fast lê o
texto do aluno em e-mail acima do teto de 2MB"*, os dois citando o incidente `531b6529` pelo id.
Os dois seguem **OPEN**, conferido agora por `gh pr list`.

**A pergunta, em 1 linha:** o **PR #41/#42** entra ou não? É o único achado desta ronda que tem
conserto escrito, parado, e com preço já cobrado de uma aluna.

⚠️ **O que eu NÃO afirmo:** não abri os 4 anexos e não sei o que os prints mostram. Não afirmo
que o quique foi a **única** causa do cancelamento — ela já vinha frustrada desde 01/09 (`#219`,
`#220`, `#223`). Afirmo o que está medido: pedimos, chegou, o sistema descartou em 111s, e 47 min
depois ela desistiu.

**Os prints não se perderam:** estão no uid 426 e saem com `ler_caixa.cjs --anexos 426` (leitura
pura). Anotei isso no `#235` para quem pegar o card não concluir que ela nunca respondeu.

### 0.2 Migration 102 — mesma pergunta de 1 linha, sexta ronda

**Aplica a migration 102?** O PR **#150** segue **OPEN** e
`scripts/102_incidents_resolved_guard.sql` segue **não aplicada**. Só o relógio andando.

### 0.3 PR #154 (gravador da Alana) — ainda fora de produção

Continua **OPEN**, aberto 17:39Z. `git log origin/main` desde 17:30Z devolve **zero commits**:
nada subiu nesta janela. Mergear/deployar é decisão humana, não é do sensor.

---

## 1. O que eu ABRI

**Nenhum chamado.** Os três achados desta ronda são objeção e medição de classe já existente —
exatamente onde a ordem de 27/08 manda que eles vão, e não na fila.

**Por que o §0.1 não virou chamado novo** (checagens do §3 da ordem de 27/08):
1. **"Já existe?"** — sim: a classe é o **`531b6529` / `#98`**, `kind='email:acima_do_teto_2mb'`,
   fechado em 24/08.
2. **"Já foi corrigido?"** — **tem PR pronto e aberto** (#41, #42), os dois citando o id do
   chamado. Abrir chamado novo faria alguém refazer trabalho que já está escrito.

Passa no teste *"se o código estivesse certo isso não teria acontecido?"* → **sim**, é defeito de
sistema. Mas classe fechada de que eu discordo vira **objeção no próprio chamado**, nunca chamado
novo. Foi o que fiz.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`531b6529` / `#98`** — a objeção do §0.1, com a cronologia inteira, o `arquivo:linha` e os PRs.
  Nota 6.
- **`d48e6a45` / `#235`** — os prints **chegaram** e estão no uid 426; como pegá-los. Mais a
  correção do "terceiro dia" (é o **segundo**), que segue na descrição do card. Nota 6.
- **`702cc916` / `#226`** — a **sétima** medição da série. Nota 15.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature`.

**6 abertos — 4 de aluno, 2 técnicos.** (eram 5 às 18hZ: `#220` fechou, `#236` nasceu, `#223` voltou)

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **344,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) |
| `506b7c3a` `#223` | 28,3h | alana_pinho | **reabriu às 20:10Z** — pedido de cancelamento |
| `51d86460` `#232` | 9,9h | *(técnico)* | reabertura automática não limpa `resolved_at` |
| `f8587cef` `#234` | 3,9h | **237 alunos** | palavra decapitada em fronteira interna (609 gerações) |
| `d48e6a45` `#235` | 3,5h | alana_pinho | gravações aparecem mas não permitem envio |
| `3a703b12` `#236` | 1,2h | alessandropantera | **novo** — animação de imagem saiu **com áudio** em inglês |

O **`#236`** nasceu 18:56Z e **já tem conserto proposto**: PR **#155** *"Animar Imagem sobe MUDO —
corte da faixa de áudio no pós-processamento"*, aberto 19:39Z. **Não abri nada** para ele.

**Crédito conferido** para os três alunos com incidente aberto — por
`ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por `kind`, armadilha de
20/08; **não** só por `generation_refund`, armadilha do `#129`):

```
alana_pinho          estornos  0 · falhas 0 · em curso 0
alessandropantera    estornos  4 · falhas 0 · em curso 0
katiasalvador32      estornos 68 · falhas 0 · em curso 0
```

**Nenhum aluno travado, nenhuma geração presa, nenhum estorno pendente.** Os 68 da Katia e os 4 do
Alessandro são histórico já tratado, não fila.

**Sobre o `#223` ter voltado:** ele reabriu com `resolved_at`, `resolved_by` e `resolved_commit`
**nulos** — ou seja, desta vez o carimbo foi limpo. Isso **não derruba** o `#232`: a reabertura das
20:10Z veio pelo caminho do agente (`route.ts`), que **tem** a guarda; o caminho quebrado é o
automático (`reportar.ts:96-105`). E **não prova** que o `#232` foi consertado — o PR **#150**
segue sem merge e não houve commit nenhum na main nesta janela. Registro como **dado neutro**, não
como conclusão.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Novo desde a ronda das 18hZ:** uids **426** (19:18Z) e **427** (20:07Z), os dois da Alana, os
dois no §0.1. Ela já tem chamado aberto (`#223`, `#235`) — **não abri nada novo**.

**Nenhum outro aluno cobrando pela segunda vez sem resposta.** Nenhum *"já pedi ajuda várias
vezes"* novo. A Alana **está** sendo respondida — o problema com ela nunca foi silêncio: é que o
conserto não sobe (`#154`) e que o sistema descartou a prova que a própria equipe pediu.

---

## 5. Fechados que voltaram a disparar

Consulta honesta: `status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
22/08 — 330h depois. **Não é reincidência ativa:** faz 11 dias que não dispara.

⚠️ **E esta consulta segue mentindo, pela terceira ronda.** O `#226` entregou **mais duas** vezes
com `exhausted>0` depois do próprio fechamento e **não aparece aqui**: o `last_seen_at` dele é de
01/09 17:52Z, **anterior** ao `resolved_at` de 02/09 16:59Z. O mesmo vale para o `#98`, que
disparou hoje 19:20Z com `last_seen_at` congelado em **22/08**.

**Não abri chamado por isso**, e a razão importa: nem `qa_exhausted_entrega_silenciosa` nem
`vigia:email-acima-do-teto:*` foram ligadas para auto-disparar — os números são calculados à mão.
Não é código que quebrou, então não passa no teste *"se o código estivesse certo isso não teria
acontecido"*. Fica registrado como **limite conhecido do meu próprio detector**. **"Nenhum fechado
disparou" vale para o que a `signature` consegue contar, não para tudo** — e hoje a diferença
entre as duas coisas foi uma aluna cancelando.

---

## 6. A série do `#226`, sétima medição

Mesma consulta em todas (`generations status='ready'`, `qa` não nulo, **sem** cláusula de
assinatura):

| medida | 00hZ | 10hZ | 12hZ | 14hZ | 18hZ | **20hZ** |
|---|---|---|---|---|---|---|
| entregues com `qa.exhausted>0` | 300/686 (43,7%) | 307/695 (44,2%) | 307/696 (44,1%) | 310/699 (44,3%) | 316/708 (44,6%) | **317/715 (44,3%)** |
| alunos distintos atingidos | 136 | 138 | 138 | 140 | 144 | **145** |
| últimas 24h | 32/66 (48,5%) | 32/65 (49,2%) | 30/63 (47,6%) | 32/64 (50,0%) | 28/52 (53,8%) | **23/46 (50,0%)** |

**Entregas com `exhausted>0` criadas depois do fechamento das 16:59:00Z** — 16 entregas `ready` na
janela, **5** com `exhausted>0`:

| geração | hora | exhausted | dur. |
|---|---|---|---|
| `db1e0480` | 17:00:32Z | 4 | 98,5s |
| `35f799fd` | 17:28:00Z | 1 | 42,6s |
| `7bac4fb9` | 17:35:23Z | 6 | 120,0s |
| `4aeefb29` | **19:03:31Z** | 1 | 113,2s |
| `a2f8fd29` | **19:23:22Z** | 5 | 44,6s |

As duas últimas são **novas desde a nota das 18hZ**. Quatro das cinco passam de 40s — não é teste.

`git log origin/main` desde 17:30Z: **zero commits**. Nenhum PR novo que segure a entrega (o #155
é de outro subsistema).

**Ressalva que eu não escondo:** `exhausted>0` diz que o QA desistiu de melhorar N trechos — **não
prova** que o áudio final está audivelmente defeituoso. Afirmar defeito exige **ouvir**, e por
ordem de 27/08 ouvir não é veredito meu. O que está provado é telemetria e cronologia.

---

## 7. Calibragem da ordem de 27/08 (a medição vence amanhã, 03/09)

```sql
select status, count(*) from incidents
 where reported_by='vigia' and created_at >= '2026-08-27' group by 1;
```

| status | n |
|---|---|
| fixed | 14 |
| ignored | 2 |
| investigating | 2 |

**`ignored` = 2 de 18 = 11,1%.** Alvo **≤10%** — está a **um chamado** de bater, sem mudança desde
as 18hZ.

⚠️ **SEGUNDA CORREÇÃO DE UMA AFIRMAÇÃO MINHA DE HOJE.** Às 18hZ eu corrigi o "zero" das 16hZ para
**2** (`#150`, `#151`), consultando `kind`. Rodando a metade que faltava — **`categoria`** — a
conta é **3**: entra o **`#153`** (`categoria='atendimento'`, criado 27/08 **14:15**, ou seja
**depois** da ordem, que é de 13:40).

Sendo justo com os dois lados: pelo **conteúdo**, o `#153` é defeito de sistema legítimo — o
título é *"o e-mail do aluno abre e fecha o chamado no mesmo segundo: 5 chamados fechados 0,8 a
1,6s depois"*, e ele gerou uma decisão do Johnny em 29/08. O que está errado é o **campo
`categoria`**, não o chamado. Mas a consulta que a ordem define pega ele, e o número honesto é
**3, não 2**. Registro as duas leituras em vez de escolher a que me favorece. **Errei o número
duas vezes hoje** — primeiro por consultar coluna errada, depois por consultar só metade.

---

## 8. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada. Não respondi aluno
nem escrevi rascunho. Não mexi em crédito, voz ou áudio. Não mergeei PR (`#41`, `#42`, `#150`,
`#154`, `#155`) nem apliquei migration (102). Não baixei nem olhei os 4 anexos do uid 426. Não
ouvi nenhum áudio: toda afirmação aqui é de telemetria e cronologia, nunca de escuta.
