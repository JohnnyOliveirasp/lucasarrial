# Vigia — ronda de 03/09/2026, ~12hZ (09h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 2** (`702cc916`/#226,
`f8587cef`/#234). Não fechei, não reabri, não mudei status de nada, não respondi aluno, não escrevi
rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Última ronda de vigia: 10hZ. Rondas das falhas de 10h, 12h e 12h30 lidas antes de tocar em qualquer
coisa.

**Hoje é 03/09 — a data marcada no §5 da ordem de 27/08 para medir se ela funcionou.** A medição
está no §7.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 ✅ SAIU da lista: o cancelamento de R$ 2.697,60 do Vinícius (#240) foi respondido

A ronda das 10hZ escalou isto como urgente. **Foi tratado às 11:25:46Z**, e eu conferi no servidor,
não no card: e-mail **uid 482** em Enviados, redirecionando o aluno ao canal que decide reembolso de
curso (`suporte@lucasarrial.com` / WhatsApp (41) 99148-1573), com as 3 transações nomeadas e a
observação de que a compra mais antiga é de 29/08. O card foi para `aguardando_aluno` às 11:26Z.

**Não escalo de novo a pergunta.** Registro só o que sobra, que não é decisão e sim consequência:
o reembolso em si **não aconteceu** — ele depende de o aluno escrever para o outro canal, e ninguém
do nosso lado acompanha se ele escreveu. A janela que a ronda das 10hZ inferiu (05–06/09, aritmética
do CDC sobre a data da compra, **não** política apurada da Hotmart) continua correndo.

### 0.2 🟡 10 chamados estão em `aguardando_aluno` — e esse número não aparece no "abertos"

Este é o achado de processo da ronda. O placar que eu venho reportando ("N abertos") conta só
`open` + `investigating`. Existem **10 cards em `aguardando_aluno`**, com aluno do outro lado, que
somem desse número. O mais velho tem **259,5h — 10,8 dias** (`#99`, lucianodepinho, áudio de 30+ min
por link).

Não é bug novo e não abri chamado: a `varredura_travados.cjs` já mostra esse bloco separado desde a
objeção de 25/08, exatamente porque "honesto não pode significar invisível". O que eu registro é que
**o número que eu reporto no Telegram não carrega esse bloco**, e quem lê "6 abertos" não vê os 10.
Daqui pra frente eu reporto os dois números.

Isto não é hipótese: **o `#222` provou hoje.** Ele estava fora do meu placar das 10hZ e reapareceu
às 10:47Z quando o Frank o tirou de `aguardando_aluno` — e ao tirar, mediu que a classe é **5× maior
do que o título diz** (46 órfãs ativas, 26 ainda vigentes, contra "5 alunos" no título). Ficou
escondido enquanto estava no balde.

### 0.3 Migration 102 — **décima** ronda com a mesma pergunta de 1 linha

**Aplica a migration 102?** Conferido agora pelo banco, não pelo registro anterior:

```sql
select tgname from pg_trigger where not tgisinternal and tgrelid='incidents'::regclass;  -- []
```

Zero triggers em `incidents`. `scripts/102_incidents_resolved_guard.sql` existe no repo e segue
**não aplicada**.

### 0.4 PRs #41/#42 (teto de 2MB) — décimo quinto dia parados

Sem mudança. E não é caso isolado: **24 PRs abertos**, o mais antigo (#4, #5) de 18/08. O buraco dos
2MB continua aberto para o próximo aluno que mandar texto grande — e ele já bateu nesta janela: a
Alana (uid 426, 2,2MB) recebeu "não consegui ler o que você mandou".

---

## 1. O que eu ABRI

**Nenhum chamado.** Nenhum fato novo da caixa (§4) e nenhuma classe nova nas medições (§5). Os
quatro alunos da janela já têm card. Abrir seria duplicar fila.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`702cc916` / `#226`** — §5 e §6: a armadilha de instrumento, a 11ª medição da série e a
  corroboração da `a8caaae1`. Nota 22.
- **`f8587cef` / `#234`** — §6: a medição independente que apoia o defeito do `eh_ultimo` achado
  pela ronda das 12h30, **com o limite n=1 escrito**. Nota 9.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de
19/08, que fez reportar "0 abertos" existindo quatro).

**6 abertos — 2 de aluno, 4 técnicos.**

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **360,1h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) |
| `59a1f024` `#202` | 84,5h | vlorandi | atendimento humano — decisão comercial |
| `3ca22d47` `#222` | 44,3h | 5 no título, **26 medidos** | compra órfã casa só por e-mail (`claim.ts:39`) |
| `702cc916` `#226` | 42,4h | 155 alunos | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 20,0h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 15,6h | *(irrespondível)* | `affected_emails` **vazio**, `signature` é uuid aleatório |

Composição mudou desde as 10hZ: **saiu** o `#240` (foi para `aguardando_aluno`, §0.1) e **entrou** o
`#222` (saiu de `aguardando_aluno` às 10:47Z, §0.2). O `#237` está na **quarta** ronda sem dono.

**Mais 10 em `aguardando_aluno`** (§0.2), que o número acima não mostra: `#99` (259,5h), `#172`
(140,7h), `#206` (71,2h), `#207` (70,2h), `#214` (62,4h), `#216` (47,8h), `#218` (45,2h), `#224`
(43,4h), `#229` (39,4h), `#240` (8,1h).

**Crédito conferido** por `ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por
`kind`, armadilha de 20/08; **não** só por `generation_refund`, armadilha do `#129`), nos 17 alunos
citados nos abertos:

**Nenhum estorno pendente, nenhuma geração falhada em aberto, nada em curso preso.** Quatro alunos
com saldo **zero**, todos conferidos um a um em vez de assumidos:

| aluno | por que o zero **não** é travamento novo |
|---|---|
| `vlorandi` | nunca comprou a assinatura do FastCloner, só os cursos — estado esperado (a cegueira do `#173`, retratada em 02hZ) |
| `ftfranzolin` / `cdmarciofernandes` | **0 gerações, 0 transações**: é exatamente o retrato do `#222` — a conta que eles usam não é a da compra |
| `flmg.liborio` | 14 gerações e 34 transações, última em 18/08: gastou até zerar. Uso normal, não travamento |

Filas: `generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. **`voices/awaiting_training` = 17**, sem mudança — não é
chamado, é o estado em que a bola está com o aluno. Registro porque o PR **#15**, que daria dono a
essa lista, está **OPEN desde 20/08**.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 700`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Nenhum e-mail novo desde a ronda das 10hZ:** o uid mais alto continua **429** (Vinícius, 03/09
04:10Z), que já tem card e já foi respondido (§0.1).

Conferi os quatro alunos da janela contra a fila em vez de assumir que estavam cobertos:

| aluno | o que ele escreveu | tem card? |
|---|---|---|
| Katia (uids 418, 421–423) | áudio cortado no s34, ameaça migrar de plataforma | `#47` aberto + `#234` |
| Alana (uids 419, 424–428) | gravações somem, "enviar treinamento" não evolui | `#235` fixed; ela disse "dia 07 te chamo" |
| Silmara/Rufino (uid 416) | cancelamento dando erro | `#230` fixed |
| Johnathan (uid 417) | *"são bandidos profissionais, já estou buscando as providências cabíveis"* | `#173` fixed |

**Sobre o Johnathan: não escalei de novo, de propósito.** É ameaça jurídica e o instinto é levantar
a mão, mas o caso já foi tratado (`#173`, R$ 2.391,00 apurados pelo `pagou_de_verdade.cjs`, tratado
pela ronda das 02hZ) e já foi escalado ao Johnny em rondas anteriores. Repetir aqui seria o erro do
Vinícius ao contrário — reapresentar como novidade o que já tem dono e resposta.

Nenhum aluno cobrando pela segunda vez **sem resposta**, nenhum *"já pedi ajuda várias vezes"* novo,
nenhum *"a equipe vai verificar"* sem card correspondente.

---

## 5. A armadilha de instrumento que eu quase reportei como "está tudo bem"

Isto é o item mais importante da ronda, e é um erro meu pego no meio.

Fui medir a série do `#226` e rodei a contagem de "entregas abaixo da régua" por
`qa->>'coverage_min'`. **Deu ZERO nos dois grupos.** Se eu tivesse acreditado, teria reportado que o
alarme do `#226` parou — o oposto da verdade.

Fui conferir o zero antes de escrever (armadilha medida de 20/08: *"imprima o campo cru antes de
acreditar em qualquer zero"*):

```
min = 0.85 · max = 0.85 · exatamente 0.85 em 476 de 476 linhas
```

`coverage_min` **não é a cobertura medida — é a régua configurada**, gravada como constante em toda
linha. Contar por ela devolve zero em qualquer janela, para sempre. O campo medido é
**`coverage_min_visto`**. Deixei isso escrito na nota do `#226` e do `#234`, porque o zero é
silencioso e a próxima ronda cairia nele igual.

---

## 6. A série do `#226`, décima primeira medição — e ela ANDOU

Campo certo (`coverage_min_visto`), janela instrumentada desde 02/09 02:32Z:

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 30 | 20 | **0 (0,0%)** | 0,867 |
| `exhausted > 0` | 34 | 19 | **14 (41,2%)** | **0,333** |

Na ronda das 10hZ era 27/0/0,867 e 31/**13**/0,333. O controle **continua nunca** caindo abaixo da
régua, agora com 30 entregas. O outro grupo **ganhou mais uma: 13 → 14.**

**A ronda das 10hZ escreveu "não subiu nenhuma" e explicou que era falta de tráfego, não conserto.
A explicação dela estava certa e se confirmou:** o tráfego voltou (7 gerações entre 10:15Z e 12:04Z)
e a série voltou a andar na mesma ronda.

Série acumulada: **338 de 752** entregues com `exhausted>0` (**44,9%**), **155 alunos** distintos
(era 335/746 e 154 às 10hZ). Últimas 24h: **32 de 60 (53,3%)**.

### 6-A. A nova abaixo da régua corrobora, por outro caminho, o defeito que a ronda das 12h30 achou

A 14ª é a **`a8caaae1`** (03/09 11:11:08Z): cobertura **0,615**, `exhausted` 2, `regens` 26,
`coverage_rescue` **1**.

É a **mesma geração** que a ronda das 12h30 isolou — por aritmética completamente diferente da minha
(`entregue_n + sem_veredito` = `coverage_medido_n`) — como a única da janela com resgate, e da qual
ela chegou ao defeito: `_resgatar_por_subdivisao` e `_resgatar_nivel_2` chamavam `_rodar_qa` **sem
passar `eh_ultimo`**, então quando o último chunk cai no resgate, o fim real do arquivo passa a ser
julgado pela régua **interna**, que está em sombra.

Duas medições independentes apontando a mesma geração: a dela pela contagem de vereditos, a minha
pela cobertura entregue. A única geração da janela que passou pelo resgate é também a pior entrega
da janela.

⚠️ **O limite, dito como limite: n=1.** Resgate acontece justamente onde o áudio já estava difícil,
então "passou pelo resgate" e "saiu com cobertura baixa" podem ter a **mesma causa** em vez de um
causar o outro. Isto não prova a relação — torna a hipótese **testável**: se o conserto entrar, a
próxima geração com `coverage_rescue=1` tem que sair na faixa do controle.

**Estado do conserto, conferido no `gh` e não no commit:** `3bc1535` está na main, mas o
**`Build RunPod Worker` dele está `in_progress`** neste momento. O conserto **não está em produção
ainda** — merge não é entrega, que é exatamente a distinção que produziu o fechamento falso do
`#226`.

---

## 7. §5 da ordem de 27/08 — a medição de 7 dias vence HOJE

A própria ordem marcou 03/09 para se auto-avaliar. Rodei a consulta que ela escreveu:

```sql
select status, count(*) from incidents
 where reported_by='vigia' and created_at >= '2026-08-27' group by 1;
```

**18 chamados abertos pelo Vigia desde 27/08:** 15 `fixed`, **2 `ignored`**, 1 `investigating`.

| alvo da ordem | resultado | passou? |
|---|---|---|
| `ignored` ≤ **10%** (linha de base 21%) | **11,1%** (2 de 18) | **quase** — melhorou de 21% para 11,1%, mas não cruzou o alvo |
| **zero** card `categoria='atendimento'` ou `kind` em `processo`/`atendimento` | 3 cards | ver abaixo |

Os 2 `ignored`: **#152** (Adriana, os 3.600 créditos — é o próprio caso que motivou a ordem, aberto
**antes** dela) e **#190** (fix do #177 não alcança a falha de link).

Os 3 cards de atendimento/processo — **datei todos antes de contar contra a ordem**, porque contar
card anterior à regra seria enviesar a favor dela **ou** contra:

| card | criado | leitura honesta |
|---|---|---|
| #150, #151 | 27/08 **12:17Z** | **antes** da ordem (escrita 13:40Z) — não contam |
| #153 | 27/08 **14:15Z** | **depois** da ordem. Mas o título é *"o e-mail do aluno abre e fecha o chamado no mesmo segundo"* — isso é **bug de sistema** com a `categoria` rotulada errada, não chamado de atendimento |

**De 28/08 em diante: zero cards de atendimento/processo.** O alvo qualitativo da ordem foi atingido;
o quantitativo (10%) ficou a 1,1 ponto, com n=18 — **um único card a mais ou a menos vira o
resultado**, então eu não trato nem o "passou" nem o "não passou" como conclusão firme.

---

## 8. Fechados que voltaram a disparar

`status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` `#8` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
**280,8h atrás**. Não é reincidência ativa. **Nada fechado voltou a disparar nesta janela.**

⚠️ **O ponto cego continua.** O `#226` tem `last_seen_at` congelado em **01/09 17:52Z** e
`occurrences` em **290** contra **338** reais. Ele está `investigating`, então sai do escopo desta
consulta por **reabertura, não por conserto**. O PR **#163** faz o detector **declarar** o ponto
cego; ele **não descongela** este contador. Se alguém fechar o `#226` de novo, a consulta volta a
mentir na hora.

---

## 9. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada — em particular **não
tirei o `#240` nem o `#222` do balde `aguardando_aluno`**, só registrei que o balde esconde 10
cards. Não respondi aluno nem escrevi rascunho: **não escrevi ao Vinícius** (reembolso é dinheiro),
**não escrevi ao Johnathan** (ameaça jurídica, já tem dono) e **não escrevi à Katia** (o card #47 é
do Frank). Não mexi em crédito, acesso, voz ou áudio. Não mergeei PR (24 abertos) nem apliquei
migration (102, **10ª ronda**). Não ouvi nenhum áudio: toda afirmação aqui é de telemetria, `gh`,
leitura de caixa e cronologia. Não peguei o `#237` (não é meu, e o serial é do Frank). Nada da
**planilha** foi lido, escrito, classificado, avisado ou reprocessado (ordem 29/08).
