# Vigia — ronda de 03/09/2026, 22hZ (19h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 3** (`3ca22d47`/#222,
`702cc916`/#226, `f8587cef`/#234). Não fechei, não reabri, não mudei status de nada, não respondi
aluno, não escrevi rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Medição feita às **22:10Z**. Ronda de vigia anterior: `20h` (20:11Z). Confirmo pela terceira ronda
seguida a nota de nomenclatura: **os nomes de arquivo não batem com o relógio**; confie no carimbo
`Z` dentro do texto.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 🔴 O `#226` ACELEROU — e a pior entrega até hoje saiu há 90 minutos

Não é continuidade, é piora medida. A ronda das 20:11Z reportou **4** entregas abaixo da régua
depois do build; agora são **8**, e **4 nasceram nas últimas 2 horas**. A pior de toda a série é
**nova**: `e87eaac2`, 20:42:57Z, cobertura **0,448** contra régua 0,85 — antes a pior pós-build era
0,727.

Todas `ready`, **todas entregues ao aluno**. Nenhuma passou pelo resgate, então o `3bc1535` não
falhou: **ele não cobre este ramo**. O defeito de `loop.py:341-344` segue inteiro.

**A pergunta, em 1 linha: o `#226` continua esperando na fila normal, ou vira prioridade agora?**
Ele está entregando áudio reprovado a um ritmo de ~2/hora e piorando de qualidade.

### 0.2 Migration 102 — **décima quarta** ronda com a mesma pergunta

Conferido agora pelo banco, não herdado: `select tgname from pg_trigger where not tgisinternal and
tgrelid='incidents'::regclass` → `[]`. `scripts/102_incidents_resolved_guard.sql` segue **não
aplicada**.

### 0.3 PRs abertos: **28**, e subindo

Eram 26 na ronda das 20:11Z. Os `#41`/`#42` (teto de 2MB, o defeito que a Alana levou na cara em
02/09) estão no **décimo sexto dia**.

### 0.4 Um log de ronda do Frank estava preso fora do `origin/main`

Ao entrar, `git status` acusava **1 commit à frente do origin**: o `4743e2b`, log da ronda das
falhas de 21:40Z (serial #222). Estava commitado na main **e nunca empurrado** — ou seja, invisível
para todo mundo, inclusive para a própria ronda seguinte, que é exatamente o problema que a regra
de registro existe para impedir. **Empurrei junto com este log** (não alterei o conteúdo dele).

---

## 1. O que eu ABRI

**Nenhum chamado.** Todo achado desta ronda passou pelo teste de bolso da ordem de 27/08 e nenhum
sobreviveu como chamado novo: o achado do convite órfão é **ocorrência de classe já aberta**
(`#222`), os dois de série são ocorrência dos próprios `#226`/`#234`, e as três armadilhas de
instrumento são registro de ronda, não defeito de produto.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`3ca22d47` / `#222`** — nota 18: o convite automático manda a instrução errada para a população
  deste chamado e depois emudece para sempre. §5.
- **`702cc916` / `#226`** — nota 26: série 4 → 8 pós-build, pior nova 0,448, e a armadilha da
  geração apagada. §4.
- **`f8587cef` / `#234`** — nota 14: `coverage_rescue` segue **n=0**, ~9h40 depois do build. §6.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de 19/08).

**6 abertos — 2 de aluno, 4 técnicos.** Todos em `investigating`; `open` = 0.

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **370,2h** | katiasalvador32 | palavra "VOCÊ" no s34 — `categoria=atendimento` |
| `3ca22d47` `#222` | 54,4h | 5 no título, **14 medidos** | compra órfã casa só por e-mail |
| `702cc916` `#226` | 52,5h | **161 alunos** | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 30,1h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 25,7h | *(irrespondível)* | relato manual sem e-mail e sem anexo |
| `52b22304` `#245` | **0,9h** | igorlramalho | insatisfeito com o realismo do Vídeo Clone |

**Movimento desde as 20:11Z: entraram 3, saiu nenhum.** `#243` (trocar senha) e `#244` (botão
Configurações desabilitado) nasceram direto em `aguardando_aluno`; **`#245` é o único aberto novo**
— chegou às 21:27Z pelo chat do app, a Carol já avisou o time no grupo do WhatsApp e ele fica aberto
até alguém responder o aluno (decisão do Johnny, 29/08, `#153`). **Não reescalei**: já tem dono.

**Mais 11 em `aguardando_aluno`** (eram 9): `#99` (**269,6h = 11,2 dias**), `#172` (150,7h), `#206`
(81,2h), `#207` (80,3h), `#214` (72,4h), `#216` (57,9h), `#218` (55,3h), `#224` (53,5h), `#229`
(49,5h), `#243` (1,0h), `#244` (1,0h).

**Placar honesto da ronda: 6 abertos + 11 esperando aluno = 17 cards com gente do outro lado.**

### 3-A. Fechado que voltou a disparar: **nenhum**

`status in ('fixed','ignored') and last_seen_at > resolved_at` devolve **1 linha só**, o `#8`
(*"Treino de voz: áudio insuficiente"*, 57 ocorrências), e ela **parou em 22/08** — 12 dias atrás.
Não há classe fechada disparando hoje. Os 40 fechados com `last_seen_at` dos últimos 5 dias têm
todos `last_seen_at` **anterior** ao `resolved_at`, que é o padrão normal de quem foi fechado depois
de parar.

### 3-B. Crédito: **zero estorno pendente** — mas o primeiro número que eu tirei estava errado

61 gerações `failed` na base inteira: **11 nunca cobradas**, **50 cobradas e estornadas**,
**0 cobradas sem estorno**. A mais recente tem 148,1h. Confere com a medição das 20:11Z.

⚠️ **Armadilha de instrumento que eu quase reportei como notícia.** Minha primeira consulta somava
`sum(amount)` por geração e devolveu *"61 de 61 nunca cobradas"* — o que contradizia a ronda
anterior. **A consulta é que estava cega:** o débito e o seu próprio estorno se anulam
(`-1999 + 1999 = 0`), então *"soma zero"* não distingue **"nunca foi cobrada"** de **"foi cobrada e
devolvida"**. Só separando por `ref_type` (`generation` × `generation_refund`) o número correto
aparece. Conferi num caso concreto antes de acreditar em qualquer um dos dois: `b6e84e2c` tem débito
de -1999 (17:15:14Z) **e** estorno de +1999 (17:20:42Z), as duas linhas existem. Registro porque uma
consulta assim, num dia ruim, faz alguém "descobrir" que ninguém foi cobrado — ou pagar em dobro.

### 3-C. Filas e sinal de vida

`generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. `voices/awaiting_training` = **17** (igual às 20:11Z) —
não é chamado, é o estado em que a bola está com o aluno; o PR **#15**, que daria dono a essa lista,
está **OPEN desde 20/08**.

**Conferi que o silêncio não é cron morto:** 79 gerações nas últimas 24h, a última às **21:53:46Z**
(16 min antes desta linha), e 3 cards novos na fila nas últimas 2h. O pipeline está vivo.

---

## 4. O `#226`: 4 → 8 entregas ruins pós-build, e a série pode ENCOLHER sem nada ser consertado

Abaixo da régua depois do build do `3bc1535` (`success`, 12:23:08Z). Todas `ready`, todas entregues:

| geração | quando | visto | régua | `exhausted` | `regens` | passou pelo resgate? |
|---|---|---|---|---|---|---|
| `5d5b7866` | 13:08:05Z | 0,667 | 0,85 | 4 | 14 | não |
| `84e3a400` | 14:44:26Z | 0,667 | 0,85 | 1 | 16 | não |
| `79309199` | 16:10:22Z | 0,800 | 0,85 | 1 | 2 | não |
| `49996c3e` | 19:52:45Z | 0,727 | 0,85 | 1 | 16 | não ← **apagada, ver abaixo** |
| **`e87eaac2`** | **20:42:57Z** | **0,448** | 0,85 | 2 | 7 | não ← **pior de toda a série** |
| `b7355097` | 20:50:29Z | 0,552 | 0,85 | 2 | 8 | não |
| `2876a712` | 21:05:48Z | 0,625 | 0,85 | 1 | 5 | não |
| `c6ec2bda` | 21:39:44Z | 0,750 | 0,85 | 2 | 13 | não |

**Quatro nasceram depois das 20:11Z**, ou seja ~2/hora, e a pior (0,448) é bem pior que a pior
conhecida até então (0,727). **Não há tendência de melhora.**

⚠️ **Armadilha nova, e é a mais importante desta ronda.** A `49996c3e` **sumiu da tabela
`generations`** entre 20:11Z e 22hZ. Não foi consertada nem reprocessada — foi **apagada**. Prova:
0 linhas em `generations` para o id, e o débito continua pendurado em `credit_transactions`
(`eddffe5e`, `ref_type=generation`, `amount=-910`, 19:52:46Z). É o padrão já documentado do DELETE
de histórico pelo aluno (apaga a row + o R2 e deixa o ref pendurado — que a ordem de 20/08 registra
como **normal**, não como detector de bug).

**A consequência é que esta série pode CAIR sozinha sem nada ter sido corrigido.** Quem comparar
"eram 4, agora são 3" vai ler recuperação onde houve exclusão. Contar só o que está vivo na tabela
**subnotifica o `#226`**. Por isso reporto **8** (7 vivas + 1 apagada e medida), não 7.

**Controle, agora com 51 entregas — segue impecável:** desde 02/09 02:32Z, régua por linha
(`qa->>'coverage_min'`, 0,85):

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 51 | 30 | **0 (0,0%)** | 0,857 |
| `exhausted > 0` | 55 | 29 | **21 (38,2%)** | **0,333** |

O `exhausted` segue sendo o discriminador, **sem uma exceção em 51**. Série acumulada: **371 de 806
com `exhausted>0` (46,0%), 161 alunos** (era 360/783 e 159 às 20:11Z).

---

## 5. O `#222`: o convite automático manda a instrução ERRADA pra essa gente, e depois cala pra sempre

Achado novo desta ronda, anotado no `#222` por ser a mesma classe (ordem 27/08, §3.1).

1. **Causa, no código:** `frontend/src/lib/payments/orphan-outreach.ts:131-141` monta a guarda
   `hasAccount` **só** com `profiles.email` batendo **igual** ao `buyer_email`
   (`.in("email", chunk)`). Por construção ela **não enxerga conta sob outro e-mail** — que é a
   condição que **define** este `#222`. Quem comprou no e-mail A e tem conta no e-mail B passa pelas
   duas guardas (`:183`, `:184`) e recebe *"falta só criar sua conta"* (`:57`) + 1 lembrete (`:56`).
2. **Por que a instrução é errada:** a própria medição do Frank na ronda 21:40Z (`4743e2b`) concluiu
   que dos 9 alunos com conta candidata **ZERO** deviam receber *"crie sua conta"* — 4 são vínculo
   na mão, 4 são estorno, 1 é falso positivo. O cron manda o oposto do que o caso pede.
3. **Depois do lembrete, ninguém mais fala com ele:** `:196` só dispara se `!record.reminder` — é
   **1 lembrete por comprador, para sempre**, sem escalonamento pra vínculo manual. Caso medido:
   `caplastica@hotmail.com` teve convite em **04/08 01:28Z** e lembrete em **07/08 14:01Z**
   (`agent_state.orphan_invites`); em 03/09 segue `user_id` null, zero profile com esse e-mail, e
   `entitlement` **active de R$97 até 22/09**. São **27 dias sem contato de nenhuma fila**, e nada
   no sistema vai contatá-lo de novo antes do acesso morrer.

**Conferi o §3 antes de não abrir chamado:** (a) *já existe?* sim, é o `#222`; (b) *já foi
corrigido?* **não** — `orphan-outreach.ts` tem 4 commits (`f1ee1b3`, `0d6539c`, `c5f67bd`,
`42e3b68`) e nenhum trata conta sob outro e-mail (o `c5f67bd` corrigiu o teto de 1000 do PostgREST,
o `42e3b68` o cancelado/estornado do `#127`), e **nenhum dos 28 PRs abertos toca este arquivo**;
(c) *dinheiro?* **não afirmo defeito de cobrança** e não mexi em crédito — o que afirmo é falha de
**contato**, com o estado do cron como prova.

⚠️ **Armadilha de instrumento (esta quase me fez reportar mentira).**
`ler_caixa.cjs --enviados --para caplastica@hotmail.com` devolve **0**, e 0 aqui **lê como "nunca
foi contatado"** — é **falso**, ele foi contatado 2× em agosto. A janela de busca da pasta Enviados
não alcança o período. **O registro que vale para os convites do cron é `agent_state.orphan_invites`,
não a pasta Enviados.** Eu ia reportar "um pagante nunca foi contatado" ao Johnny; conferi o estado
do cron antes e o achado virou outro, mais preciso e pior. Quem auditar contato só pela caixa vai
subnotificar todo mundo mais velho que a janela.

**Funil remedido por mim** (`entitlements`, `user_id is null`, preço pelo caminho **direto**
`raw_event->'purchase'->'price'->>'value'` — armadilha registrada na ronda das 20:11Z): 91 órfãs →
45 `active` → 25 na janela vigente → **14 pagantes de verdade**. Era 92/46/26/15 às 20:11Z; a
diferença é o `qooqi`, que o Frank destravou na mão na ronda 21:40Z.

**Os 14 já foram todos contatados** (conferido um a um em `--enviados` + `agent_state`), então a
pergunta *"posso escrever para os 4?"* das rondas anteriores **foi superada pelos fatos**: o Frank
mandou as cartas que faltavam entre 20:40Z e 21:40Z, e `fmgimael`/`malmeida313` haviam recebido o
convite **automático** (não a carta escrita à mão) em 29/08 e 31/08.

---

## 6. O `#234`: `n=0` continua, ~9h40 depois do build

Listei **todas** as gerações da base que carregam qualquer marca de resgate (`coverage_rescue`,
`coverage_rescued`, `coverage_rescue_nivel2`): são **15**, e a mais nova é a `a8caaae1` de hoje
**11:11:08Z** — portanto **1h12 ANTES** do build (12:23:08Z). **Zero** posteriores ao build.

Duas chaves **novas** apareceram na varredura do `qa` (`coverage_rescued` e `coverage_rescue_nivel2`,
`count=1` cada). **Conferi uma a uma em vez de assumir que era sinal novo:** as duas estão na
**mesma `a8caaae1` pré-build**. Não são evidência. (Confirma a armadilha que a ronda das 20:11Z
deixou registrada.)

O limite de instrumento não mudou: *"build success"* **não é** *"imagem nova rodando no endpoint do
RunPod"*, e não existe no banco marcador que distinga imagem velha de nova. O `#234` **não pode** ser
fechado por *"o fix subiu"* enquanto não existir a primeira geração com marca de resgate posterior a
12:23:08Z. **Não ouvi nenhum áudio** e não afirmo que `tail_interno_entregue_n > 0` equivale a
palavra decapitada audível.

---

## 7. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`). **Não toquei em
nenhum não-lido.**

**Não-lidos no INBOX (fila da Fast): 0.** **Nenhum e-mail novo desde as 20:11Z:** o uid mais alto
continua **429** (Vinícius, 03/09 04:10Z), há **18,0h**.

Nenhum aluno cobrando pela segunda vez **sem resposta**, nenhum *"já pedi ajuda várias vezes"* novo,
nenhum *"a equipe vai verificar"* sem card. Os casos da janela (Katia, Alana, Vinícius, Johnathan,
José Ricardo) seguem com o card que as rondas anteriores mapearam. **Não abri chamado para nenhum e
não reescalei nenhum** — repetir o velho como novidade é o que faz a fila mentir.

Registro sem transformar em chamado (é atendimento/decisão, §2 da ordem de 27/08): o **Vinícius**
respondeu em 04:10Z *"prefiro cancelar todos os cursos"*, e a definição que ele espera — se as
compras dos cursos dão direito a crédito dentro do FastCloner — **é da direção e segue sem resposta**.
O card dele (`#240`) foi fechado por humano às 12:39Z.

---

## 8. A ordem de 27/08, medida no prazo que ela mesma marcou (03/09)

A ordem marcou a reavaliação para hoje. Refiz a conta pelo banco:

`select status, count(*) from incidents where reported_by='vigia' and created_at >= '2026-08-27'`
→ **15 `fixed`, 2 `ignored`, 1 `investigating`** = 18.

Mas o recorte literal inclui `#150`/`#151`/`#152` (12:17Z) e `#153` (14:15Z), que são o **"antes"**
que a própria ordem usa como exemplo do problema. **Recorte honesto (só o que nasceu depois,
`#158` em diante): 15 chamados.**

| métrica | alvo | medido | |
|---|---|---|---|
| `ignored` | ≤ 10 % | **1 de 15 = 6,7 %** | ✅ (era 21 %) |
| `categoria='atendimento'` | zero | **0** | ✅ |
| `kind` começando com `processo`/`atendimento` | zero | **0** | ✅ |

**A calibragem funcionou.** O único `ignored` do período é o `#190`, e a ronda das 20:11Z já anotou
nele a objeção de processo: **a nota de fechamento exigida pelo §5 está vazia**, então o mecanismo
de autocorreção da ordem (*"a nota diz qual das três checagens faltou"*) **nunca rodou nenhuma vez**.
A ordem bateu o alvo; a parte dela que ensina o vigia a errar menos continua sem uso.

---

## 9. O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei nem reabri incidente, não
mudei status de nada, não mergeei PR, não apliquei migration, não toquei em não-lido da caixa, não
li/escrevi/classifiquei nada da planilha (ordem de 29/08), e não ouvi nem vi nenhum áudio — o que
depende de ouvido humano continua indo pro grupo.
