# VIGIA — ronda de 25/09/2026, 22hZ

Sensor (regra 14-A): varre, abre e anota. Não fecha, não reabre, não responde
aluno, não mexe em crédito. Calibrado pela ordem de 27/08 (só erro de SISTEMA
vira chamado). Ordem de 29/08 respeitada: **nada da planilha** foi lido,
escrito, classificado ou reprocessado. Canal (ordem de 31/08): o aviso desta
ronda sai **no grupo** (`notify-grupo.sh`), nada no privado do Johnny.

**Janela coberta:** 20:45Z → 22:20Z (1,6h), inteiramente dentro do turno.

**Chamado aberto nesta ronda: 1** (`#585`).
**Anotações em cartão existente: 1** (`#554`, objeção).

**O que esta ronda tem de próprio: eu fui conferir o "0" que eu mesmo venho
publicando toda ronda, e descobri que ele é produzido por um detector que não
consegue ver 13% da frota.** O número continua **certo** hoje — eu medi isso
também, contra a minha própria acusação. O que está errado é a **cobertura**.
Detalhe em §1.1.

---

## 1. Para DECISÃO do Johnny

### 1.1 🟠 O detector de percepção fica cego quando alguém carimba o cartão em lote — e é o MESMO defeito do `#554`, consertado num arquivo só

Vai primeiro porque é **correção contra o meu próprio instrumento**, e porque o
número dele entra no relatório de toda ronda desde 17/09.

A ordem permanente de 17/09 manda, com estas palavras: *"O instrumento canônico
é `percepcao_travada.cjs` — rode ELE a cada ronda, não o SQL"*. Fui ler como ele
decide:

```
percepcao_travada.cjs:206
  const ultima = i.agent_notes[i.agent_notes.length - 1];
  const marca  = marcaDe(ultima?.note);
  if (!marca) continue;
```

Ele lê **`[-1]` cru** e desconta **uma** string de boilerplate (linha 61). Quem
escreve nota de manutenção **em lote** por cima de N cartões empurra o estado
real pra `-2`, e o detector passa a avaliar o carimbo. Carimbo não tem marca de
percepção → `marcaDe` devolve `null` → o cartão é **pulado em silêncio**. Não
entra na conta nem como "olhado e descartado".

**Controle de mutação, na função pura, sem banco — o mesmo cartão duas vezes:**

```
marcaDe(<carimbo de 24/09>)                      = null
marcaDe("Nao enxergo video ... precisa assistir") = "nao enxergo"

travadosDe([cartao com SO o pedido])               -> detectado: 1
travadosDe([cartao com o pedido + carimbo em cima]) -> detectado: 0
```

**1 → 0 com uma nota de manutenção a mais.** Não é inferência, é o detector
rodando.

**O tamanho, por dois caminhos independentes:**

| caminho | nº |
|---|---|
| filtro pelo texto do retrofit de 24/09 (string escrita à mão) | **20** cartões vivos |
| `2026-09-24_nota_em_lote.cjs` — orientado a DADO, sem lista à mão ("mesmo prefixo de 200 chars em ≥3 cartões") | **22** de 164 = **13%** |
| reimplementei a definição (b) por fora | **22** ✔ |

**E o lote não é evento de um dia.** Dos 4 textos em lote que o instrumento
acha, um está **vivo**: `"=== O QUE FAZER === O ALUNO MANDOU OUTRO E-MAIL ..."`,
10 cartões, faixa 16/09 → **25/09 17:10Z — carimbo novo HOJE**. O buraco é
**realimentado**, não cicatriza com o tempo.

**O dano HOJE é ZERO, e eu medi em vez de supor — isto é contra a minha própria
objeção.** Apliquei nos 22 cegos o **mesmo recuo** que o `#554` já pôs no irmão
(andar pra trás enquanto a nota for de lote, teto de 5 pulos) e rodei `marcaDe`
na primeira nota substantiva:

```
cartoes cegos ............................ 22
que revelam pedido de percepcao REAL .....  0
```

As 3 únicas que casavam por palavra-chave crua (`#216`, `#245`, `#406`) dizem
com todas as letras que **a percepção já foi cumprida**, e a regra
`CUMPRIMENTOS` do próprio detector as anula **corretamente**.

**Portanto: o "0 travado em percepção" publicado às 20hZ e agora está CERTO.** O
que este achado afirma é mais estreito e não depende disso: **esse 0 é produzido
por um detector que não enxerga 13% dos cartões vivos.** Hoje ele acerta pelo
conteúdo, não pela cobertura. O próximo pedido de percepção que nascer debaixo
de um carimbo sai da conta em silêncio — e *"0 não pode parecer saúde"* é
exatamente o que a ordem de 17/09 existe pra impedir (a classe chegou a 13
cartões, o mais velho com 16 dias, assim).

**O que isto custa decidir:** nada do Johnny. É conserto de instrumento, tem
dono (`#585`) e a correção proposta é **reaproveitar** a regra que já existe no
arquivo irmão, não escrever uma segunda. Está aqui em §1 porque o número afetado
é o que ele lê toda ronda.

### 1.2 🔴 Nasceu na janela um pedido de cancelamento que a casa NÃO conseguiu executar — e ela já prometeu escrever de volta

`#584`, nascido **22:15:36Z** (1 minuto antes do meu), aberto pelo **próprio
sistema** (`reported_by='app-cancelamento'`), aluno `dionatas@outlook.com`.

A descrição do cartão documenta a dívida sozinha:

> *"Rota `POST /api/v1/subscription/cancel` … Motivo: nenhuma assinatura hotmart
> ativa com subscriber code neste usuário. O aluno recebeu a carta de **'pedido
> recebido'** (NÃO a de 'cancelada'), e a casa **prometeu escrever de novo
> confirmando** — essa promessa é dívida desta fila."*

**Não é meu pra resolver** (14-A: não respondo aluno, não cancelo nada). Está
aqui porque **nasceu com promessa e com relógio**, e porque o mesmo padrão —
promessa escrita ao aluno sem execução atrás — é o que produziu o `#581` do
Rafael na ronda anterior. O cartão já traz o comando da ferramenta
(`cancelar_assinatura.cjs`); quem executa é o Frank.

### 1.3 🟠 Os dois itens de decisão da ronda de 20hZ continuam intactos

Repasse, sem número novo da minha parte — **nenhum dos dois teve movimento na
janela**:

- **A promessa ao Rafael (`rafaelzan@me.com`) vence AMANHÃ** e o cartão `#581`
  segue em `aguardando_aluno`, portanto **fora de toda contagem de aberto**,
  enquanto o próximo passo prometido é da **casa**.
- **`#249` + `#250`: R$ 1.427,60 em dois pagantes parados por um "pode"** —
  `glaubermed@ig.com.br` (endereço inexistente, 550 5.1.1) e
  `andy.silvestre@icloud.com` (caixa cheia, 552 5.2.2, bounce em 95s).
  *A pergunta continua: pode chamar esses dois no WhatsApp?*

---

## 2. O que ABRI e ANOTEI

| ação | cartão | o quê |
|---|---|---|
| **ABRI** | `#585` `02581255` | `vigia:percepcao-travada-cega-em-nota-de-lote` — detector canônico lê `[-1]` cru, cego em 22/164 (13%); controle de mutação 1→0; dano hoje **0**; as 3 checagens do §3 escritas na nota (§1.1) |
| anotei (**objeção**) | `#554` `e693222b` | o fechamento nomeia o defeito no GENÉRICO (`ultimaNota() lê agent_notes[-1]`) e conserta UM consumidor; o outro leitor, que uma ordem permanente manda rodar toda ronda, ficou com o mesmo buraco. **Status não mudado** — segue `fixed` |

**1 chamado, 1 anotação.** Não fechei nem reabri cartão nenhum.

**Por que o `#585` é chamado e não anotação:** passa no teste de bolso da ordem
de 27/08 — *"se o código estivesse certo, isso não teria acontecido?"* Sim: a
regra de recuo **já existe no repositório** e não foi aplicada a este arquivo. É
código nosso, não processo. E a classe irmã (`#554`) já foi chamado quando o
dano era real (4 cartões sumiram da varredura, 3 deles dinheiro de aluno).

---

## 3. Frente 1 — todos os abertos, SEM filtro de assinatura

Consulta sem cláusula de `signature`, como manda a regra (o filtro
`fast-email:%` já fez reportar "0 abertos" existindo quatro, em 19/08).

```
129 abertos = 16 open + 113 investigating      (20hZ: 126 = 14 + 112)
 37 aguardando_aluno   (contados à parte)       (20hZ: 37)
166 vivos no total                              (20hZ: 163)
por categoria (abertos): 84 técnicos + 45 atendimento   (0 com categoria nula)
```

Fecha nos dois eixos: 84 + 45 = 129 = 16 + 113. ✔

**Movimento da janela, conciliado linha por linha:** 126 + `#582` + `#584` +
`#585` = **129**. ✔ (`#583` nasceu e fechou dentro da mesma janela — não soma.)

**Nasceram na janela: 4.** `#582` (Frank, 21:03Z — 3 pagantes sem conta, sinal
que estava em `payment_events.error` e ninguém lia), `#583` (Frank, 21:47Z,
fechado 21:53Z — as 414 guardas do worker não rodavam em CI nenhum), `#584`
(sistema, 22:15Z — §1.2), `#585` (meu, 22:16Z — §1.1).

**Fechados na janela: 1** — `#583`, pelo Frank, nascido e fechado em 6 min com
merge `83dbc12a`. Não é fechamento apressado: o cartão documenta a própria
causa e o PR está na main.

### Cartões duplicados por aluno

```
21  alunos com mais de um cartão vivo   (20hZ: 21)
43  cartões envolvidos                  (20hZ: 43)
22  EXCEDENTES                          (20hZ: 22)
```

**Parado — nenhum dos três se moveu na janela.**

⚠️ **Régua declarada, porque eu errei antes de acertar.** Minha primeira
consulta fez `unnest(affected_emails)` e devolveu **74 alunos / 183 cartões /
109 excedentes** — número que **não** é comparável com o das rondas anteriores.
A diferença não é duplicata nova: é **cartão de CLASSE**, que carrega dezenas de
e-mails e conta cada um como se fosse um cartão daquele aluno. A régua que
reproduz a série é `array_length(affected_emails,1) = 1` (um cartão, um aluno).
Registro os dois números pra ninguém confundir a série trocando a régua no meio.

### Idade dos abertos (régua canônica: `first_seen_at`)

| faixa | nº | 20hZ |
|---|---|---|
| < 24h | 10 | 11 |
| 1–3d | 16 | 15 |
| 3–7d | 25 | 25 |
| 7–14d | 42 | 41 |
| **14d+** | **34** | **34** |

Soma 127 no instante da medição (antes de `#584`/`#585` nascerem). **O 14d+ não
se move há três rondas.**

### Percepção travada (ordem de 17/09)

`percepcao_travada.cjs` — instrumento canônico, não o SQL:

```
controle positivo OK (#310) · controle negativo OK (#518 descontado)
567 incidentes varridos
👁  SO PARAM POR FALTA DE VER/OUVIR/ASSISTIR: 0
>>> 0 card(s) travado(s) em percepcao · mais velho parado ha 0d
```

**0 cartões, mais velho 0d. Nada a despachar pro `olho`/`qa`.**

⚠️ **E este é o número que eu contesto no §1.1** — não no valor, na cobertura:
ele é apurado sobre **142 dos 164** cartões vivos, porque em 22 o detector
avalia carimbo de lote em vez de estado. Recontado com o recuo aplicado à mão,
**continua 0** — mas por conteúdo, não por alcance. **`#585` aberto.**

### Fechados que voltaram a disparar

`2026-08-20_fechados_que_disparam.cjs`:

- **Família A** (disparo *depois* do fechamento): **1** — `acf8acd6` (`#8`,
  *"Treino de voz: áudio insuficiente/sem fala limpa"*, `fixed`), `last_seen`
  **22/08** 19:26Z. **Não dispara há 34 dias.** Igual às rondas anteriores.
  **Não reabri** (14-A).
- **Família B**: 16 fechados em cima do disparo, **1 com sinal nas últimas 48h**
  → `8da87d52` (`#544`). Conferido na ronda das 20hZ: `first_seen_at` =
  `last_seen_at` = `resolved_at`, `occurrences` 1 — **não é reincidência**, e a
  condição do gatilho condicional dele (aluno sem nenhuma voz pronta) **segue
  não satisfeita**. Nada mudou na janela.

⚠️ **Limite declarado do instrumento, mantido:** dos 403 fechados, **185 são
auditáveis** e **217 são CEGOS** (`last_seen_at` congelado desde o insert).
Sobre os cegos o script não diz "nenhum disparou", diz **"não dá pra saber por
aqui"**. E 30 deles carregam `occurrences > 1` **digitado no insert** — número
que nunca mais anda, e são os que ninguém reaudita.

---

## 4. Passo fixo da ronda — reconciliar os envios

```bash
node _frank/ferramentas/2026-09-18_reconciliar_envios_da_pasta.cjs \
     --corte=2026-09-14T14:06:31Z --confirmar
```

```
1313  lidas da pasta "Sent"       (20hZ: 1313 · 0 na janela)
   0  + registro local (#210 — gitignored, não existe nesta máquina)
1313  = TOTAL
1236    já tinham linha
   0    repetidas entre as duas fontes
  77    FORA DA JANELA (decisão: --corte)
   0    RECUSADAS (defeito da carta)
   0    DENTRO DA JANELA — escrituráveis
✔ 1313 = 1313: nenhuma carta sumiu na classificação
🕳️ CARTAS SEM LINHA, DENTRO DA JANELA: 0     → nada a fazer
```

Irmão de leitura, instrumento independente:

```bash
node _frank/ferramentas/2026-09-18_enviados_x_tabela.cjs
>>> VEREDITO: o buraco é PASSIVO. Depois do corte, 0 carta da pasta
    ficou fora da tabela.
```

**Os dois batem: 0.** **Nenhuma carta saiu na janela** (1313 → 1313) — coerente
com uma janela em que a casa não escreveu a aluno nenhum.

⚠️ As **77 cartas** anteriores a 14/09 14:06:31Z seguem sem decisão. Não é
recusa, é o `--corte`. Escriturá-las é decisão de produção (`cobreDesde` em
`contato-tentativas.ts`), não de ronda.

---

## 5. Frente 2 — a caixa

```bash
node _frank/ferramentas/ler_caixa.cjs --ultimos 12 --corpo 700
```

Leitura pura (`BODY.PEEK`, só `SEEN`). **O não-lido segue intacto** — é a fila
da Fast.

**NENHUM e-mail novo na janela.** O último uid segue **766** (bounce do
`andy.silvestre@icloud.com`, 20:33:45Z), exatamente onde a ronda de 20hZ parou.
**Nenhum aluno cobrando pela 2ª vez que já não estivesse reportado.**

**Confiri se a caixa esconde aluno sem cartão** — os 4 remetentes vivos da
leitura já têm ficha, nenhum chamado novo a abrir (checagem 1 do §3):

| aluno | cartões |
|---|---|
| `irleygurgel@gmail.com` | `#573` (fixed), `#574`, `#575` |
| `marcelo.branquinho@tisafe.com` | `#545` (open), `#549` (fixed) |
| `combofav@gmail.com` | `#556` (open) |
| `walsicleia_kaka@hotmail.com` | `#430`, `#525` (ambos open) |

**Fui conferir o fechamento do `#573` em vez de aceitar o rótulo**, porque o
aluno escreveu **três vezes** na mesma manhã e o cartão foi fechado às 17:06Z. A
nota de fechamento prova o conserto **pelo conteúdo do SHA deployado**
(`ff3f5233`, guarda `emailJaTemConta` na frente do `setStep("otp")`), e o defeito
descrito é *"quem JÁ TEM conta e clica Criar conta cai numa tela que promete um
código que o sistema não envia"*. **O Irley escreveu que NÃO tinha cadastro** —
então fui ver se o fechamento cobria o caso dele:

```
profiles · irleygurgel@gmail.com · criado 10/09 11:57Z
          · last_seen_at 25/09 21:54:27Z   ← dentro desta janela
```

**Ele TEM conta desde 10/09** — o fechamento cobre o caso dele, e não o
contrário. O `last_seen_at` do perfil andou às **21:54Z**, depois do deploy.
**Não afirmo que ele entrou**: afirmo que a conta registrou presença após o
conserto. **Sem objeção ao `#573`.**

**Pedidos de cancelamento/reembolso ainda sem resposta** (atendimento, **não**
chamado — ordem de 27/08), com o relógio corrigido: Danilo Paganucci (uid 752,
24/09 16:58Z, cita CDC e CPF — **29,4h**), combofav (uid 759, 13:30Z — **8,8h**),
Graziela (uid 751, 24/09 15:22Z). Magda Reis (uid 756) segue sem cartão e **não
deve ter** — é pré-venda.

---

## 6. Checagem 2 do §3 (ordem de 27/08) — "já foi corrigido?"

`git log origin/main` desde 20:45Z: **4 commits**, todos das rondas do Frank —
`ba85b7eb` e `70a0dd4a` (~21hZ), `e3c9a9bf` + merge `83dbc12a` (CI do worker,
`#583`) e `0e0e5e32` (prova das 22hZ).

`git log origin/main -- percepcao_travada.cjs`: os 5 commits mais recentes são de
17–23/09 e **todos anteriores** ao carimbo de 24/09 que criou a condição do
`#585`. Nenhum toca nota de lote.

`gh pr list --state open`: **30+ PRs**. O vizinho do `#585` é o **PR #431**
(*"#554: varredura da fila do Johnny pula nota de manutenção"*) e ele **não
cobre** o arquivo — conferi a lista: `2026-09-22_esperando_johnny.cjs`, o teste e
o backfill. **Nenhum PR aberto mexe no `percepcao_travada.cjs`.**

⚠️ **Achado de quebra, registrado porque foi medido aqui:** o head do **PR #431
NÃO é ancestral da `origin/main`**, e a regra `NOTA_NEUTRA` entrou na main por
**outro** commit (`03e8c85f`, ronda 24/09 22hZ). Ou seja o #431 é **mais um
branch concorrente de um conserto que já está no ar** — a mesma família que o
README documenta 7 vezes (`feat/onedrive-401`, `feat/fix-image-upload-retry`, as
2 da cura de referência, `fix/trava-foto-nova-8379549c`,
`fix/ritmo-da-referencia-porta-73a60bb`, `fix/estorno-treino-por-saldo-pendente`).
**Não mergear**; se for descartar de vez, apagar no origin. Não abri chamado por
isso: é decisão de branch, não defeito de sistema (ordem de 27/08).

---

## 7. O que eu NÃO fiz (14-A)

Não fechei cartão. Não reabri cartão — inclusive o `#554`, onde deixei **objeção
ao escopo** e mantive o `fixed`. Não respondi aluno. Não escrevi rascunho de
carta. Não gerei link de acesso. Não mexi em crédito, saldo ou acesso. Não
toquei em e-mail não lido. **Não mexi no `percepcao_travada.cjs`** — medi o
defeito, abri o `#585` e deixei o conserto com o dono. Não li, escrevi,
classifiquei nem reprocessei nada vindo da planilha (ordem de 29/08).
