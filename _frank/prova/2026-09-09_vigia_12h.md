# Ronda do Vigia — 09/09/2026, 12hZ (09h BRT)

Sensor. Varri, medi, **abri 1** (`#320`) e **anotei 1**. Não respondi aluno, não
escrevi rascunho, não mexi em crédito, não fechei e não reabri nada (14-A).

Repo em `main`, `pull --ff-only` (já estava em dia). `_frank/ordens/README.md` e
`_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.
Ordem de **29/08** respeitada: nada da planilha lido, escrito, classificado,
avisado ou reprocessado; nenhum chamado de causa-planilha aberto ou reaberto.
Ordem de **canal de 31/08**: aviso desta ronda vai **no grupo**.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 A decisão dos 15 vitalícios (`#313`) faz **15,5h** parada — quarta ronda pedindo

Nada mudou. O `#313` (aberto pelo Frank em 08/09 20:51Z, 2 notas) espera o Johnny
dizer se **honra ou revoga** os 15 entitlements vitalícios de produto de curso —
12 pessoas, 1 já com a plataforma viva até 2030 sem nunca ter comprado. Enquanto
não houver resposta, os 19 alunos do `#312` não podem ser tratados.

Custo já visível e não novo: dos que escreveram, **um pediu reembolso integral**
(`victor.inscriptio@gmail.com`) e **outro acha que já entregou o material e está
esperando calado** (Edésio, `#315`, 11,5h).

**Pergunta em 1 linha:** honra ou revoga os 15 vitalícios — e alguém procura
ativamente a coorte de 04-05/09 agora, ou espera?

### 2. 🟡 O `#320` que abri hoje pede uma decisão de ORDEM, não de código

O conserto do `#320` é do Frank. Mas a medição levantou uma coisa que é sua:
**dois PRs abertos (#41 e #42) escrevem de novo o mesmo defeito** noutro caminho
de leitura. Se um deles for mergeado antes do conserto, a correção nasce já
contornada — é o mesmo risco de branch concorrente que o `feat/onedrive-401` e o
`feat/fix-image-upload-retry` já criaram e que o índice de ordens registra em duas
linhas separadas. **Decisão:** os #41/#42 ficam segurados até o `#320` fechar, ou
alguém reescreve os dois em cima do conserto? Não mergeio nem fecho PR (14-A).

---

## O que eu ABRI e ANOTEI

| | onde | o quê |
|---|---|---|
| **abri** | **`#320`** `8135ff66` | `vigia:mailtext-guarda-mojibake-tudo-ou-nada` — técnico, `investigating`, 25 ocorrências, 8 alunos nomeados. Detalhe abaixo. |
| **anotei** | **`#261`** `ab485826` | nota 4: a **ressalva** que ficou por provar naquele card foi provada agora — e achou defeito real no `mailText`, em outras linhas. **Não reabri.** `agent_notes` 3 → 4. |

### O `#320`, e por que ele passou no teste de bolso

`frontend/src/lib/agent/mail-respond.ts:86-91`, no fim do `mailText()`:

```js
// Bytes UTF-8 lidos como latin1 → reconverte se sair limpo.
try {
  const round = Buffer.from(text, "latin1").toString("utf8");
  if (!/�/.test(round)) text = round;      // <-- TUDO OU NADA
} catch { /* mantém */ }
```

A guarda está **certa na intenção e errada no escopo**. Ela converte o texto
**inteiro** — que inclui a **citação** do e-mail anterior, quase sempre maior que
a mensagem nova — e só aceita o resultado se **nenhum** U+FFFD aparecer em lugar
nenhum. Um byte indecodificável na citação recusa **tudo** e devolve o mojibake
integral, inclusive a frase do aluno, que isolada converteria perfeitamente.

O byte que mais aparece é o `0xA0` (NBSP solto em latin1) que clientes de e-mail
portugueses emitem no `às` da linha de citação. Ele **não é do aluno** e **não é
do nosso texto**, e é ele que decide o que o cérebro da Fast lê.

**Como eu medi — execução, não leitura de código no olho.** Duas ferramentas
novas, commitadas junto, ambas leitura pura (`EXAMINE` + `BODY.PEEK[]`, nunca
`BODY[]`; imprimem FLAGS/fila antes e depois e acusam se mudarem):

| ferramenta | o quê |
|---|---|
| `_frank/ferramentas/2026-09-09_dump_mime_cru.cjs` | MIME cru + `BODYSTRUCTURE` de um uid |
| `_frank/ferramentas/2026-09-09_medir_mojibake_na_caixa.cjs` | roda o `mailText` **de produção** (importado via `jiti`, **não** um porte) sobre os N e-mails mais recentes e classifica |

Isso fecha a ressalva que eu mesmo tinha deixado escrita no `#261`: *"NÃO li o
MIME cru … quem pegar o card deve começar dumpando o BODYSTRUCTURE antes de mexer
no mailText"*. Desta vez a fonte veio **antes** do chamado.

**Reach medido em produção** (60 e-mails mais recentes do INBOX, com a função de
produção):

| | |
|---|---|
| **CORROMPIDO** — a Fast leu mojibake **e** dava pra converter limpo | **25** |
| corrompido-total (mojibake por outra causa) | 1 |
| ok (acento correto) | 16 |
| sem acento (nada a decidir) | 18 |

Dos **42** e-mails que tinham acento, **26 chegaram corrompidos** ao cérebro da
Fast e **25 eram recuperáveis pela própria guarda que já existe**. Não é cauda: é
a maioria de quem escreve em português com acento.

**A desproporção, que é o coração do defeito** — quanto de lixo condenou o texto
inteiro em cada caso:

| uid | aluno | U+FFFD / chars | % |
|---|---|---|---|
| 503 | duartesoaresconsultor@ | 2 / 2908 | 0,069 |
| 480 | elaneyani@ | 1 / 3001 | 0,033 |
| 477 | contatoecocannabis@ | 1 / 2808 | 0,036 |
| 482 | emanuelfmguerreiro@ | 2 / 2983 | 0,067 |
| 492 | victor.inscriptio@ | 1 / 1381 | 0,072 |
| 498 | elaneckis@ | 1 / 842 | 0,119 |
| 484 | simonelealandrade88@ | 1 / 496 | 0,202 |

Um quinto de um por cento do texto, no pedaço que o aluno **nem escreveu**,
apagando a acentuação da queixa inteira.

**O caso vivo com dano observado.** Duarte Soares, comprador em Portugal,
**pagante**:

- **11:21Z** (uid 503) ele escreve, em `text/plain` UTF-8 quoted-printable,
  3357 bytes (`BODYSTRUCTURE` conferido): **"Não consigo pagar"**.
  O `mailText` de produção entregou à Fast:
  `"NÃ£o consigo pagar Fast - FastCloner … escreveu (quarta, 9/09/2026 Ã (s) 10:50): …"`
  Contraprova no mesmo script: convertendo **só a fala do aluno** (os 18 chars
  antes da citação) sai `"Não consigo pagar"` com **zero** U+FFFD. Os 2 U+FFFD
  estão **ambos na citação**, o primeiro no índice 89, no `Ã (s)`.
- **11:25Z** a Fast responde: *"não consegui ver o conteúdo … o corpo do e-mail
  parece estar vazio ou com problema de codificação … pode reenviar a mensagem?"*
  O modelo diagnosticou o **sintoma certo** e devolveu o problema pro aluno.
- **11:29Z** (uid 504) ele: *"Não consegui pagar direto no hotmart … Vejam se
  conseguem resolver **caso contrário cancelo subscrição**."*

O e-mail dele não tinha problema nenhum. Quem não conseguiu ler foi o nosso
código, e o aluno é que foi mandado repetir.

**As três checagens da ordem de 27/08 §3, feitas antes de abrir:**

1. **Já existe?** Base **inteira** (aberta E fechada, 307 incidentes) varrida por
   `mojibake|acento|codific|utf|latin|mailtext|mail-respond|charset` em
   `signature`, `title` e `kind`: **zero**. Os parentes conhecidos são de outra
   causa: o `#261` é a escolha plain-vs-html quando o plain vem **vazio**
   (`ignored`, refutado na fonte); o `#248` é o **ler_caixa.cjs** cego a html puro
   (`ignored`, ferramenta minha); o `#259` é trava por Message-ID. Nenhum toca as
   linhas 86-91.
2. **Já foi corrigido?** `git log origin/main` em `mail-respond.ts` (8 commits,
   nenhum toca a guarda) + os **29 PRs abertos**. Os três que mexem em leitura de
   e-mail, lidos linha a linha: o **#188** mexe no `ler_caixa.cjs` e nos testes,
   não na guarda; o **#41** e o **#42** tratam o teto de 2MB e **repetem** o
   padrão (`/�/.test(utf8) ? latin1 : utf8`) em vez de corrigi-lo. Nenhum cura.
3. **Dinheiro?** **Não se aplica**, e digo de propósito: o `#320` não acusa
   cobrança indevida, débito errado nem estorno faltando; **nenhum `ref_id` está
   sendo contestado e nada deve ser estornado por ele**. É a armadilha que
   produziu o `#100`, o `#125` e o `#152`. Ainda assim há `arquivo:linha` e
   medição com a função de produção.

Teste de bolso: *se a guarda decidisse por TRECHO em vez de pelo texto inteiro —
ou tolerasse uma fração de U+FFFD, ou usasse o charset declarado no próprio
`BODYSTRUCTURE` (o uid 503 declara `charset UTF-8`, a informação estava lá e não
foi usada) — a Fast teria lido "Não consigo pagar" e respondido a pergunta.*
**Sim** → chamado.

**O que o `#320` deliberadamente NÃO afirma:** que a resposta da Fast foi ruim
(dado o texto corrompido que ela recebeu, foi razoável — o defeito está a
montante); que os 25 viraram 25 alunos prejudicados (medi que o **cérebro leu
texto degradado** em 25 mensagens; **não** medi quantas respostas ficaram piores,
e não vou afirmar). O único dano com consequência **observada** é o uid 503.

---

## FRENTE 1 — a fila inteira, sem filtro de assinatura

Consulta **sem** cláusula de `signature` (armadilha de 19/08).

**47 abertos**: **25 técnicos** e **22 de atendimento**. Eram **48** às 10hZ.
Todos em `investigating`; `status='open'` = **0** na base inteira.

A diferença, conferida uma a uma:

| | |
|---|---|
| **−2** fechados pelo Frank | `#319` `3ad6b6da` → `fixed` 11:47Z; `#318` `8a37c48e` → `fixed` 11:43Z |
| **+1** aberto por mim nesta ronda | `#320` (12hZ) |

**Os dois fechamentos do Frank eu confiro, não herdo:**

- **`#319`** — era o card que **eu** abri às 10hZ. Corrigido de verdade: PR #218
  mergeado em `ecf58d9`, commit do fix `0c47455`. **Concordo com o fechamento.**
- **`#318`** — Duarte. O Frank fechou dizendo que o aluno pagou por multibanco e a
  conta reativou sozinha. **Conferido por mim em `aluno.cjs`, não no relato:**
  acesso **ATIVO até 30/09**, **+100.000** créditos em 09/09 11:38Z
  (`subscription_grant`), saldo 100.261. **Fechamento correto.** É o caso que o
  `#320` usa como prova de dano — o defeito de leitura existiu e continua, mas o
  aluno **não** ficou trancado.

Alunos na fila: **138** somando com repetição, **110 distintos**.

Idade: **4** com mais de 7 dias — `#312` e `#313` (91,8 d de `first_seen`),
`#15` (41,0 d) e `#226` (7,8 d).

Os maiores por aluno atingido:

| chamado | desde | alunos | ocorrências |
|---|---|---|---|
| `c726c5ae` `vigia:convite-orfao-so-olha-produto-7851642` | 09/06 | 19 | 19 |
| `d3d8d1b2` `generation:capacity:executiontimeout` | **30/07** (41,0 d) | 18 | 19 |
| `f1ada07e` `assinatura_paga_em_dobro_duplicada_ativa` | 04/09 | 15 | 5 |
| `2d0509b4` `frank:entitlement-vitalicio-de-produto-de-curso` | 08/09 | 12 | 12 |
| `f8587cef` `tts:palavra_decapitada_fronteira_interna` | 02/09 | 10 | **609** |
| `8135ff66` `vigia:mailtext-guarda-mojibake-tudo-ou-nada` | **hoje** | 8 | 25 |
| `446c3ae4` `sgp-boas-vindas-nega-assinatura` | 06/09 | 8 | 15 |

### Fechados que voltaram a disparar: **0**

Teste certo (`last_seen_at > resolved_at`, não "fechado com `last_seen` recente").
Na base inteira sai só o **`#8`** (`acf8acd6`, `training:user_dataset`, `fixed` em
09/08, **último disparo 22/08** — parado há **17,7 dias**). Anotação, não motivo
para reabrir (14-A).

### Aguardando aluno: 12 — **os mesmos 12**, sem movimento

**8 passados de 7 dias:** `#99` (11,5 d), `#206` (9,0 d), `#207` (8,9 d),
`#214` (8,6 d), `#216` (8,0 d), `#224` (7,8 d), `#172` (7,8 d), `#229` (7,6 d).
Parado 7 d+ sem resposta pede **segunda tentativa**, não silêncio. Anoto; **quem
escreve é o Frank**.

## FRENTE 2 — a caixa, pra pegar o que não virou incidente

`ler_caixa.cjs --ultimos 25 --corpo 2500` — leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em não-lido.**

**Fila da Fast: 0 não-lidos** (`--fila`), conferido **duas** vezes: pelo
`ler_caixa` e de novo pelo script de medição, que imprime a contagem antes e
depois do trabalho dele (0 → 0, `PEEK provado`). Como o recorte `SEEN` cobre a
caixa inteira, "nada encontrado" aqui é **conclusivo**, não é ponto cego.

**3 mensagens novas desde as 10hZ** (uid 503, 504, 505) — **todas do Duarte**, e
todas já cobertas:

| uid | o quê | destino |
|---|---|---|
| 503 | *"Não consigo pagar"* → a Fast disse que o corpo estava vazio | **`#320`** aberto por mim |
| 504 | *"não consegui pagar direto no hotmart … caso contrário cancelo"* | idem `#320` |
| 505 | *"já consegui fazer pagamento"* + comprovativo | `#318`, **fechado e conferido** |

**Aluno cobrando pela segunda vez: 1** (Duarte) — **coberto, resolvido e
conferido**, nenhum órfão. Nenhum *"a equipe vai verificar"* sem dono. Nenhum
e-mail novo de outro aluno.

Antes de concluir "está tudo bem", conferi que **não é o cron morto**: **7
gerações nas últimas 8h, 7 `ready`, 0 `failed`, 0 presas**.

## Varredura e números de sempre

- **Pagante trancado: 0.** 0 sem prova, **31 na fronteira** (todos com acesso até
  hoje 09/09 e próxima cobrança hoje — é a virada do ciclo, não trava). Trancar
  está certo em 16 cancelados, 158 inadimplentes e 3 trials que nunca viraram
  pagamento.
- **⚠️ ANTI-ALARME, repetido de propósito:** o `estorno_confere.cjs` segue
  imprimindo **"SEM ESTORNO CASADO: 4"** — `dd4b98a3`, `db811e2f`, `678267fe`,
  `e0de4212`. **Não é falha de estorno**, e reconferi hoje em vez de repetir de
  memória: consulta direta a `credit_transactions` por esses 4 `ref_id` volta
  **vazia** — zero linhas, ou seja, nunca foram **debitados**; sem débito não há
  o que estornar. Um deles é `johnny.oliveirasp@gmail.com`, conta da casa. São de
  20 e 24/08 e não se mexem desde então.
- **Lista de estorno em dia:** nenhum estorno faltando além dos 4 acima.
- **Cenas do estúdio (48 h):** 37 `ready`, **3 `failed`** — as 3 com `debit_ref`
  **NULO** (moderação bloqueia **antes** de cobrar). Sem débito, não há estorno a
  cobrar: padrão exato do `#152`. **Não é chamado.**
- **1 `training_jobs` `queued`** obsoleto, o mesmo de sempre (job `ebf5cc56`, voz
  `f4b9b0f2` já `ready`, de 25/08).
- **`git log origin/main` desde as 10hZ: 4 commits** — `1cd2d56` (minha ronda das
  10hZ), `0c47455` + `ecf58d9` (o fix e o merge do `#319`), `f4e6b25` e `e6de402`
  (logs das rondas 11h30/12h do Frank). O Executor está **vivo e trabalhando** —
  o `#319` que abri às 10hZ estava em produção 1h30 depois. `gh pr list`: **29**
  PRs abertos (eram 28 às 10hZ; entrou o #219).
- **Árvore de trabalho limpa** antes de começar (`git status --porcelain` vazio).

## O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito ou
acesso, não editei `admin_emails`, não criei conta pra ninguém, não fechei
chamado, não reabri chamado, não mergeei nem fechei PR, não escrevi código de
produção, não toquei em e-mail não-lido, não gastei GPU nem whisper.

Escritas da ronda: **1 chamado aberto** (`#320`, via script com ensaio + busca de
duplicata sobre a base inteira antes), **1 nota** (`#261`, via
`anotar_incidente.cjs --confirmar`, que concatena e não sobrescreve), **1 aviso no
grupo** e **4 arquivos no git** (este log, as 2 ferramentas de medição e o script
de abertura).

## Escopo respeitado

Ordem de **29/08**: nada da planilha lido, escrito, classificado, avisado ou
reprocessado; nenhum chamado de causa-planilha aberto ou reaberto — tudo que esta
ronda tocou é a caixa do `suporte@`, o `mail-respond.ts`, `incidents`,
`credit_transactions`, `studio_scenes` e a Hotmart viva, todos em produção. Ordem
de **canal de 31/08**: aviso da ronda postado **no grupo**
(`notify-grupo.sh`) — nada no privado do Johnny.
