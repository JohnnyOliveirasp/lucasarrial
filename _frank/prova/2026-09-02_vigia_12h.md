# Vigia — ronda de 02/09/2026, 12hZ (09h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri 0 chamados e anotei em 5 (#232 duas
vezes, #233, #173, #226, #47, #99). Não fechei, não reabri, não mudei status de nada,
não respondi aluno, não escrevi rascunho, não toquei em crédito.**
Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado nesta ronda.
Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem do Johnny de 31/08.
Nada foi mandado pro privado.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 Johnathan Pires — restam ~12 horas e nada se mexeu nas últimas 2

Chamado `954ca6c9` (#173), `investigating`. **Zero movimento desde as 10:18Z**: a última
nota do chamado continua sendo a minha, e não há e-mail novo dele nem e-mail nosso
**para** ele na caixa (uids 396–420; os dois novos desde as 10hZ são a Alana e o Sidney).

O último contato nosso segue sendo o de 01/09 20:44 BRT — o que admitiu o **terceiro**
prazo descumprido. A última palavra dele segue a do uid 417: *"Vocês são bandidos
profissionais, já estou buscando as providências cabíveis. Aguardem!"*

**O relógio:** a garantia das três compras aprovadas de 27/08 (**R$ 2.391,00**) vence
**hoje, 02/09, às 21h BRT**. São 09h BRT. **Restam ~12 horas.** O único contato agendado
está marcado para **21h BRT — a mesma hora do vencimento**, folga zero. Se atrasar como
os três anteriores, ele perde o cancelamento automático por causa da nossa demora.

**A pergunta parada desde 27/08, causa raiz de tudo:** as três compras dão direito ao
processamento da voz dentro do FastCloner? É decisão do Johnny. Não é bug, então **não
abri chamado** (ordem de 27/08). É o item mais caro da mesa pela terceira ronda seguida,
e agora com prazo de horas.

### 0.2 A migration 102 está escrita, não aplicada, e é ela que fecha o buraco — não o merge

O PR **#150** (aberto hoje 12:00:35Z) conserta o chamado que eu abri às 10:17Z (#232) e
traz **duas peças**, só uma delas é código:

1. **código** — `frontend/src/lib/incidents/closure.ts` + os 5 caminhos que fechavam ou
   reabriam por conta própria (`reportar.ts`, `entregar.ts`, `espera.ts`, as duas rotas
   de API) + `mail-bounce-registro.ts`. Merge normal.
2. **`scripts/102_incidents_resolved_guard.sql`** — trava no banco, marcada **NÃO
   APLICADA, esperando aval do Johnny** (regra dura: migration não sobe sozinha).

O cabeçalho da própria migration justifica por que a peça 2 é a que importa, e eu li:
esta família já foi consertada **seis vezes** por chamador, e **77 arquivos escrevem em
`incidents`**, ~70 deles scripts ad-hoc de `_Bugs/` e `_frank/` que nunca vão importar
helper de TypeScript. **Mergear só o código protege 7 dos 77.**

Confirmação independente que eu medi: a própria ferramenta que uso pra anotar,
`_frank/ferramentas/anotar_incidente.cjs:163`, faz `if (commit) patch.resolved_commit =
commit` **sem olhar o status** — grava commit de fechamento em chamado aberto. Ela é um
dos ~70 e o PR #150 **não a toca** (lista de arquivos: só `frontend/src/…` e o `.sql`).

**A pergunta em uma linha: aplica a migration 102?**

---

## 1. O que eu ABRI — nada, e o motivo está medido

Duas coisas que em outra ronda teriam virado chamado e **não viraram**:

### 1.1 `training_job` preso em `queued` há 8 dias — medido e descartado

`ebf5cc56` (voice `f4b9b0f2`, `draellenca@hotmail.com`), `queued` desde **25/08 01:33**,
com `runpod_job_id` gravado, `started_at` nulo e `error_message` nulo.

**Por que não virou chamado:** a voz dele — *"Ellen 3"* — está **`ready`**. A aluna tem
**3 vozes prontas**. E a classe é de **1 linha em 1.068** (`completed` 997, `failed` 70,
`queued` 1 — 0,09 %), única em toda a história da tabela, com **0 alunos prejudicados**.
Linha velha órfã, não defeito vivo. Fica registrado aqui e não gasta chamado
(ordem de 27/08: chamado mede saúde do **sistema**, e 1 linha morta não move essa agulha).

### 1.2 Vídeo "travado nas imagens" no chat do app — não estava travado

Chat de 01/09 23:16, projeto `a83cbf98`: *"etapa travada, só imagens, não avança para as
cenas"*, e a Fast respondeu *"isso normalmente é falha técnica"*. **Fui olhar antes de
abrir:** `video_projects.a83cbf98` está **`status = done`**, com `final_video_path`
gravado e `error_message` nulo. O vídeo terminou. **Sem chamado.**

---

## 2. #233 — o conserto já estava escrito há 8 dias

Chamado aberto hoje às 10:54Z pela rotina das falhas: `fabricar_referencia.cjs` só
enxerga fronteira de frase que coincide com fronteira de segmento do whisper, e por isso
devolve **0 candidatas** em gravação longa.

**O diagnóstico está certo — não contesto uma linha.** O que registro é a **checagem 2 da
ordem de 27/08** (*"Já foi corrigido? `gh pr list --state open`"*), que não aparece na
abertura:

> **PR #54** (`fix/referencia-fronteira-de-frase-por-palavra`), aberto em **25/08
> 17:54Z**, ainda **OPEN**, toca exatamente `_frank/ferramentas/fabricar_referencia.cjs`.
> O corpo dele traz a **mesma medição** que está na descrição do #233: gravação de
> **2979 s**, **366 segmentos**, **12 (3,3 %)** terminando em pontuação → 0 candidatas.
> Números idênticos, mesmo arquivo, mesmo caso (Katia).

**Consequência prática:** o que falta neste chamado não é investigar nem escrever código,
é **decidir o merge do PR #54**. Tratar como bug novo faria alguém reescrever em 02/09 um
conserto que existe desde 25/08. São **26 PRs abertos**; um deles era a resposta.

---

## 3. #232 — eu medi errado o tamanho do meu próprio achado

Abri o chamado citando os três campos (`resolved_at`, `resolved_by`, `resolved_commit`)
mas medi o estrago **só pelo `resolved_at`** e concluí *"1 linha"*. **Errado.** Medição
correta, nos 21 não-fechados:

| campo | preenchido em chamado não-fechado |
|---|---|
| `resolved_at` | **0 de 21** |
| `resolved_by` | **0 de 21** |
| `resolved_commit` | **4 de 21** |

Os quatro: **#171** (`c7e07ab`), **#192** (`01d9cb7`), **#202** (`5b8afad`), **#226**
(`e4cc692`). No #226 o commit carimbado é o **merge do manual de enquadramento / menu
mobile**, que não tem relação nenhuma com QA de áudio esgotado.

**A causa não é a que eu apontei.** O cabeçalho da migration 102 já nomeia estas mesmas 4
linhas: *"a cópia inline em `entregar.ts` saiu com 2 dos 3 campos (esqueceu
`resolved_commit`) e deixou 4 incidentes vivos se contradizendo"*. Meu diagnóstico da
**classe** estava certo; meu **dedo no arquivo** estava incompleto — era `entregar.ts`,
não `reportar.ts`.

Registro isso contra mim porque é a segunda ronda seguida em que a minha consulta é que
erra, não o sistema (ontem foi o nome do campo `exhausted_score`).

**Ponto a favor do diagnóstico, medido agora:** o `ce6e157d` (#47) saiu de `open` para
`aguardando_aluno` às 10:53Z **pelo caminho manual** e ficou com `resolved_at` **nulo** —
ou seja, a guarda do caminho manual funciona, e a do automático é que não existia.

---

## 4. #226 — quarta ronda seguida sem cair

Mesma consulta das anteriores (`status='ready'`, `qa` não nulo, **sem** cláusula de
assinatura):

| medida | 00hZ | 10hZ | **12hZ** |
|---|---|---|---|
| entregues com `qa.exhausted > 0` | 300/686 (43,7 %) | 307/695 (44,2 %) | **307/696 (44,1 %)** |
| alunos distintos atingidos | 136 | 138 | **138** |
| últimas 24 h | 32/66 (48,5 %) | 32/65 (49,2 %) | **30/63 (47,6 %)** |

**Não leio a janela curta como melhora:** foram +1 entrega medida em 2 h, volume que não
se distingue de ruído. O que a série de quatro rondas sustenta é que o **patamar não se
moveu**: perto de 44 % no acumulado, perto de metade nas últimas 24 h.

Segue valendo o que já estava medido: o **#147 MEDE** (telemetria confirmada na GPU às
02:32Z pelo `exhausted_score_max`) e **não segura**. Nenhuma das 307 entregas teria sido
impedida por ele. **O #226 segue sem conserto que impeça a entrega.**

---

## 5. Placar honesto

Consulta **sem cláusula de `signature` e sem cláusula de `status`**:

| status | 00hZ | 10hZ | **12hZ** |
|---|---|---|---|
| fixed | 160 | 161 | **162** |
| ignored | 42 | 42 | **42** |
| aguardando_aluno | 8 | 12 | **12** |
| investigating | 11 | 7 | **9** |
| open | 0 | 1 | **0** |

**Não-fechados: 21** (era 20). **12 técnicos, 9 de atendimento.**
Por quem abriu: **3 frank** + **1 meu** (#232) + **17 nascidos de aluno** (10 `fast-help`,
7 `fast`).
Mais velho da fila: **`ce6e157d` (#47), de 19/08 — 14 dias**.

**Fila em movimento durante a própria ronda, e digo porque muda a leitura:** às 12:1xZ eu
medi `open` 2 (#220 e #233) e `investigating` 7; ao fim da ronda estavam `open` 0 e
`investigating` 9. Ninguém fechou nada — os dois foram assumidos. O `open: 0` de agora
significa "tudo tem dono", **não** "nada aberto".

**#220 voltou para `open` às 10:55Z sozinho**, por reincidência: a Alana respondeu, e a
resposta é **boa** (*"hoje à noite eu tento novamente"*). O conserto do menu mobile foi
avisado a ela e ela confirmou que o cancelamento que quer é **dos dois cursos do Lucas**,
já encaminhado. Não é bug novo.

**A objeção das últimas seis rondas continua:** contando como a rotina manda
(`open` + `investigating`) daria **9**. Os **12** em `aguardando_aluno` seguem sem dono e
fora de todo número que o Johnny vê — e continuam sendo **maioria** da fila não-fechada.

---

## 6. Fechados que voltaram a disparar

**1 de 204**, e é o conhecido: `acf8acd6` (#8), último disparo em **22/08** — **0 com
ocorrência nas últimas 48 h**. `resolved_at is null` entre os fechados: **0 de 204**.

Mantenho as duas objeções já registradas: este número é **cego** para a classe *"promessa
sem dono"* (sem assinatura, sem detector), e **chamado que se reabre some deste
contador** — o #220 disparou às 10:55Z e não aparece aqui porque deixou de ser "fechado".

---

## 7. Frente 2 — caixa, chat e WhatsApp

**Caixa (25 últimos, `BODY.PEEK`, só `SEEN`, nenhum não-lido tocado).** Duas mensagens
novas desde as 10hZ, nenhuma delas queixa:

- **uid 419** — Alana, resposta cordial (§5). Reabriu o #220 automaticamente.
- **uid 420** — envio **nosso** para o Sidney respondendo o limite de áudio.

**Nenhum aluno cobrando pela segunda vez ficou sem chamado.** Katia → #47, Johnathan →
#173, Márcio → #212, Paulo Martin → #229, Alana → #220.

**Chat do app: 16 mensagens, 2 alunos.** Um é o vídeo que não estava travado (§1.2); o
outro é o Sidney, respondido pelo uid 420. **Nenhum chamado novo.**

**WhatsApp (`agent_messages`): 0 mensagens em 14 h.**

### Uma coisa que a Fast respondeu certo, e eu confiro em vez de assumir

O uid 420 afirma ao aluno que **existe um máximo de 60 minutos** por treino. Conferi no
código antes de dar por bom: `voice-creator.tsx:18` `MIN_DURATION_SECONDS = 20*60`,
`:21` `MAX_DURATION_SECONDS = 60*60`, `:216` `overMaximum` (regra pura em
`lib/audio/medicao.ts:89`) e `:619` a mensagem `errors.overMax`. **O número está certo, a
tela avisa e o botão não libera.** Conferi também o outro fato do mesmo e-mail: a voz
*"Sidney Santos - Master"* (`a891c988`) existe e está **`ready`** desde 10:30Z. O e-mail
não prometeu o que não aconteceu.

Isso **responde a dúvida técnica do #99** (áudio de 30+ min está dentro do limite).

**Processo, 1 linha (ordem de 27/08 — não vira chamado):** o **máximo continua fora do
manual da Fast**. Quem respondeu hoje foi uma pessoa/agente, não ela. Enquanto a linha não
entrar no manual, o próximo aluno reabre a mesma dúvida — é a terceira ronda que registro
isto.

---

## 8. Saúde

**Gerações nas últimas 6 h: 5, todas `ready`.** Últimas 24 h: **77, todas `ready`** — 0
`failed`, 0 `pending`, 0 travada há mais de 1 h.
**Vozes nas últimas 24 h: 15 `ready`**, 0 falha. **`training_jobs` 24 h: 15 `completed`**,
0 falha. **`render_jobs` 24 h: 3 `done`.**
Único não-terminal em toda a tabela de treinos é o `ebf5cc56` de 25/08 (§1.1).

---

## 9. O que eu NÃO fiz

Não fechei, não reabri, não mudei status de nada. Não escrevi resposta nem rascunho para
aluno. Não mexi em crédito, não estornei. Não mergeei PR nenhum e não apliquei migration
nenhuma. Não toquei em e-mail não-lido. Não abri, li ou reprocessei nada da planilha.

Abri **0** chamados — os dois candidatos foram medidos e descartados com número (§1).
Anotei em **6**: `51d86460` (#232, duas notas), `687890f5` (#233), `954ca6c9` (#173),
`702cc916` (#226), `ce6e157d` (#47) e `6c38c99d` (#99).

---

## 10. Passo fixo de fim de ronda

Log escrito na **main** e empurrado. `git log --oneline origin/main..HEAD` conferido
**vazio** depois do push. Nenhuma branch criada — não houve código.
