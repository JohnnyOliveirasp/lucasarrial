# Vigia (sensor) — ronda das 10h (2026-08-21, 10:10–10:45 UTC)

Papel: **regra 14-A** — eu varro, ABRO e ANOTO. Não fecho, não reabro, não respondo aluno, não
escrevo rascunho, não mexo em crédito. Ordens lidas: `_frank/ordens/README.md` (índice) +
`2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐ vigente) + `_frank/rotinas/e-mail_tecnico.md`.
Ronda anterior do Frank: `2026-08-21_rotina_falhas_09h.md`. Vigia anterior: `2026-08-21_vigia_08h.md`.

**Resumo em uma linha:** a ronda das 09h do Frank **entregou de verdade** (mergeou o PR #22,
verificou o deploy e fechou 2 incidentes — a fila caiu de 6 pra 3), **nenhum e-mail novo entrou**,
e o achado desta ronda é que **o backfill da mensagem velha é maior do que ele registrou**: são
**24 vozes**, não 18, e **14 são de pagante com acesso vivo** — inclusive 2 dos 5 travados.

---

## 1. Frente 1 — fila SEM filtro de assinatura, paginada, `error` cru impresso

```
[pag 0] ERRO_CRU: null | recebidos= 72
TOTAL = 72 TALLY = {"investigating":3,"fixed":53,"ignored":16}  soma = 72 ✅
```

**Total: 3 abertos** (0 `open` + 3 `investigating`), contados sobre a tabela inteira, **sem
cláusula `signature like 'fast-email:%'`** — foi esse filtro que em 19/08 reportou "0 abertos"
existindo quatro. Era 6 na ronda das 08h; o Frank fechou `07745f61` e `b9c5a0d1` às 09h, e
`100e7ace` às ~09:50 (resolvido há 0,3h quando eu varri).

| # | incidente | idade | quem espera | estado nesta ronda |
|---|---|---|---|---|
| 1 | `2c5bab42` | 787,5h | 7 pagantes | `last_seen` 5,9h (04:17Z) — **nenhuma ocorrência nova em 6h**; anotei o backfill maior (seção 3) |
| 2 | `5c3f1f8b` | 263,5h | 5 pagantes | detector ao vivo confirma **5, nenhum novo**; anotei o estado dos 5 (seção 4) |
| 3 | `ce6e157d` | 46,0h | aluna (Katia) | anotei: **ela não gerou de novo depois do ajuste** (seção 2) |

**Abertos por tipo: 3 de aluno, 0 puramente técnicos.** Os 3 têm pagante esperando do outro lado.
Todos são do Frank. **Não abri nada nesta ronda** — os candidatos morreram na conferência
(seções 5 e 6).

### Fechado que voltou a disparar: **1**, o mesmo de sempre, e esfriando

`acf8acd6` [fixed], fechado há 297,1h por `james`, `last_seen` há **81,8h**, `occurrences=51`.
**`vivo_48h = false`.** Era 79,8h na ronda das 08h → **nenhuma ocorrência nova em 2h**, e já
passou das 08:46:39Z em que o `a713f6c` (gêmeo aritmético do `Math.round`) entrou em produção.
`fechados_sem_resolved_at = 0`; denominador **69 fechados com `resolved_at`**, 1 zumbi. É do
Frank, não toco.

Conferi também os fechados com `last_seen` nas últimas 24h: **11**, e em **todos** o `last_seen`
é **anterior** ao `resolved_at`. Nenhuma classe fechada disparando escondido — o padrão do
`8d370ef5` (que escondeu 14 ocorrências desde 17/08) **não se repetiu**.

### Integridade do `agent_notes`

**72 incidentes, 72 arrays, 0 strings corrompidas, 0 nulos.** As 3 anotações desta ronda saíram
por `_frank/ferramentas/anotar_incidente.cjs` (ensaio → `--confirmar`, "1 linha afetada"
conferida na releitura de cada uma). Nunca por script solto — foi script solto que destruiu 21
notas em 3 incidentes em 20/08.

## 2. Katia: agora dá pra provar pelo banco que o piloto está sem material

O e-mail de 20:16Z pediu uma coisa central: **"gere o mesmo texto de novo"**. Até agora as rondas
mediam isso pela caixa. Medi pelo banco:

- Ajuste de pausa aplicado na voz dela há **13,9h** (`"Minha voz"` [ready], `updated_at`).
- **Última geração dela: há 37,1h** — ou seja, **antes** do ajuste.

**Ela não gerou de novo.** Sem geração nova o veredito do piloto não tem material nenhum, e
nenhuma ronda pode concluir coisa alguma sobre o ajuste ter funcionado.

O e-mail dela (**uid 204**, 21/08 **05:53 UTC**) segue **sem resposta há 4,5h** — é o mesmo que a
ronda das 08h pegou, e o conteúdo já está registrado lá (não estava lendo texto; o áudio "robô" é
o **nosso** curado). **Acesso vence em 25,8h** (22/08 ~12:00 UTC). Saldo 78.665, 4 estornos
`generation_refund` conferidos por `ref_type`. Anotado no `ce6e157d`. Não respondi, não escrevi
rascunho.

## 3. O achado da ronda: o backfill é de 24 linhas, não 18 — e 14 são de pagante vivo

A ronda das 09h listou como item 4 do Johnny: *"backfill do `error_message` das **18 linhas**
carimbadas em 18/08"*. Medi cru, **sem filtro de data**, em `voices` com status
`rejected_too_short`/`failed` (75 linhas no universo):

**24 vozes ainda têm GRAVADA a frase que culpa o aluno** (`"Áudio total Nmin < mínimo de 20min"`),
e **14 delas são de pagante com acesso vivo e crédito**.

O fix `d12b008` corrigiu a frase **nova**; o painel lê do banco ao vivo, então ele **não alcançou
nenhuma dessas 24**. O que isso significa em gente:

| quem lê a frase errada agora | situação |
|---|---|
| `leandro.fitoway` (voz `a6bc8184`) | **1 dos 5 travados** — lê "Áudio total 10min < mínimo de 20min" |
| `jrfengenhariadf` (voz `1858c53b`) | **1 dos 5 travados**, acesso vence em 97,8h — mesma frase |
| `kelinnavelar` (voz `a046ede6`) | a **frase impossível** do `Math.round` segue gravada: *"Áudio total 20min < mínimo de 20min"* |
| `richard.moraes@hc.fm.usp.br` | pagante vivo (89.360 cred), **não está no `affected_emails` de nenhum aberto** |
| `rafaelleitemacedo` | pagante vivo (73.585 cred), **idem** |
| + `dirceu` (2), `sidbae` (4), `catarinacouras`, `fabiobraga`, `natali.marcio` (2) | pagantes vivos com a frase velha no histórico |

Os dois últimos (`richard.moraes`, `rafaelleitemacedo`) **têm voz pronta** — não estão travados,
por isso o detector não os pega e por isso **não abri incidente**. Mas eles leem no histórico uma
frase que manda regravar quando a orientação certa é reenviar.

**A gravidade real está nos dois travados:** a cura provada é *"reenvie, a MESMA gravação serve"*,
e o que está na tela deles é *"grave mais áudio"*. É a orientação errada na mão de quem já está
parado há 22 e 27 dias. Anotado no `2c5bab42`.

## 4. `5c3f1f8b`: os 5 travados, conferidos ao vivo depois do fix

Rodei o `varredura_travados.cjs` (leitura pura, conferido: nenhum `insert/update/upsert/delete/rpc`
no arquivo) **depois** do `7ee785f`: **continuam 5, nenhum novo entrou.**

| aluno | crédito | acesso vence em | sem voz há | estorno (por `ref_type`) |
|---|---|---|---|---|
| `jrfengenhariadf` | 100.000 | **97,8h** ⟵ o mais curto | 27 dias | **zero débito na vida da conta** |
| `leandro.fitoway` | 97.620 | 193,8h | 22 dias | **zero débito na vida da conta** |
| `ivanildezuca` | 200.000 | 433,8h | 13 dias | 2× `voice_train_refund` |
| `marcelopersonalthe32` | 198.950 | 361,8h | 11 dias | 1× `voice_train_refund` |
| `csitya100` | 200.655 | 553,8h | 6 dias | 1× `voice_train_refund` |

**Não há um crédito pendurado.** Confirmei a armadilha das duas pontas: conferência por
`ref_type` (nunca por `kind`), e incluindo **`voice_train_refund`** além de `generation_refund` —
foi a cegueira que a ronda das 08h descobriu. `leandro` e `jrfengenharia` têm **zero débito de
treino**: `rejected_too_short` é recusado **no portão, antes de cobrar**, então não existe estorno
a fazer. **O prejuízo dos 5 é não ter voz, não é dinheiro.**

**Nenhum dos 5 tentou de novo** desde a ronda das 08h (nenhuma voz mexida).

## 5. Frente 2 — a caixa: nenhum e-mail novo

`node _frank/ferramentas/ler_caixa.cjs --ultimos 25 --corpo 2500` (leitura pura, `BODY.PEEK`, só
`SEEN`). **Não-lidos no INBOX: 0** (`--fila`) — a fila da Fast está vazia e eu não toquei nela.

**Último e-mail da caixa: uid 204** (Katia, 05:53Z) — **o mesmo da ronda das 08h. Nada entrou nas
últimas 2h.**

- Aluno cobrando **pela segunda vez**: nenhum novo.
- *"a equipe vai verificar"* / *"vou chamar alguém da equipe"* sem incidente: nenhum caso novo.
- **Nada aberto pela Frente 2.**

## 6. Dois candidatos que eu derrubei antes de virar card

Apliquei a pergunta *"já resolveu sozinho / a queixa é verdadeira?"* antes de abrir:

**a) 3 vozes novas com a mensagem "você tem 0 créditos" — a classe do `bea487b7`, fechado há
13,6h.** Uma delas (`ac1d82e3`) nasceu **há 0,8h, depois do deploy das 08:46:39Z**. Se a mensagem
mentisse, era classe fechada voltando a morder — exatamente o que a ordem manda caçar.

**Não é.** Conferi as **13** vozes em `awaiting_training` que carregam essa frase, uma por uma:
**as 13 têm saldo 0, nenhuma compra e nenhum acesso**. A mensagem está **honesta** nas 13. É a
regra de crédito funcionando (*"quem nunca pagou e saiu do trial não gasta"*), não bug.
`bea487b7` está corretamente fechado.

**b) As outras 16 vozes paradas em `awaiting_training`** (algumas há 842h) — cheguei nelas
procurando pagante travado sem incidente. **Nenhuma é.** Conferi as 16: **8 sem acesso ou sem
crédito**, e as outras **8 são de pagante ativo que JÁ TEM voz pronta** (`ready` ≥ 1). **Zero 🔴.**
Nenhum pagante travado fora do `5c3f1f8b`.

## 7. Saúde da produção (medida)

- Últimas 6h: **2 gerações, as 2 `ready`, zero falha.**
- Últimas 24h: **22 vozes, 19 `ready` + 3 `awaiting_training`** (as 3 do item 6a, todas de conta
  sem crédito) — **zero em estado ruim**.
- **`generations` presas em `pending`/`processing`: 0.**

## 8. O que precisa de decisão (não é minha)

1. 🔴 **Katia: `ce6e157d` está bloqueado nela, e ela tem 25,8h de acesso.** Ela não gerou de novo
   depois do ajuste e está sem resposta há 4,5h. Sem geração nova, **não existe veredito do
   piloto** — ou ela responde, ou sai 1 geração da casa antes de 22/08 12:00Z.
2. 🔴 **Backfill do `error_message`: 24 linhas, 14 de pagante vivo** (não 18). Os 2 travados mais
   antigos leem a orientação errada **agora**, e `jrfengenhariadf` perde acesso em **97,8h**.
   A correção em produção não alcança quem já está carimbado.
3. 🟡 **`dirceu.moura.cruz78` perde acesso em 25,8h** — maior vítima individual (21 arquivos
   perdidos), tem voz pronta, por isso não entra em nenhum detector.
4. 🟡 **Os e-mails de 01:09Z (ivanildezuca, csitya100) seguem com a instrução errada na mão deles**
   — `07745f61` foi fechado como `fixed` e a mensagem em produção está certa, mas **ninguém
   avisou os dois**. Nenhum dos dois reenviou ainda, então a armadilha continua armada e ainda
   não disparou. **Registro a objeção, não reabro** (regra 14-A).

## 9. O que eu NÃO fiz (regra 14-A)

Não abri incidente (os candidatos morreram na conferência). **Não fechei nem reabri nada**,
inclusive o zumbi `acf8acd6`, o `07745f61` e o `100e7ace` que o Frank acabou de fechar. Não
escrevi resposta nem rascunho para aluno. Não mexi em crédito. Não gastei GPU. Não toquei em
e-mail não lido (fila da Fast segue em 0). **Três anotações** (`2c5bab42`, `ce6e157d`,
`5c3f1f8b`) são todo o meu efeito colateral.
