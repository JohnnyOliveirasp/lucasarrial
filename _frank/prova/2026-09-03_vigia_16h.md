# Vigia — ronda de 03/09/2026, ~16hZ (13h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 2** (`702cc916`/#226,
`f8587cef`/#234). Não fechei, não reabri, não mudei status de nada, não respondi aluno, não escrevi
rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

Última ronda de vigia: 12hZ. Ronda das falhas de 13hZ lida antes de tocar em qualquer coisa.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 🔴 12 pagantes órfãos sem contato esperam o "pode" para escrever — pedido feito às 13hZ, sem resposta

Não é achado meu; é da ronda das 13hZ e eu registro porque **continua parado e tem prazo correndo**.
O texto está pronto e testado (Eduardo, uid 481; Simone, uid 483). Mandar para 12 é comunicação em
**massa** e precisa da autorização (regra 8, 21/08).

O que dá urgência: são pagantes que já foram cobrados 2ª e 3ª vez e **nunca entraram**. As janelas de
garantia vencem uma a uma — a da Simone vence **05/09**, daqui a 2 dias, e ela só foi contatada hoje
porque alguém escolheu uma. As outras 12 não têm ninguém escolhendo.

**A pergunta, em 1 linha: posso escrever para os 12?**

### 0.2 Migration 102 — **décima primeira** ronda com a mesma pergunta

Conferido agora pelo banco, não pelo registro anterior:

```sql
select tgname from pg_trigger where not tgisinternal and tgrelid='incidents'::regclass;  -- []
```

Zero triggers em `incidents`. `scripts/102_incidents_resolved_guard.sql` segue **não aplicada**.

### 0.3 24 PRs abertos — sem mudança desde as 12hZ

O mais antigo é de 18/08. Os #41/#42 (teto de 2MB) estão no **décimo quinto dia**. O buraco continua
aberto: a Alana bateu nele nesta janela (uid 426, 2,2MB, recebeu *"não consegui ler o que você
mandou"*).

---

## 1. O que eu ABRI

**Nenhum chamado.** Nenhum e-mail novo (§4), nenhuma classe nova nas medições (§5), e os dois
chamados que entraram na fila desde as 12hZ (#241, #242) **nasceram sozinhos pelo chat do app** —
abrir qualquer coisa seria duplicar fila.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`702cc916` / `#226`** — nota 23: a série andou 14 → 16 e **as duas novas são posteriores ao
  build do conserto**. §5.
- **`f8587cef` / `#234`** — nota 10: o build fechou às 12:23:08Z e a predição da ronda das 12h30Z
  continua com **n=0**; mais o limite de instrumento que eu não consigo fechar. §6.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature` (armadilha de 19/08,
que fez reportar "0 abertos" existindo quatro).

**7 abertos — 3 de aluno, 4 técnicos.** Todos em `investigating`; `open` = 0.

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **364,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) |
| `3ca22d47` `#222` | 48,3h | 5 no título, **26 medidos** | compra órfã casa só por e-mail (`claim.ts:39`) |
| `702cc916` `#226` | 46,3h | **156 alunos** | QA esgota e entrega assim mesmo |
| `f8587cef` `#234` | 23,9h | 237 alunos (609 gerações) | palavra decapitada em fronteira interna |
| `92b1cc85` `#237` | 19,5h | *(irrespondível)* | relato manual sem e-mail e sem anexo |
| `07a423ff` `#241` | 1,4h | claytonpc10 | chat do app: movimento complexo no vídeo — atendimento |
| `7abddc30` `#242` | 0,6h | rodrigoaugusto | chat do app: propaganda × avatar por vídeo — atendimento |

Composição desde as 12hZ: **saiu** o `#202` (fechado por **humano**, `suporte@lucasarrial.com`, nota
"respondido via whatss", às 12:41:39Z) e **entraram** o `#241` e o `#242`, ambos `fast-help`.
O `#240` (Vinícius, cancelamento de R$ 2.697,60) também foi fechado pelo mesmo humano às 12:39:33Z —
saiu do balde `aguardando_aluno`, não dos abertos.

**Mais 9 em `aguardando_aluno`** (eram 10; saiu o `#240`), que o número acima não mostra:
`#99` (263,4h — 11,0 dias), `#172` (144,6h), `#206` (75,1h), `#207` (74,2h), `#214` (66,3h),
`#216` (51,8h), `#218` (49,2h), `#224` (47,3h), `#229` (43,3h).

**Placar honesto da ronda: 7 abertos + 9 esperando aluno = 16 cards com gente do outro lado.**

### 3-A. Crédito conferido, um a um

Por `ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por `kind`, armadilha de
20/08; **não** só por `generation_refund`, armadilha do `#129`), nos **18** alunos citados nos
abertos — incluindo os 2 novos:

**Nenhum estorno pendente. Zero gerações `failed` em aberto. Zero gerações em curso presas.**

Três alunos com saldo **zero**, conferidos um a um em vez de assumidos:

| aluno | por que o zero **não** é travamento novo |
|---|---|
| `flmg.liborio` | 14 gerações, 34 transações, última em 18/08: gastou até zerar. Uso normal |
| `ftfranzolin` / `cdmarciofernandes` | **0 gerações, 0 transações**: o retrato exato do `#222` — a conta que eles usam não é a da compra |

⚠️ **Uma correção ao meu próprio placar das 12hZ.** Eu listei os 5 e-mails do `#222` como se todos
fossem o mesmo caso. Não são, e eu conferi:

| e-mail do `#222` | conta criada | gerações | transações | leitura |
|---|---|---|---|---|
| `jkakoalves` | 19/08 | **7** | 19 | está **usando** a plataforma, saldo 168.566 |
| `diretoria@grupoperes` | 18/08 | 0 | **49** | tem conta e movimento de crédito, 0 gerações |
| `gabrielalouly` | 31/08 | 0 | 1 | conta nova, praticamente sem uso |
| `ftfranzolin` / `cdmarciofernandes` | — | 0 | 0 | o padrão órfão de verdade |

Isso **não** é objeção ao `#222` — o card é do Frank, ele mediu hoje 46 órfãs / 26 vigentes por outro
caminho, e o `affected_emails` do card é de 01/09. É só para a próxima ronda não repetir o meu erro
de tratar os 5 como um bloco homogêneo. **Não mudei nada no card.**

### 3-B. Filas

`generations` presas há +2h = **0**; `voices` em `training` paradas há +2h = **0**;
`image_generations` presas há +2h = **0**. **`voices/awaiting_training` = 18** (era 17) — não é
chamado, é o estado em que a bola está com o aluno. Registro porque o PR **#15**, que daria dono a
essa lista, está **OPEN desde 20/08**.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 700`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Nenhum e-mail novo desde a ronda das 12hZ:** o uid mais alto continua **429** (Vinícius, 03/09
04:10Z), que já tem card e **já foi fechado por humano** às 12:39Z (§3).

Nenhum aluno cobrando pela segunda vez **sem resposta**, nenhum *"já pedi ajuda várias vezes"* novo,
nenhum *"a equipe vai verificar"* sem card correspondente. Os quatro alunos da janela (Katia, Alana,
Silmara/Rufino, Johnathan) seguem com o card que a ronda das 12hZ mapeou; **não reescalei nenhum**,
inclusive o Johnathan (ameaça jurídica), porque já tem dono e repetir seria reapresentar o velho
como novidade.

Os dois cards novos (#241, #242) **não vieram da caixa** — vieram do chat do app (`fast-help`).

---

## 5. A série do `#226`, décima segunda medição — e o build NÃO a parou

Este é o achado da ronda, e ele existe para impedir uma leitura errada que está pronta para
acontecer.

**O que mudou desde as 12hZ:** o `Build RunPod Worker` do `3bc1535` — o conserto do `eh_ultimo`
achado pela ronda das 12h30Z — **terminou**: `conclusion: success`, `updatedAt` **12:23:08Z**
(run 33752907497). A ronda das 12hZ o deixou `in_progress`. Quem ler só isso conclui que o `#226`
andou.

**Não andou.** Campo certo (`coverage_min_visto`, não `coverage_min` — a armadilha de instrumento da
ronda das 12hZ), janela instrumentada desde 02/09 02:32Z:

| grupo | entregas | alunos | abaixo da régua | pior |
|---|---|---|---|---|
| `exhausted = 0` | 37 | 24 | **0 (0,0%)** | 0,857 |
| `exhausted > 0` | 38 | 20 | **16 (42,1%)** | **0,333** |

Às 12hZ era 30/0/0,857 e 34/**14**/0,333. O controle **continua nunca** caindo abaixo da régua, agora
com 37 entregas. O outro grupo ganhou **mais duas: 14 → 16**.

**As duas novas são POSTERIORES ao build**, e é isso que importa:

| geração | quando | cobertura | `exhausted` | `regens` | `coverage_rescue` |
|---|---|---|---|---|---|
| `5d5b7866` | 03/09 **13:08:05Z** | **0,667** | 4 | 14 | **ausente** |
| `84e3a400` | 03/09 **14:44:26Z** | **0,667** | 1 | 16 | **ausente** |

Nove gerações rodaram depois das 12:23:08Z; duas saíram abaixo da régua, **ambas sem passar pelo
resgate**.

**A leitura honesta, com o limite dito:** o `3bc1535` conserta o gate de fim de arquivo **no caminho
do resgate**. Estas duas não passaram por resgate, então ele **nunca teve chance de agir sobre elas**
— não é que o conserto falhou, é que ele **não cobre este ramo**. O defeito do `#226`
(`loop.py:341-344` esgota as tentativas e entrega assim mesmo) segue inteiro e segue disparando
depois do build.

Série acumulada: **354 de 775** entregues com `exhausted>0` (**45,7%**), **156 alunos** distintos
(era 338/752 e 155 às 12hZ). Últimas 24h: **30 de 64 (46,9%)**.

---

## 6. O `#234`: o conserto subiu e **continua sem teste em produção**

1. **O build fechou.** `3bc1535` está na main de verdade — conferido por
   `git merge-base --is-ancestor 3bc1535 origin/main` (sim), não por leitura de `git log`.

2. **A predição da ronda das 12h30Z continua com `n=0`.** A hipótese testável era: *"se o conserto
   entrar, a próxima geração com `coverage_rescue=1` tem que sair na faixa do controle"*. Varri
   todas as gerações de 03/09 desde 09:00Z: a **última** com `coverage_rescue` é a `a8caaae1`, de
   **11:11:08Z** — anterior ao build. Depois das 12:23:08Z rodaram **nove** gerações e **nenhuma**
   passou pelo resgate. O conserto está no ar e **ainda não foi exercitado nem uma vez**. Não há
   resultado a favor nem contra.

3. ⚠️ **O limite de instrumento que eu não consigo fechar, dito como limite.** *"Build success"*
   **não é** *"imagem nova rodando no endpoint do RunPod"*. Comparei as chaves do `jsonb qa` das
   gerações de antes e de depois das 12:23Z procurando um marcador que distinguisse imagem velha de
   nova: **não existe nenhum** — todas as chaves já existiam antes. Então eu **não tenho, no banco,
   como provar** que o worker adotou o `3bc1535`. Registro de propósito: *"merge não é entrega"* foi
   exatamente o que produziu o fechamento falso do `#226`, e a distinção seguinte — *"build não é
   adoção"* — também está **sem prova**.

4. **O que corrobora o diagnóstico, e é medição e não leitura:** entre as 16 gerações de 03/09 que
   carregam a régua nova, a `a8caaae1` (a única com `coverage_rescue=1`) é a **única** cujo `jsonb
   qa` **não tem** a chave `tail_interno_entregue_sem_veredito`. As outras 15 têm. É exatamente a
   assinatura descrita na mensagem do `3bc1535`, conferida por mim de forma independente, no banco.

5. **O que a próxima ronda precisa medir:** a primeira geração com `coverage_rescue=1` posterior a
   **12:23:08Z**. Até ela existir, o `#234` **não pode** ser fechado por *"o fix subiu"*.

---

## 7. O `#237`: refiz a checagem do zero em vez de herdar a conclusão

Quarta ronda sem dono. Eu **não** aceitei o *"irrespondível"* das notas anteriores de graça — refiz o
teste, porque conclusão herdada é como o Executor acabou caçando um uuid inexistente.

Testei o `20a69a24-4c14-48cc-a2cc-6558fbda7bd8` contra `profiles`, `help_messages` e
`agent_messages`: **0 linhas nas três**. E fui à fonte: `frontend/src/app/api/v1/admin/incidents/route.ts:71`
monta `signature: reported:${randomUUID()}` — sorteado no insert, **não identifica ninguém por
construção**. `attachment_path` é `null` e `affected_emails` é `[]` porque o e-mail é campo
**opcional** no form (`route.ts:74`). **A conclusão anterior se sustenta**, agora por medição minha.

**Fato de processo que eu registro e não transformo em chamado:** já existe um **patch pronto** para
o defeito do formulário — chave `patch_92b1cc85` no `agent_state`, base `bab1c07`, branch local
`agent/fix-reporte-manual-sem-identificacao`, com `tsc --noEmit` limpo. Ele **não foi empurrado**
porque quem o escreveu **não tem credencial de escrita**. Conserto escrito e verificado que não tem
caminho para o repositório é trabalho parado, e some se ninguém disser em voz alta.

---

## 8. Fechados que voltaram a disparar

`status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` `#8` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
**284,7h atrás**. Não é reincidência ativa. **Nada fechado voltou a disparar nesta janela.**

⚠️ **O ponto cego continua.** O `#226` tem `last_seen_at` congelado em **01/09 17:52:45Z** e
`occurrences` em **290** contra **354** reais. Ele está `investigating`, então sai desta consulta por
**reabertura, não por conserto**. Se alguém fechar o `#226` de novo, a consulta volta a mentir na
hora. O PR **#163** faz o detector **declarar** o ponto cego; ele **não descongela** o contador.

---

## 9. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada — em particular **não
toquei nos 9 de `aguardando_aluno`** nem nos dois que o humano fechou às 12:39/12:41Z. Não respondi
aluno nem escrevi rascunho: **não escrevi aos 12 pagantes órfãos** (comunicação em massa, precisa do
"pode"), **não escrevi ao Johnathan** (ameaça jurídica, já tem dono) e **não escrevi à Katia** (o
`#47` é do Frank). Não mexi em crédito, acesso, voz ou áudio. Não mergeei PR (24 abertos) nem
apliquei migration (102, **11ª ronda**). Não apliquei o patch do `#237` — quem aplica e revisa é o
Frank. Não ouvi nenhum áudio: toda afirmação aqui é de telemetria, `gh`, leitura de código, leitura
de caixa e cronologia. Nada da **planilha** foi lido, escrito, classificado, avisado ou reprocessado
(ordem 29/08).
