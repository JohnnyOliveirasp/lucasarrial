# 24/08 14:15Z — Ronda do Vigia (sensor, regra 14-A)

`git checkout main && git pull --ff-only origin main` → **atualizado** (`9214e86`).
Índice de ordens lido. Valem a `2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐), a
`2026-08-20_REGRA_FINAL_CREDITO.md` e a rotina `e-mail_tecnico.md`.

---

## Placar

| | |
|---|---|
| Incidentes abertos | **6** — 4 de aluno, 2 técnicos (3 `open`, 3 `investigating`) |
| Variação desde a ronda das 12h | **15 → 6**: 9 fechados em ~2h (§1) |
| Abertos por mim nesta ronda | **0** — o único e-mail de aluno já tem incidente aberto |
| Anotados por mim | **3** — `33c01bea` (121), `2adf0848` (109), `37bacb68` (52) |
| Fechados que voltaram a disparar | **1** pelo detector (`acf8acd6`, quieto há 42,8h) — **detector segue cego, §5** |
| E-mails lidos | 25 (INBOX, `BODY.PEEK`, só `SEEN`) |
| E-mail novo desde a ronda das 12h | **2** — uid 274 (aluno) e uid 275 (teste técnico nosso) |
| Fila não-lida da Fast | **0** em todas as pastas (`--caixas`, read-only) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito que eu mexi | **nenhum** |

---

## 1. A fila caiu de 15 para 6 — e os fechamentos são de verdade

Nove incidentes saíram entre 11:55Z e 13:27Z. **Conferi as notas de fechamento
um a um** antes de escrever qualquer coisa, porque fila que despenca em duas
horas é exatamente o padrão de fechamento por antiguidade.

| nº | quem fechou | o que a nota diz |
|---|---|---|
| `15` | frank | aceite de risco do Johnny (20/08), **explicitamente "NAO E UM FIX"** |
| `79` | frank | commit real `adb09c2` — botão "Remover foto" + `DELETE /api/v1/images/ref-default` |
| `82` | frank | entregue ao time no WhatsApp |
| `90` | frank | duplicata do `108` |
| `95` | frank | entregue ao time no WhatsApp |
| `97` | frank | entregue ao time no WhatsApp + Rafael respondido |
| `101` | claude | mergeada `feat/gravar-enviados-imap-append` |
| `102` | claude | medido: rate limit da Kie + content policy, dinheiro conferido |
| `111` | claude | varredura completa, 2.659 arquivos, 0 a curar |

**São notas honestas, não fechamento cosmético.** O `15` se declara aceite de
risco e não conserto; o `111` denuncia no próprio fecho que a operação em massa
já tinha rodado sem dono. Isso é o oposto de maquiar. **Não tenho objeção aos
fechamentos como fechamentos.**

### Duas conferências que eu fiz porque duplicata é onde aluno some

**`90` → `108`: passagem limpa.** O `90` foi fechado como duplicata do `108`.
Conferi os dois `affected_emails`:

```
n90  [fixed] kessulyl@gmail.com, katiasalvador32@gmail.com
n108 [open]  kessulyl@gmail.com, katiasalvador32@gmail.com
```

Os 2 alunos foram junto. **Ninguém caiu do radar.** Registro isso como acerto.

**`101` funcionou de verdade.** O uid 275 é o teste do próprio conserto, e a
pasta `Sent` agora tem **22 mensagens** (era invisível antes). A pergunta "esse
aluno já foi avisado?" passou a ter resposta. Isso mata a classe de erro que me
rendeu bronca em 20/08 — escrever rascunho pra aluno que já tinha sido
respondido horas antes.

---

## 2. O ACHADO DA RONDA: 0% de falha técnica e 2 alunos reclamando ao mesmo tempo

O e-mail novo (uid 274, 12:26Z) é o **Itamar Vanzin pela 3ª vez no mesmo dia**:

> *"acabei de fazer mais um teste com o turbo, mudou a cara ta muito estranho"*

### Ele fez o teste mesmo, e o sistema disse que deu certo

| | |
|---|---|
| `video_clones` `926db3e3` | criado **24/08 11:55:29Z** |
| status | **`ready`** |
| e-mail dele | **12:26Z — 31 minutos depois** |

**O sistema entregou `ready` e o aluno escreveu que está inutilizável meia hora
depois.**

### A armadilha de medição que eu quase caí (e registro pra ninguém cair)

Medi primeiro em `generations` e deu **93h sem atividade** do Itamar — o que
levaria direto à conclusão falsa *"o aluno não testou, está reclamando de vídeo
antigo"*. **`generations` é tabela de ÁUDIO. Vídeo Clone vive em `video_clones`.**
Só depois de conferir o `information_schema` é que o teste apareceu. Mesma
família das armadilhas já medidas: **conferir no artefato errado crava causa
errada**.

### O número que o painel não consegue ver

| `video_clones`, últimas 12h | |
|---|---|
| total | **37** |
| `ready` | **37** |
| `failed` | **0** |

**Taxa de falha técnica: 0%. Chamados abertos de qualidade nesse mesmo produto: 2.**

Nenhum alarme automático vai disparar aqui. `ready` não é sinônimo de entregue,
e é por isso que esta classe só aparece pela boca do aluno.

### O `109` é a mesma coisa, e ninguém sabe quem é o aluno

O `109` (aberto há 16h, via Carol/zap) descreve:

> *"parece que tem dois olhos, um em cima do outro, a boca parece que tem duas
> bocas aparecendo"*

Mesma família de artefato de rosto do Itamar. **`affected_emails` VAZIO há 16h** —
o aluno chegou pelo zap e não foi identificado, então ninguém consegue responder
nem conferir crédito dele. **Enquanto o e-mail não entrar, esse chamado é
incerrável.**

### A objeção que eu registro sem mexer em nada

Cinco chamados dessa mesma família (`79`, `82`, `90`, `95`, `97`) foram fechados
hoje. **Três deles com a nota "entregue ao time no grupo do WhatsApp".**

Entregar ao time é **passagem, não conserto**. Vale aqui a mesma régua do
estorno que já está na ordem: *aluno estornado não é aluno atendido* → **chamado
repassado não é defeito corrigido**. A prova é que a família produziu **2
chamados novos** (`109` e `121`) com os outros 5 já fechados.

**Anotei a objeção no `109` e no `121`. Não reabri nada, não mudei status, não
respondi aluno** (regra 14-A).

---

## 3. O `52` está quieto — a leitura de alarme das 10h não se sustentou

Medido direto em `generations`, não no contador do incidente:

| últimas 12h | |
|---|---|
| gerações | **30** |
| falhas | **1** (3,3%) |
| qual falha | `615a99f3`, **03:41:23Z — já conhecida desde a ronda das 10h** |

**Zero falha nova em ~10,5h.** Os 10,7% reportados às 10h eram janela pequena de
madrugada. Registrei no incidente pra quem pegar o `52` não herdar alarme falso.

### O que continua verdade e vale mais que a taxa

**Os 3 alunos que tiveram FALHA como última ação não voltaram, e a distância só
cresce.**

| aluno | sem voltar há | era às 12h |
|---|---|---|
| **janetecasarotto2** | **14,5h** | 12,6h |
| danicale | 14,6h | 12,6h |
| drelvislandi | 12,6h | 10,7h |

A **Janete** segue o caso mais duro: 8 gerações `ready` até 07/08, sumiu 16 dias,
voltou em 23/08, tentou **duas vezes** (`qa_coverage` + `executionTimeout`), as
duas falharam, não voltou mais.

Estorno reconferido por **`ref_type='generation_refund'`, nunca por `kind`** (o
estorno grava `kind='extra_purchase'`): os três receberam de volta.
**Dinheiro certo. Entrega, não.**

**O que segue sem medir, e eu não disfarço:** ninguém abriu os áudios pra saber
se o `qa_coverage` reprova **áudio bom** (defeito do conferidor) ou pega **áudio
incompleto de verdade** (defeito do gerador). Sem isso não dá pra dizer se esses
3 perderam áudio bom.

---

## 4. FRENTE 1 — todos os abertos, sem filtro de assinatura

`status in ('open','investigating')`, **sem cláusula de `signature`**. Total: **6**.

| id | nº | quem espera | idade | alunos |
|---|---|---|---|---|
| `37bacb68` | 52 | técnico — qa_coverage (§3) | 115,8h | 15 |
| `60f3e9e2` | 94 | João Rezende — feedback detalhado | 26,7h | 1 |
| `73b9f772` | 108 | técnico — referência de voz (herdou o `90`) | 16,3h | 2 |
| `2adf0848` | 109 | aluno via Carol/zap — **§2, sem e-mail** | 16,2h | **0** |
| `6e94acc6` | 120 | Sandra Diniz — pré-venda, Termos em rascunho | 4,0h | 1 |
| `33c01bea` | 121 | Itamar Vanzin — **§2** | 2,2h | 1 |

**4 de aluno, 2 técnicos.**

**Anotação de higiene:** o `52` está `investigating` **com `resolved_at`
(20/08 10:32Z) e `resolved_commit` (`2899818`) preenchidos** — mesma condição
que o `15` tinha e que fazia ele ser contado como aberto e lido como fechado.
Como está `investigating`, ele **escapa do detector** da §5, que só examina
`fixed`/`ignored`. Só anotei.

---

## 5. O detector segue cego — e o `last_seen_at` tem nuance nova

Detector rodou: **1 de 109** fechados disparou depois do fecho — `acf8acd6`
(fechado 09/08 por james, 12 ocorrências pós-fecho). **Quieto há 42,8h** — eram
41h às 12h, a diferença bate com o relógio → **zero disparo novo**. Décima
terceira ronda sem alguém confirmar produção.

**Nuance que eu não tinha medido antes, e que corrige em parte a §3 da ronda das
12h:** o `last_seen_at` **atualiza sim no caminho `fast-email`** — o `121` está
com `occurrences=2` e `last_seen_at=12:25:13Z`, ou seja registrou o e-mail novo
do Itamar corretamente. O que **não** atualizou foi o caminho de **bounce**
(`101`, medido às 12h). Então o defeito é **do caminho, não global**.

> Continua valendo o aviso prático: decisão apoiada em `last_seen_at` de classe
> técnica (bounce, `qa_coverage`) está apoiada em campo que pode não atualizar.
> Para chamado de e-mail de aluno, o campo é confiável.

---

## 6. FRENTE 2 — a caixa (25 últimos)

**Não toquei em não-lido. Fila não-lida da Fast: 0** em todas as pastas
(`STATUS (MESSAGES UNSEEN)`, read-only: Archive 0, Trash 0, Spam 0, Drafts 0,
Sent 0, INBOX 0).

**2 e-mails novos desde a ronda das 12h:**

- **uid 274** — Itamar. **Já tem incidente aberto (`121`)**, então anotei lá em vez
  de abrir outro (regra: aluno com incidente aberto não vira incidente novo).
- **uid 275** — e-mail nosso, teste técnico do `101`. Não é aluno.

Varri os 25 procurando os sinais da rotina — *"a equipe vai verificar"*, *"vou
chamar alguém da equipe"*, aluno cobrando pela segunda vez. Todos os que
apareceram **já têm incidente**: João Rezende (`94`), Kátia/Kessuly (`108`),
Sandra (`120`), Itamar (`121`). A Kátia respondeu *"vou ouvir no meu intervalo e
te respondo"* — aluna aguardando, não fila perdida; segue no `108`.

**Nenhum caso órfão nesta ronda.** O buraco do `[ESCALAR:]` sem incidente não
produziu vítima nova nas últimas 2h.

---

## O que eu NÃO fiz (regra 14-A)

- Não fechei incidente. Não reabri incidente. Não mudei `status` de nada.
- A objeção aos fechamentos "entregue ao time" ficou **anotada** no `109`/`121`,
  não virou reabertura.
- Não respondi aluno, não escrevi rascunho de e-mail.
- Não mexi em crédito.
- Não toquei em e-mail não lido.
