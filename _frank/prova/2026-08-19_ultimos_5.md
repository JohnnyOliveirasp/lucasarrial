# Prova de capacidade — os últimos 5

Frank, 19/08. Saída real, nada reescrito. 4 de 5 fechados.

## 1. Ferramentas de resgate — ✅

As três destrutivas **já nascem em modo seco**: só agem com `--confirmar`.
Rodadas agora, sem a flag:

```
resgatar_voz.cjs          -> uso: node resgatar_voz.cjs <voiceId> [--confirmar]
consertar_referencia.cjs  -> perfis conferidos: 34 · referências quebradas: 0
limpar_fantasmas.cjs      -> apagaria: 0 · mantidas (têm áudio): 0
```

`aluno.cjs` e `varredura_travados.cjs` são leitura pura, não precisam de ensaio.

### ⚠️ Quais NÃO têm modo seco

**`enviar_email.cjs` e `enviar_email.sh`.** São justamente as únicas que tocam
um ser humano de forma irreversível. Erro de endereço ou de aluno não tem
desfazer: o e-mail já saiu.

As destrutivas de dados têm ensaio; a destrutiva de reputação não tem. Se for
para colocar `--confirmar` em alguma, é nessa.

## 2. R2 — ✅

```
BUCKET voices-clone-ai-verse     LIST ok
   .../lora.safetensors                      72.397.184 bytes
   .../raw/000_onboarding_1s_...m4a          29.648.758 bytes
   .../ref/auto.wav                             960.078 bytes
   HEAD           -> 72.397.184 bytes, application/octet-stream
   URL ASSINADA   -> HTTP 206, 100 bytes lidos

BUCKET generations-ai-verse-clone  LIST ok
   .../sample.wav                               614.444 bytes
   HEAD           -> 614.444 bytes, audio/wav
   URL ASSINADA   -> HTTP 206, 100 bytes lidos
```

HTTP 206 é o correto para requisição com `Range`. Nenhum `PUT`, nenhum `DELETE`.

## 3. Conta de aluno de teste — ❌ NÃO FIZ

É o único dos cinco que ficou. Exige percorrer o cadastro no navegador e ter
um e-mail que eu controle para a confirmação. Não é leitura, é um fluxo, e não
coube nesta rodada. **Não inventei resultado de tela.**

O gate por crédito segue provado apenas no nível de código e de deploy
(`BUILD_ID` 15:22 no servidor), não na tela.

## 4. Provedores e build — ✅

```
Gemini      HTTP 200 AUTENTICA
OpenAI      HTTP 200 AUTENTICA
DeepSeek    HTTP 200 AUTENTICA   saldo USD 19,97
Kie         HTTP 200 AUTENTICA   5.049,51 créditos
Anthropic   HTTP 200 AUTENTICA
```

HeyGen do Lucas: **não toquei**, regra 16.
Apify: não existe `APIFY_TOKEN` no `.env` desta máquina, então o saldo não é
visível daqui. Não é falha, é ausência de credencial.

```
tsc --noEmit  -> exit 0, zero linhas de erro, 4s ("incremental": true no tsconfig)
eslint        -> exit 0, zero linhas
```

⚠️ Na primeira medição eu li `$?` depois de um pipe, o que devolve o status do
`tail` e não o do `tsc`. Refiz capturando o código real. Ficaria com um "exit=0"
que não provava nada.

## 5. Os dois mecanismos sobem instância dupla? — ✅ NÃO

```
PID antes                            : 1460235
systemctl start num serviço JÁ ativo
PID depois                           : 1460235
```

Mesmo PID: `systemctl start` é idempotente, não sobe segunda instância. Se o
timer e o `Restart=always` agirem juntos, o segundo vira no-op.

⚠️ O `pgrep -fc` acusou 2 processos e quase virou alarme falso: o segundo era
**o meu próprio comando** casando com a string que ele mesmo procurava. Um
único node roda, e é o `MainPID` da unit.

---

## A linha que importa: o que trava com o Johnny viajando

**Nada de infraestrutura.** Servidor, banco, R2, provedores, build, crons,
ferramentas de resgate e reinício automático: todos verificados e funcionando.

O que sobra é **um risco de processo, não de sistema**: o `enviar_email` não
tem ensaio, e é a única ferramenta cujo erro chega direto no aluno.

E segue de pé o que já estava: o backfill do `ja_pagou` precisa de paginação
mês a mês com filtro de produto antes de rodar, senão marca pagante antigo
como "nunca pagou". Até lá a trava do débito não sobe e o vazamento continua.
