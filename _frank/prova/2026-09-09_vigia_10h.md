# Ronda do Vigia — 09/09/2026, 10hZ (07h BRT)

Sensor. Varri, medi, **abri 1** (`#319`) e **anotei 2**. Não respondi aluno, não
escrevi rascunho, não mexi em crédito, não fechei e não reabri nada (14-A).

Repo em `main`, `pull --ff-only` (já estava em dia). `_frank/ordens/README.md` e
`_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.
Ordem de **29/08** respeitada: nada da planilha lido, escrito, classificado,
avisado ou reprocessado; nenhum chamado de causa-planilha aberto ou reaberto.
Ordem de **canal de 31/08**: aviso desta ronda vai **no grupo**.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 A decisão dos 15 vitalícios (`#313`) faz ~13h que está parada, e ela trava a coorte inteira

Nada mudou desde a ronda das 00hZ, e é justamente esse o problema. O `#313`
(aberto pelo Frank em 08/09 20:51Z) espera o Johnny dizer se **honra ou revoga**
os 15 entitlements vitalícios de produto de curso — 12 pessoas, 1 já com a
plataforma viva até 2030 sem nunca ter comprado. Enquanto não houver resposta,
os alunos do `#312` não podem ser tratados.

Custo já visível, medido nas duas rondas anteriores: dos que escreveram,
**um pediu reembolso integral** (`victor.inscriptio@gmail.com`) e **outro acha
que já entregou o material e está esperando calado** (Edésio, `#315`).

**Pergunta em 1 linha, terceira ronda seguida:** honra ou revoga os 15
vitalícios — e alguém procura ativamente a coorte de 04-05/09 agora, ou espera?

### 2. 🟡 Aluna travada tentando CANCELAR — não é bug nosso, mas vira chargeback se ninguém agir

**Elane** (escreve de `elaneckis@gmail.com`, conta `elaneyani@gmail.com`),
08/09 23:07 e 23:11 (INBOX uid 497 e 498). Pediu cancelamento; a Fast mandou o
caminho da Hotmart; ela respondeu:

> *"Não consegui cancelar por lá. A aba de concluir não aparece. Estou com
> iPhone. A tela não sobe."*

A tela que não rola é da **Hotmart**, não nossa — por isso **não abri chamado**
(ordem de 27/08 §2: cancelamento é atendimento, vai pro grupo). Mas registro o
risco: aluna que quer cancelar e não consegue costuma resolver por **contestação
no cartão**, e aí o custo é nosso. Conta ativa até 13/09, 49.202 créditos, uso
pesado até 22:22 de 08/09 — ela cancelou 45 min depois de usar.

**Decisão:** alguém cancela pela Hotmart no lugar dela, ou responde com o passo
a passo do iPhone? Não faço nem uma coisa nem outra sozinho.

### 3. 🟡 Comprador em Portugal quer PAGAR e não consegue — e a Fast manda ele pagar um Pix morto

**Duarte Soares** (`duartesoaresconsultor@gmail.com`) escreveu **4 vezes em 6
minutos** hoje de manhã (uid 499-502) querendo renovar. A Fast respondeu com o
roteiro de Pix/boleto. Ele:

> *"não posso fazer por piz... tem de ser por multibanco por estar em Portugal"*
> *"tem de ter entidade e referencia"*
> *"esta já não se encontra a pagamento e não sabia que a tinha recebido"* (com print)

A parte que **é nossa** virou o `#319` (abaixo). A parte que **não é nossa** é o
meio de pagamento: a Hotmart precisa dar multibanco pra ele. **Decisão:** alguém
manda um link de pagamento que funcione em Portugal, ou ele fica sem caminho?

Deixo claro o que **não** afirmo: ele **não** é pagante trancado. A recorrência
#2 dele está **OVERDUE** (cobrança existe e não foi paga), e OVERDUE não é
pagamento pela regra vigente. Trancar está **certo**, e os 261 créditos dele
foram **mantidos**, como manda a REGRA FINAL DE CRÉDITO de 20/08.

---

## O que eu ABRI e ANOTEI

| # | onde | o quê |
|---|---|---|
| **abri** | **`#319`** `3ad6b6da` | `vigia:fast-anuncia-pix-pendente-sem-janela-de-recencia` — técnico, `investigating`. Detalhe abaixo. |
| **anotei** | **`#318`** `8a37c48e` | nota 2: o defeito por trás do atendimento do Duarte é o `#319`; enquanto não corrigir, ele tende a receber o roteiro do Pix **de novo**. `agent_notes` 1 → 2. |
| **anotei** | **`#315`** `a0669176` | nota 1: mesma família do `#319` (contexto da Fast afirma estado falso), endereços e populações diferentes — **não** fundi os dois, de propósito. `agent_notes` 0 → 1. |

### O `#319`, e por que ele passou no teste de bolso

A **mesma coluna** (`profiles.pending_payment_at`) é lida por dois caminhos
nossos com regras **diferentes**:

| arquivo:linha | regra |
|---|---|
| `frontend/src/app/[locale]/app/layout.tsx:112-115` | janela de **3 dias** + `!subscribed`. O comentário diz o propósito: *"Some quando liberar/expirar."* |
| `frontend/src/lib/agent/account.ts:271` | **null check cru**. Sem recência, sem expiração. |

Ou seja: a tela do aluno **esconde** o aviso quando o Pix vence, e a Fast
continua anunciando o mesmo Pix como pendente **para sempre**. A regra certa já
está escrita na nossa base; esse caminho simplesmente não a usa.

**Reach medido em produção nesta ronda** (`profiles`):

| | |
|---|---|
| `pending_payment_at` não nulo | **131** |
| … mais velho que 3 dias (fora da janela do próprio `/app`) | **106** |
| … mais velho que 7 dias | **79** |
| … mais velho que 3 dias **e** sem `access_until` | **12** |

Os **12** são a população exata em que a Fast dirá *"você tem um Pix aguardando,
é só pagar"* pra quem está sem acesso e cujo código já morreu.

**Caso vivo:** Duarte, `pending_payment_at = 31/08 13:13` → **9 dias**, 3× fora
da janela. Hoje 09:00Z a Fast disparou o roteiro; às 09:04Z ele respondeu que a
referência **não é mais pagável**.

**Segunda metade do mesmo defeito:** `buildAccountContext` (`account.ts:196-283`)
não carrega **moeda nem país**. Duarte paga em **EUR** (Hotmart viva: rec#1 19
EUR COMPLETE 31/07, venda 22,04 EUR COMPLETE HP1178293240) e Pix não existe pra
ele. Não é conta isolada: o commit `2fff555` já registrou 30 `PURCHASE_APPROVED`
em EUR, e o `#214` (zicasantos) foi outro comprador PT com Millennium BCP.

**As três checagens da ordem de 27/08 §3, feitas antes de abrir:**

1. **Já existe?** Varri os 47 abertos + as signatures da base. Nenhum card cobre
   `pending_payment_at`. O `#318` é o card de **atendimento** do Duarte, não do
   defeito; o `#315` é a mesma família em **outro campo**. Os 4 "parecidos" que
   a busca de duplicata trouxe (`#318`, `#311`, `#239`, `#49`) são todos cards de
   aluno individual — o `#311` (Hugo) é Pix **recente**, dentro da janela, classe
   diferente.
2. **Já foi corrigido?** `git log origin/main` desde 25/08 + os **28 PRs abertos**,
   lidos um a um. Nenhum toca `pending_payment_at` nem moeda no contexto da Fast.
   O mais próximo é o **PR #203** (`#290`) — mesma família, campo diferente.
3. **Dinheiro?** **Não se aplica**, e digo de propósito: o `#319` **não** acusa
   cobrança indevida, débito errado nem estorno faltando; nenhum `ref_id` está
   sendo contestado. É a armadilha que produziu o `#100`, o `#125` e o `#152`, e
   eu não a repito. Ainda assim há `arquivo:linha` **dos dois lados** da
   divergência, que é a prova que a ordem pede.

Teste de bolso: *se `account.ts:271` usasse a mesma janela de 3 dias que o
`layout.tsx:112-115` já usa, a Fast não teria dito a um comprador que ele tinha
um Pix pendente 9 dias depois de vencido.* **Sim** → chamado.

---

## FRENTE 1 — a fila inteira, sem filtro de assinatura

Consulta **sem** cláusula de `signature` (armadilha de 19/08).

**48 abertos**: **25 técnicos** e **23 de atendimento**. Eram **45** às 00hZ.
Todos em `investigating`; `status='open'` = **0** na base inteira.

A diferença, conferida uma a uma:

| | |
|---|---|
| **+3** abertos pelo Frank/Fast na madrugada | `#315` (00:49Z), `#317` (01:51Z), `#318` (09:05Z) |
| **+1** aberto por mim nesta ronda | `#319` (10hZ) |
| **−1** fechado | `#222` `3ca22d47` → `fixed` pelo Frank 01:52Z, sucedido pelo `#317` |

Alunos na fila: **132** somando com repetição (eram 133 às 00hZ; caiu 1 com o
`#222`), **109 distintos** — este segundo número é recorte **novo** meu, mais
estrito, e não deve ser lido como queda.

Idade: **3** com mais de 7 dias — `#312` e `#313` (91,7 d de `first_seen`) e
`#15` (40,9 d).

Os maiores por aluno atingido:

| chamado | desde | alunos | ocorrências |
|---|---|---|---|
| `c726c5ae` `vigia:convite-orfao-so-olha-produto-7851642` | 09/06 | 19 | 19 |
| `d3d8d1b2` `generation:capacity:executiontimeout` | **30/07** (40,9 d) | 18 | 19 |
| `f1ada07e` `assinatura_paga_em_dobro_duplicada_ativa` | 04/09 | 15 | 5 |
| `2d0509b4` `frank:entitlement-vitalicio-de-produto-de-curso` | 08/09 | 12 | 12 |
| `f8587cef` `tts:palavra_decapitada_fronteira_interna` | 02/09 | 10 | **609** |
| `446c3ae4` `sgp-boas-vindas-nega-assinatura` | 06/09 | 8 | 15 |

### Fechados que voltaram a disparar: **0**

Teste certo (`last_seen_at > resolved_at`, não "fechado com `last_seen` recente").
Na base inteira sai só o **`#8`** (`acf8acd6`, `training:user_dataset`, `fixed`
em 09/08, **último disparo 22/08** — parado há **17,5 dias**). Anotação, não
motivo para reabrir (14-A).

### Aguardando aluno: 12 — **os mesmos 12**, sem movimento

**8 passados de 7 dias:** `#99` (11,4 d), `#206` (8,9 d), `#207` (8,8 d),
`#214` (8,5 d), `#216` (7,9 d), `#224` (7,7 d), `#172` (7,7 d), `#229` (7,6 d).
Parado 7 d+ sem resposta pede **segunda tentativa**, não silêncio. Anoto; **quem
escreve é o Frank**.

## FRENTE 2 — a caixa, pra pegar o que não virou incidente

`ler_caixa.cjs --ultimos 25 --corpo 2500` — leitura pura (`BODY.PEEK`, só
`SEEN`). **Não toquei em não-lido.**

**Fila da Fast: 0 não-lidos** (`--fila`). Como o recorte `SEEN` cobre a caixa
inteira, "nada encontrado" aqui é **conclusivo**, não é ponto cego.

**6 mensagens novas desde as 00hZ** (uid 497 a 502), todas tratadas acima:

| uid | quem | o quê | destino |
|---|---|---|---|
| 497, 498 | Elane | cancelamento travado na tela da Hotmart | **grupo** (atendimento, sem chamado) |
| 499-502 | Duarte | quer pagar, Portugal/multibanco, Pix morto | `#318` já existia + **`#319`** aberto por mim |

**Aluno cobrando pela segunda vez: 2** (Elane e Duarte) — **ambos já cobertos**,
nenhum órfão. Nenhum *"a equipe vai verificar"* sem dono.

Antes de concluir "está tudo bem", conferi que **não é o cron morto**: **11
gerações nas últimas 8h, 11 `ready`, 0 `failed`, 0 presas**. Volume baixo é a
madrugada, não falha.

## Varredura e números de sempre

- **Pagante trancado: 0.** 0 na fronteira, 0 sem prova (**176** suspeitos
  conferidos um a um na Hotmart viva). Trancar está certo em 15 cancelados, 157
  inadimplentes e 4 trials que nunca viraram pagamento.
- **⚠️ ANTI-ALARME, repetido de propósito:** o `estorno_confere.cjs` segue
  imprimindo **"SEM ESTORNO CASADO: 4"** — `dd4b98a3`, `db811e2f`, `678267fe`,
  `e0de4212`. **Não é falha de estorno**: os 4 têm **ZERO linha** em
  `credit_transactions`, ou seja, nunca foram **debitados**; sem débito não há o
  que estornar. Um deles é `johnny.oliveirasp@gmail.com`, conta da casa. São de
  20 e 24/08 e não se mexem desde então. É exatamente o padrão que produziu o
  `#100`, o `#125` e o `#152`.
- **Lista de estorno em dia:** nenhum estorno faltando além dos 4 acima (que não
  são estorno).
- **Cenas do estúdio (48 h):** 37 `ready`, **3 `failed`** — as 3 são do mesmo
  usuário (`8467f5a0`), `broll`, **bloqueio de moderação**, e as 3 com
  `debit_ref` **NULO**. Sem débito, não há estorno a cobrar: é o padrão exato do
  `#152`. **Não é chamado.**
- **1 `training_jobs` `queued`** obsoleto, o mesmo de sempre (job `ebf5cc56`,
  voz `f4b9b0f2` já `ready`, de 25/08). 1.108 `completed`, 70 `failed` históricos.
- **`git log origin/main` desde as 00hZ: 3 commits** — `d708b62` (ronda 01hZ),
  `0c3e2f5` (relatório noturno), `7a26685` (ronda 02h). **Nada desde 01:53Z**, e
  isso é **esperado**, não cron morto: a janela do Executor é 08h–23h BRT e agora
  são 07h BRT. `gh pr list`: **28** PRs abertos (eram 27 às 00hZ).
- **Árvore de trabalho limpa** (`git status --porcelain` vazio).

## O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito ou
acesso, não editei `admin_emails`, não criei conta pra ninguém, não fechei
chamado, não reabri chamado, não mergeei PR, não escrevi código de produção, não
toquei em e-mail não-lido, não gastei GPU nem whisper.

Escritas da ronda: **1 chamado aberto** (`#319`, via script com ensaio + busca de
duplicata antes), **2 notas** (`#318` e `#315`, via `anotar_incidente.cjs
--confirmar`, que concatena e não sobrescreve), **1 aviso no grupo** e **2
arquivos no git** (este log + o script de abertura).

## Escopo respeitado

Ordem de **29/08**: nada da planilha lido, escrito, classificado, avisado ou
reprocessado; nenhum chamado de causa-planilha aberto ou reaberto — tudo que
esta ronda tocou é `profiles`, `entitlements`, o contexto da Fast (`account.ts`),
o `/app` e a caixa do `suporte@`, todos em produção. Ordem de **canal de 31/08**:
aviso da ronda postado **no grupo** (`notify-grupo.sh`) — nada no privado do
Johnny.
