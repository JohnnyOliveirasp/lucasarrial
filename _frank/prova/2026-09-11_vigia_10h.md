# Ronda do Vigia — 11/09/2026, 10hZ (07h BRT)

Sensor (14-A). Varri, medi, **abri 1 chamado** (`#351`) e **anotei 3** (`#265`,
`#350`, `#337`). Não respondi aluno, não escrevi rascunho, não mexi em crédito,
não fechei e **não reabri nada** — inclusive o `#337`, onde registrei objeção
com o status intacto em `fixed`.

Repo em `main`, `pull --ff-only` (já em dia). `_frank/ordens/README.md` e as
ordens de 27/08 e 29/08 lidas antes de tocar em qualquer coisa. Ordem de
**29/08** respeitada: nada da planilha lido, escrito, classificado ou
reprocessado. Ordem de canal de **31/08**: o aviso desta ronda vai **no grupo**.

Janela coberta: **02hZ → 10hZ**, o vão da madrugada (turno noturno desligado por
ordem de 27/08). **Nenhum e-mail novo e nenhum commit** nesse período.

---

## 1. 🔴 DECISÃO DO JOHNNY: o prazo do Marcelo fecha HOJE, em ~14h

**É a mesma forma do caso Victor, e ainda dá tempo de não repetir.**

O `#265` carrega, desde **10/09 11:54Z**, um item explícito de alçada sua:

> *"(a) autorizar a devolução dos R$ 97 do Marcelo — pedido feito por ele em
> 09/09, DENTRO da janela, **último dia 11/09**"*

**Hoje é 11/09.** Medido agora (`now()` = `2026-09-11 10:12Z`): a única compra
paga dele, `HP0618766977` (R$ 97, APPROVED 05/09 14:17Z), tem
`product.warranty_date` = **`2026-09-12T00:00:00Z`**. **Restam ~14h.**

| o que | medido |
|---|---|
| pedido de saída dele | 09/09 19:37Z, uid 517, **dentro** da janela |
| evento de reembolso/chargeback | **zero** em `payment_events` |
| cartão | segue `investigating`, última nota há **~22h** |
| ele voltou a escrever? | não — 1 mensagem só na INBOX |

A casa fez a parte dela: o Frank escreveu pra ele em 10/09 (uid 1592), assumiu a
falha e **não prometeu reembolso**, porque a decisão não é dele. O que falta é
só a sua. Item (b) do mesmo cartão: encerrar a assinatura pra não cobrar de novo
em **05/10** — ação externa na Hotmart, ninguém aqui faz sozinho.

**Não estou pedindo o estorno nem afirmando que é devido.** Estou dizendo que o
relógio fecha nesta ronda e o item continua aberto.

### Por que ninguém viu isso nas três rondas anteriores

A varredura D-1 das 00h filtrou cartão por **palavra no título**
(`reembolso|cancel|duplicad|dobro`). O título do `#265` fala da janela de
garantia **no código** e não tem nenhuma das quatro palavras — então o Marcelo
caiu fora, e nem a ronda das 01h nem a das 02h o mencionam. **Rodei a mesma
varredura sem o filtro de título e ele aparece.**

## 2. A varredura D-1 sem filtro: não é 1 aluno no limite, são 5

A ronda das 01h calibrou o `#350` em **1 útil de 4** e acertou: filtro por
título traz ruído. Mas ele erra **nas duas direções** — também *perde* caso
verdadeiro. Rodei sobre **todos** os cartões abertos, incluindo
`aguardando_aluno`, sem filtro nenhum: **26 pares cartão-aluno** com garantia
viva, contra os 4 da versão filtrada.

**No limite de ~14h (12/09 00:00Z), cinco alunos:**

| aluno | cartão | situação |
|---|---|---|
| `marcelopersonalthe32@` | **`#265`** | 🔴 pedido de saída por escrito, decisão represada — **item 1 acima** |
| `leandro@aeroclubejf` | `#254` | ✅ avisado 2× (06/09 e 01hZ hoje); **bola com o aluno**, desfecho aceitável e registrado |
| `contato@fotoatleta` | `#226`, `#207` | qualidade de voz; `#207` em `aguardando_aluno` |
| `contatogrupoavip@` | `#288` | comprou com dois e-mails diferentes |

Os dois últimos **não** afirmei que são pedido de dinheiro — não abri o corpo
deles nesta ronda. Entram como *universo a filtrar*, não como fila de pendência;
ler a lista como pendência repetiria o erro de enquadramento das 00h.

**Limites declarados:** (i) cruzei por `affected_emails` — aluno citado só no
corpo não aparece; (ii) usei `max(warranty_date)`, enquanto a produção usa de
propósito a janela que **fecha primeiro** (`garantia.ts:88`), então minha lista é
mais generosa que a regra da casa; (iii) **não** apliquei o critério (c) da ronda
das 01h (descontar quem já foi respondido); (iv) não abri a Hotmart viva.

## 3. O chamado que abri: o meu próprio instrumento está cego — `#351`

**A classe do `#337` morreu na produção e sobreviveu na ferramenta com que eu
leio a caixa toda ronda.**

`ler_caixa.cjs:182` ainda **adivinha** a fronteira MIME por formato de linha, em
vez de ler o `boundary=` declarado. É a mesma regex que o `#337` tirou de
produção em `mail-charset.ts` (commit `a90e9b0`, PR #232). O Gmail abre todo
encaminhamento com `--------- Mensagem encaminhada ---------`, que casa no
palpite — e o aluno encaminha justamente **o nosso próprio aviso de cobrança**
quando está contestando dinheiro. O defeito não é aleatório: é correlacionado
com a mensagem que mais importa.

**Prova medida hoje:** `ler_caixa.cjs --de marcelopersonalthe32@gmail.com`
devolve **"(sem corpo em texto)"** para o uid 517 — a mesma mensagem que batizou
o `#337`, cujo `text/plain` tem 3.513 bytes e termina em *"Eu não quero mais
seguir no programa."* A produção corrigida lê 2.905 chars do mesmo arquivo.

**Alcance, com limite:** **1 em 100** nas últimas 100 mensagens `SEEN` do INBOX,
e a única é o uid 517. Taxa baixa, consequência alta. Não afirmo "1 na história".

As três conferências do §3 da ordem de 27/08, feitas antes de abrir: **já
existe?** não — `#248`/`#261` (ambos `ignored`) são a classe de *escolha da
parte*, outro defeito, e o `#337` fechou sobre `mail-charset.ts`. **Já foi
corrigido?** não — `git log` do arquivo para em `f3f3658` e nenhum dos 29 PRs
abertos o toca. **Dinheiro?** o cartão não afirma nada sobre dinheiro; é defeito
de instrumento, e os R$ 97 do Marcelo vivem no `#265`.

## 4. Continuam parados, sem novidade

| | idade |
|---|---|
| `#313` `2d0509b4` — 15 vitalícios de graça | **93,7 dias** (17ª ronda pedindo) |
| `#312` `c726c5ae` — saiu pra `aguardando_aluno` às 01:51Z, **de propósito** (2ª tentativa vence 15/09) | — |
| `#331` `3528dd59` — Mastroianni, reposição de crédito | ~33h |
| `#341` `b633b18c` — 147.350 (16 pessoas) | **21h** |
| PR **#92** em DRAFT | **14 dias**, 29 PRs abertos |

---

## O que eu ANOTEI

| onde | o quê | notas |
|---|---|---|
| `71410a81` (`#265`) | o prazo de ~14h com `now()` medido, a ausência de evento de reembolso, e por que o filtro por título o escondeu | 8 → **9** |
| `3a9a4854` (`#350`) | a varredura sem filtro (26 pares, 5 no limite), a evidência de que o filtro por título perde caso verdadeiro, e os 4 limites do instrumento | 3 → **4** |
| `dfdee26f` (`#337`) | **objeção ao alcance do fechamento** — status mantido em `fixed`, sem reabrir | 0 → **1** |

## Aluno cobrando mais de uma vez

- **Gustavo Sperandio** (`gusperandio2@`) — escreveu 4× em 10/09 sobre o áudio
  acelerado/entonação. ✅ **Já tem cartão** (`#344`, aberto, 9 notas) — §3.1,
  **não abri outro**. A casa respondeu e prometeu técnico; segue aguardando.
- **Victor** (`#309`) — respondido; o que sobra é dinheiro na Hotmart, prazo já
  vencido em 11/09 00:00Z.
- **Marcelo** (`#265`) — item 1 desta ronda.
- **Mastroianni** (`#331`), **Alana** (`506b7c3a`), **Valdeni** (`#338`) — sem
  novidade nesta janela. Novo bounce de caixa-cheia do Valdeni (uid 568,
  10/09 22:08Z), já é a classe conhecida do cartão.

## Números da ronda

- **71 incidentes abertos**: **49 de aluno**, **22 técnicos**.
  (`fast` 26 + `fast-help` 20 + `fast-sgp` 2 + `suporte@` 1 = 49;
  `frank` 16 + `vigia` 5 + 1 sem autor = 22.) **1 em `open`** (o `#351`, meu),
  **70 em `investigating`**.
- **A conta fecha:** 71 nas 00h **−1** (`#312` → `aguardando_aluno` às 01:51Z,
  movido pelo Frank **com motivo escrito**, regra 8 — não é fechamento e eu não
  reabri) **+1** (`#351`, meu) = **71**. Fora deles há **13** em
  `aguardando_aluno`: o número de cartões com gente esperando é **84**.
- **Nada fechado voltou a disparar.** `fixed`/`ignored` com `last_seen_at >
  resolved_at + 5min` sai **1**, o `acf8acd6` (`#8`, `training:user_dataset`),
  último disparo em **22/08** — velho e conhecido. O detector honesto pra classe
  que aquele não vê (*aberto com `resolution_note` preenchida*) dá **10 de 71**,
  igual às 20h, 22h e 00h — **nenhum voltou nesta janela**.
- Caixa: 100 mensagens lidas com `BODY.PEEK`, só `SEEN`. **Não toquei em
  não-lido.** **0 e-mails novos** desde as 00h — a mais recente segue sendo o
  uid 569 (Victor, 10/09). **Fila da Fast: 0 não-lidos**, o cron está vivo.
- **29 PRs abertos**, **0 mergeados**. **3 commits** na `origin/main` desde as
  00h, todos das rondas da madrugada (`a497042`, `75b09cf`, `45975cf`) — nenhum
  commit de código de produto nesta janela.
- 🧹 Higiene, **estável**: seguem **8 arquivos modificados** não commitados em
  `frontend/**/sgp*` e `frontend/messages/*`, mais os não rastreados em
  `_frank/rascunhos/`. **Oitava ronda seguida.** Não são meus, **não toquei**;
  commitei só o meu log.
- Custo desta ronda: leitura. Nenhuma chamada paga de visão, nenhum crédito,
  nenhuma GPU, nenhum e-mail enviado.
