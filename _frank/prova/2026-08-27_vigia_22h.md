# 27/08 22h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → já estava em dia.
Índice de ordens lido antes de tocar em qualquer coisa. Ordem vigente do
assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C) — só erro de SISTEMA
vira chamado. Também valem a `2026-08-20_dono_da_fila_e_fila_zerada.md`, a
`2026-08-20_REGRA_FINAL_CREDITO.md` (ENCERRADA — não reabri) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **20h UTC**. Janela desta: **~2h** (20:15Z → 22:15Z).
No meio dela rodou a **Rotina das Falhas das 21h e das 22h** (o Frank), que
mexeu na fila e abriu o `#164`.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **8** — 5 `atendimento` (`99`, `120`, `151`, `153`, `160`), 3 `técnicos` (`11`, `161`, `164`) |
| Variação vs. 20h | eram 7; **−1** fechado (`157`), **+1** reaberto pela aluna (`151`), **+1** novo do Frank (`164`) |
| **Abertos por mim nesta ronda** | **0** — pela 14-C, nada da janela era erro de sistema novo |
| **Anotados por mim** | **2** — `52` (a faixa que quebrava finalmente foi exercitada) e `154` (objeção: promessa de reembolso sem dono) |
| **Fechados que voltaram a disparar** | **0** — por 3 réguas independentes (§4) |
| Fora da contagem | 7 em `aguardando_aluno` (`47`, `72`, `124`, `133`, `139`, `156`, `162`) |
| E-mails novos desde as 20h | **1** — uid 340 (Zethe, 21:07Z), **respondida 2×** (21:10Z e 21:48Z) |
| Fila de não-lidos da Fast | **0** (`--fila`). **Não toquei em não-lido** |
| Chat do app na janela | 20 mensagens, 10 de aluno, **3 alunos** — nenhum vira chamado (§5) |
| Gerações em 4h | **20 — todas `ready`**, 0 falhas, **0 presas** >30min |
| Falhas em 6h | 6, **todas anteriores ao deploy** (17:01Z–17:30Z) |
| Vozes em 24h | 14 `ready` + 1 `awaiting_training` (§6 — **não é bug**) |
| Cenas do estúdio / imagens presas | **0** e **0** |
| PRs abertos | **23** (eram 20 às 20h; entraram #73, #74, #75) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**, incluindo `fixing` e `investigating`.
Conferido: `status` distintos na tabela são `fixed` 109, `ignored` 32,
`aguardando_aluno` 7, `investigating` 6, `open` 1, `fixing` 1.

---

## 1. Uma correção na MINHA ronda das 20h: a Rosilene não estava esperando

A ronda das 20h escreveu que a Rosilene (uid 339, 19:01Z) estava **"sem
resposta até 20:15Z"** e a escalou pro Johnny como item de decisão (§8, item 5).

**Isso estava errado, e o erro foi meu.** Conferi agora o que a ronda das 20h
não conferiu — a caixa de **enviados**:

| | |
|---|---|
| e-mail dela | uid 339, **19:01:14Z** |
| resposta nossa | uid 203, **19:10:23Z** — **9 minutos depois** |
| é mesmo a resposta dela? | sim: mesmo assunto e responde as **5 perguntas numeradas** dela, uma a uma (WhatsApp, foto busto, roupas/penteado, onde está o clone, custo diário) |

A ronda das 20h olhou a caixa de entrada e concluiu "sem resposta" sem rodar
`--enviados` pra ela. **Lição pra próxima ronda: "aluno sem resposta" só pode
ser afirmado depois de olhar os ENVIADOS daquele endereço** — a caixa de
entrada sozinha não sabe o que a Fast respondeu.

**Sendo justo com a ronda das 20h no outro caso:** ela disse que o Marlon (uid
337) "só agradeceu". Fui conferir achando que era outro erro do mesmo tipo, e
**ela estava certa** — o uid 337 é literalmente "Obrigado". O que há de errado
no caso do Marlon é outra coisa, e está no §3.

---

## 2. `#164` — o chamado que o Frank abriu nesta janela é VERDADEIRO, conferi no código

A 14-C manda conferir se o que o Vigia aponta é real. Apliquei a mesma régua no
chamado que **o Frank** abriu às 21:50Z, porque ele nasceu dentro da minha
janela e é técnico.

**Confirmado, e a prova é uma linha de arquivo:**

```
frontend/messages/pt-BR.json:55    "createButton": "+ Gerar Nova Voz"
frontend/messages/pt-BR.json:1476  "rejectedCta":  "+ Treinar nova voz"
```

O **mesmo arquivo** chama a mesma ação de dois jeitos. O botão que abre o treino
diz "Gerar", e o resto do sistema diz "Treinar" — que é exatamente o que a aluna
procurou e não achou ("*não tem a opção treinar voz, já vim aqui várias vezes*").
Não é interpretação, é divergência literal no arquivo de tradução.

**Estado do conserto:** commit `446fdb3`, branch `fix/inc151-botao-treinar-voz`,
**PR #75 aberto**. Conferi por `git merge-base --is-ancestor`: **NÃO está na
`origin/main`**. Então o botão continua errado em produção, e o que o Frank
escreveu pra aluna ("vai passar a se chamar assim que a correção entrar no ar")
está honesto. **Sem objeção.**

---

## 3. `#154`/Marlon — promessa de REEMBOLSO num chamado fechado, sem dono

**Anotado no próprio `#154`. Não reabri, não mudei status** (14-A).

O `#154` foi fechado às **14:30:16Z**, `fixed`, por `carol (entregue ao time)`,
**0,83s depois** da última ocorrência — é um dos 5 casos da classe do `#153`,
que segue **aberto** com PR #73. O mecanismo já tem dono; não é isso que eu
objeto.

**O que eu objeto é o conteúdo.** Às 14:30Z a Fast prometeu ao aluno, por
escrito:

> *"Vou encaminhar seu pedido de cancelamento e **reembolso** para a equipe
> resolver isso com você o mais rápido possível."*

Marlon (`marlon@bianchitour.com`) mandou **5 e-mails entre 14:15Z e 14:39Z**
("*Eu pedi para cancelar essa mersa*", "*Não quero nada desse fast cloner não*").
Medido às 22h UTC: passaram **~8h**, o chamado está **fechado**, e a promessa de
reembolso **não aparece em nenhuma fila aberta**. Ninguém é dono dela.

**O que eu NÃO afirmo:** que houve cobrança indevida. **Não abri o código que
cobra e não casei `ref_id`** — pela 14-C, sem `arquivo:linha` eu não tenho base
pra afirmar nada sobre dinheiro, e não vou repetir o #152. Afirmo só o que medi:
**promessa de reembolso feita ao aluno, chamado fechado, sem dono.**

Reembolso é dinheiro, logo é decisão do Johnny. Vai no relatório, **não** vira
chamado meu (é atendimento pela 14-C). Existe **PR #4** aberto desde 18/08 que o
índice de ordens associa a este caso.

---

## 4. "Fechado que voltou a disparar": 0, medido por três réguas

Mantive as três réguas da ronda das 20h, porque o detector oficial tem ponto
cego conhecido (fecha 0,8–1,6s depois do disparo e nunca aparece):

| régua | resultado |
|---|---|
| detector oficial (`2026-08-20_fechados_que_disparam.cjs`) | 141 fechados examinados; **1** histórico (`training:user_dataset`), última ocorrência **123h atrás** — **0 vivos em 48h** |
| minha, **sem carência nenhuma** (`last_seen_at > resolved_at`, 72h) | **0 linhas** |
| minha, pelo padrão do auto-fechamento (fechado em **menos de 2s** depois do disparo, 48h) | **5** — `154`, `126`, `82`, `145`, `141`, todos `resolved_by='carol (entregue ao time)'` |

Os 5 da terceira régua são a classe do `#153`, que está **aberto e é dono dela**.
**Nenhum é novo nesta janela**: o mais recente (`154`) disparou 14:30Z, e a
janela começa 20:15Z. Conferi também todos os fechados com `last_seen_at` nas
últimas 24h (13 deles): em **todos**, o `resolved_at` é posterior ao último
disparo.

**Conclusão: 0 classes fechadas disparando** — pela régua e pelo mundo.

---

## 5. FRENTE 2 — a caixa e o chat, e por que nada virou chamado

`ler_caixa.cjs --ultimos 25 --corpo 2500`, **redirecionado pra arquivo e com os
uids contados antes de concluir qualquer coisa** (lição da ronda das 18h, em que
o truncamento do harness engoliu os e-mails mais novos). Desta vez: `exit=0`,
**36.192 bytes, 25 uids (316–340)**, nada faltando.

**1 e-mail novo: uid 340, 21:07Z, Zethe Castro** — "*não consegui introduzir os
áudios para serem clonados. Preciso de ajuda*". **Já tem dono e já foi
respondida duas vezes** (21:10Z pela Fast, 21:48Z pelo Frank, com a causa do
`#164`). Ela é a dona dos chamados `#151` (aberto) e `#164`. **Nada a abrir.**

**Chat do app — 3 alunos na janela, nenhum vira chamado:**

| aluno | o que disse | por que não é chamado |
|---|---|---|
| `zicasantos37` (20:45Z) | "*já fiz a assinatura dos R$97 e pede pra assinar cada passo*" → depois: "*vi agora, o pagamento na hotmart ainda está pendente. Ok, vou aguardar*" | **resolveu sozinha e o sistema agiu certo**: pagamento pendente, gate bloqueia. Se o código estivesse certo, aconteceria igual |
| `instrutormarciopaz` (21:10Z) | "*QUERO CANCELAR*", "*SEMPRE SAI ROBOTIZADO, FALTA DE SINCRONIA LABIAL*" → depois: "*ACHEI [o botão]..OBRIGADO*" | **atendimento/churn** pela 14-C. A queixa de qualidade é a mesma classe do `#156`/`#162`, já abertos. Ver §7 |
| `casamaecohab` (20:23Z) | "*COMO EU CONECTO MINHA CONTA COM O HEYGEN*" | já respondida no e-mail das 21:48Z ("integração não é nossa") |

**Varredura por sinal de escalação silenciosa** ("a equipe vai verificar", "vou
chamar alguém", aluno cobrando 2ª vez) nos 25 e-mails: os achados são Luziélia,
Fábio, Marlon, Rosilene e Zethe — **todos de fora da janela e todos com dono**
(`157`/`158` fechados hoje, `154`, `151`, `164`). O único sem dono real é a
promessa de reembolso do Marlon, no §3.

---

## 6. A voz em `awaiting_training`: quase abri chamado, e teria sido falso

A varredura devolveu **1 voz em `awaiting_training` criada 21:10Z** (63 min,
**17 arquivos**, sem erro) — bem no horário em que a Zethe estava sendo tratada.
Cheirava a voz presa com o aluno esperando, e a `awaiting_training` é um estado
que a varredura oficial **não enxerga** (é o que o PR #15 conserta, aberto desde
20/08). Ia abrir.

**Fui conferir antes, pelas três checagens do §3 da 14-C, e não é bug:**

1. **De quem é:** `luis.felipe.bertozo@gmail.com` — **não é a Zethe**. A
   coincidência de horário era só coincidência.
2. **O que o estado significa:** abri o código. `awaiting_training` é estado
   **legítimo de espera pelo clique do aluno** — o áudio passou na validação e o
   treino só dispara quando ele clica. A própria tela
   (`voice-cloning/page.tsx:142`) diz que houve voz parada "*até 43 dias em
   awaiting_training sem clicar*". 63 minutos é latência normal de gente.
3. **Já tem PR:** a cegueira da varredura pra esse estado é o **PR #15**.

**Nada aberto.** Terceira ronda seguida em que a checagem do §3 segura um
chamado falso.

**Mas o número merece registro, como ANOTAÇÃO e não como chamado:** há
**17 vozes** em `awaiting_training`; **14 delas com áudio válido (sem erro) e
mais de 24h**, de **13 alunos distintos**, a mais velha com **44,8 dias**. São
alunos que subiram o áudio, passaram na validação e **nunca tiveram voz** porque
o fluxo termina num botão que ninguém clicou. Não é defeito de código —
é desenho — mas é gente parada, e ninguém mede isso hoje.

---

## 7. `#52` — a faixa que quebrava finalmente foi exercitada (anotado)

**Anotado no `#52`. Status inalterado (`fixed`).**

A ronda das 20h registrou, com razão, que o fix do PR #70 tinha **sinal e não
prova**: 7 gerações pós-deploy, nenhuma na faixa que falhava (1500+ chars,
7,56% de falha em 14 dias). Medi de novo, janela `>18:56Z`:

| | |
|---|---|
| gerações pós-deploy | **18 — todas `ready`, 0 falhas** |
| maior texto | **1825 chars** |
| na faixa 1500+ | **1** — e passou |

**A faixa foi exercitada uma vez e passou.** Isso sai de "não testado" e vira
**evidência fraca a favor** do fix. **Continuo não chamando de prova:** 1
amostra contra uma taxa de 7,56% não decide nada sozinha.

As 6 falhas de hoje (todas `qa_coverage`, campo `error_message` lido cru) são de
**17:01Z a 17:30Z — todas anteriores ao deploy**. Nenhuma falha nova desde
então, em 5h.

**A pendência da própria nota de fechamento segue aberta:** refazer o texto do
Ronald por conta da casa **não foi feito** — a última geração dele continua
sendo a de 17:36Z, 400 chars. Como o chamado está `fixed`, essa promessa não
aparece em fila nenhuma. É o mesmo padrão do §3.

---

## 8. Situação de cada aberto

| # | cat. | idade | alunos | onde está travado |
|---|---|---|---|---|
| `11` | técnico | **887h (37,0 dias)** | 3 | migration `scripts/97` não aplicada. Bloqueio único: **aval** |
| `99` | atendimento | 101,4h | 1 | Luciano, áudio de 30+ min por link |
| `120` | atendimento | 83,9h | 1 | Sandra, pré-venda (prazo alegado: 30/08) |
| `151` | atendimento | 9,9h | 1 | Zethe — **reaberto 21:10Z** pela aluna; tratado às 22h, PR #75 em aval |
| `153` | atendimento | 7,9h | 5 | causa medida; **PR #73** em aval |
| `160` | atendimento | 3,5h | 1 | Telma, mais movimento no Vídeo Clone (pedido de produto) |
| `161` | técnico (`fixing`) | 3,3h | 4 | 190 entitlements `active` com `subscription.status=CANCELED`; espera aval do backfill |
| `164` | técnico | 0,3h | 1 | conferido no §2; **PR #75** em aval |

**5 de 8 são `atendimento`** — a fila continua medindo atraso de gente. É o
passivo que a 14-C corrige daqui pra frente, não retroativamente.

---

## 9. O que precisa de DECISÃO do Johnny

1. **Marlon — reembolso prometido às 14:30Z, sem dono há 8h** (§3). Dinheiro,
   logo não é minha alçada, e o chamado está fechado. PR #4 relacionado, aberto
   desde 18/08.
2. **Marcio da Paz cancelou hoje 21:10Z citando qualidade** ("*sempre sai
   robotizado, falta de sincronia labial*"). Conferido: acesso ativo até
   **26/09**, **154.169 créditos**, o que **bate com a regra final de crédito**
   (não é bug). Mas é churn com motivo declarado, e o motivo é a mesma queixa
   do `#156`/`#162`.
3. **23 PRs abertos**, o mais velho de **18/08**. Três nasceram hoje (#73, #74,
   #75) e os três destravam chamado aberto — o **#75** é o `#151`/`#164`, com
   aluna esperando agora.
4. **Migration `scripts/97` — trava o `#11` há 37 dias.** Reconferida: segue não
   aplicada.
5. **`#161` — backfill dos 190 entitlements**, parado em aval desde 18:51Z.
6. **`#52`: a prova real prometida não foi feita** (§7) — refazer o texto do
   Ronald por conta da casa. Gasta GPU, então não é minha alçada.
7. **14 vozes de 13 alunos paradas em `awaiting_training`**, a mais velha com
   44,8 dias (§6). Não é bug, é desenho — mas são 13 pessoas que subiram áudio e
   nunca tiveram voz. Decisão: avisar essas pessoas ou não?

---

## 10. Objeções e limites desta ronda

- **1 objeção registrada**, no `#154` (§3): fechamento não cobre a promessa de
  reembolso. Anotada no próprio chamado, **sem reabrir**.
- **1 erro meu da ronda anterior, corrigido no §1:** a Rosilene não estava sem
  resposta; foi respondida 9 min depois. Escalei ela ao Johnny à toa às 20h.
- **1 chamado falso que eu não abri** (§6): a voz em `awaiting_training` era
  estado legítimo, de outro aluno, e já tem PR.
- **Uma coisa que eu declarei como NÃO provada:** que o fix do `#52` funciona.
  1 geração na faixa que quebrava não é prova.
- **Uma coisa que eu me recusei a afirmar:** qualquer conclusão sobre cobrança
  indevida do Marlon. Não abri o código que cobra nem casei `ref_id` — sem isso,
  pela 14-C, eu não falo de dinheiro.
- O `#156` e o `#162` (queixas de voz clonada) seguem fora da contagem por
  estarem em `aguardando_aluno`. Ambas dependem de **ouvido humano**, que pela
  ordem nunca é veredito meu. A queixa do Marcio (§9.2) é da mesma natureza e
  **também não é veredito meu** — eu não ouvi o áudio dele.

## 11. O que eu NÃO fiz

Não abri chamado nenhum — pela 14-C, nada na janela era erro de sistema novo.
Não fechei nem reabri incidente. Não respondi aluno nem escrevi rascunho —
**inclusive Marlon, Marcio, Zethe e Rosilene**, que são os nomes concretos desta
ronda. Não toquei em crédito, acesso, assinatura nem estorno: só **conferi** o
extrato do Marcio. Não toquei em e-mail não-lido (fila da Fast: 0). Não apliquei
migration — em particular **não apliquei a 97 nem a 82**. Não mergeei PR (em
particular **não mergeei o #75**, mesmo com aluna esperando). Não gastei GPU,
não refiz áudio, não retreinei voz. Não mexi em cron nem em ordem. Não escrevi
código: li `incidents`, `generations`, `credit_transactions`, `profiles`,
`voices`, `help_messages`, `studio_scenes`, `image_generations`,
`information_schema`, e rodei `ler_caixa.cjs` (entrada, enviados e `--fila`),
`aluno.cjs` e o detector `2026-08-20_fechados_que_disparam.cjs`. As duas únicas
escritas foram as **2 anotações** (`#52` e `#154`), via `anotar_incidente.cjs`,
que concatena e não sobrescreve.
