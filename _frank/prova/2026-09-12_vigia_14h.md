# Ronda do Vigia — 12/09/2026, 14hZ (11h BRT)

Sensor (14-A). Varri, medi, **abri 1** (`#365`) e **anotei 1** (`#308`). Não
respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei e não
reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido,
escrito, classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**.

Janela coberta: **12:13Z → 14:11Z**. `now()` medido no banco = **2026-09-12
14:11:14Z**.

---

## 1. 🔴 DECISÃO DO JOHNNY — o Rodrigo tem 9,7h de garantia e pediu devolução total DUAS vezes

Vai em primeiro lugar porque é o único item da ronda com relógio correndo.

`rodrigo.limas.1978@gmail.com`, os cartões `#306` e `#363` (é **a mesma
pessoa**, contada duas vezes pela ferramenta). **RESTAM 9,7h** — eram **11,8h**
às 12hZ e **13,8h** às 10hZ. Três rondas seguidas medindo a mesma pessoa
descendo.

Ele escreveu **duas vezes** na caixa, e as duas estão lidas:

| uid | quando | o que ele diz |
|---|---|---|
| 586 | 11/09 22:37 −03 | *"Quero reembolso"* |
| 587 | 11/09 23:45 −03 | *"Devolução total por favor!"* |

A segunda é resposta ao nosso próprio e-mail que abriu a conta dele inteira pra
ele (R$ 741 + R$ 356,90 + mensalidade). Ele leu, e a resposta foi **devolução
total**. Não é dúvida, é decisão tomada do lado dele.

**O que eu NÃO faço:** não respondo, não escrevo rascunho e não toco em
crédito. Isso é 14-A e não muda. **O que eu registro:** a janela vence hoje, e
quem decide é o Johnny.

O terceiro dos "vence em 48h" é `evelyn.cheida@gmail.com` (`#341`), **restam
33,7h** — folga de mais uma ronda.

## 2. 🟢 A objeção que eu registrei às 12hZ foi ATENDIDA — e eu confiro, não acredito

Registro isto com o mesmo rigor com que reclamei. Às 12hZ eu anotei no `#364`,
**por antecedência**, que fechar o cartão pelo estado terminal deixaria a perna
do **aviso** sem dono. O cartão foi fechado às **13:49Z** — e o aviso foi junto.

**Conferido no código da `main`, não na nota de quem fechou.** Em
`frontend/src/lib/sgp/etapas.ts`, o ramo do fracasso deixou de terminar no
banco: ele agora chama `processarTransicao(...)` com `avisarAluno:
avisoSgpFalhou` e `escalar: escalarNoGrupo`, e a decisão mora em
`frontend/src/lib/sgp/fracasso.ts`, que é puro e tem teste. O cadeado atômico é
o próprio `.neq("status","falhou")` do update — só a chamada que **de fato**
virou o pedido recebe a linha e manda o e-mail.

Os três PRs estão na `main`: `786742d` (#246, estado terminal), `1aed583`
(#247, retry cobre moderação), `45f27c69` (#248, o aviso).

**A objeção mudou o desfecho.** É o que o §4 da 14-A existe pra fazer.

## 3. 🟡 DECISÃO DO JOHNNY — a metade que CULPA o aluno está no ar, a metade que conserta está parada no PR #249

Não é chamado novo, **e explico no §5 por que não abri**. É item de decisão.

O que está em produção desde as 13:49Z: o pedido que morre **manda e-mail ao
aluno** citando o motivo gravado em `erro`.
O que **não** está em produção: limpar o `erro` quando o pedido se recupera.
Isso é o **PR #249** (`feat/sgp-limpa-erro-recuperacao`), **aberto, não
mergeado** — o próprio Frank nomeou a sobra na nota de fechamento do `#364`.

Somando as duas: um pedido que falhou, foi recuperado e falhar de novo por
outro motivo pode mandar e-mail citando a causa **velha** — ou seja, **culpar o
aluno por uma falha nossa**, por escrito, num e-mail que ele paga pra receber.

**A exposição HOJE é ZERO, e eu medi antes de dramatizar:**

| medição | resultado |
|---|---|
| pedidos com `erro` preenchido e status ≠ `falhou` | **0** |
| pedidos em `falhou` | **0** |
| `sgp_pedidos` por status | 100 `dados` · 46 `pronto` · 35 `foto` · 13 `audio` |

Nenhuma linha está exposta neste instante. O risco é do **próximo** pedido que
falhar e recuperar. Custo de fechar: mergear um PR que já existe e já foi
escrito.

## 4. 🔵 ABRI O `#365` — o e-mail do código do SGP não diz qual tela nem dá link

**Provado, com `arquivo:linha`:** `frontend/src/lib/sgp/codigo.ts:29-34`. O
corpo inteiro do e-mail é o código mais *"Digite ele na tela pra continuar a
configuração do seu clone"*. **Nenhuma URL.** Quem fechar a aba, ler no celular
ou voltar depois não tem caminho de volta. O comentário na linha 6 do próprio
arquivo registra que o template do Supabase mandava um **link** e foi trocado
por código — na troca o link saiu e nada entrou no lugar.

**O gatilho veio de aluna pagante, em duas palavras:** Angela Cleomar
(`angelacleomar…@gmail.com`) respondeu ao próprio e-mail do código, uid 584,
11/09 20:32:01Z: ***"Qual tela??"***.

⚠️ **E aqui eu corrijo a MINHA primeira leitura, que estava errada.** Ia abrir
o cartão como "aluna travada". Fui ao banco antes: ela verificou o e-mail às
**20:32:38Z** — **37 segundos depois** de mandar a pergunta — e o pedido dela
(`4d0e4dd1`) seguiu pra `foto`. **Ela não travou.** Cartão dizendo que travou
seria falso positivo do tipo `#152`. O valor da mensagem dela é outro: é a prova
de que o e-mail não se explica sozinho — **e ela era o caso fácil, com a tela
aberta na frente**.

**Quem está exposto, medido às 14:05Z:**

| grupo | n | idade média |
|---|---|---|
| tem código pendente e **nunca verificou** (todos em `dados`) | **9** | **80,2h** (3,3 dias) |
| em `dados` que **nunca chegaram a pedir código** | 90 | 47,8h |

Os 90 abandonaram **antes** desta tela — **não os conto e não os atribuo a este
defeito**. O número do cartão é **9**.

⚠️ **Correlação, não causa provada, e está escrito assim no cartão:** eu não
demonstrei que foi a falta do link que travou os 9. Pode ser desistência, spam
ou código vencido. **Provado** está o defeito no arquivo; os 9 são o tamanho de
quem está exposto a ele. **Não afirmo nada sobre dinheiro** — nenhum `ref_id`
casado, nenhuma palavra sobre cobrança ou estorno.

**Controle positivo na mesma janela:** o pedido `97fd0520`
(`jlzpasqual@gmail.com`) nasceu 13:58Z e verificou **13:59:28Z** — 90 segundos,
já em `foto`. Quem está na tela passa. O defeito morde quem sai dela.

## 5. As três checagens da ordem de 27/08, feitas antes de decidir

**Para o `#365` (abri):**
1. **Já existe?** **Não.** Varri `incidents` por `title`/`signature` com
   "codigo", "qual tela" e "link", aberto **e** fechado. Os acertos de "link"
   são todos aluno mandando Drive/OneDrive — assunto diferente.
2. **Já foi corrigido?** **Não.** `git log origin/main -- sgp/codigo.ts`: um
   único commit, `9da13e4`, o que criou o arquivo. Dos **33 PRs abertos**,
   **zero** tocam `sgp/codigo.ts` (`gh pr list --json files`).
3. **Dinheiro?** Não afirmo nada. Sem `ref_id` casado e sem abrir o arquivo que
   cobra, eu não digo uma palavra.
4. **Teste de bolso:** *"se o código estivesse certo, isso teria acontecido?"*
   **Não** — e-mail transacional que pede ação numa tela é obrigado a levar o
   aluno até a tela. **Sistema, não atendimento.**

**Para o §3 (NÃO abri):** a checagem 2 mata o cartão. O conserto **já existe e
já está escrito**, no PR #249. Abrir chamado pra bug que já tem PR é pedir pra
alguém refazer trabalho pronto — é literalmente o exemplo da ordem. Vai pro
relatório como decisão, não pra fila.

**Para o §6 (NÃO abri):** classe aberta = ocorrência, não cartão (foi ignorar
isto que fez o `#112` nascer duplicata).

## 6. Anotei o `#308` — voltou a disparar, e o contador do cartão está congelado

Quatro bounces novos *"Address not found … The email account that you tried to
reach does not exist"* para `victor@lucasarrial.com` — **uids 588, 589, 590 e
591**, todos 12/09 07:00 PDT (~14:00Z), **dentro da janela**.

Não abri cartão: é a mesma classe do `#308`, que está aberto.

**O achado novo é o instrumento, não o bounce.** O cartão **não está sendo
realimentado**: `occurrences` segue em **6** e `last_seen_at` segue em **08/09
18:17Z** — parado há 4 dias enquanto a falha dispara todo dia. Quem abrir a fila
lê *"seis ocorrências, visto pela última vez em 08/09"* e **subestima o tamanho
e a atualidade**. E casa com a causa descrita no próprio cartão: o detector de
bounce descarta o retorno, então nada realimenta a assinatura — **o cartão é
cego pela mesma razão que o bug existe**.

**Número honesto: no mínimo 10** (6 medidos em 08/09 + 4 medidos agora). É
**piso, não total** — não varri 09, 10 e 11/09 e não invento o que não contei.

## 7. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0**;
- **e-mail novo na janela: 4**, e os quatro são os bounces internos do §6.
  **Nenhum e-mail novo de aluno**;
- **nenhum aluno cobrando pela 2ª vez** dentro desta janela. O Rodrigo cobrou
  duas vezes, mas em **11/09** — é o §1, já contado, não é fato novo desta
  janela;
- Fernanda Duarte (uid 585) respondeu *"Refeito e enviado"* ao nosso e-mail de
  falha nossa — **ela foi atendida**, não vira chamado;
- nada da Frente 2 precisou virar chamado além do `#365`.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). **Não toquei em não-lido.**

## 8. Geração: janela limpa

`created_at > 12:13Z`, quatro tabelas: **24 entregas, TODAS `ready`, zero
falha.** (`image_generations` 15 · `generations` 7 · `video_clones` 2 ·
`studio_scenes` sem linha nova.) Nada a abrir.

## 9. Placar

- **80 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **43 de atendimento**, **37 técnicos**. Mais **9** em `aguardando_aluno`.
  (Medido: **zero** em `status='open'` — os 80 são todos `investigating`. Por
  isso a contagem varre os dois status.)
- **Delta contra as 80 das 12hZ, reconciliado inteiro:** `−1` fechado (`#364`,
  `fixed` por frank às 13:49Z, técnico) `+1` aberto por mim (`#365`, técnico,
  14:08Z). 80 − 1 + 1 = **80**. Atendimento ficou em **43**, inalterado.
  **Nenhum chamado nasceu nesta janela além do meu.**
- **Fechados que voltaram a disparar: 0.** Recorte de 72h com
  `last_seen_at > resolved_at`: **vazio**. Na base inteira sai um só, o `#8`,
  última ocorrência em **22/08** — velho, não é reincidência nova.
- `garantia_na_fila.cjs` 14:1xZ — 80 abertos · 19 parecem pedido de reembolso ·
  78 pessoas · **controle positivo OK (2/2)** · **24** perderam a janela ·
  **3** vencem em 48h · **0** na renovação.
- **Os 24 que perderam a janela continuam sendo os mesmos 24 das 12hZ**, e
  **nenhum virou nesta janela**. A composição é a do §3 da ronda das 12hZ (18 do
  `#312` + 6 antigos); o dano é de 88 e de 36 dias atrás. **Ninguém perdeu nada
  nas últimas 2h.**
- **Calibragem do Vigia** (ordem 27/08 §5): dos **32** cartões com
  `reported_by='vigia'` desde 27/08, **4** `ignored` = **12,5%** (era 12,9% com
  31). Alvo ≤10%; linha de base 21%. (22 `fixed`, 6 `investigating`.) O
  movimento é o `#364` virando `fixed` e o `#365` entrando.

⚠️ O rótulo *"SEM LINHA NO NOSSO BANCO p/ este e-mail"* que a ferramenta imprime
continua **NÃO sendo veredito de pagamento** — é ausência de linha local.
**Nona ronda** registrando. Ninguém deve ler aquela coluna como "não pagou".

## 10. Nota de processo — não é chamado

- `main` segue com **arquivos do SGP modificados e não commitados**
  (`frontend/src/lib/sgp/sessao.ts`) mais os `_frank/rascunhos/` não rastreados.
  **Nona ronda** registrando. Não são meus e não toquei. Commitei **só** este
  log, por caminho explícito.
- O `#364` fechou **bem**: nota com o que era, o que foi feito, o que foi
  medido, e a sobra nomeada em vez de escondida. Registro porque nas rondas
  passadas eu reclamei do contrário.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado
para classe que já tem cartão aberto, não abri chamado para bug que já tem PR,
não abri chamado de dinheiro sem `ref_id` que case, e não toquei em e-mail não
lido.
