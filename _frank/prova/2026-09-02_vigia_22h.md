# Vigia — ronda de 02/09/2026, ~22hZ (19h BRT)

Sensor (regra 14-A + calibragem de 27/08). **Abri ZERO chamados e anotei em 3** (`d48e6a45`,
`702cc916`, `ce6e157d`). Não fechei, não reabri, não mudei status de nada, não respondi aluno,
não escrevi rascunho, não toquei em crédito, não mergeei PR, não apliquei migration.

Ordem de 29/08 respeitada: nada da planilha foi lido, escrito, classificado, avisado ou
reprocessado. Canal: relatório vai para o **grupo** (`notify-grupo.sh`), ordem de 31/08.

---

## 0. O que precisa de DECISÃO do Johnny

### 0.1 O Gravador foi consertado **duas vezes, por dois caminhos** — um está no ar, o outro está parado e pode derrubá-lo

Achado desta ronda. Medido por `git diff --stat`, não deduzido.

| | |
|---|---|
| **`fb06a43`** — 02/09 **21:07Z**, **direto na main**, já em produção | *"gravou = salvo na CONTA, não no navegador"*. Ordem do Johnny de hoje (*"a pessoa tem um tremendo trabalho e se perde"*). Novo `api/v1/voice-clips/route.ts` (136 linhas), `voice-recorder.tsx` **+131**, `voice-creator.tsx` **+45**, novo `lib/audio/clip-sync.ts`, os 3 `messages/*.json`. |
| **PR `#154`** — branch `feat/gravador-nao-perde-audio`, **OPEN**, **fora da main** | Mesmo defeito, **outro caminho**: `marca-gravacao.ts`, `entrega-gravador.ts`, `clip-store` no IndexedDB. `voice-creator.tsx` **+240**, `voice-recorder.tsx` **+141**, os **mesmos** 3 `messages/*.json`. |

**A base do `#154` é `cc73be3`** — **anterior** ao `fb06a43`. Os dois mexem no **mesmo par de
arquivos** (`voice-creator.tsx`, `voice-recorder.tsx`) e nos mesmos 3 arquivos de tradução.

Este é **exatamente** o padrão que o README das ordens já documentou **quatro vezes**:
`feat/onedrive-401`, `feat/fix-image-upload-retry` e os dois branches da cura de referência —
branch concorrente, sem PR mergeado, que se entrar **derruba o fix que está em produção**.

**A pergunta, em 1 linha:** o `#154` é **rebaseado em cima do `fb06a43`** (mergeando só o que ele
acrescenta: as marcas de gravação e os 3 arquivos de teste, que o `fb06a43` não tem) ou é
**fechado e o branch apagado no origin**?

⚠️ **O que eu NÃO afirmo:** não tentei o merge e **não afirmo que vai quebrar**. Afirmo a
sobreposição de arquivos, medida. Anotei no `#235` (que segue `investigating` — nota 8).

### 0.2 PRs #41/#42 (teto de 2MB) — décimo primeiro dia parados

Sem mudança desde as 20hZ: os dois seguem **OPEN**, conferido agora por `gh pr list`. É o conserto
que teria evitado o descarte dos prints da Alana ontem à noite. **Ela não está mais esperando** —
uid 428, 20:55Z: *"Dia 07 te chamo aqui e vc me diz no q já avançaram. Por hora, vou dar um tempo
nisso"* — mas o buraco no código continua aberto para o próximo aluno.

### 0.3 Migration 102 — mesma pergunta de 1 linha, sétima ronda

**Aplica a migration 102?** PR **#150** segue **OPEN**, `scripts/102_incidents_resolved_guard.sql`
segue **não aplicada**.

---

## 1. O que eu ABRI

**Nenhum chamado.** Os três achados desta ronda são objeção, medição de classe existente e
correção de número meu — os três destinos que a ordem de 27/08 define fora da fila.

**Por que o §0.1 não virou chamado** (checagens do §3 da ordem de 27/08):
1. **"Já existe?"** — sim, `#235` está **aberto** e é o chamado do Gravador. Nota nele, não chamado novo.
2. **"Já foi corrigido?"** — sim, `fb06a43` está na main. O risco é o **contrário** do normal: não
   é bug sem conserto, é conserto **a mais** disputando o mesmo arquivo. Isso é decisão de merge,
   humana — não passa no teste *"se o código estivesse certo isso não teria acontecido"*.

**`#237` e `#238` — não dupliquei.** A rotina das falhas das 22hZ (commit `b58e0f6`) já provou que
o `signature` do `#237` é `reported:${randomUUID()}` (`admin/incidents/route.ts:71`) e não
identifica ninguém — **conferi por conta própria**: `select from profiles where id =
'20a69a24-…'` devolve **0 linhas**, bate com o que ela mediu. E o `#238` (SGP perde foto/áudio por
corrida) já é dela, com PR **#156** aberto. Nada a acrescentar.

## 2. O que eu ANOTEI (sem reabrir, sem mudar status)

- **`d48e6a45` / `#235`** — o §0.1 inteiro, com os `git diff --stat` dos dois caminhos. Nota 8.
- **`702cc916` / `#226`** — a **oitava** medição da série. Nota 16.
- **`ce6e157d` / `#47`** — **correção de um número meu** (§6). Nota 39.

---

## 3. FRENTE 1 — todos os abertos, **sem filtro de assinatura**

Consulta: `status in ('open','investigating')`, **sem** cláusula de `signature`.

**7 abertos — 4 de aluno, 3 técnicos.** (eram 6 às 20hZ: `#223` fechou às 21h; nasceram `#237` e `#238`)

| # | idade | quem | o quê |
|---|---|---|---|
| `ce6e157d` `#47` | **346,0h** | katiasalvador32 | palavra "VOCÊ" no s34 (7 ocorrências) |
| `51d86460` `#232` | 11,9h | *(técnico)* | reabertura automática não limpa `resolved_at` |
| `f8587cef` `#234` | 5,9h | **237 alunos** | palavra decapitada em fronteira interna (609 gerações) |
| `d48e6a45` `#235` | 5,5h | alana_pinho | gravações aparecem mas não permitem envio |
| `3a703b12` `#236` | 3,2h | alessandropantera | animação de imagem saiu com áudio em inglês |
| `92b1cc85` `#237` | 1,5h | *(irrespondível)* | "não conta nada na plataforma", `affected_emails` **vazio** |
| `46283bd2` `#238` | 0,4h | wallanadaphiny | SGP perde foto/áudio por corrida (6 fotos viram 1) |

**Dois já têm conserto na main ou em PR:** o `#236` fechou o ciclo — PR **#155** foi **mergeado**
(`2f1e620`) nesta janela. O `#238` tem o PR **#156** aberto (21:58Z). Não abri nada para nenhum
dos dois.

**Crédito conferido** para os quatro alunos com incidente aberto — por
`ref_type like '%refund%' OR ref_type='estorno_de_engano'` (**não** por `kind`, armadilha de
20/08; **não** só por `generation_refund`, armadilha do `#129`):

```
alana_pinho          estornos 0 · falhas 0 · em curso 0
alessandropantera    estornos 4 · falhas 0 · em curso 0
katiasalvador32      estornos 4 · falhas 0 · em curso 0   <- ver §6, corrigi o numero
wallanadaphiny       estornos 0 · falhas 0 · em curso 0
```

**Nenhum aluno travado, nenhum estorno pendente.** Filas: `generations` presas há +2h = **0**;
`voices` em `training` paradas há +2h = **0**.

**`voices/awaiting_training` = 17**, a mais velha parada há **1218,8h (~51 dias)**
(`dacmedia16@gmail.com`; a segunda é `lucas.m.arrial@gmail.com`, conta do próprio time). **Não é
chamado**: `awaiting_training` é o estado em que a bola está com o **aluno**, que não terminou de
gravar — não passa no teste *"se o código estivesse certo isso não teria acontecido"*. Registro o
número porque o PR **#15** (*"varredura passa a enxergar voices/awaiting_training"*) está **OPEN
desde 20/08** e é exatamente quem daria dono a essa lista.

---

## 4. FRENTE 2 — a caixa

`ler_caixa.cjs --ultimos 25 --corpo 2200`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei em nenhum não-lido** — a fila é da Fast.

**Um só e-mail novo desde a ronda das 20hZ:** uid **428** (20:55:23Z), da Alana, respondendo ao
e-mail em que a equipe leu os 4 prints dela e resolveu o cancelamento:

> *"Dia 07 te chamo aqui e vc me diz no q já avançaram. Por hora, vou dar um tempo nisso.. MT
> trabalho a ser feito aqui."*

Isso **fecha o caso do §0.1 da ronda anterior pelo lado do atendimento**: ela foi respondida, os
prints foram lidos, e ela mesma marcou a data do retorno. **A bola está com ela, por escolha
dela.** Não abri nada, não escrevi nada.

**Nenhum aluno cobrando pela segunda vez sem resposta.** Nenhum *"já pedi ajuda várias vezes"*
novo. Nenhum *"a equipe vai verificar"* sem incidente correspondente.

---

## 5. Fechados que voltaram a disparar

Consulta honesta: `status in ('fixed','ignored') AND last_seen_at > resolved_at`.

**Um só:** `acf8acd6` (*"treino de voz: áudio insuficiente"*), fechado 09/08, última ocorrência
22/08 — 330h depois. **Não é reincidência ativa:** faz 11 dias que não dispara.

⚠️ **E esta consulta segue mentindo, pela quarta ronda.** O `#226` entregou **mais 7** vezes com
`exhausted>0` depois do próprio fechamento e **não aparece aqui**: `last_seen_at` dele está
congelado em 01/09 17:52Z, **anterior** ao `resolved_at` de 02/09 16:59Z, e `occurrences` parado
em **290** contra 324 reais.

**Não abri chamado por isso**, e a razão importa: a `signature` `qa_exhausted_entrega_silenciosa`
nunca foi ligada para auto-disparar — os números são calculados à mão. Não é código que quebrou,
então não passa no teste. Fica registrado como **limite conhecido do meu próprio detector**:
**"nenhum fechado disparou" vale para o que a `signature` consegue contar, não para tudo.**

---

## 6. A série do `#226`, oitava medição

Mesma consulta em todas (`generations status='ready'`, `qa` não nulo, **sem** cláusula de assinatura):

| medida | 00hZ | 12hZ | 14hZ | 18hZ | 20hZ | **22hZ** |
|---|---|---|---|---|---|---|
| entregues com `qa.exhausted>0` | 300/686 (43,7%) | 307/696 (44,1%) | 310/699 (44,3%) | 316/708 (44,6%) | 317/715 (44,3%) | **324/728 (44,5%)** |
| alunos distintos atingidos | 136 | 138 | 140 | 144 | 145 | **148** |
| últimas 24h | 32/66 (48,5%) | 30/63 (47,6%) | 32/64 (50,0%) | 28/52 (53,8%) | 23/46 (50,0%) | **29/55 (52,7%)** |

**Depois do fechamento das 16:59:00Z:** 29 entregas `ready` na janela, **12** com `exhausted>0`
(eram 5 às 20hZ). As **7 novas** desde a nota anterior, todas posteriores a 19:23Z:

| geração | hora | exhausted | dur. |
|---|---|---|---|
| `d9887f34` | 20:11:18Z | 1 | 40,2s |
| `09b94982` | 20:44:02Z | 3 | 20,4s |
| `82779d24` | 21:05:39Z | 2 | 25,5s |
| `56cbc620` | 21:09:57Z | 1 | 22,8s |
| `51a7599b` | 21:14:58Z | 1 | 21,3s |
| `2fef3d07` | 21:17:48Z | 1 | 20,2s |
| `36cca5dc` | 21:25:36Z | 2 | 11,1s |

`git log origin/main` desde 20:20Z: **4 commits, nenhum neste subsistema** (`2f1e620` = merge do
#155/áudio do Animar Imagem; `fb06a43` = gravador; `b58e0f6` e `10d6423` = registro de ronda).
O chamado segue fechado e o defeito segue entregando — agora por **4h30 seguidas** após o
fechamento.

**Ressalva que eu não escondo:** `exhausted>0` diz que o QA desistiu de melhorar N trechos — **não
prova** que o áudio final está audivelmente defeituoso. Afirmar defeito exige **ouvir**, e por
ordem de 27/08 ouvir não é veredito meu.

### 6-B. CORRIJO UM NÚMERO MEU: os "68 estornos" da Katia não existem

A minha nota das 20hZ afirmou `katiasalvador32 estornos 68`. **Está errado.** Rodando agora a
mesma consulta que aquela nota declara ter usado, dá **4**. E 68 é **aritmeticamente impossível**:
o total de linhas em `credit_transactions` dela, **de todos os tipos**, é **24** —
`image_generation` 8, `generation` 5, **`generation_refund` 4**, `image_video` 2, `payment_event`
2, `video_clone` 2, `voice` 1. Perfil único, sem duplicata.

**Não sei de onde saiu o 68 e não vou adivinhar** — não consigo reconstruir a consulta que o
produziu. O que está medido é **4**. A conclusão não muda (nenhum estorno pendente), mas o número
estava errado e foi repetido. Anotado no `#47` para quem herdar a ronda.

**Quarto número meu que corrijo hoje** (16hZ → 18hZ → 20hZ na calibragem, e agora este).

---

## 7. Calibragem da ordem de 27/08 (a medição vence amanhã, 03/09)

```sql
select status, count(*) from incidents
 where reported_by='vigia' and created_at >= '2026-08-27' group by 1;
```

| status | n |
|---|---|
| fixed | 14 |
| ignored | 2 |
| investigating | 2 |

**`ignored` = 2 de 18 = 11,1%.** Alvo **≤10%** — está a **um chamado** de bater. Sem mudança desde
as 18hZ (não abri chamado nesta ronda nem na anterior, então o denominador não anda).

**Chamados com `categoria='atendimento'` ou `kind` `processo`/`atendimento`: 3** — `#150`
(`processo:canal_sem_dono`), `#151` (`atendimento:voz_reprovada_pela_aluna_sem_dono`) e `#153`
(`categoria='atendimento'`). Mantenho a leitura das 20hZ: os dois primeiros são de **27/08
12:17Z**, *antes* da ordem (13:40); só o `#153` (14:15Z) é posterior, e pelo **conteúdo** ele é
defeito de sistema legítimo — o errado nele é o **campo `categoria`**, não o chamado. Registro as
duas leituras em vez de escolher a que me favorece.

---

## 8. O que eu NÃO fiz

Não abri chamado. Não fechei nem reabri incidente. Não mudei status de nada. Não respondi aluno
nem escrevi rascunho. Não mexi em crédito, voz ou áudio. Não mergeei PR (`#41`, `#42`, `#150`,
`#154`, `#156`, `#157`) nem apliquei migration (102). Não tentei o merge/rebase do `#154` — só
medi a sobreposição de arquivos. Não ouvi nenhum áudio: toda afirmação aqui é de telemetria,
cronologia e `git diff`, nunca de escuta. Nada da planilha foi tocado (ordem de 29/08).
