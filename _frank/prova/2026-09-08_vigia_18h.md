# Ronda do Vigia — 08/09/2026, 18hZ (15h BRT)

Sensor. Varri, medi, **abri 1** e **anotei 1**. Não respondi aluno, não escrevi
rascunho, não mexi em crédito nem em `admin_emails`, não fechei e não reabri
nada (14-A).

Repo em `main`, `pull --ff-only` (já estava em dia). `_frank/ordens/README.md` e
`_frank/rotinas/e-mail_tecnico.md` lidos antes de tocar em qualquer coisa.
Ordem de 29/08 respeitada: **nada da planilha** foi lido, escrito, classificado,
avisado ou reprocessado, e nenhum chamado de causa-planilha foi aberto ou
reaberto. Ordem de canal de 31/08: aviso desta ronda vai **no grupo**.

---

## O que precisa de DECISÃO do Johnny

### 1. 🔴 Tem um endereço da EQUIPE que não existe recebendo aviso da casa há 4 dias — e o sistema joga o retorno fora

Abri o **#308** (`20ba24a1`). É a única coisa nova da ronda, e é erro de
sistema pelo teste da ordem de 27/08 §2.

**O que está medido, e não é inferência:**

| | |
|---|---|
| Bounces na caixa do `suporte@` | **6** — uid 472 (07/09 07:00 PDT) e uids **485, 486, 487, 488, 489** (08/09 07:00 PDT, ~14:00Z) |
| Texto do servidor | *"Address not found … The email account that you tried to reach does not exist"* |
| Destinatário | `victor@lucasarrial.com` |
| Onde ele está cadastrado | `public.admin_emails`, role **suporte**, `added_by` **lucas.m.arrial@gmail.com**, `created_at` **04/09 18:27:47Z** |
| `profiles` com esse e-mail | **0** — não é aluno, é linha de allowlist |
| Chamados que esses 6 bounces geraram | **ZERO** |

**Quem manda:** `frontend/src/lib/payments/aviso-orfao-canal.ts:95-102`
(`mandarPorEmail`) monta `destinos = SUPPORT_EMAIL + toda a allowlist
admin_emails` e chama `sendEmail` com um único `to`. Mesmo padrão em
`frontend/src/lib/payments/orphan-outreach.ts:240`.

**Por que ninguém soube — o defeito, com `arquivo:linha`:**

1. `mail-bounce-registro.ts:53` põe `@lucasarrial.com` em `enderecosInternos()`
   → `victor@` entra como `interno: true`.
2. `mail-bounce.ts:145` classifica o retorno como `inexistente`.
3. `mail-bounce.ts:354-358` define `ORIENTACAO.inexistente.nossa = false`.
4. `mail-bounce.ts:465-473` (`planoDoBounce`): sem aluno no bounce, ele só
   procura interno cuja classe tenha `nossa = true`; não achando, retorna
   `{ alunos: [], interno: null }` — **o bounce é descartado em silêncio**.

O desenho é proposital (o comentário nas linhas 469-471 diz que só é sinal
"quando a culpa é nossa"), e o buraco está exatamente aí: **endereço interno que
não existe é 100 % cadastro nosso**, nunca se resolve sozinho e repete todo dia.
Bounce de **aluno** com essa mesma classe abre chamado (`132f7808`, `94d3015d`);
o interno some.

**O que isso custa, sem exagero:** quem está nessa linha da allowlist (perfil de
suporte, cadastrado em 04/09) **não recebe nenhum aviso da casa desde então**, e
ninguém foi informado. Como o envio é **um único `to` com vários destinatários**,
os **demais destinatários receberam** a mensagem normalmente — o chamado **não**
afirma que o aviso de órfão falhou para a equipe toda.

**Não afirmo nada sobre dinheiro** (§3.3): nenhum crédito, débito, cobrança ou
estorno foi tocado por este achado, e nenhum aluno ficou sem resposta por ele.

**A pergunta em 1 linha:** tira/corrige a linha `victor@lucasarrial.com` da
allowlist, ou o detector passa a levantar bounce interno `inexistente` — ou os
dois? Eu **não** mexi em `admin_emails`.

Checagens §3 feitas **antes** de abrir:
1. *Já existe?* **Não.** Nenhum incidente, aberto ou fechado, com assinatura
   `fast-bounce:interno:*` ou título de bounce de admin. O `b2651a6f`
   (`email:sem-registro-de-envio-e-bounce-ignorado`, **fixed** 24/08) fechou só a
   metade *"sem registro de envio"* (grava cópia em Enviados via IMAP APPEND) —
   a nota de fechamento dele **não trata bounce de destinatário interno**.
   Classe inédita.
2. *Já foi corrigido?* `git log origin/main` de hoje até 18hZ = `8251bb0`,
   `91c2981`, `0adb9e2`, `68f69c0`, `4075b84`, `88f4c54`, **todos de prova/ronda**,
   nenhum toca `mail-bounce`. `gh pr list` = **26 PRs abertos**; os de e-mail
   (#193 dedupe de envio, #188 corpo vazio, #187 teto de anexo, #186 trava por
   Message-ID) **não** tocam `planoDoBounce` nem a lista de internos.
3. *Dinheiro?* Não se aplica — ver acima.

### 2. 🟡 Nada mais precisa de decisão nesta ronda

Os dois casos que a ronda das 16h levantou (Edésio / `grupouniprox`, nota 4 do
`03e7b34b`; e a queixa técnica da Francislaine dentro do `09a26f8b`) **seguem
como estavam** — não houve e-mail novo nem mudança de estado deles em 2h.

---

## O que eu ABRI e ANOTEI

| # | onde | o quê |
|---|---|---|
| **abri** | **#308** `20ba24a1` — `vigia:bounce-interno-inexistente-descartado` | técnico, `open`, 6 ocorrências, 1 endereço afetado. Descrição com os 4 `arquivo:linha` e as 3 checagens §3. |
| **anotei** | **#305** `54c14038` (`frank:aviso-orfa-nao-disparou-UKC2COC2`) | nota de **contexto sobre a prova**, não de causa: o canal de e-mail desse mesmo aviso tem destinatário morto desde 04/09, então *"ninguém da equipe recebeu"* **não prova sozinho** que o aviso não saiu. Deixei explícito que **não medi** o contrário do que o #305 afirma. Via `anotar_incidente.cjs --confirmar` (concatena, não sobrescreve): 3 → 4 notas. |

---

## FRENTE 1 — a fila inteira, sem filtro de assinatura

Consulta **sem** cláusula de `signature` (armadilha de 19/08 — o filtro
`fast-email:%` já reportou "0 abertos" existindo quatro).

**38 abertos** (`open` + `investigating`), pela coluna `categoria`:
**19 de atendimento** e **19 técnicos**. Eram **37** às 16h (19/18); a diferença
é o **#308**, aberto por mim agora. **Ninguém mais abriu e ninguém fechou nada**
entre 16h e 18h.

Idade: **3** com mais de 7 dias, **14** entre 3 e 7 dias, **21** com menos de 3
dias. Somando `affected_emails` de todos: **94** e-mails de aluno na fila.

Os maiores por aluno atingido, sem novidade nesta ronda:

| chamado | desde | alunos | ocorrências |
|---|---|---|---|
| `d3d8d1b2` `generation:capacity:executiontimeout` | **30/07** (40 dias) | 18 | 19 |
| `f1ada07e` `assinatura_paga_em_dobro_duplicada_ativa` | 04/09 | 15 | 5 |
| `f8587cef` `tts:palavra_decapitada_fronteira_interna` | 02/09 | 10 | **609** |
| `446c3ae4` `sgp-boas-vindas-nega-assinatura` | 06/09 | 8 | 15 |
| `03e7b34b` `sgp-lote-04-09-sem-reconcile` | 06/09 | 7 | 7 |
| `3ca22d47` `frank:orfao-preso-conta-com-outro-email` | 01/09 | 5 | 5 |

### Fechados que voltaram a disparar

Teste certo (`last_seen_at > resolved_at`, não "fechado com `last_seen` recente"):
**nenhum** nas últimas 36 h. Os 6 fechados com `last_seen` recente
(`18bf275c`, `ff95507f`, `9acd4ede`, `a5027bb0`, `5b85cd18`, `adc3ed99`) todos
têm o último disparo **antes** do fechamento — é ordem normal, não classe morta
disparando. O único da base inteira que dispara depois de fechado segue sendo o
**#8** (`training:user_dataset`, `fixed` 09/08, último disparo **22/08**) —
velho, sem disparo novo, **anotação, não motivo para reabrir** (14-A).

## FRENTE 2 — a caixa, pra pegar o que não virou incidente

`ler_caixa.cjs --ultimos 25 --corpo 2500` — leitura pura (`BODY.PEEK`, só
`SEEN`). **Não toquei em não-lido.**

**Nenhum e-mail de aluno novo desde a ronda das 16h.** O mais recente segue
sendo o **uid 491** (Francislaine, 08/09 12:56 -0300 = 15:56Z), que já está no
`09a26f8b`, aberto pela própria Fast, com dono — não é meu e não mexi.

O que mudou na janela lida foram **5 mensagens** — e as 5 são os bounces do
`victor@` (uids 485-489), que viraram o **#308**.

Ninguém cobrando pela **segunda vez sem chamado**. Nenhum *"já pedi ajuda várias
vezes"* órfão. Todos os remetentes da janela têm chamado com dono:

| uid | quem | onde está |
|---|---|---|
| 482/483 | Emanuel Guerreiro | `7578c587` |
| 484 | Simone Andrade | `86de22c6` |
| 490 | Edésio / Grupo Uniprox | nota 4 do `03e7b34b` (ocorrência, §3.1) |
| 491 | Francislaine Wiczneski | `09a26f8b` (aberto pela Fast) |
| 485-489 | *(bounces do `victor@`)* | **#308**, aberto agora |

## O que eu NÃO fiz

Não respondi aluno, não escrevi rascunho de e-mail, não mexi em crédito ou
acesso, **não editei `admin_emails`** (a linha morta continua lá — a decisão é
do Johnny), não fechei chamado, não reabri chamado, não mergeei PR, não escrevi
código de produção, não toquei em e-mail não-lido, não gastei GPU nem whisper.

Escritas da ronda: **1 chamado aberto** (`#308`, `INSERT` único, número pela
sequence `incidents_numero_seq` — assinatura conferida como inédita antes de
gravar), **1 nota** (`#305`, via `anotar_incidente.cjs --confirmar`), **1 aviso
no grupo** e **1 arquivo no git** (este log).

## Escopo respeitado

Ordem de **29/08**: nada da planilha lido, escrito, classificado, avisado ou
reprocessado; nenhum chamado de causa-planilha aberto ou reaberto. Ordem de
**canal de 31/08**: aviso da ronda postado **no grupo** (`notify-grupo.sh`) —
nada no privado do Johnny.
