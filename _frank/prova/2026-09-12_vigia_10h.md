# Ronda do Vigia — 12/09/2026, 10hZ (07h BRT)

Sensor (14-A). Varri, medi, **abri 1 chamado** (`#364`) e **anotei 1** (`#363`).
Não respondi aluno, não escrevi rascunho, não mexi em crédito, não fechei e não
reabri nada.

Repo em `main`, `pull --ff-only` limpo (1 commit novo: `4534ccd`, só o log da
rotina das falhas de 01h). `_frank/ordens/README.md`, a ordem de **27/08** (só
erro de sistema vira chamado) e a de **29/08** (planilha desligada) lidas antes
de tocar em qualquer coisa. Nada da planilha foi lido, escrito, classificado ou
reprocessado. Ordem de canal de **31/08**: o aviso desta ronda vai **no grupo**.

Janela coberta: **00:15Z → 10:20Z**. `now()` medido no banco = **2026-09-12
10:10:56Z**.

**Por que a janela tem 10h e não 2h:** a ordem do turno da noite
(`2026-08-20_decisoes_55_trancadas_e_turno_noite.md`, item 2 — Executor 08h–23h
BRT) desliga a ronda de madrugada. 04hZ/06hZ/08hZ = 01h/03h/05h BRT, madrugada,
**corretamente puladas**. Isto não é cron morto e eu não vou registrar como
falha. Registro a **consequência**, que é real: o Rodrigo confirmou o reembolso
às 02:45Z e ficou **7,5h sem ninguém ver**. Ver item 1.

---

## 1. 🔴 DECISÃO HOJE: o Rodrigo confirmou por escrito e restam 13,8h

Este é o item que precisa do Johnny, e é o único da ronda.

`rodrigo.limas.1978@gmail.com`. Ele respondeu **duas vezes**, por escrito, na
caixa do suporte:

| uid | quando | o que ele escreveu |
|---|---|---|
| 586 | 12/09 **01:37Z** | "Quero reembolso" |
| 587 | 12/09 **02:45Z** | "**Devolução total por favor!**" |

Medido pelo `garantia_na_fila.cjs` às **10:12:12Z**: janela
`2026-09-06 → 2026-09-13T00:00:00Z`, **RESTAM 13,8h**. O mesmo relógio vale para
os **dois** cartões dele, `#306` e `#363` — mesmo e-mail, mesma janela.

O valor que ele pede de volta, **conforme o e-mail da Fast que ele respondeu**:
R$ 1.194,90 (SGP 741,00 + Fábrica de Conteúdo Invisível 356,90, ambos 30/08, +
mensalidade 97,00 em 06/09). ⚠️ **Eu NÃO conferi esses valores na base e não os
afirmo.** É o que a Fast escreveu a ele. Pela ordem de 27/08 §3.3, quem for
estornar casa `ref_id` antes — foi ignorando isso que o `#152` quase pagou
R$ 3.600 em dobro, e foi por isso que a ronda das 00hZ teve que desdizer o
R$ 297 do Wellington. **O que eu afirmo é a data**, e ela está medida.

Se passar sem decisão, ele vira o **7º** nome da lista "perdeu a janela enquanto
esperava na nossa fila". O Leandro (`#254`) virou o 6º **há 10,2h**, exatamente
assim: avisado, dentro do prazo, sem decisão, prazo fechou. Anotado no próprio
`#363` (nota 4).

Estado da fila: `#363` aberto 01:40Z pela Fast, última ocorrência 02:50Z. A
Carol avisou o time no WhatsApp 2× (notas 1 e 3). O executor triou às 02:25Z.
**Nenhum movimento desde então.**

## 2. 🔴 ABRI O `#364` — o SGP não tem estado terminal para a perna da foto

Achado novo da ronda, e é erro de **sistema** pelo teste de bolso da ordem de
27/08: se o código estivesse certo, isto não teria acontecido.

**Caso-prova:** `rafaelzan@me.com`, pedido `fe00d4e2`, comprador do SGP.

| campo | valor medido 10:12Z |
|---|---|
| `enviado_em` | 11/09 **17:37Z** (4 fotos, 2 áudios, origem `portal`) |
| `voz_pronta_em` | 11/09 **17:43Z** — a perna da voz fechou em 6 min |
| `foto_pronta_em` | **NULL** |
| `status` | **`processando` há 16,6h** |
| `erro` | **NULL** — nada carimbado, ninguém avisado |

O avatar dele (`image_generations` `e568b3cd`, `idea='onboarding_avatar'`) está
**`failed`** desde 17:37Z. `kie_raw_error`: *"your prompt was flagged by website
as violating content policies"* — moderação do Kie numa foto comum de pessoa.

**O defeito, com arquivo:linha.** Em `onboarding/pronto.ts:145-146`:

    const falhou = onboarding && !pronto && pendentes === 0 && !vozTreinando && vozMorta;

`falhou` exige **`vozMorta`**. Só a VOZ tem estado terminal; avatar `failed` não
entra nessa conta em lugar nenhum. Com a voz `ready`, `vozMorta=false`, logo
`falhou=false`. E `pronto` (linha 126-130) exige avatar pronto, logo
`pronto=false`. **Nem pronto, nem falhou: limbo.** Daí `sgp/etapas.ts:90` grava
`'processando'` — toda vez que rodar, de novo, para sempre — e `etapas.ts:101`
mostra a foto em **"andamento"** para uma imagem que morreu há 16,6h.

E não há nada que destrave sozinho: `estadoDasEtapas` só roda pelo polling da
tela e pelos webhooks (`etapas.ts:26-45`). Não existe varredor de pedido parado.
Fechou a aba e o webhook já passou, fica assim até alguém olhar na mão.

**É a mesma classe que já foi consertada — só que só para a voz.** O comentário
de 22/08 em `pronto.ts:54-58` descreve o defeito idêntico na perna da voz
("ficava 'Em Andamento' segurando a fila"). A correção daquele dia criou
`vozMorta`/`falhou` e cobriu **só a voz**. A foto ficou com o buraco aberto.
Parente do `#146` (guarda que julga pelo estado velho).

**Controle positivo, porque o número sozinho engana:** dos **30** pedidos com
`enviado_em` nos últimos 10 dias, **29 fecharam em `pronto`** com
`foto_pronta_em` carimbado em 1..13 min — inclusive o `hugolacunha` às 17:55Z,
**17 minutos antes** do caso-prova. O pipeline da foto funciona. O que falta é
o que fazer quando ele falha.

**Alcance honesto: 1 aluno.** Cruzei `sgp_pedidos` × `image_generations` na base
inteira (`prontos=0 AND falhos>0 AND pendentes=0`): sai **uma** linha, a do
Rafael. Mas é 1 por sorte do gatilho, não por estreiteza do defeito — o gatilho
é "o avatar do onboarding falhar", e todo pedido que cair nisso vai pro mesmo
limbo, em silêncio.

**O que eu NÃO afirmo:** não fiz conta de dinheiro. O `error_message` da própria
linha diz que o crédito foi devolvido automaticamente, e onboarding é conta da
casa (pode ficar negativo, autorizado 21/08). Não abri o arquivo que cobra,
então não tenho `arquivo:linha` do débito e **não afirmo nada sobre cobrança**.

**O aluno está calado.** Não escreveu pro suporte, não está na caixa. Está
olhando uma tela que diz "em andamento".

**As 3 checagens do §3 (ordem 27/08), feitas antes de abrir:**

1. **Já existe?** Não. Varri a fila aberta E fechada por sgp/processando/
   travado/preso — 25 cartões lidos. O mais próximo, `#238` "SGP perde foto/áudio
   em silêncio", é outra causa (confirmador lê-modifica-grava, fechado 02/09).
2. **Já foi corrigido?** Não. Zero commits em `origin/main` desde 00:15Z. Dos
   **32 PRs abertos**, nenhum toca `onboarding/pronto.ts` nem `sgp/etapas.ts`
   (conferido por `gh pr list --json files`).
3. **Dinheiro?** Não afirmo nada — bloco acima.

## 3. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0**;
- **3 mensagens novas** entre 00:15Z e 10:20Z, todas até 02:45Z:
  - uid **585** (00:45Z) — Fernanda Machado: *"Refeito e enviado. Obrigada."*
    **Conferi e o envio dela chegou:** pedido `59e5a0b8`, enviado 00:25Z,
    `pronto` com foto 00:27Z e voz 00:26Z. Nada a fazer, e é o controle
    positivo do `#364`;
  - uid **586** e **587** — Rodrigo, item 1. **Já existe cartão (`#363`)**, então
    é anotação, não chamado novo;
- **nenhum aluno cobrando pela 2ª vez** nesta janela;
- nada precisou virar chamado novo pela Frente 2.

Leitura pura (`SEEN` + `BODY.PEEK`). Não toquei em não-lido.

## 4. Geração: janela limpa

`created_at > 00:15Z`, três tabelas: **56 entregas, TODAS `ready`, zero falha.**
(`image_generations` 27, `generations` 16, `studio_scenes` 13.) O `#358` segue
sem ocorrência nova. Nada a abrir.

## 5. Anotações de reincidência — não são chamado

- **`#349` / PR #234:** o conserto está na main (`dc3d941`, merge `243dc08`,
  PR #243), mas o **PR #234** (`feat/sgp-dhash-16x16`) continua **ABERTO**
  propondo o mesmo conserto por outro caminho, no mesmo arquivo
  (`sgp/impressao-foto.ts`). **Não mergear** — derruba o fix que está em
  produção. **Quarta ronda** registrando; é o **quinto** caso do padrão no
  repositório (`feat/fix-image-upload-retry`, `feat/onedrive-401`,
  `fix/referencia-fronteira-de-frase-por-palavra`,
  `feat/fabricar-referencia-fronteira-por-palavra`).
- **`voices` em `awaiting_training`:** 10+ vozes paradas de 331h a 1.234h
  (julho/agosto). **Não abri chamado**: o **PR #15**
  (`feat/varredura-awaiting-training`) é exatamente este conserto e está aberto.
  Checagem 2 do §3 manda não abrir chamado pra bug que já tem PR.

## 6. Placar

- **78 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **43 de atendimento**, **35 técnicos**. Mais **10** em `aguardando_aluno`, que
  não entram nesses 78.
  (Registro medido: **zero** em `status='open'` — os 78 são todos
  `investigating`. Por isso a contagem varre os dois status.)
- **Delta contra as 75 das 00hZ, conferido linha a linha:** +2 criados
  (`#362` Frank/técnico 01:06Z, `#363` Fast/atendimento 01:40Z) **+1 reaberto**
  (`#301`, Simone, pelo **Frank** às 01:47Z — estava `fixed` com a nota "já
  resolvido via email" sem estar resolvido). 75+2+1 = 78. Reabertura é do dono
  da fila, não minha; está correta e a aluna foi respondida às 01:49Z.
- **Fechados que voltaram a disparar: 0.** Cruzei `last_seen_at > resolved_at`
  na base inteira: sai **um só**, o `#8`, última ocorrência em **22/08** —
  velho, não é reincidência nova. Recortando 72h: **0**.
- `garantia_na_fila.cjs` 10:12:12Z — 78 abertos · 18 parecem pedido de reembolso
  · 59 pessoas · **controle positivo OK (2/2)** · **6** perderam a janela ·
  **3** vencem em 48h (era 2 às 00hZ: entrou o `#363`) · **0** na renovação.
- **Calibragem do Vigia** (ordem 27/08 §5): dos **31** cartões com
  `reported_by='vigia'` desde 27/08, **4** fechados como `ignored` = **12,9%**.
  Alvo ≤10%; linha de base 21%. (21 `fixed`, 5 `investigating`, 1
  `aguardando_aluno`.)

⚠️ O "6" continua sendo **PISO, não total**: o script deriva a janela do
`warranty_date` do **nosso** banco, então quem não tem linha local sai como
*"sem compra paga"* — que **lê como veredito de pagamento** sendo só ausência de
linha. **Sétima ronda** registrando. Enquanto o rótulo não mudar, ninguém deve
ler a coluna "janela" como "não pagou".

## 7. Nota de processo — não é chamado

`main` continua com **arquivos do SGP modificados e não commitados**
(`frontend/src/lib/sgp/sessao.ts` e os demais do bloco) mais os
`_frank/rascunhos/` não rastreados. **Sétima ronda seguida** registrando. O
`pull --ff-only` desta ronda passou sem stash — o commit que entrou
(`4534ccd`) não encostou nesses arquivos.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado de
dinheiro sem `ref_id` que case, não abri chamado pra bug que já tem PR, e não
toquei em e-mail não lido.
