# 28/08 14h UTC — Ronda do Vigia (sensor, regra 14-A + 14-C)

`git checkout main && git pull --ff-only origin main` → já estava atualizado.
Índice de ordens lido antes de tocar em qualquer coisa. Ordem vigente do
assunto: `2026-08-27_vigia_so_erro_de_sistema.md` (14-C) — só erro de SISTEMA
vira chamado. Também valem `2026-08-20_dono_da_fila_e_fila_zerada.md`, a
`2026-08-20_REGRA_FINAL_CREDITO.md` (ENCERRADA — não reabri) e a rotina
`e-mail_tecnico.md`.

Ronda anterior minha: **12h UTC**. Esta: **14h UTC**. Janela de 2h, no ritmo.

---

## ⚠️ ABRO PELO MEU PRÓPRIO ERRO, DE NOVO: arranquei o Frank da branch — 2 por 2 hoje

Isto vem antes dos números porque **é o mesmo estrago que eu prometi que ia
repetir**, e repetiu na ronda seguinte.

O meu passo obrigatório de abertura (`git checkout main`) rodou enquanto o
Frank estava em `fix/inc169-heygen-video-title`. Prova no reflog:

```
d6393ff HEAD@{0}: checkout: moving from fix/inc169-heygen-video-title to main
9d97794 HEAD@{1}: commit: fix(#169): manda video_title (nao title) no av4/generate do HeyGen
```

**Desta vez não havia nada em risco** — eu conferi antes de concluir: árvore
limpa (`git status --short` vazio) e o commit `9d97794` **já estava no origin**
(`git ls-remote` devolve o mesmo sha para `refs/heads/fix/inc169-heygen-video-title`).
Ou seja, nenhum trabalho pendurado. **Devolvi a árvore para a branch dele** no
fim desta ronda, como fiz às 12h.

O ponto não é o dano desta vez, é a **taxa**: nas duas rondas de hoje em que o
Frank estava consertando algo, **as duas** me viram arrancá-lo da branch. Às 12h
havia 163 linhas não commitadas na jogada; foi sorte, não desenho. A causa é
estrutural — dividimos a **mesma árvore de trabalho** e a minha ordem de abertura
troca de branch incondicionalmente. É **processo**, não bug de código: pela 14-C
vai como 1 linha aqui e 1 mensagem pro Johnny (§7 item 1), sem chamado.

---

## Placar

| | |
|---|---|
| Incidentes abertos no fim | **9** — 4 `atendimento` (`120`, `153`, `167`, `169`), 5 `técnicos` (`11`, `108`, `165`, `168`, `170`) |
| Variação vs. 12h | **+1** (eram 8) — entraram `168`, `169`, `170`; saiu o `166` (fechado) e o `151` (foi para `aguardando_aluno`) |
| **Abertos por mim nesta ronda** | **0** — os 3 novos foram abertos pelo `fast-help` |
| **Anotados por mim** | **1** — nota de medição no `169` (§3) |
| **Fechados que voltaram a disparar** | **0** — por 2 réguas independentes (§4) |
| Total de incidentes na tabela | **162** (era 159 às 12h) |
| Fora da contagem | 9 em `aguardando_aluno` |
| E-mails novos desde as 12h | **1** — uid 347 (Eder Souza, 13:49Z) |
| Aluno sem resposta na caixa | **0** — respondido em **1 minuto** (§5) |
| Aluno sem resposta **fora** da caixa | **1 — grave** (§1, o `169`) |
| Fila de não-lidos da Fast | **0**. **Não toquei em não-lido** |
| Gerações em 24h | **97** — 91 `ready`, 6 `failed`, **todas as 6 de 27/08 17:01–17:30Z** (§2) |
| As 6 falhas foram estornadas? | **sim, 6 de 6**, 1 estorno cada, 10.286 créditos (§2) |
| Gerações/cenas/clones/imagens presos >30min | **0 / 0 / 0 / 0** |
| Vídeo Clone 24h | **43 `ready`** + 1 `generating` + 1 `pending` (ambos < 30min) |
| Cenas do estúdio 24h | **29 — todas `ready`** |
| Imagens 24h | **164 — todas `ready`** |
| Vozes 24h | **23 — todas `ready`** |
| Estornos em 12h | **0** (`ref_type='generation_refund'`, nunca por `kind`) |
| `awaiting_training` +24h | **16 vozes / 15 alunos / 45,4 dias** — **estável** vs. 12h (§6) |
| PRs abertos | **23** (eram 21; entraram #81 e #82, saiu o #80 mergeado) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou assinatura que eu toquei | **nenhum** |

Contagem **sem cláusula de assinatura**. `status` distintos: `fixed` 113,
`ignored` 31, `aguardando_aluno` 9, `investigating` 6, `open` 3.

---

## 1. ALUNO PARADO HÁ DIAS, SEM UMA LINHA DE RESPOSTA — vai na frente de qualquer número

**`169` (`a89a67d3`), `jrsolucoescorporativas@gmail.com`**, aberto pelo
`fast-help` às 13:32Z. A frase dele, crua:

> *"continuo aguardando retorno e não recebo. Estou parado aguardando há dias."*

Ele pede duas coisas: (1) retreinar/curar a voz "Minha Voz" (timbre agudo,
cadência diferente da real — quer mandar gravação nova) e (2) um erro técnico
no HeyGen: `video_title is invalid: Field required` **mesmo com o título
preenchido**.

**Conferi antes de acusar fila perdida, e a acusação se confirma:** a caixa de
enviados não tem **nenhum** e-mail para esse endereço — critério `ALL TO`,
**0 resultados no histórico inteiro**. Ele já constava no `150` em **27/08
12:17**, então são **26h+ de silêncio medidos**, e o "há dias" dele indica que
começou antes disso, no chat do app.

**Não é o `150` mal fechado.** Conferi a nota de fechamento: o `150` prometeu
que a escalação do chat **abriria chamado**, e abriu — o `169` existe
justamente porque aquele conserto funcionou (`PR #68`, merge `7e9ca9a`). A nota
diz, com todas as letras, que o chat **não tem resposta humana por desenho** e
que a resposta **tem que sair por e-mail**. O conserto entregou o chamado; o
que faltou foi **gente respondendo o chamado**. Por isso **não registrei objeção
no `150`** — seria objeção falsa.

Pela 14-C isto é **atendimento**: não abri chamado (o `169` já existe e está
aberto), **não escrevi rascunho e não respondi** (14-A). Vai como decisão do
Johnny, §7 item 1.

## 2. Produção: nada de sistema novo, e o dinheiro voltou

As 6 falhas de geração das últimas 24h são **as mesmas 6** da ronda das 12h:
todas de **27/08, entre 17:01Z e 17:30Z**, todas com o mesmo erro cru —
`qa_coverage: audio gerado nao contem o texto completo apos esgotar
regeneracoes`. É a classe do **`52`**, fechado às 19:23Z de 27/08, e **todas
são anteriores ao fechamento**. Depois do fix: **91 gerações, zero falha**.
Imprimi o `error_message` cru antes de acreditar no zero.

**Estorno conferido pelo caminho certo** — `credit_transactions.ref_id` casado
com `generations.id` e `ref_type='generation_refund'`, **nunca por `kind`**
(a armadilha que quase pagou em dobro para 13 alunos):

| geração | estornos | créditos |
|---|---|---|
| `c0ba5304` | 1 | 1.826 |
| `44ac6f60` | 1 | 1.598 |
| `b6e84e2c` | 1 | 1.999 |
| `eac78047` | 1 | 1.921 |
| `62a235f8` | 1 | 1.773 |
| `7bfc94b0` | 1 | 1.169 |

**6 de 6 estornadas, 10.286 créditos devolvidos, nenhuma em dobro.** Registro a
ressalva que a minha ordem exige: **falha estornada não é caso resolvido** —
esses 6 alunos receberam o crédito de volta, mas **continuam sem o áudio que
queriam**. A classe está corrigida, então uma nova tentativa deve passar; ninguém
avisou nenhum deles disso.

Nada preso: a régua de >30min devolveu **0** nas quatro tabelas. Os 2 Vídeo
Clone em voo (`generating`, `pending`) são recentes e estão dentro do normal.

## 3. O `169`: medi, anotei, **não investiguei causa e não fechei**

Três achados independentes, gravados como nota no próprio chamado:

**(1) Prova, por dado, de que o HeyGen nunca funcionou pra ninguém.**
`heygen_videos` tem **ZERO linhas em toda a vida da tabela** —
`count = 0`, `min(created_at)` e `max(created_at)` **nulos**. Isso corrobora
por medição, e não por leitura de código, a conclusão do Frank de que o
`/v2/video/av4/generate` recusava **toda** chamada. Nenhum aluno jamais recebeu
um vídeo por esse caminho.

**(2) Não abri chamado, e por qual checagem.** §3.2 da 14-C ("já foi
corrigido?") segurou: o conserto **já existe** — commit `9d97794`, branch
`fix/inc169-heygen-video-title`, **PR #82 aberto hoje**, trocando `title` por
`video_title`. Li o diff pra confirmar que é o mesmo defeito, e é. A §3.1 ("já
existe?") também segura: o chamado existe e está aberto.

**(3) Observação de classificação — eu não mudo, quem decide vê.** O `169` está
gravado como `categoria='atendimento'`, mas **carrega um defeito de SISTEMA
provado**. O detalhe do HeyGen mora **só no campo `description`**: busca em
`incidents` por `title`/`signature`/`sample_error` com `ilike '%heygen%'` ou
`'%video_title%'` devolve **ZERO linhas, inclusive esta**. Efeito prático: no
placar o defeito conta como atendimento, e quem procurar a classe pelo caminho
normal **não acha**. Pela 14-A eu **não reclassifico**; fica anotado.

## 4. Fechado que voltou a disparar: zero

Duas réguas, nenhuma com cláusula de assinatura:

1. **Fechado com `last_seen_at` nas últimas 48h** — **19** incidentes, e nos
   **19** o `last_seen_at` é **anterior ou igual** ao `resolved_at`
   (`count(*) filter (...)` = 19, e o filtro de "disparou 10min+ depois" = **0**).
   Fechados **depois** da última ocorrência, que é o desenho certo.
2. **Fechado que disparou 10min+ DEPOIS do fechamento** (histórico inteiro) —
   devolve **1 linha só**, o `#8` (`acf8acd6`), visto pela última vez em
   **22/08 19:26**, ou seja **5,8 dias frio**. Não está disparando; resíduo
   histórico já registrado nas rondas anteriores.

O `8d370ef5` (`#34`), que a minha ordem manda vigiar pelo nome, **não aparece
em nenhuma das duas**. Encerrado de verdade.

## 5. A caixa: 1 e-mail novo, respondido em 1 minuto

**uid 347, 28/08 13:49Z — Eder Souza** (`eder.liderancacomercial@gmail.com`).
É **pré-venda**: entrou hoje pra assinar e travou em 3 dúvidas antes de dar
andamento (quantos avatares distintos cabem; se Vídeo Clone e Clonagem de Voz
são a mesma coisa — mandou 3 prints da tela mostrando a confusão; e a duração
máxima de cada vídeo).

Conferi os enviados **antes** de acusar: a Fast respondeu no **uid 240, às
13:50:19Z — 1 minuto depois**, com as três respostas objetivas (avatares
ilimitados, a distinção vídeo × voz, e o teto de 90s com o custo por segundo).
**Não é aluno esperando, não é cobrança de segunda vez, não é erro de sistema.**
Não abri chamado e não escrevi rascunho.

Vale registrar o que ele mostrou nos prints, que **não é bug e não vira
chamado**: uma tela fala em "gerar vídeos clones" e a seguinte em "clonagem de
VOZ com IA", e ele leu como produtos diferentes e **parou antes de assinar**.
É confusão de texto de tela custando conversão — vai como observação, não como
defeito.

Conferi também a **`120`** (pré-venda da Sandra Diniz, aberta há 4,2 dias) antes
de repetir que "está sem dono": **ela foi respondida 3 vezes** (24/08 12:50,
26/08 19:55 e mais uma). O que segue aberto ali é a **resposta dos fundadores
sobre os documentos em revisão jurídica** (itens 6 e 7), que é decisão do
Johnny, não fila perdida. Corrijo, portanto, qualquer leitura minha anterior de
que aquilo era aluno sem resposta.

Fila de não-lidos da Fast: **0**, e **não toquei em nenhum não-lido**.

## 6. `awaiting_training`: estável pela segunda janela seguida

| | 00h | 10h | 12h | **14h** |
|---|---|---|---|---|
| vozes `awaiting_training` +24h | 14 | 16 | 16 | **16** |
| alunos distintos | 13 | 15 | 15 | **15** |
| a mais velha | 44,9 d | 45,3 d | 45,4 d | **45,4 d** |

**Parado há 4h.** Segue sendo desenho, não defeito — pela 14-C não vira chamado.
O acúmulo de 15 alunos parados num botão continua de pé como decisão de produto
(§7 item 4).

O `training_jobs` órfão segue **1 linha só**: job `ebf5cc56` em `queued` desde
25/08 01:33, com a voz já **`ready`** (escrituração pendente, ninguém esperando).
**Não abri chamado** — está medido na nota do `108`, que está aberto e com dono.
Chamado novo seria duplicata de classe.

## 7. Pro Johnny — o que precisa de decisão

1. **Tem um aluno parado há dias e ninguém escreveu pra ele** (§1). O
   `jrsolucoescorporativas@gmail.com` está no `169`, com **zero e-mails**
   enviados no histórico inteiro. A metade técnica dele (HeyGen) já tem conserto
   no PR #82; a metade humana **não tem dono**. Eu não respondo aluno (14-A) —
   precisa do teu "pode", ou de alguém do time.
2. **Eu arranco o Frank da branch dele, e agora são 2 por 2** (§ do topo).
   Hoje aconteceu nas duas rondas em que ele estava consertando algo. Devolvi a
   branch nas duas e nada se perdeu, mas **vai repetir a cada 2h** enquanto
   dividirmos a mesma árvore. O conserto seria eu rondar num `git worktree`
   só meu. Decisão tua.
3. **23 PRs abertos**, o mais velho de 18/08. O gargalo segue sendo o aval, não
   o diagnóstico. Dois deles fecham coisa que estou reportando toda ronda:
   o **#82** (o HeyGen do §1) e o **#11** (`[ESCALAR:]` avisa a equipe, parado
   há 9 dias — é o buraco que deixa promessa de "a equipe vai ver" sem dono).
4. **16 vozes / 15 alunos parados num botão**, a mais antiga há 45,4 dias.
   Estável há 4h, mas o estoque continua. O fluxo passa a cutucar esse aluno,
   ou o treino dispara sozinho?
5. **Menor, mas custa venda:** o Eder (§5) parou antes de assinar porque uma
   tela diz "vídeo clone" e a seguinte diz "clonagem de voz", e ele achou que
   eram produtos diferentes. Mandou print. Não é bug — é texto de tela.

## 8. O que eu NÃO fiz (14-A)

- **Não abri chamado nenhum** — os 3 novos (`168`, `169`, `170`) são do `fast-help`.
- **Não fechei, não reabri e não reclassifiquei incidente.**
- **Não registrei objeção falsa**: conferi o `150` e o fechamento dele está
  certo, então não anotei objeção onde não cabia.
- **Não respondi aluno e não escrevi rascunho.**
- **Não toquei em crédito, acesso ou assinatura.**
- **Não toquei em e-mail não lido** (fila da Fast: 0).
- **Não mergeei PR, não apliquei migration, não gastei GPU.**
- **Não commitei arquivo do Frank** — o meu commit leva só este log.
