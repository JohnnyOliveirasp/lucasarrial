# 25/08 14h UTC (10h EDT) — Ronda do Vigia (sensor, regra 14-A)

`git checkout main && git pull --ff-only origin main` → já em dia. Índice de
ordens lido. Valem a `2026-08-20_dono_da_fila_e_fila_zerada.md` (⭐), a
`2026-08-20_REGRA_FINAL_CREDITO.md`, a `2026-08-21_passagem_incidentes_para_claude.md`
e a rotina `e-mail_tecnico.md`.

Ronda anterior: 12h UTC. Duas horas de janela.

---

## Placar

| | |
|---|---|
| Incidentes abertos | **5** — 3 `atendimento`, 2 `tecnico` (todos `investigating`, zero `open` antes desta ronda) |
| **Fora da contagem de "abertos"** | **3** em `aguardando_aluno` (`65`, `120`, `124`) — gente esperando que o filtro padrão não mostra |
| Abertos por mim nesta ronda | **1** — `129` (§1) |
| Anotados por mim | **4** — `72` (§2), `97` (§3), `126` (§4), `124` (§5) |
| Fechados que voltaram a disparar | **1** de 113 (`acf8acd6` / nº `8`), último disparo 22/08 19:26Z = **66,8h** — nenhum disparo novo |
| E-mails lidos | 25 (INBOX uid 271–290, `BODY.PEEK`, só `SEEN`) |
| E-mails novos desde a ronda das 12h | **1** (uid 290, Cássio) |
| Incidente que eu fechei ou reabri | **nenhum** |
| Aluno que eu respondi / rascunho que escrevi | **nenhum** |
| Crédito, acesso ou `entitlements` que eu toquei | **nenhum** |

Abertos, por número: `72` (téc, 7 alunos, 887h), `97` (atend, 1 na lista — ver
§3, 46h), `99` (atend, 1, 45h), `108` (téc, 2, 40h), `123` (atend, 1, 22h).

Contagem feita **sem cláusula de assinatura**, como manda a ordem:
`status in ('open','investigating')`, nada de `signature like 'fast-email:%'`.

---

## 1. ABRI o `129`: a coluna "já pagou" está `false` para os 1.515 perfis

`profiles.ja_pagou` = **false em 1.515 de 1.515** (100%). `ja_pagou_em` nulo em
1.515, `ja_pagou_origem` nulo em 1.515. A coluna carrega **zero informação**.

A causa está escrita na própria migration, `scripts/79_profiles_ja_pagou.sql`:

> "O BACKFILL vai em commit SEPARADO, com fonte no GET /sales/history da
> Hotmart cruzado com extra_purchase|stripe_session."

Esse commit nunca saiu. Subiu o `ALTER TABLE ... NOT NULL DEFAULT false` e mais
nada.

**Não está trancando ninguém hoje — conferi antes de alarmar.** Nenhum código de
app lê a coluna: `grep -rn "ja_pagou"` casa só em `_frank/prova/*`,
`_frank/ordens/*` e no próprio `scripts/79_*.sql`. Dentro de `frontend/` os três
arquivos que casam estão em `frontend/_Bugs/` (rascunho de investigação, fora do
app) e **zero** em `frontend/src`.

**O risco é de leitura, e ele está armado.** O `_frank/ordens/README.md` lista
como **vigente**: *Como saber se "já pagou" (migration 79)*. Quem seguir a ordem
ao pé da letra consulta `ja_pagou`, recebe `false` para todo mundo e conclui que
nenhum aluno jamais pagou. Cruzado com a regra final de crédito ("Aluno pagou,
tem créditos"), isso **nega crédito a pagante** — a mesma família de acidente que
o README já registra em 20/08 ("Confundir as duas já trancou aluno pagante").

**Armadilha pra quem for consertar** (está na própria migration): `payment_event`
**não prova pagamento** — o trial gratuito da Hotmart grava o MESMO carimbo de
+100.000. Por isso eu **não afirmo** quem pagou de verdade. Medi que existem
**829** perfis com carimbo `payment_event` positivo e **esse número inclui
trial**. Backfill derivado de `credit_transactions` marcaria trial como pagante.
A fonte certa é o `GET /sales/history` (valor > 0 **E** COMPLETE/APPROVED;
`OVERDUE` não é pagamento), como já fazem `pagou_de_verdade.cjs` e
`cancelamentos_ontem.cjs`.

Decisão de quem é dono, não minha: **sai o backfill pela fonte certa, ou a coluna
é aposentada e o índice de ordens para de apontar pra ela.**

## 2. "SEM ACESSO" não tranca porta nenhuma — medido no código (`72`)

A ronda das 12h reportou, com razão, que o `jrfengenhariadf` venceu
(`access_until` 25/08 12:00Z, agora −2,2h) e que `aluno.cjs` imprime
**`acesso: SEM ACESSO`**. Fui conferir o que essa frase significa na prática,
porque ela **lê como cadeado** e ninguém tinha medido:

- `app/layout.tsx`, comentário no próprio arquivo: *"Entrada LIVRE: todo usuário
  logado entra na plataforma e vê os menus. O paywall não bloqueia mais o acesso
  — ele aparece como popup na AÇÃO quando faltam créditos."*
- `app/roteiro/page.tsx`: *"`subscribed` continua existindo SÓ pra escolher o
  texto do aviso e o destino do CTA — **não tranca mais nada** (ordem do Johnny
  18/08)"*. O gate real é `canGenerate = team || creditsTotal >= CUSTO`.
- `api/v1/voices/[id]/generate/route.ts:139-148`: `hasActiveAccess` só é chamado
  **dentro** do ramo `bal.total < creditCost`, pra escolher o texto do 402. Com
  saldo, nem é consultado.

**O jrf tem 100.000 créditos intactos: entra, vê tudo e gera normalmente hoje.**
O que o vencimento tira é **recarga futura**, não acesso. Então "estender o
acesso" não é a ação urgente que o texto sugere.

O dano real dele não mudou e não depende de relógio: pagou 2 ciclos, a única voz
("joab") está `rejected_too_short` desde 25/07 (**31 dias**), nunca teve uma voz
`ready`.

**E não é um, são dois:** `leonardonogueiramv` (chamado `124`) venceu **no mesmo
minuto**, 25/08 12:00Z, também com 100.000 créditos. A ronda das 12h listou só o
jrf.

**Ressalva honesta:** li os gates de `layout`, `roteiro` e `generate`. **Não**
enumerei todas as telas e rotas; se existir ponto que ainda tranca por
`access_until`, me escapou.

## 3. O `97` subconta os alunos, e o detector de reincidência não pegaria

`affected_emails` tem **1** endereço (`rafapaga@uol.com.br`). Existe um segundo,
com estorno pago **citando este número**: `kessulyl@gmail.com`,
`credit_transactions` 23/08 21:44Z, +9.240, `ref_type='video_clone_refund'`,
nota crua: *"estorno do Video Clone 4e35fd9c: drift de rosto medido (incidente
97) - falha nossa, regra 6"*. A casa reconheceu o defeito no caso dela e devolveu
o crédito — e ela não está na lista. Quem usar `affected_emails` pra dimensionar
erra por 2x.

Pior: `last_seen_at` do `97` é **23/08 16:00Z**, ou seja **5h44 ANTES** dessa
ocorrência conhecida. O detector de "fechado que voltou a disparar" compara
`last_seen_at` com `resolved_at` — e **estorno não escreve `last_seen_at`**. É o
mesmo buraco já registrado no `101` e no `126`: o que a casa faz **fora** do
pipeline (estorno, promessa, resposta manual) não deixa rastro nos campos que os
detectores leem.

## 4. FRENTE 2 — a caixa: o Cássio escreveu pela TERCEIRA vez

25 lidos (uid 271–290), **1 novo** desde a ronda das 12h. Não toquei em não-lido.

**uid 290, 25/08 13:26Z** (~1h antes desta ronda): *"Bom dia. Por favor,
agradeço. Eu fiz um teste na plataforma mas achei que o avatar ficou com
qualidade muito ruim, por esse motivo cancelei. Vou aguardar vocês melhorarem a
plataforma…"*

Duas coisas novas, nenhuma delas registrada em lugar nenhum:

1. **O motivo do cancelamento, dito por ele: qualidade do avatar.** Até agora o
   caso era tratado como "cancelou + recebe e-mail automático indevido". É também
   **um cliente perdido para o defeito que o `97` rastreia**. Anotei nos dois.
2. **O tom**: ele **não** está cobrando com raiva, agradece e diz que vai
   esperar. Importa pra quem responder — é fechar um ciclo, não apagar incêndio.

**O que continua devendo:** a promessa de 24/08 20:40 (*"Eles te respondem em
breve com a confirmação"*). Passaram **16,8h** e a confirmação não foi enviada. O
`126` foi fechado 24/08 23:40:13Z, **3 segundos** depois da promessa, com
fundamento "entregue ao time".

**NÃO abri chamado novo**, e o motivo é a própria ordem: existe incidente fechado
com o mesmo aluno **e** existe resposta enviada. Registrei no `126` em vez de
inflar a fila. A resposta segue apurada desde as 12h e nada mudou: compra
`status='canceled'` desde 19/08 02:23Z; não existe profile com esse e-mail; o
defeito dos e-mails automáticos foi corrigido em `42e3b68`, já na main. **Falta
só alguém enviar — e enviar não é minha função.**

Demais correspondentes conferidos, todos com dono: Katia (uid 287 — respondeu que
o 2º áudio melhorou mas 2 frases ainda têm entonação estranha e há um vão longo;
vai anotar hoje → `108`, bola com ela), Luciano (uid 288/289 → `99`, bola com ele,
vai mandar a foto), Itamar (uid 273/274, conferido na ronda das 06h), Tullio
(uid 276, ciclo fechado). Bounces uid 271/277: o permanente (`luctec`) já está
anotado como objeção no `101` desde a ronda das 06h/10h — e `luctec` não tem
compra, 0 créditos, nenhuma voz; o uid 277 é atraso em endereços da própria
equipe, não aluno.

## 5. O `124` — vim acusar e estava errado eu

Suspeitei de promessa não cumprida: a mensagem de 24/08 16:28Z diz *"Estou
retreinando agora sem custo; em ~15 min mando amostra"* e o chat **não tem
nenhuma mensagem nossa depois disso**. Parecia 15 minutos com 22h de atraso.

Conferi e **desmente a suspeita**: a amostra foi entregue **por e-mail**, não pelo
zap — Enviados **uid 56, 24/08 21:35:57Z**, *"Sua voz clonada: ouvimos, geramos e
está na sua conta"*, com onde ouvir e a confirmação de que nenhum crédito dele foi
gasto. E o próprio aluno respondeu **depois** disso: *"Combinado"*, 23:36Z. Somado
ao que a ronda das 00h já tinha medido (voz `d1477574` com `trained_at` 24/08
21:24Z, retreino no MESMO registro — por isso o `created_at` continua 03/08), a
ação saiu inteira do papel.

**`aguardando_aluno` está correto aqui. Nenhuma objeção.** Deixei escrito no
chamado porque a próxima ronda vai bater no mesmo silêncio do zap e merece achar a
resposta pronta em vez de refazer o caminho.

---

## O que eu NÃO fiz (regra 14-A)

Não fechei incidente. Não reabri incidente (as objeções do `97`, do `126` e do
`72` foram **anotadas**, o status de nenhum deles mudou). Não respondi aluno, não
escrevi rascunho de e-mail. Não toquei em crédito, em `entitlements` nem em
e-mail não lido.

## Objeção que fica de pé para o dono da fila

O placar oficial diz "5 abertos". Existem **8** com gente esperando: os 5 mais o
`65` (3 pagantes travados, **363h**), o `120` (Sandra Diniz, pré-venda, 28h) e o
`124`. `aguardando_aluno` some do filtro padrão — e no caso do `65` são três
pagantes parados há 15 dias.
