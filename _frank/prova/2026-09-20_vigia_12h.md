# Vigia — ronda de 20/09, 12hZ

Sensor (regra 14-A). Varri, medi, **abri 2 cartões e anotei 3**. Não fechei, não
reabri, não mudei status de nada, não respondi aluno, não escrevi rascunho, não
toquei em crédito, não mexi em código.

Ordens lidas antes de tocar em qualquer coisa: `_frank/ordens/README.md` (índice),
`2026-08-29_desligar_vigia_e_frank.md` (planilha desligada — **não li, não escrevi
e não classifiquei nada dela**), `2026-08-27_vigia_so_erro_de_sistema.md`,
`2026-09-17_percepcao_nao_e_desculpa_pra_parar.md`, `_frank/rotinas/e-mail_tecnico.md`,
regra 14-A. Ronda anterior do Vigia: `2026-09-20_vigia_10h.md`. Rondas das falhas
mais recentes: `2026-09-20_rotina_falhas_11h.md` e `..._13h.md`.

Canal: ordem do Johnny de 31/08 — **tudo de FastCloner vai no GRUPO**
(`notify-grupo.sh`), nada no privado.

---

## 0. O achado da ronda: a casa escreveu para o endereço que ela mesma marcou como inexistente

Cruzei `emails_enviados` com os cartões `fast-bounce:*` procurando carta que saiu
**depois** do bounce daquele mesmo endereço. Cinco casos. Três são `caixa-cheia`
(classe **temporária** — reenviar ali é legítimo e não entra em acusação nenhuma:
#250, #338, #460). Sobraram dois permanentes, e um deles é de **hoje**.

### O caso

**Luciana Sato**, pedido SGP `864d653a-dacb-42d5-8ef0-6f9902ad581e`
(nome no pedido "Lucisna Thai Hossoe Sato", whatsapp `5511991579222`):

| quando | o quê |
|---|---|
| 16/09 12:28:07Z | `profiles`: **`lucianadox@gmail.com`**, display_name "LUCIANA SATO" |
| 16/09 13:04:39Z | `sgp_pedidos` criado com **`lucianadox1@gmail.com`** (com "1") |
| 16/09 13:04:43Z | `emails_enviados` `f2e94149` (origem `sgp-codigo`) → lucianadox1 |
| 16/09 13:50:04Z | **bounce do Google** carimbado: `bounce_classe='inexistente'`, *"The email account that you tried to reach does not exist."* (cartão **#427**, fechado `ignored` às 14:28Z) |
| **20/09 11:28:29Z** | `emails_enviados` `db7b20ab` (origem **ronda-manual**) → **lucianadox1**, assunto *"Seu áudio do SGP: falta pouco, e a confusão foi nossa"* |

Quatro dias depois de o próprio banco escrever "inexistente" naquele endereço, a
resposta de conteúdo da ronda saiu para ele. **O script não avisou nada.**

E a casa tinha, no instante do envio, **dois canais bons na mão**: o
`profiles.email` sem o "1", criado **36 minutos antes** do pedido, e o WhatsApp
no próprio registro do pedido.

### A causa, com `arquivo:linha`

`_frank/ferramentas/enviar_email.cjs` **para** o envio (`process.exit(2)`) quando
suspeita de aviso **repetido** — linhas 408-440, `envios.decidir(...)` + `--forcar`.
E **não tem guarda nenhuma** contra destinatário com bounce **permanente** já
gravado pela própria casa. Medido por grep no arquivo inteiro: `bounce_em` e
`bounce_classe` não aparecem em checagem pré-envio nenhuma; as únicas ocorrências
de "bounce" são comentário e a escrituração **pós**-envio.

> A assimetria **é** o defeito: a casa desconfia de mandar duas vezes, e não
> desconfia de mandar para o vazio.

### Teste de bolso (ordem de 27/08)

> *"Se o código estivesse certo, isso não teria acontecido?"* → **SIM.** O dado
> está a uma consulta de distância, na **mesma tabela** que o script já escreve.

**Cartão aberto: #495** (`76eeb07a`), `open`, `reported_by vigia`, categoria
`tecnico`, assinatura `vigia:envio-nao-consulta-bounce-classe`.

### ⚠️ Fio solto — registrado como fio solto, não como acusação

Há uma **contradição que eu não resolvi**, e quem for consertar precisa resolver
antes de tratar este endereço como morto:

```
16/09 13:15:29Z  2º código enviado para lucianadox1 (linha fd53e32a, bounce_em NULL)
16/09 13:15:47Z  sgp_pedidos.email_verificado_at   <- 18 SEGUNDOS depois
```

Fui **ler a rota antes de acusar**: `app/api/v1/sgp/codigo/route.ts` só carimba
`email_verificado_at` quando `hashCodigo(codigo) === pedido.codigo_hash`, e nesse
sucesso zera `codigo_hash` e `codigo_tentativas` — ou seja, o estado atual dela
(`codigo_hash` NULL, `codigo_tentativas` 0) é o estado **normal de quem acertou**.
**A rota está correta e não tem buraco.** Logo ela parece ter recebido o 2º código
naquele endereço, o que briga com o "does not exist" do Google no 1º.

Por isso **não afirmo que a carta de hoje se perdeu**. Afirmo que **ninguém
conferiu**, que é o defeito. Saídas possíveis, nenhuma verificada por mim: bounce
casado por Message-ID à carta errada; falso-positivo do Google no 1º; ou o 2º
também quicou e o bounce nunca foi carimbado (`bounce_em` NULL é **ausência de
notícia**, não prova de entrega — está escrito no próprio `mail-envio.ts:23-24`).

---

## 1. O erro que eu cometi nesta ronda, e o cartão que ele gerou

**O infrator fui eu.** Ao anotar a objeção no #427 rodei o que a casa manda rodar:

```
node _frank/ferramentas/anotar_incidente.cjs 427 --por vigia --nota "..." --confirmar
```

A ferramenta gravou no **#138** (uuid `42741499-…`, *"A varredura rotula trial R$0
como pagante"*) e imprimiu **`✅ GRAVADO (conferido na releitura, 1 linha afetada)`**.
Sem erro, sem aviso, sem ambiguidade. Só peguei porque o título impresso não era o
que eu esperava. **Quem não ler o título não pega.**

**Causa:** `anotar_incidente.cjs:56-71`, `resolverId()` — `data.filter((i) =>
String(i.id).startsWith(alvo))`, e só. Não conhece a coluna `numero`. "427" é
prefixo de exatamente **um** uuid, então a recusa por ambiguidade (a única
proteção que existe) nunca dispara. A verificação de escrita funciona: confere
que gravou, **no alvo errado**.

**O agravante:** `_frank/ferramentas/_incidente_nota.cjs` foi escrito em **15/09**
(commit `697f07ab`) **exatamente por causa disto** — *"Só-dígitos é NÚMERO do
incidente, nunca prefixo de uuid… deixar o prefixo ganhar foi o que apontou pro
#399"* — está testado, e é usado por **`cancelar_assinatura.cjs` e mais ninguém**.
O conserto foi aplicado no script que motivou o achado e não foi levado à
ferramenta que o próprio cabeçalho chama de *"a ÚNICA maneira segura de anotar/
fechar incidente"*, a que Vigia e Frank rodam toda ronda. **Mesma forma do #492
que abri às 10hZ** e da família dos branches stale do README: conserto que existe,
na mesma pasta, e que não viaja.

**Exposição medida (não estimada)**, perguntando ao banco o que a ferramenta faria
para cada um dos 481 números:

| | |
|---|---|
| **65 (13,5 %)** | **envenenados em silêncio** — casam com 1 uuid, de **outro** cartão. Grava no errado e imprime sucesso. Ex.: 53→#98, 446→#290, 54→#305, 95→#173, 37→#52, **427→#138** |
| 64 (13,3 %) | ambíguos — recusa em voz alta. Seguro. |
| 352 (73,2 %) | sem prefixo casando — recusa limpa. Seguro. |

≈ **1 em cada 7** anotações feitas pelo número cai no cartão de um estranho, com
carimbo verde. E cresce: cada uuid novo pode envenenar mais um número.

**O que eu NÃO consegui provar, e registro como não-provado.** Tentei medir se
isso já mordeu antes. Escrevi um detector (notas de vigia/frank nas 65 vítimas
citando e-mail fora do `affected_emails`) e ele acusou 18 cartões. **Fui ler o mais
forte antes de acreditar:** no #98 (vítima do número 53, 4 notas acusadas), as
**sete** notas são legitimamente do #98 (Adriane, "material por e-mail sem resposta
em 38h") e os e-mails "de fora" são referência cruzada normal. **O detector é
ruído e não deve ser usado.** A exposição está medida; **não tenho evidência de
que o defeito tenha mordido antes de hoje**. A única ocorrência confirmada é a
minha.

**Cartão aberto: #496** (`0982e073`), `open`, `reported_by vigia`, categoria
`tecnico`, assinatura `vigia:anotar-incidente-casa-numero-como-prefixo-de-uuid`.
⚠️ A mesma porta **fecha** cartão (`--status`) — fechar o errado é o mesmo bug com
consequência pior, e cabe na correção.

**O estrago, e o que fiz com ele.** Nada foi apagado. Li o array do #138 antes (3
notas + `resolution_note` intactos) e troquei **só o texto da nota 4** (a minha,
por `jsonb_set` em `{3,note}`, com guarda `by='vigia'` e `jsonb_array_length=4`)
por um aviso explicando o engano e apontando pro #496. Status `fixed` do #138
intocado. O #427 recebeu a nota de verdade, desta vez chamando a ferramenta pelo
**uuid completo** — o único jeito seguro hoje.

---

## 2. O que eu ANOTEI (3 cartões, nenhum status mudado)

- **#427** (`168e8269`) — objeção + ponteiro pro #495. Fechado `ignored` em 16/09
  como se o assunto tivesse acabado no bounce; não acabou, a carta de hoje saiu
  pro mesmo endereço. Status `ignored` inalterado.
- **#138** (`42741499`) — o aviso do engano acima. Status `fixed` inalterado.
- **#492** (`2fd36981`, meu, das 10hZ) — **andamento**: duas horas depois de aberto
  já existe **PR #366** (`feat/sgp-audio-conta-a-verdade`, *"a tela de áudio do SGP
  passa a CONTAR o que falta (caso Catarina, 37h parada)"*). Anotei pra ninguém
  duplicar nem refazer trabalho pronto. **Não conferi o conteúdo do PR** e não
  afirmo que ele cobre as duas metades — quem fechar precisa checar se traz também
  a **persistência** (`/api/v1/sgp/audio/ciencia` + `cienciaInicial`), não só o
  texto do botão: é a metade (b) que faz a Catarina perder os 4 checkboxes a cada
  recarga. **A Catarina segue parada** (pedido `a3cf2b08`, `ciencia_audio` NULL,
  sem login — WhatsApp +244925203633 ou e-mail).

---

## 3. FRENTE 1 — a fila inteira, SEM filtro de assinatura

Consulta sem cláusula de `signature` (a armadilha de 19/08):

```
status in ('open','investigating')  ->  92   (eram 88 às 10hZ)
  investigating  87
  open            5
```

- **61 `tecnico` · 31 `atendimento`**
- **38 dos 92 têm 7 dias ou mais.** O mais velho continua sendo o **#15**
  (`d3d8d1b2`, 30/07, *"Geração de áudio: tempo de execução estourado"*).
- Fora dos abertos, **36 em `aguardando_aluno`** — registrados aqui porque tem
  gente esperando.

Os 4 que entraram desde as 10hZ: **#492** (meu), **#493** (bounce da Aline, aberto
pela `fast`), **#494** (Frank, `video-clone:realismo-e-corpo-parado-nao-lipsync`,
9 alunos) e **#495/#496** (meus, desta ronda).

### Fechados que voltaram a disparar: **0**

Medi com a consulta honesta (`last_seen_at > resolved_at`), que devolve **1 linha
em todo o banco**: o **#8** (`acf8acd6`, `training:user_dataset`), última ocorrência
**22/08** — velho, nada novo. Os 4 `fixed` com `last_seen_at` nas últimas 24h
(#485, #483, #482, #479) têm `resolved_at` **posterior** ao `last_seen_at`: foram
fechados *depois* da última ocorrência. **Nesta ronda: zero.**

### Percepção travada (ordem de 17/09): **2 sinalizados, 0 parados por nós**

`percepcao_travada.cjs` acusa 2, os **mesmos** das 10hZ, e os dois já têm desfecho
escrito no cartão: **#450** já foi declarado *não é caso de percepção* (casamento
falso do detector) e **#473** (Katia) já foi despachado, medido e está esperando
**a aluna ouvir** o áudio refeito — bloqueio real, com parte nomeada (item 2 da
ordem de 17/09). **Nada a despachar**, e registro isso com o motivo, não com
silêncio.

---

## 4. FRENTE 2 — a caixa (o que não virou incidente)

`ler_caixa.cjs --ultimos 25 --corpo 2500`. Leitura pura (`BODY.PEEK`, só `SEEN`).
**Não toquei no não-lido** — é a fila da Fast.

**Nenhum e-mail novo de aluno desde a ronda das 10hZ.** O único item novo na
janela é o **uid 682**, bounce de máquina de 20/09 10:22:13Z
(`alinedutra_@hotmail.com.br`, *550 5.5.0 mailbox unavailable* do Outlook) — e ele
**já tem cartão**: **#493**, aberto pela `fast` às 10:25Z, **e já tem PR**: o
**#367** (`fix/bounce-550-e-inexistente`). Nada a abrir.

O mais recente de aluno segue sendo o da **Rachel Nigro** (19/09 21:32Z), já
conferido e desfechado na ronda das 10hZ (ela entrou e usou a plataforma).
**Nenhum aluno escrevendo "já pedi ajuda várias vezes" nesta janela.**

---

## 5. Passo fixo desde 18/09 — reconciliar os envios

```
node _frank/ferramentas/2026-09-18_reconciliar_envios_da_pasta.cjs --corte=2026-09-14T14:06:31Z --confirmar
```

```
828 lidas da pasta "Sent"  =  751 já tinham linha
                            +  77 fora da janela (--corte)
                            +   0 recusadas
                            +   0 dentro da janela, escrituráveis
✔ 828 = 828: nenhuma carta sumiu na classificação
🕳️ cartas que saíram e não têm linha, dentro da janela: 0
```

Conferido com o irmão de leitura, que é instrumento independente
(`2026-09-18_enviados_x_tabela.cjs`): **veredito "0 carta depois do corte"**, o
buraco está **passivo**. Nada a escriturar.

Nota de saúde: eram 799 lidas / 722 com linha às 10hZ, agora 828 / 751 — as **29
cartas novas** da janela entraram **todas** com linha. O controle compensatório
está funcionando.

⚠️ As **77 anteriores a 14/09 14:06:31Z** seguem **sem decisão** — é o que o
`--corte` exclui, e continua sendo decisão de produção (`cobreDesde` em
`contato-tentativas.ts`), não de ronda.

---

## 6. Para o Johnny DECIDIR

1. **#496 — a ferramenta de anotar escreve no cartão errado e diz que deu certo.**
   É o mais urgente porque contamina o instrumento: 65 dos 481 números caem em
   cartão de estranho com carimbo verde, e a mesma porta também **fecha** cartão.
   O conserto é adotar o `_incidente_nota.cjs` que já está na pasta desde 15/09,
   testado. É transporte, não invenção.
2. **#495 — a casa escreve para endereço que ela mesma marcou como inexistente.**
   `enviar_email.cjs` barra envio repetido e nunca olha `bounce_classe`. Enquanto
   não olhar, a ficha de contato conta como "aluno avisado" quem talvez nunca
   tenha sido alcançado.
3. **Luciana Sato precisa de gente, não de conserto** — e antes disso alguém
   precisa **decidir por qual endereço falar com ela**: `lucianadox@gmail.com`
   (o do perfil) ou o WhatsApp `5511991579222`. Eu não escrevo para aluno, e de
   propósito **não decidi** qual endereço é o bom.
4. **Catarina (#489/#492) continua parada** — agora há ~4h a mais que às 10hZ.
   O PR #366 existe; o desbloqueio dela cabe numa mensagem (*"faltam ~30 segundos
   de fala"*), por **WhatsApp +244925203633 ou e-mail**, porque ela é SGP puro e
   não tem login.

---

## 7. O que eu NÃO fiz

Não fechei nem reabri nenhum cartão. Não mudei o status de nada (o #138 seguiu
`fixed`, o #427 seguiu `ignored`, o #492 seguiu `open`). Não escrevi para aluno
nem rascunho de e-mail. Não toquei em crédito (nenhum estorno nesta ronda; os dois
achados não envolvem dinheiro e não há `ref_id` a casar). Não mexi em código — nem
no `anotar_incidente.cjs`, que foi quem me traiu: quem conserta e fecha é o Frank
(14-A). Não li, não escrevi e não classifiquei nada vindo da planilha — ordem de
29/08.
