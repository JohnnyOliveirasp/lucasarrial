# Ronda do Vigia — 12/09/2026, 12hZ (09h BRT)

Sensor (14-A). Varri, medi e **anotei 2** (`#364` e `#312`). **Não abri chamado
novo**, e o motivo está medido no item 2. Não respondi aluno, não escrevi
rascunho, não mexi em crédito, não fechei e não reabri nada.

Repo em `main`, `pull --ff-only` limpo. `_frank/ordens/README.md`, a ordem de
**27/08** (só erro de sistema vira chamado) e a de **29/08** (planilha
desligada) lidas antes de tocar em qualquer coisa. **Nada da planilha foi lido,
escrito, classificado ou reprocessado.** Ordem de canal de **31/08**: o aviso
desta ronda vai **no grupo**.

Janela coberta: **10:20Z → 12:13Z**. `now()` medido no banco = **2026-09-12
12:11:08Z**.

---

## 1. 🟢 O `#364` foi aberto e consertado DENTRO da janela — 1h34

É a primeira vez nestas rondas que um achado meu nasce e morre entre duas
rondas, então registro com número.

| momento | o quê |
|---|---|
| **10:16Z** | abri o `#364` (SGP sem estado terminal na perna da foto) |
| **11:50Z** | `786742d` (**PR #246**) entra na `main` |
| **12:13Z** | medido por mim no banco: o pedido curou |

**Controle positivo, medido e não presumido.** O pedido `fe00d4e2`
(`rafaelzan@me.com`) saiu de `processando` (16,6h em limbo, `erro` NULO) para:

    status = 'falhou'
    erro   = 'não foi possível gerar o seu clone a partir das fotos enviadas'

E o resto da base continua limpo: das linhas com `enviado_em`, **esta é a única
que não está `pronto`**. O conserto fez o que o cartão pedia.

## 2. 🔴 O que o conserto NÃO fez: o pedido agora morre com motivo, mas morre CALADO

Achado desta ronda. **Anotei no `#364`**, não abri cartão novo — e a razão está
no §4.

**Com arquivo:linha, na `main` de hoje.** Em `frontend/src/lib/sgp/etapas.ts` a
perna do SUCESSO avisa e a do FRACASSO não:

| linha | ramo | avisa? |
|---|---|---|
| `etapas.ts:79` | `fotoPronta` → `avisoFotoPronta(email, …)` | **sim** |
| `etapas.ts:89` | `vozPronta` → `avisoVozPronta(email, …)` | **sim** |
| `etapas.ts:92-101` | `status === 'falhou'` → grava `status` + `erro` | **não** |

O ramo do fracasso escreve no banco e **mais nada**: nenhuma chamada de e-mail
ao aluno, nenhum `escalarNoGrupo` — que **existe e está disponível**, em
`onboarding/avisos.ts:251`. Não é falta de mecanismo; é ramo não ligado.

**Consequência medida, não projetada.** O Rafael está há **18,6h** sem nenhum
contato nosso. Ele **não escreveu ao suporte** (conferido na caixa) e a fila de
não-lidos está em **0** — ou seja, ninguém vai tropeçar nele por acaso. Antes o
sistema não sabia que ele tinha falhado; **agora sabe, carimbou o porquê, e
continua sem avisar ninguém.** E a moderação do Kie é desfecho **definitivo** —
o próprio commit registra que reprocessar a mesma foto cai no mesmo lugar —
então não há nada que se resolva sozinho enquanto ele espera.

**Irmão na perna da voz:** `#362` ("voz recusada por upload perdido não vira
chamado nem contato"), aberto 01:06Z. Mesma forma nas duas pernas: **o desfecho
existe, o contato não.**

## 3. 🟡 O `garantia_na_fila` pulou de 6 para 24, e NÃO são 18 pessoas novas

Isto vai em destaque porque o número, sozinho, mente. **Anotei no `#312`.**

| ronda | "perdeu a janela na fila" |
|---|---|
| 10hZ (10:12:12Z) | **6** |
| 12hZ (12:12:20Z) | **24** |

**A conta, linha a linha:** dos 24, **18 são do `#312`**; os outros **6 são
exatamente os mesmos 6 das 10hZ** (`#254`×2, `#350`, `#309`, `#299`, `#282`).
Mudou **uma** coisa: a ronda das 11hZ passou o `#312` de `aguardando_aluno` para
`investigating`. **A ferramenta só enxerga incidente ABERTO.**

**O dano é velho:** 4 delas viraram há **2124,2h** (88 dias) e 14 há **36,2h**,
com *"esperou 2239,8h de prazo dentro da fila"*. **Nenhuma virou nesta janela.**
Ninguém perdeu nada nas últimas 2h.

**O que isso mede, e é o achado:** o custo do `aguardando_aluno` como arquivo
morto deixou de ser argumento e virou número. **18 pessoas que pagaram, nunca
tiveram conta e perderam a garantia dentro da NOSSA fila estavam fora do
instrumento que existe exatamente para achar quem está perdendo a garantia
dentro da nossa fila.** Invisíveis até as 11hZ de hoje. O log de 11hZ já tinha
registrado *"`aguardando_aluno` como arquivo morto: terceira ocorrência"* — esta
é a primeira vez que a ocorrência vem **com o tamanho medido**.

Não conferi valor nenhum nesta ronda e **não digo quanto se deve a quem**: a
medição de dinheiro dessas 19 pessoas está na nota da ronda das 11hZ, feita por
quem abriu a Hotmart viva um a um.

## 4. Por que NÃO abri chamado nesta ronda

As três checagens do §3 da ordem de 27/08, feitas antes de decidir:

1. **Já existe?** **Sim, nos dois casos.** O silêncio da perna da foto é o
   próprio `#364`, aberto e ainda `investigating`; os 18 são o `#312`, aberto.
   Classe aberta = ocorrência, não cartão. (Foi ignorar isto que fez o `#112`
   nascer duplicata.)
2. **Já foi corrigido?** Para o silêncio, **não**: dos **32 PRs abertos**,
   **nenhum** toca `sgp/etapas.ts`, `onboarding/avisos.ts` ou
   `onboarding/pronto.ts` (conferido por `gh pr list --json files`).
3. **Dinheiro?** **Não afirmo nada.** Não abri o arquivo que cobra e não tenho
   `ref_id` casado em nenhum dos dois. Sem `arquivo:linha` do débito, eu não
   digo uma palavra sobre cobrança ou estorno — foi assim que o `#152` quase
   pagou R$ 3.600 em dobro.

**Objeção registrada por antecedência (14-A), na nota do `#364`:** se o cartão
for fechado como `fixed` com base no estado terminal, a perna do **aviso** fica
sem dono e sem cartão, porque ela só aparece ali. Quem fecha decide; eu registro
que, no instante da nota, o aluno segue sem ter sido avisado.

## 5. Frente 2 — a caixa

- não-lidos no INBOX (fila da Fast): **0**;
- **nenhum e-mail novo** na janela. O topo da caixa continua no uid **587**
  (Rodrigo, 02:45Z), o mesmo das 10hZ;
- **nenhum aluno cobrando pela 2ª vez** nesta janela;
- nada precisou virar chamado pela Frente 2.

Leitura pura (`EXAMINE` + `BODY.PEEK`, só `SEEN`). Não toquei em não-lido.

## 6. Geração: janela limpa

`created_at > 10:15Z`, quatro tabelas: **9 entregas, TODAS `ready`, zero
falha.** (`image_generations` 9; `generations`, `studio_scenes` e `video_clones`
sem linha nova — sábado de manhã.) Nada a abrir.

## 7. Placar

- **80 abertos** (`open` + `investigating`), **sem filtro de assinatura**:
  **43 de atendimento**, **37 técnicos**. Mais **9** em `aguardando_aluno`.
  (Registro medido: **zero** em `status='open'` — os 80 são todos
  `investigating`. Por isso a contagem varre os dois status.)
- **Delta contra as 78 das 10hZ, reconciliado inteiro:** `+1` criado (`#364`,
  vigia, 10:16Z, técnico) `+1` que entrou pelo relabel (`#312`, técnico, saiu de
  `aguardando_aluno` na ronda das 11hZ). 78+2 = **80**, e
  `aguardando_aluno` 10 → **9**. Atendimento ficou em 43, inalterado.
  **Nenhum chamado novo nasceu nesta janela** além do meu de 10hZ.
- **Fechados que voltaram a disparar: 0.** Na base inteira sai **um só**, o
  `#8`, última ocorrência em **22/08** — velho, não é reincidência nova.
  Recortando 72h: **0**.
- `garantia_na_fila.cjs` 12:12:20Z — 80 abertos · 19 parecem pedido de reembolso
  · 78 pessoas · **controle positivo OK (2/2)** · **24** perderam a janela (ver
  §3) · **3** vencem em 48h · **0** na renovação.
- **Vence em 48h, os 3:** `#306` e `#363` são **a mesma pessoa**, o Rodrigo —
  **RESTAM 11,8h** (era 13,8h às 10hZ). O terceiro é
  `evelyn.cheida@gmail.com` (`#341`), **restam 35,8h**.
- **Calibragem do Vigia** (ordem 27/08 §5): dos **31** cartões com
  `reported_by='vigia'` desde 27/08, **4** `ignored` = **12,9%**. Alvo ≤10%;
  linha de base 21%. (21 `fixed`, 6 `investigating`, 0 `aguardando_aluno`.)
  Inalterado desde 10hZ — o único movimento foi o `#312` trocando de status.

⚠️ O rótulo *"SEM LINHA NO NOSSO BANCO p/ este e-mail"* que a ferramenta imprime
continua **NÃO sendo veredito de pagamento** — é ausência de linha local. **Oitava
ronda** registrando. Ninguém deve ler aquela coluna como "não pagou".

## 8. Nota de processo — não é chamado

- **O `#364` está `fixed` na prática e `investigating` na fila.** O conserto está
  na `main` desde 11:50Z e o caso-prova curou. Fechar é do dono da fila, não meu
  (14-A) — registro só que a fila está 1 cartão mais pessimista que a realidade,
  e que a nota do §2 é o que falta decidir antes de fechar.
- `main` segue com **arquivos do SGP modificados e não commitados**
  (`frontend/**/sgp*`, `frontend/messages/*`) mais os `_frank/rascunhos/` não
  rastreados. **Oitava ronda** registrando. Não são meus e não toquei. Commitei
  **só** este log, por caminho explícito.

---

**O que eu NÃO fiz:** não respondi aluno, não escrevi rascunho de e-mail, não
mexi em crédito, não fechei incidente, não reabri incidente, não abri chamado
para classe que já tem cartão aberto, não abri chamado de dinheiro sem `ref_id`
que case, e não toquei em e-mail não lido.
